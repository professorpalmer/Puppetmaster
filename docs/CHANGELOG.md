# Changelog

## v0.9.59

**Fix: Hermes workers no longer inherit Puppetmaster's `PYTHONPATH` (cross-interpreter poisoning).** Hermes is the first Python-CLI adapter; the others spawn Node CLIs that ignore `PYTHONPATH`. The Hermes worker env was built with `inject_worker_cli_env`, which prepends this install's source root (the pyenv-3.9 site-packages) to `PYTHONPATH` — correct for `python -m puppetmaster codegraph` children, but poison for the foreign Hermes interpreter, which then imports the parent's stale `python-dotenv` and crashes with `load_dotenv() got an unexpected keyword argument 'override'`. Full suite **743** green (+2 tests).

- **`scrub_foreign_interpreter_env` (new, in `codegraph.py`).** Strips `PYTHONPATH`/`PYTHONHOME` from an env before it is handed to a non-Puppetmaster Python. Fixes the whole bug class: any future foreign-Python adapter gets the same treatment instead of re-discovering the clash.
- **`HermesAdapter._run_implement` / `_run_analyze` use the scrub** instead of `inject_worker_cli_env` — Hermes workers don't self-serve `python -m puppetmaster codegraph` from the parent's tree, so they never needed the injected source root. Node-based adapters (Cursor/Claude/Codex) are unchanged: they ignore `PYTHONPATH` and still get the CLI-resolving injection.
- Regression tests pin the helper's behavior and that both Hermes paths scrub (and never re-introduce `inject_worker_cli_env`).

## v0.9.58

**Coupling-aware delegation — stop fanning out a single feature into a swarm.** The invocation gate was steering *every* delegated task toward a "fan it out to a swarm" framing, even single implementations. Fanning out one tightly-coupled change makes parallel workers re-ingest the same context and stack commits that are unaware of each other → conflicts, rework, broken delivery, and zero net token savings. The gate now matches the verb to the task shape. Full suite **741** green (+4 tests).

- **Implementation intent routes to ONE implement worker, not the swarm default.** `infer_role_and_verb` previously matched only a narrow verb list (`refactor|implement|build|…`), so plain feature work ("add a CSV export endpoint", "create the webhook handler", "wire up retries") fell through to the read-only `*_swarm` default — the exact task-shape mismatch behind the collision reports. The implement pattern is broadened to catch ordinary feature/fix verbs and maps to `puppetmaster_start_implement` (a single worker in a clean worktree that yields one reviewable `PATCH`).
- **Verb-aware injected directive.** `DelegationDecision.directive()` is no longer one-size-fits-all: implement verbs are described as a single clean-worktree worker ("not a fan-out swarm; parallel editors stack commits that are unaware of each other"), CodeGraph verbs as a structural lookup, and only genuinely read-only analysis keeps the swarm framing. The implement directive explicitly reserves swarms for the explore/review/audit passes *around* the feature.
- **Trivial carve-out hardened against the broadening.** An explicit trivial signal ("add a comment", "fix a typo", rename, one-line/single-file) now stays inline regardless of the role-inflated capability score, so widening implement detection never starts delegating routine one-liners (the fastest way to train users to disable the gate).
- **Guidance follows the code.** `AGENTS.md` gains a "match the verb to the task shape" section: single feature → single implement worker; independent slices → fan out split by non-overlapping files; broad investigation → read-only swarm. It also states the honest positioning — Puppetmaster wins on mixed workloads, trivial-heavy batches, long-context horizons, and zero-token artifact recall, and output-compression tools (e.g. RTK) are **additive**, not competitors.

## v0.9.57

**Reasoning-effort routing for Hermes + two frontier models on your own keys.** Hermes lets you dial each model's reasoning depth (`minimal`→`xhigh`); the router now treats `(model × effort)` as a first-class cost/capability dimension, so a hard task can land on a high-effort mid-tier model instead of paying frontier prices. Full suite **737** green (+6 tests).

- **`HermesAdapter` honors `payload.reasoning_effort`.** Hermes has no `hermes chat` effort flag — the headless source of truth is `agent.reasoning_effort` in the loaded `config.yaml` (read at every CLI startup). The adapter applies per-task effort via an **ephemeral `HERMES_HOME`** that symlinks the real home (preserving `auth.json`, `.env`, sessions, and MCP servers verbatim) and overrides only `config.yaml` with the chosen level. No mutation of the user's `~/.hermes`, so parallel swarm workers can run at different efforts safely; the temp home is torn down after the run. Degrades to the default effort when the level is empty/invalid, PyYAML is absent, or the home can't be read — a routing knob never fails a worker.
- **`models set <id> effort=<level>` and the setup wizard now support `hermes`** (`minimal`/`low`/`medium`/`high`/`xhigh`), alongside the existing `openai`/`codex` support. Effort is stamped as `payload_defaults.reasoning_effort` with an `effort:<level>` tag and an output-token-cost multiplier so estimates stay honest (high effort ≈ 2× output burn). The wizard's effort-variant builder now **merges** onto the base entry's defaults instead of replacing them, so Hermes' adapter-critical `provider` survives a variant (also benefits any future provider-bearing model).
- **Frontier models added to the curated Hermes catalog:** `gpt-5.5` (cap 96, OpenAI) and `claude-opus-4-8` (cap 99, Anthropic) — so `models discover --source hermes` seeds a full frontier suite on your own API keys, and they survive a registry reset.

## v0.9.56

**Hermes joins the model router — credential-aware, so `auto_route` can pick Hermes the moment setup finishes without ever landing on a provider you can't call.** v0.9.53 made Hermes a routable worker and shipped a curated catalog; v0.9.55 wired the host MCP. This closes the last gap: the curated Hermes models are now *seeded into the router registry* during `setup`, filtered to the providers you actually have credentials for. Full suite **731** green (+9 tests).

- **Per-provider credential detection.** New `available_hermes_providers()` resolves which Hermes providers (`gemini`/`anthropic`/`openai-api`) are callable — reading process env, `~/.hermes/.env`, and OAuth state in `~/.hermes/auth.json` (so a `hermes login` provider counts too). `GOOGLE_API_KEY` or `GEMINI_API_KEY` satisfies `gemini`. The existing `hermes_credentials_available()` is refactored onto the same shared key-detection helper (behavior unchanged).
- **Credential-filtered seeding (no dead routes).** `curated_to_specs` / `merge_curated_into_registry` take an optional `allowed_providers` filter; a curated entry whose `payload_defaults.provider` lacks a credential is skipped and reported under `skipped` (with its provider), so a missing key surfaces as an actionable line instead of a model the router would route to and then fail on. `allowed_providers=None` is the default and preserves every existing caller byte-for-byte.
- **Wizard auto-seeds on enable.** `setup` step 7 (now "install-hermes-mcp + router registry") seeds the credential-backed Hermes models into `models.json` right after the MCP install, prints `added`/`refreshed`/`skipped`, and — when no provider credential is found — tells you exactly how to fix it (`add a key, re-run models discover --source hermes --write`). Best-effort: registry seeding never aborts the wizard.
- **`models discover` is credential-aware for Hermes.** `--source hermes` now seeds only callable models (filtered + `skipped` reporting), and `--source all` folds Hermes in **only when the `hermes` CLI is actually present** — so users who don't run Hermes don't get `hermes/*` entries injected just because they happen to have an `OPENAI_API_KEY` set.

## v0.9.55

**`puppetmaster install-hermes-mcp` — turnkey host wiring so Hermes can orchestrate Puppetmaster.** v0.9.53 made Hermes a routable *worker*; this closes the other direction (Hermes-on-top), registering Puppetmaster's MCP server into Hermes for parity with `install-cursor-mcp` / `install-codex-mcp` / `install-claude-mcp`. Verified end-to-end: Hermes lists `puppetmaster ✓ enabled` from the config we write. Full suite **722** green (+10 tests).

- **`install-hermes-mcp` (idempotent, schema-correct).** Unlike Codex/Claude, `hermes mcp add` is discovery-first and drops into an interactive tool-selection checklist that can't run headlessly — so the installer edits the `mcp_servers.puppetmaster` block in the `config.yaml` Hermes owns directly (the same direct-write approach `install-cursor-mcp` uses for `mcp.json`), honoring `$HERMES_HOME`. It preserves every other server/section and any user-set `env` / `tools` / `timeout` keys on the entry, runs the same MCP `tools/list` handshake smoke test before writing (refuses to register a broken launch command), and reports `unchanged` on a match. `--force`, `--dry-run`, `--skip-handshake`, `--path` mirror the sibling installers; a matching `uninstall_hermes_mcp` is wired into `puppetmaster uninstall`, and `setup` gains a gated Hermes step.
- **Zero-dependency contract intact.** Editing Hermes' YAML needs PyYAML, so the installer imports it lazily and prints an actionable hint (`pip install puppetmaster-ai[hermes]`) when it's absent — the core package still declares no runtime dependencies. PyYAML is declared only in the new optional `[hermes]` extra.
- **Rules already flow for free; hooks deliberately deferred.** Hermes auto-injects `AGENTS.md` on every session, so the Puppetmaster directive `install-rules` already writes reaches Hermes today — no Hermes-specific rules target needed. A Hermes hooks target is intentionally *not* shipped: Hermes hooks are shell-script hooks gated by a first-use consent allowlist (`shell-hooks-allowlist.json`), and auto-installing one would subvert that security boundary by design. MCP + AGENTS.md is the established parity bar for a non-Cursor/Claude platform (Codex has no hooks target either).

## v0.9.54

**Docs: README polish + a visual setup guide.** No code changes — refreshes the project page on GitHub and PyPI.

- Adds a one-glance setup graphic (install once, wire your platform, verify) to the README, hosted via an absolute raw-GitHub URL so it renders identically on GitHub and PyPI.
- Tightens the Install section: condensed the dense auto-invocation paragraph to its essentials and pointed the detail at the existing [Auto-invocation](https://github.com/professorpalmer/Puppetmaster#auto-invocation) section.

## v0.9.53

**NousResearch Hermes is now a first-class Puppetmaster platform — analyze + full-edit implement across Gemini, Anthropic, and OpenAI — with hermetic worker isolation.** The Hermes CLI (`hermes chat`) joins Cursor / Claude Code / Codex as a routable adapter, verified end-to-end across all three providers for both modes. Full suite **712** green (+10 tests).

- **`HermesAdapter` (analyze + implement).** Shells out to `hermes chat` headlessly. Two Hermes quirks are handled at the root: it kills its own process group on exit (every run uses `start_new_session=True` so teardown can't reach the orchestrator), and it returns unreliable exit codes (implement success is read from the git diff and emitted as a `PATCH` artifact; analyze success is parsed from the structured stdout). Failure classification maps provider errors — including `HTTP 404: model` → `model_unavailable` — to actionable reasons.
- **Hermetic worker isolation (the correctness fix).** Hermes leaks state across sessions two ways: it injects stored **memory** into the system prompt, and the **`session_search`** tool lets a worker read prior sessions (observed: a worker recalled an unrelated earlier session's secret). Workers now run with `--ignore-rules` (suppresses memory injection + the memory tool) **and** default toolsets that exclude `memory`/`session_search` (`file,terminal,code_execution,web,vision` for implement; `file,web,vision` for analyze). A forced cross-session recall now returns `UNKNOWN`. Power users can opt back in with `--use-hermes-rules` / a custom `--toolsets`.
- **First-class platform wiring.** Added to the platform lock's known adapters, the MCP implement priority + command builder (`puppetmaster_start_implement` can target `adapter="hermes"`), a `puppetmaster hermes` CLI subcommand (`--mode implement|analyze`, `--provider`, `--model`, `--max-turns`, `--toolsets`, …), and `doctor`/`adapter_status` configured-detection (CLI on PATH + a reachable provider credential).
- **Auto-routing across providers.** A curated, API-billed Hermes catalog (gemini-2.5-flash/pro, claude-sonnet-4-5, gpt-5) stamps the required `provider` via `payload_defaults`, so the router picks the cheapest sufficient model *and* the worker reaches the right credential/wire protocol (a bare model name would otherwise route to unconfigured OpenRouter). Seed it with `python -m puppetmaster models discover --source hermes --write`.
- **Upstream fix in flight.** The OpenAI Responses-vs-Chat-Completions mismatch (GPT-4.1/4o failing on the direct `api.openai.com` Responses path) is fixed in [NousResearch/hermes-agent#47820](https://github.com/NousResearch/hermes-agent/pull/47820) rather than worked around with a non-portable local config.

## v0.9.52

**Community PR batch: Codex MCP credential inheritance, read-only generated swarms, throttled worker heartbeats, compact `status`, and a cross-project dashboard — each validated end-to-end before merge.** Five external contributions (four from @kbentonferguson, one from @jwellman80) reviewed under hard scrutiny, integrated with authorship preserved, and hardened with follow-up fixes/tests where they had gaps. Full suite **702** green (+19 tests).

- **Codex MCP credential inheritance is explicit and secret-safe (#10).** `install-codex-mcp` gains `--env`, `--inherit-env`, `--env-file`, `--map-env`, and `--force-env`: users opt specific provider variables into the MCP server environment instead of depending on whatever the host launched with. Secret-like values and env-file contents go through a managed Python wrapper backed by a private `0600` JSON env file, so raw credentials never land in Codex TOML, installer logs, or output; existing env keys are preserved unless `--force-env`. Codex billing detection now reads `$CODEX_HOME/auth.json` before `~/.codex/auth.json`, and `doctor --json` surfaces credential-env visibility and billing-source evidence without printing values. **Follow-up fix:** the PR also tightened `adapter_status` to gate Codex availability on billing health, which would have demoted an `OPENAI_API_KEY`-only setup to "unconfigured." Availability and billing are now decoupled — the CLI plus *any* credential signal marks Codex usable, while the `billing:codex` check reports the account context independently (non-regressive superset of prior behavior, pinned by a regression test). The whole env-inheritance path is opt-in: with no env flags the install is byte-for-byte the previous behavior, no wrapper file created.
- **Generated MCP swarms stay read-only when routed to an edit-capable adapter (#11/#12).** Generated analysis swarms now carry `read_only`/`sandbox: "read-only"`/`dangerously_bypass_approvals_and_sandbox: false` through their task payload, so when auto-routing lands on Codex the worker reviews the caller's dirty diff on the adapter's read-only path instead of tripping the full-edit clean-tree guard. Implement/edit paths (`mode=implement`) still require a clean tree. Defense in depth: classification skips the guard *and* the adapter physically can't write.
- **Worker heartbeat noise throttled without weakening lease safety (#11).** Lease renewal / heartbeat moved off the hot `poll_seconds=0.1` loop to a dedicated cadence (default 2s, capped at `lease/3` so short leases still get ≥3 renewals before expiry); `--heartbeat-seconds` is explicit, with non-default `--poll-seconds` honored as the compatibility fallback. Inline worker mode now scopes `PUPPETMASTER_STATE_DIR` to the owning store root so adapter sidecar logs land under the correct project state dir.
- **Compact `status` for prompt-heavy jobs (#13).** `status --compact` (and the MCP `puppetmaster_status` tool's `compact` arg) omits high-churn prompt/goal/instruction bodies and replaces them with deterministic `chars` + `sha256` refs, so identity is still comparable without paying the context cost. Default output is unchanged. Measured **97.4%** reduction on a prompt-heavy job (149,644 → 3,886 bytes) in end-to-end validation.
- **Dashboard `--all-projects` for a single cross-project view (#14).** A new `--all-projects` switch aggregates jobs from every Puppetmaster project state dir on the machine, labeling each row with the digest-stripped project slug; cross-project deep-links resolve via `find_state_dir_for_job`. The existing job-id traversal guard still rejects bad ids with `400` before any path join, and the server stays loopback-only unless `--allow-external`. **Follow-ups:** added the cross-project aggregation test the PR was missing, and wired `--all-projects` through the MCP `puppetmaster_dashboard` tool for CLI parity.

## v0.9.51

**Routing and fallback now gate on whether an adapter can *actually run*, not just whether it's enabled — plus Bedrock-aware Claude Code model IDs.** Field report (Rishi): a worker tried Cursor-style Claude IDs (`claude-opus-4-8`) that Bedrock rejected as invalid, then fell back to a Codex that wasn't installed and died. Two independent root causes, both fixed at the root with defense in depth. Full suite **683** green (+11 tests).

- **Fallback no longer cascades into an uninstalled CLI.** All three reroute paths already gated on the platform lock *and* billing health — but a stale `~/.codex/auth.json` / `~/.claude.json` keeps an adapter reading billing-*healthy* long after its CLI binary is gone, so billing-healthy ≠ runnable. A new single-source `preflight.adapter_cli_present` resolves the adapter's real binary on PATH (honoring `CLAUDE_CODE_COMMAND` / `CODEX_COMMAND`); CLI-less adapters (Cursor's bundled SDK runner, OpenAI's HTTP path) stay "present" since their readiness is purely a credentials question. Reroute now skips an adapter whose binary isn't there and leaves the task cleanly FAILED instead of dispatching a doomed worker.
- **The router won't first-pick a model it can't launch either.** After registry reconciliation, `_route_specs` drops CLI-missing adapters — with a strict **never-fail-closed** guard: if that would empty the registry (e.g. CI with no CLIs at all), it keeps the registry intact and lets dispatch/fallback surface the precise error. Emits `router.adapter_cli_missing` for the audit trail.
- **Claude Code on Bedrock gets a real model id or a precise error — never an invalid one.** The `claude` CLI on Bedrock accepts only inference-profile IDs / ARNs (`us.anthropic.claude-opus-4-1-20250805-v1:0`), never the registry's short `claude-opus-4-8`. `resolve_claude_code_model` is now Bedrock-aware: off Bedrock, unchanged; on Bedrock, precedence is an explicit override (`ANTHROPIC_MODEL` / `payload.bedrock_model`) → an already-Bedrock-shaped request → **omit `--model`** so the CLI uses its configured default, with a clear, actionable note (rather than forwarding a short name Bedrock will reject). No region-specific ARNs are guessed.
- **Setup detection unchanged — already correct.** `setup` already auto-detects and pre-selects only genuinely usable platforms (CLI resolvable, `CURSOR_API_KEY` + SDK, `OPENAI_API_KEY`); the new runtime gating is the defense-in-depth that also protects zero-config users who never run `setup`.

## v0.9.50

**Setup honors the platform lock, real Cursor-SDK token metering, a fixed Windows `doctor` probe, and CodeGraph that reaches every agentic harness.** A batch of field-reported rough edges (Pawel's setup/`doctor` notes plus a large parallel-swarm test) resolved end-to-end. Full suite **672** green; the token fix verified against a live Cursor composer run.

- **Setup no longer installs Cursor when Cursor isn't selected.** The step-2 platform lock is now the single source of truth across the whole wizard, not just the SDK bootstrap. `install_rules` and `install_hooks` gained an optional `enabled_adapters` filter (default `None` = unfiltered, so standalone `install-rules`/`install-hooks` are unchanged), and steps 4/7/8 thread the locked set through. A Claude-only machine now gets Claude hooks/rules + the cross-tool `AGENTS.md`, with Cursor cleanly skipped and a "skipped — disabled by the platform lock" line instead of silent always-on installs.
- **`doctor` no longer surfaces a misleading `npm → WinError 2` on Windows.** `subprocess(shell=False)` can't launch the `npm.cmd` shim that `shutil.which` finds via PATHEXT (CreateProcess only runs real binaries), so probing bare `npm` raised `FileNotFoundError`. A new `_resolve_probe_command` routes `.cmd`/`.bat` shims through `%COMSPEC% /c`. Actually fixed, not a "harmless quirk."
- **CodeGraph is impossible to miss (and to misinstall) in `doctor`.** Both messages now name it as the shared repo-intelligence layer, give the exact Node/npm command (`npm install -g @colbymchenry/codegraph`), explicitly warn against the unrelated `pip install codegraph`, and point init at `python -m puppetmaster codegraph init --index`.
- **Cursor-SDK token accounting is real now, not a ~500× undercount.** `Agent.prompt()`'s `RunResult` carries no usage, so the runner always read `null` and the Python side fell back to a `char/4` estimate that made tokens useless as a cost axis. The runner now drives `Agent.create` + `agent.send({ onDelta })` and accumulates measured input/output/cache tokens off the streaming `turn-ended` delta (verified live: a trivial prompt reports ~10.9k input tokens, not ~20). Cache read/write tokens are preserved for the cost axis; a runtime that reports no usage still falls back to a clearly-labeled estimate (`tokens_estimated: true`) rather than a fake-measured zero.
- **CodeGraph CLI resolves to the current tree in every agentic harness.** Only the Cursor adapter wrapped its worker env with `inject_worker_cli_env` (source root → `PYTHONPATH`); Codex and Claude omitted it, so on a host with a stale pip `puppetmaster` their workers hit `unknown command` on `python -m puppetmaster codegraph` and fell back to grep — defeating the CodeGraph-first directive in their own prompts. Both now match Cursor, with a regression test pinning the invariant across all three harnesses.

## v0.9.49

**Fix: v0.9.48's CodeGraph auto-provisioning could hang the MCP server on any plain-Node host — `doctor` (and every per-worker adapter) synchronously triggered a global `npm install -g` and blocked.** v0.9.48 added the npx leg to `codegraph_available()`, so on a host with Node but no `codegraph` shim and no Cursor (i.e. most non-Cursor users — and the CI runners), the cheap "is CodeGraph ready?" probe started returning **true**. That probe gates hot paths: `doctor` then ran `codegraph status` → `run_codegraph_cli` → `ensure_codegraph_provisioned()` → a blocking `npm install -g @colbymchenry/codegraph` (up to 300s) on the request thread. Caught by red CI on the v0.9.48 `main` lane — `test_parallel_doctor_calls_do_not_kill_mcp_server` failed `windows-latest` with `handshake never returned` (the doctor reply never came back because it was stuck installing), reproducing twice. Full suite **659** green (+1 regression test).

- **`codegraph_available()` is a cheap, no-network probe again.** It counts only what resolves instantly — the `codegraph` shim on PATH or Cursor's bundled Node + installed package — and never the npx leg. npx "availability" means *Node exists*, not that CodeGraph is warm; treating it as ready is what dragged health checks and adapters into a cold install. Restores v0.9.47's fast-skip on non-Cursor hosts.
- **The npx fallback + provisioning now live behind the explicit-command gate only.** `run_codegraph_cli()` gates on `ensure_codegraph_provisioned()`'s return (False only when there's literally no Node), so an *explicit* `puppetmaster codegraph status/search/context` still bootstraps via npx on any Node host — Victor's v0.9.48 win is preserved — while `doctor`, prompt-injection, and per-worker context never pay a provisioning cost.
- **Regression test.** `test_codegraph_available_is_cheap_and_never_provisions` asserts the probe reports not-ready on an npx-only host **and** shells out to nothing (`subprocess.run` asserted uncalled), pinning the invariant that broke.

## v0.9.48

**Fix: CodeGraph is now auto-provisioned on any host with Node — it stops giving up with "codegraph CLI not on PATH" the moment a global shim and Cursor's bundled Node are both absent (field report: Victor on Codex, `IdeaProjects/ff-data-engineering`, no Cursor + no global install → CodeGraph unreachable).** Previously the resolver had exactly two legs (global `codegraph` shim, or Cursor's bundled Node), so a non-Cursor host without the manual `npm install -g` got a hard `ok: false`. Now there's a universal third leg and a one-time provisioner, so the only remaining floor is "no Node at all" — which a Node CLI genuinely cannot cross. Full suite **658** green (+4 tests); verified end-to-end by masking the global shim off PATH and running `python -m puppetmaster codegraph status` through the npx fallback.

- **Universal `npx` fallback.** When neither the shim nor Cursor's Node resolves, CodeGraph runs via `npx -y @colbymchenry/codegraph` — zero manual setup on any machine with Node. Because the package's native `better-sqlite3` is built and run under the *same* (system) Node, the ABI matches, so this path never falls back to slow WASM SQLite (that only happens when crossing Node runtimes). Opt out with `PUPPETMASTER_CODEGRAPH_NO_NPX=1`.
- **One-time provisioning on first use.** `ensure_codegraph_provisioned()` runs once before the first timed CLI call so it doesn't race a cold npx download/native build against a tight timeout. It prefers a persistent global install (`npm install -g @colbymchenry/codegraph` → a real shim on PATH, no per-call npx overhead, this session and every session after); if that can't run or doesn't land on PATH, it warms the npx cache instead. Best-effort and fail-open at every step. Opt out of the global install with `PUPPETMASTER_CODEGRAPH_NO_GLOBAL_INSTALL=1`; tune the bootstrap ceiling with `PUPPETMASTER_CODEGRAPH_PROVISION_TIMEOUT_SECONDS` (default 300).
- **Honest floor + clearer hint.** The passive prompt-injection path stays non-blocking (no provisioning stall on worker dispatch — it still degrades to "no context" gracefully). The only `ok: false` left is a host with no Node, and the hint now says exactly that: install Node 18+, and that Puppetmaster auto-provisions the CLI once Node is present.

## v0.9.47

**Fix: the MCP server no longer self-reaps on a false orphan signal under Codex — the root cause of Victor's recurring "Transport closed."** The input-staleness watcher treated `os.getppid() == 1` as "my host died, reap me" **unconditionally**, even on Codex where idle-reaping is deliberately off. But `ppid == 1` only means the *immediate* parent exited: when a host spawns the server through an intermediate that exits, the server reparents to `launchd` **while the real owner (Codex) is alive and still holding the stdio pipe** — and then kills itself. Codex sees stdin EOF and reports "Transport closed"; Cursor masks the same reap by transparently respawning, which is exactly why Codex was "the only outlier." Reproduced deterministically (grandchild reparented to init + Codex handshake + owner alive holding the pipe → server self-reaps: `reason=sigint_clean_shutdown`), then fixed and re-verified to stay alive. Validated by driving real `codex exec` (codex-cli 0.134.0) against the MCP — doctor, a `start_codex` worker (completed, 2 artifacts), and a 5-call sustained run all clean, no transport drop. Full suite **654** green (+1 regression, 1 stale test flipped).

- **Parent-death reaping is host-gated, like idle-staleness.** The authoritative owner-death signal for a stdio server is **stdin EOF** — the host holds the write end, so when it dies the pipe closes and the main loop exits cleanly on every host, no watcher needed. The `ppid == 1` heuristic now only fires on hosts that transparently respawn a reaped server (Cursor, or an explicit `PUPPETMASTER_MCP_INPUT_STALE_FORCED`), where a wrong reap costs nothing. On Codex/Claude Code a leaked-but-alive server is far cheaper than a false "Transport closed."
- **Live handshake override closes the env-leak hole.** The reap gate freezes at startup from env, but Codex scrubs `CODEX_*` from the server env, so a Codex session launched from a Cursor terminal leaks `CURSOR_*` and is misread as a respawn host. Once the `initialize` handshake reveals a Codex client (`_client_is_codex()`), the server never self-reaps regardless of the startup env guess.
- **Known limits.** This fixes the reproducible transport drop; it does not address the separate "review worker stalled with no output" report (could not reproduce — a Codex worker completes normally and a keyless Cursor worker fast-fails with `preflight_blocked`, not a stall). If that recurs it needs the exact verb and the job's `stderr_path` to trace.

## v0.9.46

**Fix: the auto-invocation hook no longer wedges native tooling — it stops hard-denying the built-in `Task`/`Agent` tool and goes fully silent when no Puppetmaster MCP server is reachable (field reports: Victor on Codex, and a Cursor session, both blocked from delegating to native subagents while the swarm verbs weren't connected).** The pre-tool hook denied every `Task`/`Agent` call and redirected to `puppetmaster_start_*swarm` — but when those MCP tools aren't connected the turn is wedged (native tool blocked, suggested verb uninvokable), and the blanket deny also broke the native subagents Puppetmaster explicitly does **not** replace (`browser-use`, `ci-investigator`, `cursor-guide`, `best-of-n-runner`). Verified end-to-end across cursor/claude/codex hosts; full suite **653** green.

- **Native `Task`/`Agent` is never hard-denied.** Steering toward a swarm stays at the non-blocking prompt-submit layer (inject a directive, never block the tool). The hook payload can't tell a swarm-worthy fan-out from a browser-use run, so blocking the agent tool was both a wedge and an over-reach.
- **No live MCP server → the hook is a complete no-op.** New availability probe reads the per-user MCP server registry; with nothing alive there's nothing to redirect to, so the hook allows every native tool and injects no directive (it never nudges toward a verb the agent can't call). Fails closed to "allow" on any error. `PUPPETMASTER_HOOK_ASSUME_TOOLS=1/0` overrides the probe.
- **The lone remaining redirect is recoverable.** A genuinely recursive/repo-wide *shell* search (`rg -r`, `grep -R`, `find -name`) is still steered to CodeGraph (only when a server is alive), and the deny now names the CLI fallback (`python -m puppetmaster codegraph search '<query>'`) so it's never a dead-end even if the MCP transport is down. Read-only shell (`git log`, `ls`, `cat`) and narrow `grep pattern file` stay allowed as before.
- **Known limits.** The registry probe can't detect a "server process alive but the host's MCP transport silently dropped" state; since the agent-tool deny is gone, the only thing that can fire there is the recursive-shell redirect, which now points at the CLI. The kill switch (`PUPPETMASTER_AUTO_INVOKE_DISABLED=1`) still disables the whole gate.

## v0.9.44

**CI/test reliability: green the macOS release lane — no product behavior change from v0.9.43.** The v0.9.43 *tag* CI went red on `macos-latest` only (the same commit was green on `main`): `test_input_staleness_watcher_resets_when_message_arrives` failed on `self.assertFalse(triggered.wait(timeout=0.1))`, and a rerun reproduced it — so it's environmental, not a rare flake. By construction that test uses `stale_after=0.2s` with a fresh inbound mark, so the idle-staleness path *mathematically cannot* fire inside the 0.1s window; the only other trigger is the watcher's parent-death reap (`os.getppid() == 1`), and loaded macOS GitHub runners transiently reparent a test process to `launchd`. That orthogonal orphan signal was leaking into three idle-focused watcher tests. Full suite **647** green.

- **Idle-staleness tests pin off the parent-death reap.** All three `_InputStalenessWatcher` idle tests now patch `_parent_is_dead -> False`, so each exercises only the idle-staleness guarantee it claims. Parent-death keeps its own dedicated test (which patches it `True`), so coverage is unchanged.
- **Product code untouched.** `mcp_server.py` and every shipping module are byte-identical to v0.9.43; the Codex auto-detach fix from v0.9.43 stands. This release exists solely to carry the test de-flake on a green release tag.

## v0.9.43

**Fix: synchronous worker verbs are now Codex-safe — they auto-detach to a `job_id` instead of blocking past Codex's hard tool timeout (investigation: Victor's recurring "Transport closed" on Codex).** Driving real Codex (codex-cli 0.134.0) against the MCP with full `rmcp=trace` exonerated the prime suspects with receipts: the idle keepalive's `notifications/message` frames are parsed and accepted by rmcp (7 frames over 45s, zero errors), the `2024-11-05` handshake is accepted, and a per-tool timeout firing mid-call does **not** drop the transport (the next call succeeds). The one real Codex gap: Codex enforces a hard `tool_timeout_sec` and sends **no `progressToken`**, so our progress keepalive can never extend it — any synchronous worker verb (cursor/claude/codex/openai review·plan·implement) that runs past the budget gets cancelled and a healthy job reads as failed. Full suite **647** green (+8 focused tests); fix live-verified end-to-end against real Codex.

- **`run_worker_cli` auto-detaches on hard-timeout hosts.** The four blocking worker verbs (`puppetmaster_cursor_review`/`_cursor_plan`/`_cursor_implement`, `_claude_implement`, `_codex`, `_openai`) now route through a wrapper that, under a Codex host, starts the same worker asynchronously and returns its `job_id` immediately with poll guidance (`puppetmaster_await_job`/`status`/`show`) — the only delivery Codex can actually complete. Work and artifacts are identical; only inline-blocking becomes poll-for-result. Cursor and Claude Code (no hard cap) keep the inline behavior unchanged.
- **Host detected from the `initialize` handshake, not env.** Codex spawns the MCP server with a scrubbed environment (no `CODEX_*` vars), so env sniffing misses it — verified empirically. We now capture `clientInfo` at `initialize` (Codex sends `name: "codex-mcp-client"`, `title: "Codex"`) and key off that, falling back to `CODEX_*` env markers only when the handshake identity is unknown. A known non-Codex client is never misread from stray env vars.
- **Escape hatches.** `PUPPETMASTER_MCP_SYNC_AUTODETACH=0` keeps verbs blocking inline (for users who raised Codex's `tool_timeout_sec` high enough to wait out a run); a per-call `autodetach` boolean overrides both directions.
- **Known limits.** This does not change Victor's root cause if his drop is environmental (still-alpha codex-cli, or a server that can't import puppetmaster from his cwd) — those need his `codex --version` / `python -m puppetmaster mcp list` output. It hardens the one reproducible Codex-specific failure mode found. Claude Code is treated as non-hard-timeout (no field-proven hard cap); revisit if that changes.

## v0.9.42

**Fix: "Cursor SDK not found" on machines where it was installed and working (field report: Zane, with a valid `@cursor/sdk` at `site-packages/puppetmaster/node_modules`).** The diagnostics probe checked exactly one directory level (`parent.parent` of the package — `site-packages/node_modules`), but `cursor_sdk_runner.mjs` resolves the SDK with Node's upward `node_modules` walk, whose *first* hop is the package dir itself. Result: runtime worked, diagnostics reported the SDK missing, detection/setup disagreed with reality. Our dev tree masked the bug because the repo root happens to have `node_modules` at the one level the old probe checked. Full suite **640** green (+1 regression test on the exact reported layout); `ruff` clean; probe live-verified.

- **Probe mirrors Node's resolution.** `_find_cursor_sdk_install` now walks `node_modules/@cursor/sdk` from the package directory up through every ancestor — same algorithm the runner uses — plus the workspace root and `PUPPETMASTER_HOME` as before. Diagnostics and runtime can no longer disagree about where the SDK lives.
- **Doctor advice un-staled.** The cursor-sdk warning said "run `npm install` in the Puppetmaster package dir" — pre-v0.9.39 advice that points pipx users into a venv they shouldn't touch. It now says what works: "run `puppetmaster install-cursor-mcp` to bootstrap @cursor/sdk (needs Node/npm)".
- **Known limits.** The probe checks directory existence, not SDK integrity — a corrupt or half-installed package still reads as present; the preflight generation check remains the runtime truth.

## v0.9.41

**Fix: implement workers no longer look "degraded" after doing the job — the final report now becomes durable artifacts (field report: the v0.9.40 CI fix itself, where a cursor implement worker shipped the right patch and correct diagnosis but the job read "Findings: None").** The cursor implement path never asked for a report and never parsed the agent's final message; Claude's path had the same gap; Codex labeled every prose-reporting write run a degraded RISK. Full suite **639** green (+9 focused tests); `ruff check` clean on touched files.

- **Reporting contract in implement prompts.** Cursor's implement wrapper and Claude worker prompts now end with a contract: close your final message with what changed, files touched, and what you ran to verify. Prompts that already carry the structured JSON artifact contract (swarm review/plan roles) are left alone — no conflicting instructions.
- **Final message parsed into artifacts, with a prose fallback.** `implement_report_artifacts` first tries the structured JSON contract (typed findings/decisions/risks, same parser as analyze mode); anything else is preserved verbatim as a FINDING (`claim` = report headline, `payload.report` = full text, evidence `report:final-message`). Cursor implement, Claude runs, and write-capable Codex runs all route through it on success. Quality verdicts flip from "degraded" to "ok" organically because a substantive artifact now exists.
- **Codex prose semantics split by capability.** A workspace-write/bypass Codex run that reports in prose passes with a report FINDING; read-only (review-style) runs keep the strict degraded-RISK semantics — their whole contract is structured findings.
- **CLI surfaces the report.** `puppetmaster cursor --implement` (and every edit-mode run) prints a `report:` headline after the outcome line, plus the `puppetmaster artifacts <job_id>` pointer to the full report.
- **Known limits.** Reports are capped at 20k chars (tail-biased) inside the FINDING; the full transcript stays in the stdout sidecar. Failed runs (non-zero exit) intentionally emit no report finding — output from a crashed agent isn't a report. The OpenAI adapter's analyze-only degraded semantics are unchanged.

## v0.9.40

**Fix: green CI on Windows runners (the v0.9.39 npm bootstrap built a backslash path the test rightly rejected) plus a timing flake on macOS runners.** Two distinct failures were red on `main`: `test (windows-latest, 3.12)` since v0.9.39, and an intermittent keepalive flake that even hit a README-only commit. Diagnosed from the actual Actions logs, not local guessing — the local macOS suite was green the whole time.

- **Windows: `ensure_cursor_sdk` now passes npm a forward-slash prefix.** `str(Path)` stringifies to backslashes on Windows (`\fake\site-packages`), which both failed the exact-command test and risks Git-Bash-style mangling in the field — the same bug class as the v0.9.34 hook-path fix. `as_posix()` is correct on every platform; npm accepts forward slashes everywhere.
- **Flake: `test_keepalive_emits_progress_only_with_token` polls instead of sleeping.** A fixed 0.12s window assumed the keepalive thread got scheduled promptly; loaded CI runners starved it past the window (observed: `'notifications/message' not found in set()` on a docs-only commit). The test now polls up to 5s for the frames it asserts on — deterministic on slow runners, still fast (~0.05s) locally.
- **Known limits.** The Windows fix is verified by the platform-exact unit test on CI, not by a live `npm install` on a Windows machine. The flake fix removes the observed starvation mode; other timing-sensitive tests in the suite were not audited in this pass.

## v0.9.39

**Fix: PyPI installs never had the Cursor SDK — setup now bootstraps `@cursor/sdk` via npm (field report: our own demo recording showed "cursor (not detected on this machine)" on a Cursor machine).** Wheels can't ship `node_modules`, so every pip/pipx install landed without the SDK that `cursor_sdk_runner.mjs` resolves at runtime: platform detection read cursor as absent and the cursor adapter itself couldn't run. Dev-tree installs masked this for months because the repo has `node_modules` sitting next to the package. Full suite **630** green (+7 focused tests); `ruff check puppetmaster/` clean; verified end to end on a fresh wheel venv — detection True pre-bootstrap, `install-cursor-mcp` npm-fetches the SDK into site-packages, Node resolves it from the runner's directory, MCP handshake 42 tools, `setup` shows `[on ] cursor` unannotated.

- **`ensure_cursor_sdk` bootstrap.** `install-cursor-mcp` (and `setup` step 4, when the platform lock enables cursor) now runs `npm install @cursor/sdk --prefix <package dir>` when the SDK is missing — the same directory Node's resolution walks up to from the runner script, so diagnostics and runtime agree afterward. Idempotent (`unchanged` when resolvable), honest when blocked (`skipped` with remediation when npm is absent, `error` with npm's last line on failure).
- **Detection counts a bootstrappable SDK.** `_detected_platforms` no longer demands the SDK be pre-installed: cursor is detected when `CURSOR_API_KEY` is set and either the SDK resolves *or* npm is available to fetch it — so first-run setup doesn't lock cursor off one step before the bootstrap that would fix it.
- **Step 1 stops overclaiming.** The billing probe printed "Cursor SDK authenticated" while only checking an env var — directly contradicting step 2's "not detected" in the same run. It now says what it verified: "CURSOR_API_KEY is set; work bills against the Cursor plan."
- **Known limits.** The bootstrap needs Node/npm on PATH — without it, cursor is honestly not detected and the skip message says what to install. npm fetches over the network at setup time (one-time, ~5s); air-gapped machines must vendor `node_modules` manually next to the installed package. Detection still does not validate the API key itself — doctor's billing checks remain the auth truth.

## v0.9.38

**Docs: the PyPI/README tagline now credits AWS Bedrock as a Claude Code billing path.** Bedrock support itself shipped in v0.9.27 (posture detection, credential health, live-verified completion through `CLAUDE_CODE_USE_BEDROCK`) but the project description never mentioned it. Docs-only release so the frozen PyPI project page picks it up; no code changes.

- **Known limits.** Bedrock remains a Claude Code billing path, not a standalone adapter — there is no native Bedrock API worker, and Vertex AI (`CLAUDE_CODE_USE_VERTEX`) is still undetected.

## v0.9.37

**Managed rules now mandate CodeGraph-first exploration, not just suggest it.** The installed rules (Cursor `.mdc` + the `AGENTS.md` managed block read by Claude Code and Codex) forced Puppetmaster delegation hard — trigger convention, delegate-first gate, hooks — but CodeGraph got one "prefer" sentence buried in the gate. Now it has its own must-obey section. Full suite **623** green (+1 focused test); `ruff check puppetmaster/` clean; repo `AGENTS.md` block refreshed via `install-rules` live.

- **"CodeGraph-first exploration (must obey)" section in the managed rule body.** Graph every directory you interact with: check `puppetmaster_codegraph_status` before exploring any directory (root or subtree), `puppetmaster_codegraph_init` (`index: true`) if no `.codegraph/`, then answer where/what-calls/what-implements questions from `_search`/`_context`/`_affected`/`_files` instead of crawling the tree.
- **Unsupported-language policy: partial coverage is still coverage.** CodeGraph indexes the languages it supports; ungraphable files simply don't enter the graph. The rule now tells agents to keep answering from the graph for everything it covers, scope native search narrowly to the ungraphed paths only, and reuse that shared context — never let multiple workers/agents each re-crawl directories the graph already covers.
- **Honest carve-outs and fallback baked in.** Native search stays legitimate for plain-text matches, single known file paths, and explicit "just grep"; transport failures route to the ABI-safe `python -m puppetmaster codegraph …` passthrough, never a bare `codegraph` shell call.
- **Known limits.** This is rule pressure, not deterministic enforcement — a host agent can still ignore prose; the `install-hooks` deny-redirect gate remains the hard backstop for broad native exploration. Existing installs pick up the new body on the next `puppetmaster install-rules` (or `setup`) run; rules already on disk don't self-update.

## v0.9.36

**Fix: `setup` locks the platform set to what's actually on the machine (field report: "it still says I have cursor… I assumed it detects them, it simply supports them").** The platform-lock step defaulted to all-on, so a Claude-Code-only Windows user ran for a day with cursor enabled — cursor verbs recommended, cursor preferred by implement routing, "[on ] cursor" in every status display. Full suite **622** green (+4 focused tests); `ruff check puppetmaster/` clean; detection verified live on this machine (cursor+codex+openai found, npx-only claude correctly not; existing lock respected, not stomped).

- **Detection-based first-run default.** On a first run (no `platform.json` yet), the non-interactive path now locks to the platforms detected on the machine; the interactive prompt offers the detected set as the Enter default. Probes are presence checks, not auth audits: cursor = bundled SDK + `CURSOR_API_KEY`; claude-code/codex = CLI resolvable; openai = `OPENAI_API_KEY`.
- **Existing locks are never stomped.** A re-run of `setup` respects a prior platform choice (printed as `unchanged`); explicit `--platforms` still always wins, now with an honest note when it enables something undetected.
- **Status display says why.** Every platform listing in the setup step annotates undetected entries — `[on ] cursor   (not detected on this machine)` — so "why does it say I have cursor" answers itself.
- **Known limits.** Detection is resolvability-based: an npx-only Claude Code (no `claude` on PATH, no `CLAUDE_CODE_COMMAND`) is not detected — set `CLAUDE_CODE_COMMAND` or use `--platforms`. Codex login state is not probed (subscription auth is opaque); a logged-out codex CLI still counts as detected, and doctor's billing checks remain the auth truth. Existing all-on locks created by older setups are left as-is — re-run `puppetmaster setup` after `rm ~/.puppetmaster/platform.json`, or use `puppetmaster platform only ...`, to adopt detection.

## v0.9.35

**Fix: idle self-termination is now Cursor-only (field report: Claude Code "Connection status failed" after an idle gap), plus non-git directories fail fast with a real remediation (field report: `not_a_worktree` in experiment dirs).** Full suite **618** green (+10 focused tests); `ruff check puppetmaster/` clean; live smoke — server handshake under a simulated Claude Code env (42 tools), preflight exercised against real `git rev-parse` calls.

- **Host-gated idle reaping.** The staleness watcher (reap after 10 idle minutes, zero in-flight calls) exists to clean up Cursor's lease-cycle orphans — and only Cursor transparently respawns a reaped server. Claude Code and Codex mark the connection failed and stay dead until the user restarts their session: that was Claude Code's "Connection status failed" this morning and part of Codex's "Transport closed" pattern. Idle reaping now requires Cursor host markers (`CURSOR_*` env) and is vetoed by Claude/Codex markers or any unknown host — a leaked-but-alive server is far cheaper than a dead transport. `PUPPETMASTER_MCP_INPUT_STALE_FORCED=1` re-enables it anywhere; `..._DISABLED=1` still kills it everywhere.
- **Parent-death reaping (every host).** True orphans are still caught, host-agnostically: when the spawning host process dies and the server is reparented to init, it reaps immediately — no 10-minute wait, no host guessing.
- **Worktree preflight on full-edit verbs.** `start_implement` and the cursor/claude/codex implement verbs now check cwd is inside a git work tree *before* spawning a worker, returning a structured error in the same turn instead of a failed job to excavate. Remediation now leads with `git init` (restores diff capture completely); read-only Codex sandboxes are exempt, matching the worker-level guard.
- **`allow_non_worktree` is finally reachable.** The worker-level guard has always documented a `payload.allow_non_worktree` escape hatch that no CLI flag or MCP arg actually exposed — an agent following our own remediation hit a dead end. Now plumbed end-to-end: `--allow-non-worktree` on the cursor/claude/codex CLI commands and an `allow_non_worktree` arg on the implement-capable MCP verbs (schema-visible, so agents discover it).
- **Known limits.** Host detection is env-marker-based and biased toward not reaping: a Cursor host that strips its own `CURSOR_*` vars idle-leaks servers instead of reaping them (the parent-death check still bounds the leak's lifetime). Parent-death detection is POSIX-only — Windows has no reparenting signal, so Windows falls back to idle-only behavior, gated the same way. The Claude Code reap was reproduced by code-path analysis matching the field report's shape, not by a live multi-hour Claude session on this machine.

## v0.9.34

**Fix: hook commands survive Windows — forward-slash python paths instead of backslash escape-layer roulette (field report: Claude Code on Windows, "when claude tried to execute the hook there were no \ at all").** `install-hooks` embedded `sys.executable` verbatim in the hook command string; on Windows that path has to survive two unescape layers (JSON decode, then the POSIX-style shell — Git Bash — that hosts run hook commands through). It survived only the first, so Git Bash ate every backslash and the invocation gate never executed. Full suite **608** green (+3 focused tests); `ruff check puppetmaster/` clean.

- **`_shell_safe_executable`.** The shared gate-command builder (Claude + Cursor hooks) now writes the python path with forward slashes — valid Windows separators at every layer, nothing to escape — and double-quotes it when it contains spaces (`Program Files`-style paths), which is safe in both POSIX shells and cmd.
- **Affected users:** anyone on Windows who ran `puppetmaster setup` or `install-hooks`. Re-run `python -m puppetmaster install-hooks --force` after updating to rewrite the broken command; the merge logic identifies our entries by marker, so user-authored hooks are untouched.
- **Known limits.** macOS/Linux installs are byte-identical unless the python path contains a space (then it gains quotes — harmless, but the entry rewrites once). Verified by unit tests asserting the rendered command shape, not by a live Windows host; Pawel's machine is the live confirmation loop.

## v0.9.33

**Feature: `install-claude-mcp` — `puppetmaster setup` now registers the MCP server with Claude Code (field report: "ran setup, Claude enabled, but no MCP in Claude's list").** Setup wired Cursor and Codex hosts but never Claude Code — the "Claude enabled" line users saw was the platform lock (Claude as a *worker*), not host registration, so driving Puppetmaster *from* Claude Code silently got nothing. Full suite **605** green (+6 focused tests); `ruff check puppetmaster/` clean; verified live end to end — real `claude mcp add` (user scope), `claude mcp get` shows `✔ Connected`, re-run reports `unchanged`.

- **`puppetmaster install-claude-mcp`.** Mirrors the Codex installer: shells out to `claude mcp add --scope user` (Claude Code owns `~/.claude.json`'s schema — it also stores OAuth state there, so we never hand-edit it), runs the same pre-registration handshake (spawn server, `tools/list`, refuse to register a broken entry), and is idempotent via `claude mcp get`. `--force`, `--dry-run`, `--skip-handshake`, and `--claude <cmd>` (multi-word OK, e.g. `npx -y @anthropic-ai/claude-code`) match the sibling installers; the CLI resolves from `claude` on PATH, then `CLAUDE_CODE_COMMAND`.
- **Wired into `setup` (now 8 steps) and `uninstall`.** Setup step 6 registers with Claude Code when the `claude-code` platform is enabled and the CLI is resolvable — same gating shape as the Codex step. `puppetmaster uninstall` removes the user-scope entry alongside the Cursor and Codex ones.
- **User scope on purpose.** Cursor's installer defaults to workspace-local because `.cursor/mcp.json` travels with the repo; Claude Code registration goes to user scope so the server is available in every project, matching how people drive Puppetmaster from Claude.
- **Known limits.** Only the user scope is managed: an existing local/project-scope `puppetmaster` entry shadows ours in Claude's precedence order and is left untouched. The installer needs the Claude CLI resolvable at install time (`npm install -g @anthropic-ai/claude-code` or `CLAUDE_CODE_COMMAND`); there is no config-file fallback, by design. No timeout tuning is written — Claude Code's MCP timeouts are env-var driven (`MCP_TIMEOUT`/`MCP_TOOL_TIMEOUT`), and the server's own block cap keeps calls short.

## v0.9.32

**Fix: deny-redirect hooks no longer recommend Cursor-only verbs on non-Cursor hosts (field report: Claude Code on Windows, no Cursor installed).** The pre-tool hook denied the native Agent/Task tool and pointed at `puppetmaster_start_cursor_swarm` — a verb that host cannot invoke — so the turn was blocked with an unusable recommendation. Full suite **599** green (+3 focused tests); verified live through the real CLI hook path (`invocation-gate --host claude --event PreToolUse`).

- **Host-portable verb translation.** Hooks now map cursor-specific verbs to the platform-routing equivalents for any non-Cursor host: `start_cursor_swarm`/`_review`/`_plan` → `puppetmaster_start_swarm`, `start_cursor_implement` → `puppetmaster_start_implement`. Applied to both the pre-tool deny reason and the prompt-submit directive; Cursor hosts keep the cursor daily-drivers.
- **Known limits.** Mapping keys off the hook's `--host` flag, not off which platforms are actually installed — a Cursor host with a missing Cursor SDK still gets cursor verbs (the platform lock catches that at execution time, not at recommendation time). The generic verbs assume the MCP server is registered with the host; if only hooks are installed without the MCP, any recommendation remains uninvokable — `PUPPETMASTER_AUTO_INVOKE_DISABLED=1` stays the escape hatch.

## v0.9.31

**Feature: per-model effort levels in the registry, plus an interactive registry wizard (field request: "the same model on different effort can have a huge impact on cost and capability").** Effort variants are now plain registry rows — `codex/gpt-5-5-high` and `codex/gpt-5-5-medium` compete in routing as distinct models with their own capability scores and honest token-burn cost estimates. No source edits required: everything is data in `models.json`, managed via `puppetmaster models setup` (interactive wizard) or `puppetmaster models set` (one-shot). Full suite **596** green (+13 focused tests); `ruff check puppetmaster/` clean; verified live end to end — routed effort variants executed against the real OpenAI API (`reasoning_effort=high` in the request) and the real Codex CLI (`-c model_reasoning_effort=high` on the invocation).

- **`payload_defaults` on registry entries.** A free-form dict merged under the task payload whenever the router picks that entry (explicit task-payload keys always win — single `merge_routing_payload` seam used by all four routing stamp sites). Effort rides the existing adapter knobs: `{"reasoning_effort": "high"}` for openai, `{"extra_args": ["-c", "model_reasoning_effort=high"]}` for codex. ROUTING artifacts record what the entry injected, keeping the audit trail complete. Non-dict values are rejected at registry load, not at routing time.
- **`output_token_multiplier` for honest cost estimates.** Effort changes token *volume*, not per-token price (a high-effort run can burn 3–9x the output tokens of a medium run on the same task — see Artificial Analysis' index measurements). The multiplier scales estimated output tokens in `estimate_cost_usd`, so a high-effort variant can't masquerade as the same price as its medium sibling in routing decisions or budget caps.
- **`puppetmaster models setup` — the interactive wizard.** Numbered table of your registry (with an effort summary column), guided add-effort-variant flow (suggested derived id, prefilled capability score, suggested multiplier per level), field editing, add/remove entries, y/n previews before every mutation, atomic private-perms save. EOF/closed stdin exits cleanly instead of looping; duplicate ids are refused.
- **`puppetmaster models set <id> key=value ...` — the scriptable one-shot.** `effort=high` applies the same per-adapter translation as the wizard, *merging* into existing `payload_defaults` (unrelated keys survive) and swapping the `effort:*` tag. Also accepts `capability_score`, `enabled`, `notes`, `billing`, `output_token_multiplier`, and raw `payload_defaults.<key>=<json>`.
- **Honest platform gaps, enforced in tooling.** Only openai (`none|low|medium|high|xhigh`) and codex (`low|medium|high`) expose an effort knob today. The wizard and `models set` refuse to fabricate effort variants for cursor and claude-code entries with a clear message instead of writing flags those platforms would ignore.
- **Rode along: MCP protocol-stream isolation.** The MCP server now reserves the real stdout fd for JSON-RPC frames and repoints fd 1 at stderr, so a stray `print()` (ours, a library's, or an fd-inheriting child's) can't corrupt the protocol stream — Codex's rmcp client treats the first non-frame byte as a fatal, unrecoverable transport error. Best-effort with a stderr warning when fd isolation is unavailable.
- **Known limits.** Effort for claude-code (thinking budgets) and cursor is not expressible until those CLIs/SDKs grow a knob — entries can still carry arbitrary `payload_defaults`, but nothing downstream consumes an effort field for them. The payload merge is shallow by design: a task that explicitly sets `extra_args` fully replaces the entry's `extra_args` (documented precedence — explicit task values win). Bedrock-billed Claude Code inherits the same no-effort-knob limit; Bedrock posture detection itself is unchanged from v0.9.27 and was not re-verified live this release (no AWS credentials on the release machine). `output_token_multiplier` is a user-asserted estimate, not a measured value.

## v0.9.30

**Community fix wave: stalled jobs can't masquerade as success, diff outcomes are attributable, and MCP diagnostics stop assuming Cursor.** Three contributor PRs (#7, #8, #9 — a community contributor), merged with one post-merge tidy. Full suite **584** green (+4 focused tests); `ruff check puppetmaster/` clean.

- **Stalled `await` is an error (#8).** `stalled` was already a terminal state, but CLI `await` exited 0 and MCP `puppetmaster_await_job` returned `isError: false` for it — automation gating on await would sail past a degraded run. Both entry points now match `wait`'s existing behavior: non-zero exit / `isError: true`, with regression coverage for each.
- **Diff-source outcome reporting (#7).** The ambiguous `diff_present=True` is unpacked into three honest signals on full-edit verification and PATCH payloads, edit-run CLI outcome lines, and status outcome snapshots: `baseline_diff_present` (tree was dirty before the worker), `worker_diff_present` (the worker made an attributable change), `patch_artifact_emitted` (a PATCH artifact actually exists). The patch-emission gate and the new labels share one `snapshot_has_diff` predicate, so they can't drift. `diff_present` is kept as a legacy alias of `patch_artifact_emitted` for existing consumers.
- **Client-neutral MCP diagnostics (#9).** `mcp doctor` / `mcp cleanup` no longer tell every user to "restart MCP in Cursor Settings" — Puppetmaster runs from Codex and Claude Code hosts too. Same failure-mode explanations, host-neutral wording; the no-server remedy now names both installers.
- **Known limits.** `worker_diff_present` is `False` for unattributable changes in a pre-dirty tree without a worker-scoped diff — by design, matching the patch-emission gate. Outcome lines on stderr changed format (`diff_present=` → the three new keys); anything scraping that human-facing line needs updating, while the structured `outcome` snapshot keeps `diff_present`.

## v0.9.29

**Feature: `puppetmaster_dashboard` MCP verb — "show me the job dashboard" is now a first-class ask in any IDE with the MCP registered.** Until now the dashboard only launched via the CLI, and nothing told agents that; an agent fielding "open the dashboard" had to already know the fallback. Full suite **580** green (+4 focused tests); verified live — real server spawn, HTTP probe, idempotent reuse, deep link.

- **The verb.** `puppetmaster_dashboard [job_id] [port]` checks whether a dashboard already answers on the port (reused if so — no duplicate servers), otherwise spawns the CLI server detached (`start_new_session`, loopback-only as always) and waits for it to come up before returning. The response carries the URL (deep-linked via `?job=` when `job_id` is given), pid, state dir, and an explicit `started` / `already_running` so the agent can tell the user what happened. A server that dies on startup returns a structured error with the CLI command to reproduce.
- **Agents are taught the trigger.** The managed rules block (`install-rules`) now includes: when the user asks to see/open the job dashboard, call `puppetmaster_dashboard` and open the returned URL in a browser tab — CLI fallback `python -m puppetmaster dashboard [job_id]`. Docs updated (`CURSOR_AGENT_MCP.md`, `AGENTS.md`).
- **Rode along:** the two pre-existing ruff errors (F541 f-string, F401 unused import) are cleared; `ruff check puppetmaster/` is clean again.
- **Known limits.** Port reuse is honest but blind: an already-listening dashboard may be serving a *different* project's state dir — the response says so rather than pretending. `--allow-external` is deliberately not exposed over MCP; exposing the unauthenticated board beyond loopback stays a manual CLI decision.

## v0.9.28

**Hardening + dashboard release: concurrency/security audit fixes, privacy-by-default state, and a real ops-console dashboard.** Rolls up two audit-driven fix waves (jobs `job_50c71c87c5ff` → `job_21250fad1985` and `job_20c605263497` → `job_2caeeb332399`), three community PRs (#4 parent-process visibility, #5 no-edit classification, #6 hermetic test env), and the dashboard overhaul. Full suite **576** green on all platforms (Windows CI fixed in-release); dedicated end-to-end stress harness ALL GREEN (33 checks: subprocess swarms on both backends, 12-thread claim races, lease fencing, lock TTL, redaction, permissions, live dashboard HTTP + security probes).

- **Concurrency correctness (audit fixes).** SQLite task claims are now a single conditional `UPDATE ... WHERE` (two racing workers can't both win — verified under a 12-thread race); terminal status writes are lease-fenced by `worker_id` on both backends; the worker heartbeat detects a lost lease and aborts instead of publishing artifacts it no longer owns; gated tasks fail **closed** when the gate engine errors (ungated tasks still pass); orchestrator worker subprocesses are always reaped via `try/finally`; SQLite events are written in the same transaction as the rows they describe; file-store locks carry owner+timestamp JSON with TTL-based stale-lock breaking.
- **Privacy/security defaults (hardening pass).** All sensitive state is owner-only on POSIX (dirs `0700`, files `0600` — stores, sidecars, registries, locks; CodeGraph lock was world-readable `0644`); secret-bearing payload keys (`*_api_key`, `*_token`, `*_secret`) are scrubbed at the store serialization seam so credentials never persist while live in-memory payloads keep working; live-log sidecars and shell/OpenAI stdout/stderr tails pass through `redact_secrets` before disk, which now also catches GitHub/AWS/Slack/JWT token shapes and explicitly registered MCP argument keys; the `OPENAI_BASE_URL` host allowlist moved to shared `openai_security.py` and is enforced in `api_discovery` and `preflight` too (no path can send `OPENAI_API_KEY` to an unvalidated endpoint); the CodeGraph usage ledger stores a `sha256[:12]` hash of cwd instead of the raw path; gate commands never touch a shell (`shlex` argv on POSIX, raw CreateProcess string on Windows).
- **Dashboard ops console.** Per-task cards with role/status/model/attempts, readable markdown output (hand-rolled, escape-first renderer with XSS tests executed in Node), color-coded unified-diff viewer from PATCH artifacts, plain-English routing/verification "why" lines, summary bar with per-status chips + total cost, task filters, and flicker-free 1.5s live refresh. Jobs index redesigned as aligned rows with status filter chips and relative timestamps. Token chips (`↑ in / ↓ out`) now appear for **every** adapter — estimated counts are marked `~` so they're never mistaken for measured; cost/turns/duration chips appear when the adapter records them (Claude Code envelopes).
- **Community PRs.** #4: `mcp list` shows parent PID + process name per server. #5: payloads explicitly marked `read_only`/`no_edit`/`dry_run` classify as analysis (and Codex `read-only` sandboxes keep the worktree guard even with the bypass flag). #6: pytest isolates `PUPPETMASTER_MODELS_PATH` and the platform lock, so personal config can't flip suite results.
- **Known limits.** POSIX file modes are best-effort no-ops on Windows. Payload scrubbing means a worker restarted from *persisted* state must re-read credentials from env (by design). Per-task cost/duration chips require adapter-recorded data — cursor NDJSON runs show token estimates only. Routing receipts predating this release are not rewritten.

## v0.9.27

**Feature: AWS Bedrock account support for Claude Code (field report: a Bedrock-authenticated VSCode-extension user read as "not authenticated" and lost every claude-code model).** `detect_claude_billing` only knew OAuth and `ANTHROPIC_API_KEY`, so a `CLAUDE_CODE_USE_BEDROCK` setup had no recognizable credentials: the probe returned unhealthy, `reconcile_registry` dropped all claude-code models, and `doctor` told a working Bedrock user to sign in. Full suite **537** green (+6 focused tests); verified live end to end — aws-vault STS credentials → probe reports `api`/healthy with region → `doctor` flips to ok → real Claude Code completion through Bedrock (`us.anthropic.claude-3-5-haiku`).

- **Bedrock posture detection.** Bedrock mode is recognized from a truthy `CLAUDE_CODE_USE_BEDROCK` in the process env *or* the `env` block of `~/.claude/settings.json` (how the CLI and VSCode extension persist it), parsed defensively like the OAuth probe. The branch is evaluated before `ANTHROPIC_API_KEY`, matching Claude Code's own routing precedence when both are set.
- **Honest billing classification.** Bedrock bills per-token to the AWS account, so the posture is `api` (out-of-pocket) — never misreported as subscription-covered. Detail includes the AWS region when `AWS_REGION`/`AWS_DEFAULT_REGION` is set.
- **Presence-based credential health.** `healthy=True` when AWS credentials look usable, checked in order with distinct evidence: `AWS_BEARER_TOKEN_BEDROCK` → `AWS_ACCESS_KEY_ID`+`AWS_SECRET_ACCESS_KEY` → `AWS_PROFILE` → non-empty `~/.aws/credentials`/`config`. Bedrock on with no credentials is honestly unhealthy, with an actionable detail (`aws configure` / `AWS_PROFILE` / `AWS_ACCESS_KEY_ID`).
- **Known limits.** Credential checks are presence-only by design (no boto3, no network, matching the pure-probe philosophy) — expired STS/SSO sessions still read healthy and fail at dispatch time instead of preflight. Bedrock enabled solely in the VSCode extension's internal settings (not env or `settings.json`) is invisible to subprocess probes. Vertex AI (`CLAUDE_CODE_USE_VERTEX`) is not yet detected.

## v0.9.26

**Feature: `puppetmaster uninstall` — the exact inverse of `setup` (field report: "How do I uninstall?" had no good answer).** Until now `pip uninstall puppetmaster-ai` removed the code but stranded every host-side integration: dead MCP entries in Cursor/Codex config, `beforeSubmitPrompt` hooks firing a missing module on every prompt submit, and orphaned rules instructing agents to use tools that no longer exist. The new command removes all of it, then tells you to run `pip uninstall puppetmaster-ai` as the final step. Full suite **531** green (+6 focused tests); dry-run verified live against a real install.

- **MCP registrations.** Removes only the `puppetmaster` entry from workspace and global `.cursor/mcp.json` (other servers untouched) and the `[mcp_servers.puppetmaster]` table from `~/.codex/config.toml` — prefers `codex mcp remove`, with a TOML-strip fallback so a missing CLI can't strand the table.
- **Rules.** Deletes `.cursor/rules/puppetmaster.mdc` (owned wholesale) and strips the managed marker block from `AGENTS.md`, `CLAUDE.md`, `~/.claude/CLAUDE.md`, and `~/.codex/instructions.md`, leaving surrounding content byte-identical; files left whitespace-only are deleted.
- **Hooks.** Removes Puppetmaster hooks from `.cursor/hooks.json` / `.claude/settings.json` at both project and global scopes; foreign hooks are preserved.
- **Processes & state.** Kills stale MCP daemons (same logic as `mcp cleanup --kill-stale`). State dirs (`~/.puppetmaster/`, `<cwd>/.puppetmaster/`, `.codegraph/`) are kept unless `--purge-state`.
- **UX.** `--dry-run` previews every action without writing; `--yes` skips confirmation; per-target status lines (`removed` / `unchanged` / `would_remove` / `error`); idempotent — a second run reports `unchanged`; exit 1 if any target errored.
- **Known limits.** Project-scoped artifacts are removed only for the `--cwd` workspace — other repos where `install-rules` / `install-hooks` ran at project scope must run `uninstall` from each of those directories (global-scope artifacts are removed once, for all). The pip package itself is step 2: `pip uninstall puppetmaster-ai`.

## v0.9.25

**Fix: every relative link in the README 404'd on PyPI (field report from Slack — `pypi.org/project/puppetmaster-ai/docs/WHY.md`).** GitHub rewrites relative markdown links into the repo; PyPI's renderer does no such rewriting, so all `docs/*.md` links, directory links, and the three `<img src="docs/...">` tags were broken on the project page. All 27 relative refs are now absolute (`github.com/...` for links, `raw.githubusercontent.com/...` for images). Docs-only; no code changes. (v0.9.24 shipped the absolute-link rewrite; v0.9.25 follows up because PyPI *also* strips heading `id`s — verified against `readme_renderer`, PyPI's own pipeline — so the in-page anchor links `#what-it-does` / `#quickstart` / `#auto-invocation` pointed at nothing and now target the GitHub README's anchors.)

- **Known limits.** Anchor links on the PyPI page now bounce readers to GitHub instead of jumping in-page — the only behavior PyPI supports. PyPI never re-renders a published version's description, hence the two version bumps for a README fix.

## v0.9.23

**Fix: a Claude-Code-plan user's swarms never routed to Claude Code (field report, reproduced with diagnostics).** Three coordinated defects, all fixed: the router costed every model at its *nominal* registry price (so a plan-billed Claude model with real $5/$25 prices lost every cost comparison to a $0 plan peer and got costed out of budgets on dollars the user would never spend); the main routing path (`_apply_auto_routing`) never reconciled detected billing/health onto the registry (OAuth-plan Claude stayed `unknown`-billed, credential-less adapters stayed candidates and could only fail preflight); and even when a plan model was picked, the ROUTING artifact reported the fictional nominal cost, poisoning the savings ledger. Full suite **525** green; cross-platform stress harness ALL GREEN.

- **Marginal-cost routing, honest end to end.** New `ModelSpec.marginal_cost_usd()` — $0 for in-subscription (plan-billed) models, the registry rate otherwise. `route_task` optimizes on marginal cost in the budget gate, every policy's selection keys, and every "pricier than" rejection. `RoutingDecision.estimated_cost_usd` is now the marginal truth, with new `nominal_cost_usd` / `baseline_nominal_cost_usd` fields preserving registry-rate transparency on the artifact; the savings baseline compares marginal-to-marginal.
- **Audited registry reconciliation before routing.** `reconcile_registry()` (platform_billing) upgrades `unknown` billing to each adapter's *detected* posture and drops adapters with no usable credentials — fail-open on detection errors, never returns empty. The orchestrator now runs it on the main routing path, emits a `router.registry_reconciled` event, and appends dropped candidates to each ROUTING artifact's `rejected` list, so "why not X" stays answerable. No silently vanished candidates.
- **TTL-cached billing detection.** `detect_adapter_billing_cached()` (default 300s, `PUPPETMASTER_BILLING_TTL_SECONDS`, `clear_billing_cache()` for tests) makes per-job reconciliation affordable — detection can shell out with a 15s subprocess timeout. The auto-fallback path uses the same cache.
- **Known limits.** Billing posture is cached for up to 5 minutes, so signing into an adapter mid-session may take one TTL window (or a new process) to be noticed. Reconciliation drops adapters only on a *definitive* unhealthy probe; an erroring probe keeps the adapter as a candidate (fail-open by design). Routing receipts created before this release recorded nominal costs for plan-billed picks and are not rewritten.

## v0.9.22

**Feature: Claude Fable 5 is the new frontier tier, with graceful degradation when an account doesn't have it.** Anthropic shipped Claude Fable 5 (API id `claude-fable-5`, 2026-06-09) above Opus 4.8 on effectively every published benchmark — but access is genuinely uneven: fully available on the Claude API and consumption-based Enterprise, included in subscriptions only through 2026-06-22, and absent from many seat-based enterprise plans. Puppetmaster now routes the hardest tasks to Fable 5 wherever it exists and degrades loudly-but-automatically where it doesn't. Full suite **514** green; new cross-platform mock stress harness (`bench/stress_fable5_rollout.py`) ALL GREEN (39 checks, zero spend).

- **Registry seeds.** `cursor/fable-5` (capability 100, plan-billed $0 — SOTA on CursorBench) and `claude-code/fable-5` (`claude-fable-5`, $10/$50 per MTok, 1M context, `billing="unknown"` with honest staged-access notes). Opus 4.8 stays at 99 as the declared fallback tier. Cursor catalog discovery learned a frontier-kin alias (`fable-5` → `claude-fable-5`) so a plan-discovered Fable ranks at its true capability instead of the conservative seed.
- **Classifier ceiling 99 → 100.** `classify_capability_needed` now demands the new flagship for the absolute-hardest tasks instead of saturating one notch below it. Trivial and mid-tier routing is unchanged — the 2× price tier is structurally reserved for work that needs it.
- **`model_unavailable` is now a recoverable failure.** Adapters classify provider-shaped "model not on this account" responses (Anthropic `not_found_error` / `permission_error`, Cursor forbidden/unknown-model rejections, OpenAI/Codex `model_not_found`) into `model_unavailable`, and the existing auto-fallback loop re-routes the task to a funded platform with `fallback_reason` recorded on the ROUTING artifact. An enterprise account without Fable degrades to Opus 4.8 instead of failing the task.
- **Known limits.** No pre-dispatch availability probe yet — the first dispatch to a Fable-less account spends one failed worker run before fallback re-routes (deliberate: build the probe when fallback artifacts show real churn, likely after the 2026-06-22 subscription cliff). Fable 5's own server-side safety classifiers route some cyber/bio-flavored prompts to Opus 4.8 (<5% of sessions, per Anthropic) — precisely the security-review/audit roles most likely to score capability 100 — and that handoff is invisible to Puppetmaster's audit trail. Savings receipts now baseline against the pricier flagship, so routing "savings" rise mechanically; read them accordingly.

## v0.9.21

**Fix: Codex couldn't keep the Puppetmaster MCP server alive (field report).** Codex's MCP client enforces a hard per-tool `tool_timeout_sec` — default **60 seconds** — that cancels a call no matter what, while Cursor tolerates arbitrarily long calls as long as bytes flow. Puppetmaster's long-poll tools (`live_artifacts_follow`, `await_job`) passed the caller's `timeout_seconds` straight into a blocking wait, so a Codex agent that asked for a multi-minute block got guillotined mid-wait, saw a dead tool, and concluded Puppetmaster was broken. Three compounding gaps, three layers of fix — verified live against the real Codex client (`codex exec`, gpt-5.5): a 120s requested block now returns capped in 45s with a clean completion. No breaking API changes; full suite **510** green (+8 focused tests).

- **Server-side block cap (the keystone).** `live_artifacts_follow` and `await_job` now clamp their effective block to `PUPPETMASTER_MCP_MAX_BLOCK_SECONDS` (default **45s**, comfortably under Codex's 60s ceiling; `0` disables). Both tools are designed to be re-called (`next_cursor` / `timed_out`), so the cap turns one long block into a sequence of short polls — the push-feeling stream still works, on every client. When the cap shortens a request the response is stamped `capped: true` with `requested_timeout_seconds` / `effective_timeout_seconds`, so the agent reads "nothing yet, call me again" instead of inferring an empty job.
- **Spec-compliant progress notifications.** The tool-call keepalive now reads `params._meta.progressToken` from each `tools/call` and emits strictly-monotonic `notifications/progress` frames alongside the existing `notifications/message` liveness logs — the actual MCP mechanism compliant clients use to keep a long call alive. Only clients that send a token receive progress; Cursor's behavior is unchanged. `initialize` now also advertises the `logging` capability, making the keepalive frames spec-legal for strict clients.
- **Install-time Codex timeouts.** `install-codex-mcp` now writes `startup_timeout_sec = 30` / `tool_timeout_sec = 300` into the `[mcp_servers.puppetmaster]` table (`codex mcp add` exposes no flag for them). Absence-only: an existing user value is never clobbered, and a missing table is a warning, not an error. Cold Python imports no longer race the 10s startup default, and a genuinely-long single call survives up to 300s.
- **Known limits.** The synchronous verbs (`puppetmaster_swarm` et al.) still block until the job finishes by design — on Codex they survive only up to the configured 300s ceiling. For long jobs on Codex, use the async pattern: a `start_*` verb, then bounded `await_job` / `live_artifacts_follow` re-polls. The post-install guidance now says exactly that.

## v0.9.20

**Fix: Codex (and Claude Code) adapter runs looked stalled because they ran behind a flat, silent blocking wait (field report).** A long `codex exec` produced **zero visible output and no liveness signal** until it either finished or hit the 600s timeout — so an actively-working run was indistinguishable from a hung one, and at least one user stopped relying on the Codex adapter for exactly that reason. The Cursor implement adapter already solved this in v0.9.12 (stream output to a live sidecar log + a `still working` heartbeat); Codex and Claude Code never got that treatment and kept using `subprocess.run(..., capture_output=True)`. This routes both through the same proven `run_streamed_subprocess` path. No breaking API changes; full suite **502** green + the E2E stress harness (`bench/stress_invocation.py`) still ALL GREEN.

- **Codex and Claude Code now stream.** Both adapters run via `run_streamed_subprocess`, teeing stdout/stderr line-by-line to `<state>/jobs/<job>/tasks/<task>/{codex_exec,claude_implement}_live.log` and writing a `[puppetmaster] still working: Ns elapsed …` heartbeat every 30s, so a multi-minute run is visibly alive. The verification (and timeout) artifacts now carry a `live_log` path for parity with Cursor, so `puppetmaster show <job>` points the operator straight at the streaming log.
- **stdin is closed for every streamed run (the actual stall fix).** The shared runner now launches the child with `stdin=subprocess.DEVNULL`. A CLI that reads stdin on a non-interactive worker would otherwise block forever waiting on terminal input — a silent, indefinite stall that no timeout shorter than the wall-clock cap would surface cleanly. The legacy blocking path passed `input=""` to get this EOF behavior; the streamed path now guarantees it for Codex, Claude Code, and Cursor alike. (Safe no-op for the Cursor node runner, which takes its input via env, not stdin.)
- **Timeout semantics unchanged.** A timed-out run still reports `failed` + `timeout`, preserves any partial diff as a `PATCH`, and now also surfaces the `live_log` so you can see exactly how far it got. The default 600s ceiling is intentionally untouched — the fix is visibility + a closed stdin, not a shorter leash that would fail legitimately-long runs.
- Added 4 focused tests: Codex/Claude route through the streamed runner and never touch the blocking `subprocess.run`, both surface `live_log` (incl. on timeout), and a behavioral test proving a stdin-reading child sees EOF immediately instead of hanging.

## v0.9.19

**Fix: auto-invocation hooks were over-aggressive on read-only inspection (field report).** v0.9.16/0.9.17's pre-tool deny-redirect hard-denied *every* native `Grep`/`Glob`, and any shell command where a search tool was followed by a `/` anywhere — so benign read-only work (a plain `git log`, listing a config dir, grepping a single file, even reading this changelog) got blocked, contradicting the "never wedge the session" contract the hooks promise. The classifier is now scoped to redirect only genuinely broad operations, with an explicit read-only carve-out. No breaking API changes; full suite **498** green + the E2E stress harness (`bench/stress_invocation.py`) extended with carve-out checks.

- **Native search tools (`Grep` / `Glob` / `codebase_search`) are no longer hard-denied.** Their per-call scope isn't visible in the host hook payload, so there's no reliable way to tell "grep one file" / "list one config dir" from a repo-wide sweep — and blocking them all obstructs legitimate inspection. Steering toward Puppetmaster for broad search now rides entirely on the **non-blocking** prompt-submit directive. The built-in `Task` fan-out (unambiguously "should be a swarm") is still redirected.
- **Read-only shell inspection is explicitly carved out** — `git log` / `show` / `diff` / `status` / `blame`, `ls`, `cat`, `head` / `tail`, `tree`, etc. always pass through.
- **Broad-shell detection tightened.** Only a search tool with an explicit recursive signal (`-r` / `-R` / `--recursive`, `**`, or `find -name/-path`) is redirected; `rg pattern src/app.ts` and `grep TODO file.py` are not. The old rule matched any `/` in the command, so a single-file path tripped it.
- Also made a non-hermetic doctor test hermetic — it read the real `~/.codex` / `~/.claude`, which started failing once v0.9.17's `--global-rules` made global rule installs a normal thing. The check itself was correct; the test now pins a clean home.

## v0.9.18

**Semantic review gate — a strictly-stronger model must approve an implement diff before it can COMPLETE.** The hole this closes: when Puppetmaster fans out implement work across many auto-routed (often cheap) workers, the only objective acceptance check was `require_diff` — "files changed", not "the code is good". A worker that exited 0 got a hardcoded `confidence=0.9` regardless of quality (`adapters.py`: `confidence=0.9 if returncode == 0`), and the confidence-based auto-escalation keyed off exactly that self-report, so a confidently-mediocre cheap model sailed through. This adds the missing semantic acceptance control as a non-bypassable, fail-closed completion gate, riding the existing gate chokepoint with no new lifecycle machinery. Off by default (one extra model call is a deliberate, flagged spend); flip `PUPPETMASTER_REVIEW_GATE=1` to enforce. No breaking API changes; full suite **495** green (+23 focused tests).

- **New `review` gate kind (`puppetmaster/gates.py`).** After the cheap gates pass, a judge model reads the actual diff and must approve it before the task reaches COMPLETE. `resolve_judge_model` picks the cheapest model **strictly stronger** than the implementer (the router-stamped `router_model_id`), falling back to the top-tier peer when the implementer is already at the ceiling — so the writer is never its own judge. `build_review_prompt` / `parse_review_verdict` are pure and unit-tested; the live call is isolated behind an injectable `_REVIEW_JUDGE` seam so the suite never hits a model. **Fail-closed:** a judge that runs but returns no parseable verdict, times out, or crashes rejects the diff. The one deliberate fail-*open* is "no judge configured / feature off" → no-op, so an unconfigured environment is never bricked.
- **Auto-attached for implement behind one flag.** `task_gate_specs` adds the review gate to `mode=implement` tasks when `review=true` (per task) or `PUPPETMASTER_REVIEW_GATE` (globally), ordered **last** so a no-op or test-failing task never pays for a judge call. Opt a single task out with `review=false` / `allow_unreviewed`. Cost controls: bounded diff (`max_diff_chars`), deterministic per-task `sample` fraction for spot-check mode on large swarms.
- **Objective escalation (`puppetmaster/orchestrator.py`).** A review rejection now re-routes the task one capability tier up (`_auto_review_escalate` / `_reroute_failed_review`), keyed off the **review GATE result** rather than self-reported confidence — and therefore **on by default**, unlike the confidence path. Bounded by `_MAX_ESCALATION_ATTEMPTS`; a routing artifact records each upgrade.
- **Fail-closed reachability fix.** A review-rejected task is FAILED, which would have made `_run_workers` raise (fail-closed) *before* the escalation sweep ran — leaving the better model no turn. `_review_pending_reroute_ids` treats a rejected task with remaining budget as "pending re-route" (like a recoverable failure) so escalation gets its turn; once the budget is exhausted the job still fails loudly. (Caught by the design swarm's redteam pass and covered by a regression test.)
- **Known limits.** The guarantee is only as strong as the judge you point at it — it's a quality bar, not a proof of correctness; pair it with a `command`/test gate for objective checks. The judge reads a bounded slice of large diffs. Capability scores remain hand-authored (no feedback loop into routing — deliberately not built). With the cursor platform lock + plan-billed models, judge calls bill against the plan ($0 marginal); on api-billed models a rejection's re-implementation is real out-of-pocket spend, capped at the escalation bound.

## v0.9.17

**Global (user-level) auto-invocation hooks — cover every repo without re-running setup per project.** v0.9.16 installed hooks per-workspace (`.cursor/hooks.json` / `.claude/settings.json`), which only protects the repo you ran `setup` in. This adds a `global` scope that writes user-level hooks to `~/.cursor/hooks.json` and `~/.claude/settings.json`, so a single install makes the host agent delegate-aware in *every* repo the user opens. No breaking API changes; full suite **472** green + the E2E stress harness (`bench/stress_invocation.py`) extended with global-scope checks.

- **`puppetmaster install-hooks --global`** and **`puppetmaster setup --global-hooks`** install at user scope instead of the workspace. Only the base directory differs between scopes — the `.cursor` / `.claude` subpaths are identical — and the hook command is an absolute `python -m puppetmaster invocation-gate` (no relative script path), so a user-level Cursor hook (which Cursor runs from `~/.cursor/`) resolves the same as a project one. Idempotent and non-destructive at both scopes; an unknown scope is a loud error rather than a silent no-op.
- **`setup` reports the scope it used** and nudges toward `--global-hooks` when it installed per-repo, so the broader option is discoverable.
- Added 5 focused tests (global writes under `~` not cwd, idempotent + labeled, unknown-scope error, `install-hooks --global`, `setup --global-hooks`) plus a global round-trip in the stress harness.

## v0.9.16

**Auto-invocation: make host agents reach for Puppetmaster on their own, with as little user prompting as possible.** The #1 field complaint was that Cursor / Claude Code / Codex grind multi-file work inline unless the user manually reminds them to delegate — the soft rule text Puppetmaster writes decays with context distance and task momentum. This release adds a layered, classifier-gated enforcement system on top of the rules: a pure delegation gate, deterministic host hooks, and an optional provider proxy — wired into `setup` so a fresh install gets it all. Honest about the ceiling: universal *deterministic* invocation is impossible on closed harnesses (you can't sit on Cursor's provider wire or force an MCP call), so the design is explicitly tiered — soft everywhere, hard where hooks/proxy exist — and fully kill-switchable. No breaking API changes; full suite **467** green + a 6-stage end-to-end stress harness (`bench/stress_invocation.py`).

- **The delegation gate (new `puppetmaster/invocation_gate.py`).** `should_delegate(prompt) -> DelegationDecision` reuses the router's existing pure-function classifier (`classify_capability_needed`) — one capability heuristic in the codebase, no second model, no LLM call, microsecond-scale. It layers the delegate-vs-inline *policy* on top: a trivial-task carve-out first (typos / renames / format / one-liners / quick factual questions stay inline), a conservative default threshold (60), explicit `use puppetmaster` / `do it inline` overrides, a broad-scope override that catches *implied* multi-file work (audit / refactor / migrate / trace / data-flow), and `infer_role_and_verb` mapping each prompt to the right Puppetmaster verb. Dry-run it with `puppetmaster should-delegate "<prompt>"`.
- **Deterministic host hooks (new `puppetmaster/hook_runner.py` + `puppetmaster/hook_installers.py`).** `puppetmaster install-hooks` writes idempotent, non-destructive entries into Cursor's `.cursor/hooks.json` and Claude Code's `.claude/settings.json`. On prompt submit they **inject** a "delegate now with `<verb>`" directive; on a broad native tool call (Grep / Glob / built-in Task / repo-wide shell search) they **deny-redirect** to the Puppetmaster / CodeGraph equivalent — always exempting `puppetmaster_*` and focused reads. Each hook shells into `python -m puppetmaster invocation-gate`, stays import-light, and **fails open** so a hook can never wedge the host session.
- **Optional provider proxy (new `puppetmaster/provider_proxy.py`).** A zero-dependency OpenAI-compatible local proxy (`puppetmaster proxy`) for the API-key / SDK clients that closed harnesses can't hook — runs the gate on inbound prompts and either *advises* (synthetic local delegate reply, no upstream) or *injects* a system directive and forwards to a vetted upstream. An HTTPS + loopback allowlist refuses to forward keys to an arbitrary host. Headroom's `wrap` / proxy referenced as transport prior art only — not adopted (its compression / memory / code-graph layers overlap what Puppetmaster already owns).
- **Sharper soft layer + one-shot setup.** `install-rules` now tells the agent to prefer Puppetmaster *before* native Grep/Glob/Task and to run the gate when unsure; the `puppetmaster_*` MCP tool descriptions self-advertise against the built-in Task tool. `puppetmaster setup` gains a 7th step that installs the hooks, so `pipx install -U puppetmaster-ai && puppetmaster setup` provisions the whole stack. Global kill switch: `PUPPETMASTER_AUTO_INVOKE_DISABLED=1` (alias `PUPPETMASTER_INVOCATION_GATE_DISABLED=1`); tune the bar with `PUPPETMASTER_AUTO_INVOKE_THRESHOLD`.
- Added 28 focused tests (gate policy, hook payload translation, proxy logic + upstream allowlist, idempotent + non-destructive installer, `setup` step gating); full suite **467** green.

## v0.9.15

**Windows hotfix for v0.9.14's per-worktree port isolation.** The B1 `reserve_port` probe set `SO_REUSEADDR`, which has inverted semantics on Windows — it lets `bind()` succeed *over* an actively-listening socket, so the liveness check reported a busy port as free and `reserve_port` handed back occupied ports. On Windows, collision avoidance was effectively a no-op. The probe now only sets `SO_REUSEADDR` on POSIX (where it correctly distinguishes a live listener from a reclaimable `TIME_WAIT` socket); Windows uses the default exclusive bind, which already fails on an occupied port. Verified green on the `windows-latest` CI matrix. POSIX behavior is unchanged. No API changes.

## v0.9.14

**Data-loss guards, parallel-worktree isolation, and progress-aware lifecycle — the safety pass on top of v0.9.13's loud-failure work.** A real multi-worktree migration session surfaced the next tier of sharp edges: state GC that could in principle reach the active worktree, fixed ports that collided when many worktrees ran at once, "implement" tasks that could complete without a diff, generated artifacts polluting worker commits, and long-but-healthy workers killed by a flat timeout. This release hardens the destructive paths, isolates per-worktree runtime, makes completion honest about whether work actually landed, and adds a generic seam for "validate only affected specs." No breaking API changes; full suite **439** green + a 10-scenario end-to-end stress harness (`bench/stress_new_features.py`).

- **State deletion can't escape its job tree (D1) — `puppetmaster/store.py` + `sqlite_store.py`.** `delete_job` now routes through a `_assert_safe_job_dir` containment guard that refuses any path resolving outside the store's jobs root (traversal/symlink/`..`), so a malformed job id can never delete unrelated state. The CLI `gc --force --all-projects` sweep additionally **never hard-deletes the active worktree's own state** — the current project is reported dry-run-only even under a machine-wide force sweep, while other projects reap normally.
- **Per-worktree port isolation (B1) — new `puppetmaster/ports.py`.** Workers no longer share a fixed port. A deterministic hash maps each worktree to a port block (range capped below 32768 to stay clear of the Linux ephemeral range), and a `reserve_port` bind-test-and-increment finds an *actually free* port from that hint — collapsing observed collisions at 16 parallel worktrees from a measured rate to zero by falling back to an OS-assigned port when a block is busy. Injected into the Cursor and Claude adapter subprocess env via `apply_worktree_ports`.
- **Implement tasks must show a diff (A3) — `puppetmaster/gates.py`.** `mode=implement` tasks now default to `require_diff`, so a worker can't report `COMPLETE` after a silent no-op; opt out explicitly only when a no-diff outcome is legitimate.
- **Declarative write-scope + cross-worker conflict prediction (B3/C1) — new `puppetmaster/conflicts.py` + a `write_scope` gate.** A task can declare the path globs it's allowed to touch; the gate fails a worker that writes outside its declared scope. Before launch, the orchestrator predicts overlapping write scopes across parallel tasks and emits a conflict-warning artifact so colliding edits surface up front instead of as a merge surprise.
- **Generated artifacts stay out of commits (C2).** The `committed` gate's auto-commit accepts an `exclude` list: matching paths are unstaged and appended to `.gitignore` instead of being committed, so build output / caches / logs don't pollute a worker's diff.
- **Progress-aware worker timeouts (A4) — `puppetmaster/orchestrator.py`.** The flat `process.wait` is replaced by an adaptive wait that extends a worker's deadline while it's demonstrably progressing (rising event count) and still hard-kills a genuinely wedged worker at a cap — long, healthy runs no longer die on the clock, and stuck ones still get reaped with a clear timeout artifact.
- **Honest terminal-quality signal (A2/F2).** `status` and run completion now surface an `outcome` block — run quality plus whether a diff and a commit are actually present — so a "completed" job that produced nothing is visible at a glance instead of masquerading as success.
- **"Validate only affected specs" seam (B2) — new `puppetmaster/affected.py` + `puppetmaster affected` CLI.** Puppetmaster supplies the changed set (explicit, stdin, or `git diff` range); you supply the changed-file→spec mapping via declarative rules (with `{stem}`/`{dir}` templating and glob resolution) or a command. Resolves the affected spec/test paths generically without Puppetmaster guessing anyone's test layout.
- **Carries the prior in-flight batch:** `partial_summary` phrasing, collapsed/quieter `logs` output, honest MCP `launcher_pid` reporting, worker memory-distillation in the adapters, and a `--no-edit` run flag — all previously uncommitted, now shipped together.

## v0.9.13

**Loud failures, non-bypassable completion gates, and honest token metering — driven by a long real migration session (35 jobs / 37 workers / 18 worktrees).** The throughline of the field report: Puppetmaster's orchestration was solid but its failure modes were too quiet — silent no-ops on a dirty tree, workers that "succeeded" without committing, degraded SDK results indistinguishable from empty ones, and no token truth for the dominant plan-billed runtime. This release makes failures loud, bakes in an anti-drift gate, meters consumption across every adapter, and adds lifecycle tooling for efforts that sprawl across many worktrees. No breaking API changes.

- **Zero-work runs fail loudly (#1, #10).** A worker that refused to run (`result="blocked"`, e.g. a dirty worktree) or produced only verification/degraded artifacts no longer reports `COMPLETE`. A new `_blocked_verdict` veto forces such tasks to `FAILED`, a built-in `assess_run_quality` classifies every run `ok`/`degraded`/`empty`/`blocked`, and the CLI prints the verdict and exits non-zero so a "completed" job that did nothing can't masquerade as success.
- **Non-bypassable completion gates (#2, #11) — new `puppetmaster/gates.py` + `ArtifactType.GATE`.** An agent can't mark a task `COMPLETE` unless its post-conditions hold: `require_diff` (an edit task must produce a diff), `command` (a semantic oracle must exit 0), `ratchet` (a monotonic metric the runtime owns and only ever tightens — gaming the scalar gets caught by the oracle), and `committed` (clean tree, with optional runtime-performed commit + author). Each gate emits a `GATE` artifact so audits explain exactly why a task passed or failed. **New `puppetmaster gate` CLI + `puppetmaster_gate` MCP verb** replay the same gates against any working tree on demand, for a parent agent or CI.
- **Honest token metering, now universal (#5, #6).** Per-run `tokens_in`/`tokens_out` are captured even when marginal cost is `$0` (plan-billed): measured from the SDK usage object where available, else a clearly-flagged `tokens_estimated` char/4 approximation. Wired through **every adapter** — Cursor and Claude Code via the runner's JSON `usage`, Codex via `turn.completed.usage`, OpenAI via the API `usage` block — so the dominant runtime is no longer invisible. Pre-flight router numbers are now explicitly labeled routing-only estimates, never presented as consumption.
- **Salvage degraded stdout + loud liveness (#3, #9).** When SDK capture comes back empty but the structured result is sitting in raw stdout, the analyze path re-parses stdout before declaring a degraded run. `status` now prints a glanceable liveness verdict (pid alive? heartbeat age?) so a wedged job screams instead of quietly showing `running`.
- **Workers can self-serve the CodeGraph CLI (#4).** Every worker subprocess is handed a `PYTHONPATH` that puts this install first (`inject_worker_cli_env`), so a worker's `python -m puppetmaster codegraph …` resolves the same subcommands the parent has instead of a stale pip build.
- **Effort rollup + state GC (#7, #8) — new `puppetmaster/lifecycle.py`.** Jobs spread across many worktrees can be tagged with one `effort_id` (`PUPPETMASTER_EFFORT_ID` / `run --effort`) and aggregated with **`puppetmaster rollup`** (jobs/artifacts/cost/tokens across every project state dir; **`puppetmaster_rollup`** MCP verb). **`puppetmaster gc`** reaps durable state for old terminal jobs so per-worktree state dirs stop piling up — dry-run by default, `--force` to delete, `--all-projects` to sweep the machine (**`puppetmaster_gc`** MCP verb).
- **Windows store hardening.** `read_json`/`write_json` retry briefly on a Windows sharing-violation (`PermissionError`) to fix a flaky CI file-lock race under concurrent worker I/O.
- Added ~30 focused tests (gates, quality verdicts, token aggregation, salvage/liveness, lifecycle GC/rollup, universal Claude metering, gate-replay CLI, gc/rollup/gate MCP verbs); full suite **416** green.

## v0.9.12

**Field-report fix pass: a dead orchestrator can no longer masquerade as a running job, and the run/CLI surface stops hiding read-only swarms and silent worker output.** Driven by a real JAD UI migration session where a backgrounded orchestrator got reaped and left its job wedged in `running` forever, `show` crashed with a raw `Errno 2`, and an analysis swarm's `implement` role wrote nothing while looking like it should. Eleven grounded fixes; no breaking API changes.

- **Stalled-job liveness + reaper (new `puppetmaster/liveness.py`).** The orchestrator now stamps a heartbeat sidecar (`orchestrator.json`: pid/host/time) and refreshes it across worker batches. A new reaper transitions a dead-orchestrator job `running -> stalled` (new `JobStatus.STALLED`) when its pid is provably gone — or when no task holds a live lease and nothing has progressed past `PUPPETMASTER_STALL_AFTER_SECONDS` (default 180) — and **auto-requeues lease-expired tasks** so no manual `recover` is needed. Wired into `status`/`jobs`, plus a new `reap` command. A dead job is never reported as live again.
- **`finalize` / `wait` / graceful `show`.** `show` now degrades to a live preview with a friendly "job not finalized — run `finalize`" note instead of a raw `[Errno 2] … stitched.md`. New `puppetmaster finalize <job>` force-stitches and completes a job whose orchestrator died before stitching. New `puppetmaster wait <job> --timeout` blocks until `complete`/`failed`/`stalled` and exits non-zero on the bad states; `await` now treats `stalled` as terminal.
- **Honest swarm mode banner.** A read-only analysis run no longer looks like it edits code: a mode classifier + one-line banner (`mode=analysis (read-only)` vs `mode=edit`) prints on `run`/`cursor`/`claude`/`codex`/`openai` and emits a `job.mode` event; the analysis-swarm `implement` role is documented as plan-only.
- **`cursor --implement` streams progress.** Implement runs tee stdout/stderr to a live sidecar log line-by-line with a `still working` heartbeat every 30s — no more 0-byte-log-looks-hung. Worker startup failures now leave a trace (`startup_error-*.log` + `worker.startup_failed` event) instead of vanishing.
- **Router on direct adapters.** `cursor`/`claude`/`codex`/`openai` gain `--auto-route` (+ `--routing-policy` / `--max-cost-usd` / `--min-capability`), pinned to the invoked adapter, so a direct run emits a ROUTING artifact + cost stamp like a swarm does.
- **Quality-of-life.** `codegraph` passthrough accepts `--cwd`/`--timeout` after the subcommand (e.g. `codegraph init --cwd …`) and disables child pagers when stdout isn't a TTY. New `mcp doctor` distinguishes a dropped stdio pipe (daemon healthy → restart MCP in Cursor) from a genuinely dead server.
- Added 11 focused friction-fix tests; full suite **388** green.

## v0.9.11

**User-feedback pass: fresh-perspective audits, delegate-first agent rules, and first-class Codex verbs.** Driven by field reports that PM audits were inheriting their own prior conclusions (circular self-evaluation) and that IDE chat agents (e.g. JetBrains AI Chat + Codex) kept solving multi-step tasks inline instead of delegating. This release makes evaluative and swarm runs judge fresh by default, hardens the rules `install-rules` writes from a soft nudge into a delegate-first directive, and promotes the existing Codex CLI adapter to first-class MCP verbs. No breaking API changes.

- **Audits/reviews now run with a fresh perspective by default** — evaluative roles (`audit`, `redteam`, `review`, `conflict-auditor`, `test-coverage-reviewer`, …) no longer inherit prior promoted conclusions, which previously let an audit anchor on the very claims it was meant to test. Added a `disable_memory` payload override (`True` → always fresh, `False` → always inject, absent → role-based). Implement runs still receive memory for continuity.
- **Swarm runs default to a fresh perspective** — `puppetmaster_start_swarm` / `puppetmaster_start_cursor_swarm` (the read-only analysis entry points) stamp `disable_memory: true` on every worker unless the caller passes `disable_memory: false`; the CLI `run` verb gains `--disable-memory` / `--enable-memory`.
- **Hardened delegate-first agent rules** — `puppetmaster install-rules` now writes a trigger convention (`Use Puppetmaster to …` / `PM this …`) and a delegate-first gate for multi-file/audit/refactor work, with a tight inline carve-out for trivial edits and a native-tooling fallback. One change updates the Cursor `.mdc`, `AGENTS.md`, Codex instructions, and `CLAUDE.md` alike.
- **First-class Codex MCP verbs** — `puppetmaster_codex` and `puppetmaster_start_codex` mirror the Claude/OpenAI worker verbs, driving the existing `codex exec --json` CLI adapter directly instead of only via `start_implement --adapter codex`.
- Full suite **377** green; `ruff check puppetmaster/` clean.

## v0.9.10

**Workers can now self-serve CodeGraph mid-task; plus the deferred lint cleanup.** Puppetmaster runs each worker in a hermetic SDK sandbox, so CodeGraph reaches workers as an injected one-time context snapshot rather than as live tools — which meant reverse-dependency / blast-radius checks fell back to ripgrep/git once the snapshot went stale. This release tells workers they can refresh that view themselves. No sandbox or behavior change to the orchestrator.

- **Workers told they can refresh their own CodeGraph view.** Every adapter (cursor/claude/codex) injects CodeGraph through one helper, `codegraph_prompt_section`. It now instructs workers that the snapshot is a one-time view and that they can refresh or expand it on demand — to trace callers, reverse dependencies, or a change's blast radius — via the ABI-safe `python -m puppetmaster codegraph search/context/affected` CLI they can already shell to, with a graceful ripgrep/git fallback when the CLI isn't importable in the worker's environment. This is prompt-level guidance only: no change to the `settingSources: []` sandbox and no per-worker MCP boot.
- **Lint cleanup (carried over from the v0.9.9 cycle).** Cleared the pre-existing ruff errors that predated the audit work: hoisted `dashboard.py`'s models/store/stitcher imports to the top of the module (E402) and moved `worker_runtime.py`'s `SwarmStore` import under `TYPE_CHECKING` (F821, zero runtime cost with `from __future__ import annotations`). `ruff check puppetmaster/` is now clean.
- Added `test_codegraph_prompt_section_advertises_self_serve_cli`; full suite **368** green.

## v0.9.9

**Fifth-pass audit: fixes two store regressions the v0.9.8 optimization pass introduced, plus parity hardening.** A follow-up CodeGraph + review swarm against this repo caught that two of the v0.9.8 store changes diverged the SQLite backend from the file backend. This patch restores exact cross-backend parity (with one verified repro fixed) and tightens the new batch APIs. No public API changes.

- **REGRESSION fix — SQLite `retrieve_memory` multi-term queries silently dropped results.** v0.9.8 pushed each query term into SQL as a separate **ANDed** `instr(lower(data), ?)` clause, so a record had to contain *every* term to be fetched — while the file backend keeps any record matching *any* term (score > 0). A two-term query like `"workers nonexistent"` returned matches on the file store but **0 rows** on SQLite. The term clauses are now combined into a single **OR** group, AND'd with the exact-match filters, so SQL is a pure superset prefilter and the existing Python scoring stays authoritative — results now match the file backend exactly.
- **REGRESSION fix — `save_artifacts` event atomicity.** v0.9.8's batch `save_artifacts` wrote all artifact rows in one transaction, then emitted `artifact.saved` events afterward in separate transactions; a crash in between could leave artifacts durable with missing events (unlike `save_tasks`, which writes rows + events atomically). `save_artifacts` now inserts the event rows via `executemany` in the **same** transaction as the artifact upserts.
- **Parity — batch list methods.** The `SwarmStore` base defaults for `list_tasks_for_jobs` / `list_artifacts_for_jobs` now de-duplicate `job_ids` (first-seen order) to match the SQLite overrides; the contract (dedup, no per-job ordering guarantee) is documented on the base methods.
- **Scale — chunked `IN` lists.** `get_artifacts_by_ids` and `list_artifacts_by_type(job_ids=...)` now use the `_chunked` (900) helper so very large id/job windows can't exceed SQLite's bound-variable limit.
- Added four focused parity/edge tests (multi-term memory parity across backends, batch `artifact.saved` event emission, duplicate-`job_id` dedup, chunked `IN` queries); full suite **367** green.

## v0.9.8

**CodeGraph-driven performance audit pass: N+1 query elimination, hot-path optimization, and dead-code cleanup.** A fourth-pass audit (run through Puppetmaster's own CodeGraph + review swarm against this repo) surfaced a batch of read-amplification and recompute issues; this release fixes the high-confidence ones with batched store APIs and per-tick snapshots, plus the usual lint/dead-code sweep. No behavior changes — store contract preserved across both backends.

- **N+1 query elimination across the orchestrator, CLI, audit, and stitcher.** `_reroute_low_confidence` was calling `_latest_verification_confidence` once per completed task, and that helper re-ran `list_artifacts(job.id)` on *every* call — O(tasks × all-artifacts) per reroute pass. It now loads artifacts once and builds a `task_id → latest-verification-confidence` map. `cli.artifact_job_id` no longer scans every job calling `list_artifacts` per job; a new `get_artifact_job_id()` store method does a single indexed `SELECT job_id FROM artifacts WHERE id = ?` under SQLite (file backend keeps a direct existence check). `audit.collect_records` now batches via new `list_tasks_for_jobs()` / `list_artifacts_for_jobs()` (SQLite uses chunked `WHERE job_id IN (...)`) instead of two reads per job. Task creation and routing-artifact emission use new batch `save_tasks()` / `save_artifacts()` (SQLite `executemany` + matching events in one transaction) rather than a transaction per row; stitching uses a new batch `promote_memories()`. Every new method is on the `SwarmStore` base with a default loop implementation so both backends satisfy the contract.
- **Hot-path / resource optimization.** The daemon wait loop (a 0.2s poll) now takes one tasks snapshot and one artifacts snapshot per tick and passes them into `_has_hard_failure`, `_daemon_settled`, and `_recoverable_failure_by_task` instead of each re-reading and re-deserializing full state. Router signal patterns (`_HARD/_EASY/_VISION/_DETAILED_VISION_SIGNAL_PATTERNS`) are precompiled with `re.compile()` at module load instead of recompiling on every routing call. The reads/usage ledger loaders (`reads_log.py`, `codegraph_usage.py`) stream the JSONL line-by-line instead of `read_text().splitlines()` so a long-lived log doesn't allocate the whole file on every savings report. `retrieve_memory` pushes term filters into SQL (`instr(lower(data), ?)`) so filtered-out rows aren't JSON-decoded.
- **Dead code & lint.** Removed the unreferenced `codegraph_status_line()` and the legacy `_CODEGRAPH_AUTOHEAL_STATE` alias; dropped unused imports (`CODEGRAPH_COMMAND` in `mcp_server`/`codegraph_index_runner`, `AdapterInfo` in `diagnostics`, `Any` in `installers`, `field` in `savings`); `redact_secrets` returns its final substitution directly (RET504).
- Added `test_batch_store_methods_across_backends` covering the new batch/lookup APIs on **both** the file and SQLite backends; full suite **363** green.
- *Known limits:* the memory-search optimization is the conservative version only — no FTS5 index was added this pass (deferred as too invasive), so very large memory tables still do a full candidate scan after the cheap SQL prefilter.

## v0.9.7

**CodeGraph Node-ABI self-heal + ABI-safe CLI passthrough; removed the stale Cursor extension.**

- **CodeGraph now self-heals the better-sqlite3 Node-ABI break instead of just hinting about it.** The recurring Cursor footgun: a bare `codegraph` call from your shell runs under your shell's Node, whose ABI differs from Cursor's bundled Node that compiled `better-sqlite3`, so it dies with `NODE_MODULE_VERSION` / "compiled against a different Node.js version" (or silently drops to slow WASM). Every CodeGraph call routed through `run_codegraph_cli` (the `puppetmaster_codegraph_*` MCP tools and the new CLI passthrough) now detects that failure, rebuilds the native binding against Cursor's Node, and retries — no manual `repair-codegraph` needed. Opt out with `PUPPETMASTER_CODEGRAPH_AUTOHEAL=0`. Hardening: the trigger uses a **strict** ABI-load signature (`NODE_MODULE_VERSION` / "compiled against a different Node") so an unrelated non-zero exit can't kick off an expensive `npm rebuild`; the rebuild is **claimed under a lock** so concurrent MCP worker threads run at most one at a time; the command is **only retried when the rebuild succeeds**; a *failed* rebuild is gated by a 5-minute cooldown instead of permanently wedging the process, while a *successful* one latches for the process lifetime; and auto-heal is skipped when a custom install is pinned via `PUPPETMASTER_CODEGRAPH_NODE`/`_JS`. The native-broken backend detector still recognizes the hard module-load error surface, not just the WASM-fallback status line.
- **New `python -m puppetmaster codegraph <args>` passthrough.** Runs the CodeGraph CLI under Cursor's bundled Node (via `resolve_codegraph_invocation()`) with the same auto-heal, so `status` / `search` / `context` / `init --index` / etc. work regardless of your shell's Node. By default the passthrough imposes **no timeout** (so long ops like `index` / `affected` aren't cut off at the 30s context default); cap it with `--timeout SECONDS`, and pass CodeGraph's own flags after a literal `--` (e.g. `codegraph -- --version`). This is now the documented MCP-down fallback (AGENTS.md + rules updated); the old guidance to run a bare `codegraph …` was the source of the ABI error. `codegraph_available()` also returns true when the Cursor-Node invocation resolves even if the global shim isn't on PATH, and that lookup (which shells out to `npm root -g`) is now memoized so repeated availability checks don't re-probe the filesystem.
- **Removed the Cursor/VS Code extension (`cursor-extension/`).** It was stuck at `0.2.0-beta.1` with a command set predating implement/routing/savings/platform-lock, and added a Node CI leg plus eight doc references for no current value. The MCP tool surface is the supported Cursor integration. (Note: this removes the source only; any published marketplace listing must be unpublished manually.)
- Tests added for auto-heal trigger/skip/disable, strict-predicate gating, env-pinned skip, one-at-a-time + cooldown claim, retry-only-on-success, passthrough routing/no-timeout, and shim-less availability; full suite green.

## v0.9.6

**Cross-platform implement + an audit pass of hardening, correctness, and store performance.**

- **Implement is no longer Claude-Code-only — Cursor can ship code, and a generic `implement` verb routes to whatever platform you're locked to.** Previously a cursor-only platform lock could plan/review/analyze but not implement: the Cursor adapter only ran read-only analysis and the MCP path hard-coded `--dry-run`. Now the Cursor adapter has a full-edit **implement mode** (`payload.mode="implement"`) that mirrors the Claude/Codex contract — clean-worktree guard (override `payload.allow_dirty`), the agent edits files, and the resulting `git diff` is captured as a `PATCH` artifact (partial diffs are still captured on timeout/failure so work isn't lost). New surfaces: CLI `puppetmaster cursor --implement [--allow-dirty]`; MCP `puppetmaster_cursor_implement` / `puppetmaster_start_cursor_implement`; and a platform-agnostic `puppetmaster_start_implement` that picks the enabled full-edit adapter (cursor preferred, then claude-code), rejecting an explicitly-requested adapter that the lock disables.
- **Dashboard binds loopback-only by default.** The unauthenticated dashboard now refuses to bind to a non-loopback host (which would expose durable job state to the local network) unless `allow_external=True` / `--allow-external` / `PUPPETMASTER_DASHBOARD_ALLOW_EXTERNAL=1` makes the exposure a deliberate choice.
- **Orchestrator: no orphaned prerequisite workers.** When a prerequisite worker batch times out or one fails mid-batch, the remaining workers are now terminated (then killed) in a `finally` instead of being left to outlive the job.
- **Self-heal picks the *latest* recoverable failure per task.** `_recoverable_failure_by_task` now compares `created_at` so a stale failure reason can't win over a newer one produced by a retry.
- **Store correctness: torn-write and double-claim fixes.** SQLite `save_task` now writes the task row and its `task.saved` event in a single transaction (no crash window that leaves state with no event). `release_lock(owner=…)` only unlinks a lock the caller actually holds, so a late/stale caller can't free another worker's lock and let a second worker double-claim a task. Malformed persisted `confidence` values are coerced to `0.0` instead of crashing memory retrieval.
- **Store performance.** Idempotent SQLite `init()` short-circuits after first run; added `idx_tasks_job_id`, `idx_artifacts_job(id)`, and `idx_artifacts_type`; `last job` is a single indexed query; new `count_artifacts` / `get_artifacts_by_ids` / `list_artifacts_by_type` let status snapshots, the artifact feed, and the savings ledger avoid deserializing every artifact of every job; the claim sweep resolves dependencies from one in-memory task map instead of re-fetching each dependency. `schema_version` is no longer blindly overwritten on init, so a DB written by a newer Puppetmaster is surfaced by `schema_status()` rather than masked.
- Tests added across the implement feature and the store/orchestrator hardening; full suite **345** green.

## v0.9.5

**Cumulative savings ledger + counterfactual dollar headline.** A read-only, local, numbers-only receipt for what Puppetmaster actually saved — measured routing dollars, a counterfactual "avoided spend vs metered APIs" figure, CodeGraph exploration estimates, reliability counts, and $0 follow-up reads. Core stays emission-free; the org-wide rollup lives in a separate private plugin. Also fixes a routing baseline honesty bug and a savings double-counting bug surfaced by live benchmarks.

- **Cumulative savings ledger (`puppetmaster savings`).** A read-only, local, numbers-only receipt for what Puppetmaster actually saved — built so the value is auditable, not asserted. Two measured pillars: (1) **routing dollars saved**, where every routing decision now snapshots a `baseline_cost_usd` (the strongest model you could've used, same tokens) *at decision time* so the ledger compares like-for-like with no recompute drift; only cost-optimizing policies (`balanced`/`cheap`) count, while `quality`/`escalating` show as deliberate spend, never as a loss. (2) **CodeGraph exploration**, via a new global usage log that captures every query through the `puppetmaster_codegraph_*` tools — from *any* platform's agent, not just swarm jobs — recording context tokens fed (measured) plus avoided directory-crawl tokens (estimate, stated baseline). Plus a **reliability** line (real counts, never dollarized): tasks auto-recovered off a dead/unfunded provider and tasks re-run for confidence, read straight from existing fallback/escalation artifacts. Plus **$0 follow-up reads** — a numbers-only count of user-facing result reads (`show`/`artifacts`/`partial_summary`/`feed`) served from durable state at zero model cost; instrumented at the command entry points only (never the store's internal reads) so it can't over-count, and the streaming `feed --follow` long-poll is excluded. Plus a derived **`metrics`** block (rates, no $) for org dashboards — `capability_match_rate`, `escalation_rate`, `fallback_rate`, `reuse_reads_per_job`, `context_tokens_per_job`, each over an explicit `sample` denominator and `null` (never a misleading `0`) when the denominator is empty. Emits nothing over the network. `--window DAYS`, `--all-projects`, `--json`. Tune the estimate with `PUPPETMASTER_EXPLORATION_BASELINE_TOKENS` / `_PRICE_PER_MTOK`; disable the usage log with `PUPPETMASTER_CODEGRAPH_USAGE=0`.
- **Counterfactual "avoided spend" dollar headline.** `savings` now reports a `counterfactual` block that prices the exact token volume you actually routed against a single reference model at metered API rates and subtracts what the work actually cost (`avoided_usd = naive_cost_usd - actual_cost_usd`). On a plan-billed setup actual ≈ $0, so the avoided figure ≈ the naive cost — the big, monotonically-growing number leadership asks for, while staying honest: it's labeled a counterfactual ("what this would have cost on `<reference>` at API rates"), never "cash off your bill". Reference defaults to the highest-capability *priced* model in the registry; override with `PUPPETMASTER_COUNTERFACTUAL_MODEL`. An unpriced reference yields $0 and says so. Computed from the `estimated_tokens_in/out` already stored on each routing artifact — no schema change, no network emission. The org-wide rollup of this number lives in a separate private plugin so the public package stays emission-free.
- **Routing double-counting fix.** `collect_routing_records` now dedups `created_by="router"` artifacts by `task_id`, so a re-dispatched task can't inflate savings dollars or counts — surfaced by a live cursor-only review swarm.
- **Routing baseline honesty fix.** The decision-time savings baseline is now snapshotted from the *same eligible candidate set the pick is drawn from* — after every hard constraint (platform lock, required tags, cost cap, billing gate), not just platform lock. Previously a constrained run (e.g. `required_tags`, or `allow_api_billing=False`) could be measured against a model it could never actually have run, which both under-reported savings on a tag-restricted run and risked *over*-reporting them when the strongest model was billing-gated out. Surfaced by a live routing benchmark. `capability_match_rate` is now judged by model **identity** (ran a model other than the strongest available) rather than by dollars, so it stays meaningful for plan-billed shops where every model is $0. +3 regression tests.

## v0.9.4

**Routing self-audit + docs polish.** Closes the routing loop: a read-only recommender that reads back the artifacts you already store and tells you whether your capability scores still match reality — proposing conservative `models.json` changes you apply by hand. Also adds the "a layer above your agents" diagram to the README.

- **Routing self-audit (`puppetmaster audit`).** A read-only recommender that aggregates the routing/escalation/verification artifacts you already store and reports how each model *actually* behaved (picks, mean confidence, escalation rate, spend). It proposes conservative `models.json` score changes only for the defensible case — an under-delivering model that keeps getting escalated away from or finishing with low confidence — and lowers its score so harder work routes elsewhere. "Over-used" (a strong model on trivial work) is flagged but never auto-adjusted, since proving a cheaper model would've sufficed needs a counterfactual this audit doesn't run. Dry-run by default; `--apply` writes the diff. `--window DAYS` scopes it to recent jobs. Human stays in the loop — the registry is your assertion, not the audit's.

## v0.9.3

**Confidence-based mid-run escalation.** The counterpart to auto-fallback: when a cheap model finishes a task but isn't sure of itself, re-run it one capability tier up before accepting the result — directly addressing "tasks that look simple turn out hard once a worker is in them." Off by default so the cost-saving default is untouched. Also drops the internal `TALKING_POINTS.md`.

- **Confidence-based mid-run escalation** (opt-in). Static upfront routing can under-provision — a task that *looks* simple can turn out hard once a worker is in it, and the cheap model it was routed to says so via a low-confidence `VERIFICATION` artifact. When a confidence floor is configured (`$PUPPETMASTER_ESCALATE_CONFIDENCE=0.7` globally, or per-task `payload.min_confidence`), the orchestrator now re-dispatches a COMPLETE-but-low-confidence task to the cheapest **strictly stronger** funded + platform-enabled model and re-runs it *before* the result is accepted. **Off by default** so the cost-saving promise holds unless you opt in; bounded by `_MAX_ESCALATION_ATTEMPTS` (a task already on the top tier is left as-is); only escalates router-placed tasks (never a hand-pinned model); respects the platform lock and billing posture exactly like auto-fallback. Each escalation writes a `router-escalation` ROUTING artifact (with `escalated_from_model`, `escalated_from_confidence`, `confidence_threshold`) and emits a `router.auto_escalate` event; the local dashboard's reroute panel now surfaces both fallback and escalation. +8 tests; full suite **311** green.
- **Removed `TALKING_POINTS.md`** — it was an internal positioning/messaging cheat-sheet not meant to ship publicly. Dropped the file and the references to it in `README.md`, `bench/README.md`, `docs/CLAIMS.md`, and `docs/CODEGRAPH.md`. The defensible-numbers source of truth remains [docs/CLAIMS.md](CLAIMS.md) + the harnesses in [`bench/`](../bench/).

## v0.9.2

**Platform lock + launch packaging.** Lock Puppetmaster to the platforms you actually pay for (so a stray Claude Code / Codex login can never be picked behind your back), plus the security doc, positioning comparison, and self-healing README hero that make it launch-ready.

- **Platform lock — restrict Puppetmaster to the platforms you actually pay for** (`puppetmaster/platform_lock.py`, `puppetmaster platform …`). Single-platform users (e.g. a Cursor plan and nothing else) can now hard-lock routing to one adapter so a stray Claude Code / Codex login can never be picked, auto-discovered, or used for fallback — even if its CLI is installed and funded. Free-tier bouncers can flip several on and rotate across them. New CLI: `platform status` (shows each platform on/off + source), `platform only cursor`, `platform enable claude-code codex`, `platform disable openai`, `platform reset`. State is a tiny denylist persisted next to the model registry (`~/.puppetmaster/platform.json`); default is everything-on (no file, zero behavior change for existing users). Env override `PUPPETMASTER_ONLY_ADAPTERS=cursor,openai` wins for CI/ephemeral use. The lock is enforced at **all three** decision points — routing (`TaskSignals.allowed_adapters` rejects disabled adapters with a clear reason), first-run plan-catalog auto-discovery (disabled platforms are never enumerated), and auto-fallback (a failed run never reroutes onto a disabled platform) — and surfaced in `doctor` (`platform-lock` check). Per-task escape hatch via `payload.allowed_adapters`. +11 tests; full suite **303** green.
- **Security & threat model doc** ([docs/SECURITY.md](SECURITY.md)). The "is it safe to hand it my repo and my plan?" answer a skeptical first user looks for before installing: what Puppetmaster can do (spawn agent subprocesses, edit files, run shell, read local auth), how credentials are handled (uses existing auth; keys never inlined/stored — evidence records presence like `cursor_api_key:set`, never values), the exact edit-permission/sandbox model per adapter with dirty-tree guards, the complete network-egress list (only the providers your workers use; telemetry off by default; no phone-home/analytics), data-at-rest (local SQLite only), hardening recommendations, and honest limitations (it orchestrates autonomous agents — auditable and reversible, not risk-free; no independent audit yet). Linked from both indexes.
- **Launch packaging: positioning + a self-healing hero.** Added [docs/COMPARISON.md](COMPARISON.md) — an honest, calibrated side-by-side answering "why this over LangGraph / CrewAI / Claude Agent SDK / native subagents," with an explicit "pick X instead if…" section (Puppetmaster orchestrates the agent CLIs you already pay for; it isn't a framework for authoring an agent from primitives). Rebuilt the README hero around **two receipts** — 💸 cheaper (98.8% live OpenAI A/B) and 🔁 self-healing (the live `job_d82715bebc5d` auto-fallback: a `$0` Claude balance → rerouted to `cursor/gpt-5.5` → completed) — so the differentiated, shareable moment is above the fold. Docs index updated; all internal links verified.

## v0.9.1

**Plan-first for everyone, and a probe that proves it.** Closes the gap where only Cursor users got subscription-contained routing and where the live probe only covered API keys — so the "frontier work always rides the subscription you already pay for, and we verify it can actually serve before spending" promise now holds for Claude Code and Codex users too.

- **Live 1-token probe now covers the plan/subscription path, not just API keys.** The probe's whole reason to exist is the funded-*looking* account that's actually exhausted — and for subscriptions that case is real (a Cursor/ChatGPT/Max plan can be authenticated, expose the model, and still be rate-limited or over its monthly allowance). Claude Code (OAuth) and Codex (ChatGPT) already did a real CLI round-trip; the gap was **Cursor**, whose live probe was only a catalog reachability ping. Added `preflight._probe_cursor` — a minimal real generation through the same `cursor_sdk_runner.mjs` the adapter uses — so a plan that's out of allowance surfaces at preflight instead of mid-job. The probe classifier gained plan-side exhaustion markers (rate limit / usage limit / monthly limit / 429 / too many requests), and the block-only-on-definitive-rejection semantics are now shared across all adapters (`_verdict_from_probe`): a probe that can't reach a verdict (node missing, timeout, transient) degrades to an unverified pass, never a false block. *Verified live: `preflight cursor --live` did a real ~7s generation on the developer's plan and returned `live_probe:ok` (a genuine served token, not a catalog ping).*
- **Clean subscription detection (most users are subscribed, so this is the common path).** Plan-first routing only fires if we can *reliably* tell a subscription is signed in. The old heuristics were fragile: Claude Code detection trusted "`~/.claude.json` exists" (but that file stores onboarding/config state and **survives a logout**, a false positive), and Codex detection shelled out to `codex login status` (slow, text-parsed). Now both read the real local auth files directly: Claude Code parses the `oauthAccount` block (requires an `accountUuid`/`emailAddress`, and surfaces `seatTier` + `organizationName` in the detail/evidence), and Codex reads `~/.codex/auth.json` (`auth_mode`/`tokens` → `apikey` is out-of-pocket, `chatgpt` is plan-covered), falling back to `codex login status` only when that file is absent. Deterministic, subprocess-free for the common case, and `doctor`'s `billing:<adapter>` lines now name the actual subscription (e.g. *"signed in via OAuth (max seat, org 'Acme')"*).
- **Plan-first auto-discovery for Claude Code & Codex (curated catalogs).** Cursor was the only adapter that got first-run plan-catalog auto-discovery, because it's the only platform that self-enumerates its models (`Cursor.models.list()`). The two CLI agent loops — Claude Code (OAuth) and Codex (ChatGPT) — expose *billing mode* but no queryable model list, so a Cursor-less user on a Claude Max / ChatGPT subscription still fell off to a per-token account on hard tasks. New `puppetmaster/static_catalog.py` ships a **hand-curated catalog** of what each of those platforms offers (the one place to update when they ship models) plus the same auto-merge Cursor gets: when an `auto_route` job starts, the registry has no plan-billed frontier, and **Cursor didn't supply one**, the orchestrator detects the first subscription-authenticated CLI loop and merges its curated catalog in as **plan-billed** (`$0` marginal, capability/id/tags preserved from any existing registry row, new models seeded), so frontier work routes through the subscription the user already pays for. Idempotent (per-source discovery marker), never blocks a run (degrades to a loud `router.plan_catalog_unavailable` event), opt-out via `PUPPETMASTER_AUTODISCOVER=0`. Also exposed manually: `python -m puppetmaster models discover --source claude|codex` (billed as the detected subscription/API posture, dry-run by default). +7 tests; full suite **288** green.

## v0.9.0

**Truthful failure status, auto-fallback, live preflight, full-platform discovery, and real observability.** This release closes the gap between "looks healthy" and "actually verified before spending your money," and makes a degraded run re-route itself instead of silently lying.

- **Plan-first by default: first-run catalog auto-discovery.** The promise is "frontier work always routes through the subscription you already pay for, never falls off to a per-token or depleted account." That only holds if the plan-billed frontier models are actually *in* the registry — but a fresh install ships a thin starter registry (`cursor/gpt-5-5` cap 78 as its ceiling) and discovery was historically manual, so any task needing capability ≥ 79 silently fell through to a standalone adapter. Now, when an `auto_route` job starts and the registry has no plan-billed frontier (`capability_score ≥ 85`), the orchestrator enumerates the authenticated Cursor plan's catalog **once** and merges it in (plan-billed, capability inherited by model-name), so a cap-90 audit/redteam/implement task routes to `cursor/claude-opus-4-7` (`$0`, in-subscription) instead of a depleted API account. Idempotent (a discovery-meta marker prevents re-enumeration; steady-state runs pay nothing), never blocks a run (a node/SDK failure degrades to a loud `router.plan_catalog_unavailable` event + the existing `doctor` nudge), and opt-out via `PUPPETMASTER_AUTODISCOVER=0`. *Verified live: a thin 12-model starter → first swarm auto-discovered 28 plan models → `cursor/claude-opus-4-8`/`4-7` became routable; second swarm skipped with no node call.*
- **Fixed a correctness defect: a preflight block / adapter billing rejection now FAILS the task.** `WorkerRuntime.run_once` previously discarded the `AgentRun` returned by `LocalWorker.run` and marked every dispatched task `COMPLETE` — so a preflight-blocked task (or an adapter that came back with a `billing_or_quota`/auth verification artifact) was recorded as a success. That made telemetry lie, made `await_job` report success over a doomed run, and made auto-fallback impossible. The runtime now honors the worker's `FAILED` verdict and converts any recoverable adapter failure (`RECOVERABLE_FAILURES`: billing/quota, preflight-blocked, missing key/CLI, auth) into a truthful `FAILED` status.
- **Auto-fallback on auth/billing failure.** After the main worker pass, the orchestrator re-routes any task that FAILED because *its adapter's account* was unfunded/unauthenticated to the cheapest sufficient model on a *different, verified-funded* adapter, then re-runs it (bounded: `_MAX_FALLBACK_ATTEMPTS=2`, `_MAX_FALLBACK_ROUNDS=3`). Each re-route writes a `router-fallback` ROUTING artifact and emits a `router.auto_fallback` event. Genuine errors (bad adapter, exceptions) still fail closed; a billing failure with no funded alternative surfaces loudly in the stitched **Alerts** section instead of nuking the whole job.
- **Live 1-token preflight probe** (`preflight.live_probe`, `preflight_check(..., live=True)`, `puppetmaster preflight --live`, per-task `payload.live_preflight`). Static detection can't see a funded-*looking* account whose balance is actually $0 — only a real call can. The probe does the smallest possible round-trip per adapter (Claude/Codex CLI, OpenAI `/v1/chat/completions`, Cursor = plan-catalog reachability) and classifies the result with the existing adapter classifiers. Off by default (adds a real call); fully dependency-injected for tests. **A probe only *blocks* on a definitive provider rejection (`billing_or_quota`/`auth`); a probe that can't reach a verdict (missing optional CLI, timeout, network blip) degrades to an "unverified" pass so `--live` never false-blocks a working account.** The OpenAI probe sends `max_completion_tokens` (GPT-5+ rejects the legacy `max_tokens`), with an automatic legacy fallback for older OpenAI-compatible endpoints.
- **Catalog discovery for OpenAI + Anthropic** (`puppetmaster/api_discovery.py`). Both expose `/v1/models`, so `python -m puppetmaster models discover --source openai|anthropic|all` now enumerates them too (additive merge — never drops hand-tuned entries; inherits capability/price from matching registry rows, seeds the rest). Cursor remains the default source.
- **Catalog staleness tracking.** `models discover --write` records per-source `{refreshed_at, count}` in a `models.discovery.json` sidecar; `doctor` gains a `catalog-freshness` check that nudges when a catalog is >30 days stale or never discovered.
- **Real observability** (`puppetmaster/telemetry.py`): OTel **metrics** (`puppetmaster.jobs` / `.tasks` counters + `cost_usd`), **live per-task spans** emitted by workers as they finish (`PUPPETMASTER_OTEL_LIVE`), and **cross-process trace-context propagation** — the orchestrator mints a W3C `traceparent`, exports it to worker subprocesses via `TRACEPARENT`, and anchors the assembled job span to it, so a swarm renders as one correlated trace in Jaeger/Datadog. All gated/zero-dep by default.
- **TypeScript blocking-await client** (`clients/typescript/`). `awaitJob(jobId, opts)` blocks until a job is terminal and returns its final state + stitched summary by driving the durable CLI (`python -m puppetmaster await --json`) — true blocking await for CI/scripts/backends, free of the MCP stdio constraint. Zero runtime deps; typechecks clean under `--strict`.
- **End-to-end validation (live, real adapters).** The marquee resilience flow was proven against real providers, not mocks: a `claude-code` worker hit a genuine `$0` Anthropic balance → classified `billing_or_quota` → the task was marked `FAILED` → `router-fallback` re-routed it to `cursor/gpt-5.5` (plan-billed, `$0`) under the balanced policy → the funded adapter **completed** the task (job `job_d82715bebc5d`; `created_by` audit trail: `worker-implement` → `router-fallback` → `worker-implement`). Concurrency was validated by `bench/mcp_stress.py` (**6/6** scenarios: 64 simultaneous senders, sustained traffic, large payloads, idle→busy→idle keepalive — 0 missing responses, 0 frame errors, server stayed alive). Benchmark receipts regenerated against this build: `router_savings` **35.1% cheaper** on a mixed workload (100% on routable tasks), `router_live_ab` **98.8% cheaper / 81.1% faster** with real OpenAI token receipts, `followup_cost` **0 adapter calls / 0 tokens** for 32 follow-up reads.
- **Bug fix (live probe false-block).** `preflight openai --live` blocked a fully-funded account because the probe sent the deprecated `max_tokens`, which GPT-5+ rejects with a 400 `unsupported_parameter` — misclassified as `probe_failed`. Fixed to `max_completion_tokens` (+ legacy fallback) and the block-only-on-rejection semantics above. Regression-tested.
- **Local dashboard** (`puppetmaster/dashboard.py`, `python -m puppetmaster dashboard [<job_id>]`). A zero-dependency live web board served from `http.server` — task graph, typed-artifact rollup, estimated cost, **auto-fallback reroutes**, and the same Alerts the stitcher surfaces. The page is fully inlined (no Flask/React/CDN; works offline) and polls every ~1.5 s, so it's live while a swarm runs and instant for a finished one. OTel remains the export path for Jaeger/Datadog; this is the no-collector local view. Data layer (`build_job_snapshot`) is a pure function; an HTTP smoke test drives the real server. **Hardened via dogfooding:** a Cursor swarm reviewing this very module (which itself survived a live `claude-code` `$0` failure → `router-fallback` → `openai/gpt-5.5` completion) flagged three real issues, now fixed — `/api/job` rejects non-job-id requests before the store path join (no `..`/absolute traversal), the catch-all handler returns a generic 500 instead of leaking `str(exc)` (paths/backend details), and the cost rollup tolerates a non-numeric `estimated_cost_usd` instead of 500-ing.
- **Cross-platform CI.** The GitHub Actions matrix now runs the full suite + CLI/demo smokes on **Linux, macOS, and Windows** (Python 3.9 + 3.12) with `shell: bash` so the heredoc steps run identically everywhere. Fixed a Windows-only runtime breaker: `mcp_registry` hard-kill used `signal.SIGKILL` (absent on Windows → `AttributeError`); now falls back to `SIGTERM` (which `os.kill` maps to `TerminateProcess` on Windows). The first live matrix run did its job and surfaced more: a flaky macOS timing assertion in the input-staleness watcher test (the watcher's first check could race the inbound-message mark — fixed by marking before start), and a real cross-platform bug in `codegraph._pid_is_alive` — `os.kill(pid, 0)` doesn't report a missing pid as dead on Windows, so a stale codegraph lock held by a long-gone process was unbreakable; now uses a Win32 `OpenProcess`/`GetExitCodeProcess` probe that distinguishes "missing pid" (dead) from "access denied" (alive, mirrors POSIX EPERM). A follow-up Windows-hardening pass then fixed the real cross-platform defects the matrix exposed: (1) **SQLite connection leak** — every store op used `with self.connect()`, which only commits/rolls back and leaves the handle open; harmless on POSIX but Windows holds a mandatory file lock, so temp-dir cleanup failed with `WinError 32`. A `_session()` context manager now always closes the connection. (2) **`command_parts` mangled Windows paths** — `shlex.split` defaults to POSIX mode and ate backslashes in `C:\…\claude.exe`, so every adapter reported `missing_cli`; now splits in non-POSIX mode on Windows. (3) **CodeGraph lock was a no-op on Windows** — the advisory lock was `fcntl`-only, so without `fcntl` *no* mutual exclusion happened; added an `msvcrt` non-blocking byte-range lock so concurrent indexers actually serialize. (4) **`doctor` could crash on Windows** — a CLI probe that raised (e.g. a node-shim that isn't a valid Win32 exe) aborted the whole report; each check is now guarded into an `error` Check. (5) De-flaked the macOS input-staleness watcher test and made the codex/claude adapter test fakes run via `sys.executable` (cross-platform) instead of relying on shebang exec; the two `codex` install tests that stub a **bash** CLI are skipped on Windows (POSIX-only scaffolding, not a product gap). A second pass fixed the last three Windows failures — the `msvcrt` lock was locking the same byte the PID text occupies (mandatory locks block reads, so the busy-check couldn't read the holder PID), now fixed by locking a sentinel byte at a 1 GiB offset; and `diagnostics._sqlite_state_check` leaked its connection the same way the store did. **Windows is now green and `continue-on-error` has been removed — all three OSes are required blocking legs.**
- **Demo.** `scripts/demo.sh` — a 60-second, no-API-key tour (cost routing → 6-role swarm fan-out → stitched summary → `$0` follow-up reads → dashboard pointer) that runs on a clean machine; `scripts/demo.tape` regenerates `docs/demo.gif` via VHS (GIF-as-code), and the rendered `docs/demo.gif` (~1.1 MB, 24 fps) is committed so the README shows it without a local VHS install.

## v0.8.0

**Platform-contained billing + dynamic Cursor catalog discovery.** Puppetmaster now treats the subscription you already pay for (Cursor plan, Claude Max, ChatGPT/Codex) as the default place to spend, and can discover what your Cursor plan actually exposes instead of relying on a hand-maintained registry. Also fixes a **critical packaging bug** that broke the Cursor adapter for every `pip install` user.

> **Packaging fix (affects all prior PyPI releases):** `puppetmaster/cursor_sdk_runner.mjs` — the Node runner the Cursor SDK adapter resolves via `Path(__file__).with_name(...)` — was **never included in the wheel/sdist** (no `package-data` rule), so `pip install puppetmaster-ai` shipped a Cursor adapter that couldn't launch. Added `[tool.setuptools.package-data] puppetmaster = ["*.mjs"]`; verified the runner is now present in both wheel and sdist and resolvable after a clean-venv install. **Anyone using the Cursor adapter from a pip install must upgrade to 0.8.0.**

- **First-class `ModelSpec.billing`** (`plan` | `api` | `unknown`, default `unknown`). Plan = covered by a subscription (no marginal API spend); api = out-of-pocket per-token; unknown = resolve at runtime. Serializes only when non-default; round-trips through `load_registry`/`save_registry`. Starter registry tags Cursor entries `plan` and OpenAI entries `api`. New `is_plan_billed` property.
- **Billing-aware router.** `TaskSignals` gains `prefer_plan_billed` (default **True**) and `allow_api_billing` (default **True**). At sufficient capability the router prefers a plan-billed model over an out-of-pocket one (tie-break across `cheap`/`balanced`/`quality`/`escalating`); `allow_api_billing=False` is a hard plan-only gate that rejects api-billed models with an explicit reason and raises `NoEligibleModelError` if nothing plan-billed qualifies. The picked model's `billing` is now stamped into the `ROUTING` artifact for audit. Overridable per task via `payload.prefer_plan_billed` / `payload.allow_api_billing`.
- **Cursor catalog discovery** (`puppetmaster/cursor_discovery.py` + `cursor_sdk_runner.mjs` `mode: "list-models"`). `python -m puppetmaster models discover` enumerates the authenticated Cursor plan's models via `Cursor.models.list()` and reconciles them into the registry as **plan-billed** entries (`--write` to persist, dry-run by default, `--json` for machines). Cross-adapter **capability inheritance**: a model the plan exposes (e.g. `claude-opus-4-8`) inherits its native capability/context (cap 99) by model-name match, so a plan-billed frontier ranks like the frontier it is. Stale cursor entries no longer in the catalog are dropped; non-cursor entries pass through untouched. *Live result on the developer's plan: 28 plan-billed models discovered, and a hard audit re-routed from `claude-code/opus-4-8` (api, ~$0.13/call) to `cursor/claude-opus-4-8` (plan, $0).*
- **Platform billing/auth detection** (`puppetmaster/platform_billing.py`). Pure, dependency-injected probes for each adapter: Cursor (keyed → plan), Claude Code (`ANTHROPIC_API_KEY` → api, else `~/.claude` OAuth → plan), Codex (`codex login status`: API key → api, ChatGPT → plan), OpenAI (api). Returns `BillingStatus{billing, healthy, detail, evidence}`. `doctor` now prints a `billing:<adapter>` line per platform.
- **Universal preflight** (`puppetmaster/preflight.py` + `python -m puppetmaster preflight <adapter> [--model] [--no-api-billing]`). Verifies an adapter can actually run *before* dispatch — credentials present, plan-vs-api gate, and (Cursor) the routed model is in the live catalog. Catalog-fetch failures degrade to a pass with a note (never block a run because discovery was offline). This is the safety net for the "returned in 391ms with billing failure and zero edits" degraded-run class.
- **Loud failure surfacing.** The stitched summary now leads with an **`## Alerts (action required)`** section that hoists hard failures (`billing_or_quota`, `missing_api_key`, `missing_cli`, `model_unavailable`, `permission_denied`, …) to the top with remediation, instead of burying them in a low-confidence verification line.
- **`await_job`.** New `puppetmaster_await_job` MCP tool (bounded long-poll: blocks up to `timeout_seconds` for a terminal state, returns `timed_out` so you can re-call) and `python -m puppetmaster await <job_id>` (true blocking await for the CLI/SDK path, `--timeout-seconds 0` blocks until done).
- **Pre-dispatch preflight gate.** `LocalWorker` now runs the fast (no-network) auth/billing check before dispatching to a paid adapter (cursor/claude-code/codex/openai). An unauthenticated/missing adapter — or an api-billed one under a plan-only task — yields a `blocked` verification artifact (`failure=preflight_blocked`) and a FAILED run instead of a wasted dispatch; opt out with `payload.skip_preflight`. Pairs with billing-aware routing (auto_route jobs already steer hard work to the plan-billed Cursor frontier, so they avoid a depleted API account entirely). Surfaced in the stitcher Alerts section. *Live token probes for the funded-but-zero-balance case remain a follow-up.*
- **Optional OpenTelemetry tracing** (`puppetmaster/telemetry.py`, `pip install 'puppetmaster-ai[otel]'`). Zero-cost and zero-dependency unless enabled via the standard env vars (`OTEL_EXPORTER_OTLP_ENDPOINT`, or `OTEL_TRACES_EXPORTER=console`, with a `PUPPETMASTER_OTEL_ENABLED` override). On completion each job emits a trace — a `puppetmaster.job` session span with `puppetmaster.task` children carrying role, adapter, `gen_ai.request.model`, status, and `puppetmaster.cost_usd`/`gitagent.cost_usd` — assembled from durable records (faithful per-worker tree without threading span context across the subprocess boundary) and exported via OTLP or printed to the console. Trace assembly (`build_job_trace`) is a pure, SDK-free function; a telemetry failure never breaks a swarm. Verified end-to-end with the console exporter.
- **Bug fix:** `_server_version()` looked up the dist as `puppetmaster`, but it's published as `puppetmaster-ai` — pip users got `None` in MCP `serverInfo.version`. Now tries `puppetmaster-ai` first, falls back to the legacy name.
- **Tests:** +27 tests (`214 → 241`) across billing field round-trip, billing-aware routing (plan tie-break, api gate), platform detection (all auth branches), Cursor discovery (catalog fetch/failure modes, overlay + cross-adapter inheritance, stale-drop merge), preflight (ready/blocked/catalog-unavailable), stitcher alerts, and `await_job_state` (terminal + timeout). Full suite green; clean-venv install of the built wheel verified end-to-end.

## v0.7.3

Adds **Anthropic Claude Opus 4.8** (released 2026-05-28) as the router's new frontier flagship — the "big bad" the hardest tasks route to.

- **New starter-registry tier `claude-code/opus-4-8`** (`adapter_model_name: claude-opus-4-8`, `capability_score: 99`, `$5 / $25` per MTok, **1M context window**, tags `frontier, vision, detailed-vision, reasoning, code`). Opus 4.8 ships at the *same price* as 4.7 with across-the-board benchmark gains and a 4x-larger context, so it strictly dominates 4.7 for the hardest work (security audits, red-team, deep reasoning, detailed vision). `claude-code/opus-4-7` is retained (capability 98) so existing routing configs and cost history stay valid, with its `notes` updated to mark it as the previous flagship. Starter registry is now **12 tiers** (was 11). `docs/sample-models.json` aligned.
- **Classifier ceiling raised 98 → 99** in `puppetmaster/router.py:classify_capability_needed`. The max capability-needed score tracks the current flagship's `capability_score`; with 4.7 (98) as the previous top, the hardest tasks saturated at 98 and the balanced policy's equal-cost tie-break (prefer the right-sized, lower-capability model) kept routing them to 4.7. Bumping the ceiling to 99 means the absolute-hardest tasks now *demand* 99 and route to Opus 4.8 — while merely-hard tasks (need ≤ 98) still right-size to 4.7 or below. No change to the tie-break itself, so the `$0` Cursor tier and every other pairing is unaffected.
- **Routing impact**: `route "Security audit across every endpoint" --role audit` (and other top-tier tasks classified at 99) now pick `claude-code/opus-4-8` instead of `claude-code/opus-4-7`. Under `quality` policy the flagship is also 4.8. Detailed-vision tasks below the ceiling (e.g. `OCR every detail of the diagram`, need ~92) still right-size to `claude-code/opus-4-7` since both carry `detailed-vision` at equal cost. The new entry was appended to the developer's existing `~/.puppetmaster/models.json` (preserving all prior entries) and verified live.
- **Tests**: updated the detailed-vision quality-policy assertion to expect `claude-code/opus-4-8`; extended `test_starter_registry_encodes_four_tiers` to assert 4.8 > 4.7, that 4.8 is the single highest-capability model, same price as 4.7, and a larger context window; added `test_starter_registry_routes_hardest_task_to_opus_4_8` locking the flagship as the destination for the hardest tier. Full router/registry subset (33 tests) green.

Also in this release (the previously-deferred "last-mile" setup work):

- **`python -m puppetmaster install-rules`** (new). Writes agent rule files that nudge the host (Cursor, `AGENTS.md`, Codex, Claude Code) to reach for Puppetmaster's MCP tools on the right tasks — the MCP installers give a host the *capability* to call Puppetmaster, but without a rule the agent won't reflexively use it. Auto-detects targets from cwd + tools on PATH; `--target` to pin a subset, `--global` for user-level rule files, `--force` to rewrite, `--dry-run` to preview. Uses a merge-block protocol (`<!-- puppetmaster:rules:begin/end -->`) so it inserts/updates managed content in existing files (e.g. `AGENTS.md`) without clobbering user content. New module `puppetmaster/rules.py` with `RulesInstallResult` / `TargetOutcome`.
- **`python -m puppetmaster setup`** (new). One-shot first-run wizard: `doctor` + `models init` + `install-cursor-mcp` + `install-codex-mcp` + `install-rules`, idempotently, skipping any step whose tool isn't present. This is now the single command the README leads with after `pip install`.
- **`doctor` gains an `agent-rules` check** that flags the half-installed state where an MCP host is registered but no agent rule file exists (returns `optional` when no MCP host is present, `ok` once a rule is found).
- **README thinned from a wall to a scannable page** (~1100 lines removed). Deep content moved into dedicated docs: `docs/MODEL_ROUTING.md`, `docs/CODEGRAPH.md`, `docs/WHY.md`, `docs/TROUBLESHOOTING.md`, `docs/CLI_REFERENCE.md`.

## v0.7.2

One-line MCP installers for Cursor and Codex, plus the previously-deferred Codex MCP documentation that sat ahead of the v0.7.1 tag.

> **First PyPI release** (2026-05-28). Published as `puppetmaster-ai` (https://pypi.org/project/puppetmaster-ai/0.7.2/) because PyPI's PEP-503 name normalization treats `puppetmaster` and `puppet-master` as the same name, and the bare `puppet-master` is held by an [abandoned 2019 single-release project](https://pypi.org/project/puppet-master/) that hasn't had activity in 6+ years. Import name stays `puppetmaster`; CLI, GitHub repo, and README branding are unchanged — only `pip install <name>` differs. Name-reassignment request tracked separately. Install:
>
> ```bash
> pip install puppetmaster-ai
> python -m puppetmaster install-cursor-mcp   # or install-codex-mcp
> ```

- **`python -m puppetmaster install-cursor-mcp`** (new). Resolves the exact Python that has `puppetmaster` importable via `sys.executable` — the single biggest source of "I followed the README but the MCP server won't load" friction was `command: "python"` in `mcp.json` resolving to a different interpreter under Cursor's launch environment than the one the user installed Puppetmaster into. Defaults to `<cwd>/.cursor/mcp.json`; pass `--global` to write `~/.cursor/mcp.json` (visible in every workspace) or `--path PATH` for explicit control. Runs a `tools/list` handshake against the registered Python before writing anything, so a broken interpreter or missing package is caught with a clear error rather than producing a half-broken config. Fully idempotent (re-run reports `unchanged`); `--force` repoints at the current Python even when the existing entry matches. Critically: merges into the existing JSON instead of overwriting — unrelated MCP servers and any pre-existing `env` block on the puppetmaster entry (CURSOR_API_KEY, OPENAI_API_KEY, PYTHONPATH, etc.) are preserved verbatim.
- **`python -m puppetmaster install-codex-mcp`** (new). Same pattern, targets the Codex CLI. Shells out to `codex mcp add puppetmaster -- <sys.executable> -m puppetmaster.mcp_server` so the registration goes through Codex's own CLI (forward-compatible with future TOML schema changes). Runs the same `tools/list` handshake before registration. Idempotent: if `codex mcp get puppetmaster` already reports a matching command + args, returns `unchanged` without touching `~/.codex/config.toml`. Prints the Codex sandbox guidance (interactive TUI approval flow vs `--dangerously-bypass-approvals-and-sandbox` for `codex exec`) as part of "next steps" output.
- **`puppetmaster/installers.py`** (new module). Hosts `handshake_mcp_server()`, `install_cursor_mcp()`, `install_codex_mcp()`, plus the `InstallResult` and `HandshakeResult` dataclasses. Designed so unit tests can stub the `codex` CLI with a shell script and assert on exact argv shape without touching the user's real `~/.codex/config.toml`. Both installers support `--dry-run` (preview without writing) and `--skip-handshake` (rare escape hatch for environments where spawning a subprocess is undesirable, e.g. CI bootstrap).
- **README quickstart updated** to call `install-cursor-mcp` and `install-codex-mcp` as third-and-fourth steps after `doctor` and `models init`. The Cursor and Codex MCP setup sections both lead with the one-line installer and demote the manual `mcp.json` / `codex mcp add` recipes to "equivalent if you prefer". The Codex MCP setup section itself (the one that sat ahead of the v0.7.1 tag) is now packaged in this release.
- **Tests**: +10 new tests in `tests/test_puppetmaster.py::InstallerTests` covering: handshake against the real MCP server (asserts 10+ tools advertised), handshake-with-missing-python failure path, Cursor install into an empty file, Cursor install preserving unrelated servers + existing env keys, Cursor install idempotency (file mtime unchanged on rerun), Cursor `--force` rewrites even when matching, Cursor `--dry-run` does not write, Codex install with missing CLI returns `error`, Codex install argv shape verified via shell-script stub, Codex install idempotency when entry already matches. **All 10 pass in 0.5s isolated.**
- **Live smoke**: both installers exercised end-to-end on a developer machine against a real Cursor `~/.cursor/mcp.json` (3 pre-existing servers, 4-key env block on puppetmaster entry) and a real Codex CLI install (codex 0.134.0). Both installs upgraded the brittle `command: "python"` to `command: "/Users/cary/.pyenv/versions/3.9.7/bin/python"`, preserved all 4 env keys verbatim, and left navdata + portable-llm-wiki MCP server entries untouched. `codex mcp get puppetmaster` confirmed the registration.

## v0.7.1

Polish pass after v0.7.0 — diagnostics, doctor honesty, and README freshness for new readers.

- **`puppetmaster doctor` now actually reflects the v0.7.0 adapter surface.** Added a `codex` check that reports `ok <command>` when the `codex` CLI is on PATH (or `CODEX_COMMAND` resolves) and `optional` with install instructions otherwise. The pre-existing `claude-code` check downgraded its severity from `missing` (which read as an alarm) to `optional` with cleaner install instructions, matching `codex`. Both adapters are genuinely optional; `missing` was misleading when only an alarm-level severity should be used for things doctor needs to function.
- **`puppetmaster adapters` no longer false-reports `openai` and `codex` as `configured=False` despite being usable.** The `adapter_status()` diagnostics function gained explicit branches for both: `openai` is `configured` when `OPENAI_API_KEY` is set; `codex` is `configured` when the CLI is installed AND `OPENAI_API_KEY` is set (a decent proxy for "you have something usable" without introspecting the codex login file outside Puppetmaster's purview). Before this fix, four out of six adapters showed `configured=False` regardless of state — a Cursor engineer skim-reading the output would have concluded nothing worked.
- **README headline benchmark numbers refreshed.** The opening A/B headline previously cited a specific 2026-05-08 run ($0.019530 → $0.000141, 99.3% cheaper / 83.3% faster). Re-running the same harness 3 times on 2026-05-28 against the current registry produced 98.1–98.7% cheaper / 68–88% faster. Updated to today's representative numbers ($0.006900 → $0.000132, 98.1% cheaper / 72.4% faster) and explicitly framed the cost ratio as "98.1–98.7% across 3 consecutive runs" rather than a single-point claim. Honest about variance.
- **README copy synced to the v0.7.0 adapter surface.** Opening sentence now lists all four production adapters (`cursor`, `claude-code`, `openai`, `codex`). Architecture diagram updated to show `Codex CLI` in the inbound row. "11-tier starter registry" called out (was "8-model" / "nine"). Model registry table extended to include the four OpenAI tiers + two Codex tiers (previously only the Cursor + Claude tiers appeared). CodeGraph integration paragraph now lists Codex as a 4th adapter that auto-injects context.
- **TALKING_POINTS.md updated** with the fresh A/B numbers and v0.7.0's four-adapter framing.
- **bench/README.md** sample-receipt table updated to today's run.
- Quickstart `puppetmaster doctor` comment now says "14 health checks" (was "12") and enumerates them so a new reader knows what to expect.
- **Tests**: still 189 passing. No code-behavior changes outside diagnostics.

## v0.7.0

- **Codex is no longer a stub — full Codex CLI integration shipped.** The `codex` slot has been a placeholder `UnconfiguredProviderAdapter("codex", "Codex")` since `puppetmaster/adapters.py` was first written. v0.7.0 promotes it to a real adapter, sibling to `CursorAdapter`, `ClaudeCodeAdapter`, and `OpenAIAdapter`. Puppetmaster now ships **four production adapters** instead of three.
- **`CodexAdapter` shells out to the official OpenAI Codex CLI (`codex exec --json`).** Required: `npm install -g @openai/codex`, then `printenv OPENAI_API_KEY | codex login --with-api-key` (or `codex login` for ChatGPT-account auth). The adapter is modeled on `ClaudeCodeAdapter` for subprocess + git-snapshot + sidecar-spool semantics, with one major addition: it parses Codex's structured JSONL event stream and captures **real billing-grade token telemetry** into the verification artifact payload — `input_tokens`, `output_tokens`, `cached_input_tokens`, `reasoning_output_tokens`, and `thread_id` per turn. This makes `codex` strictly more telemetry-rich than any other adapter Puppetmaster ships; `claude-code` doesn't surface usage at all, and `openai` only surfaces the basic prompt/completion split.
- **Default execution mode is non-interactive and safe.** `approval_policy="never"` (no automation hangs on approval prompts), `--sandbox workspace-write` (can edit files in `cwd`, not outside), `--ephemeral` (no persistent Codex sessions), `--skip-git-repo-check` (works in non-git directories). Callers can downgrade to `payload.sandbox="read-only"` for review/plan roles, or opt in to `payload.dangerously_bypass_approvals_and_sandbox=true` when the surrounding environment is already externally sandboxed.
- **Two new starter registry tiers.** `codex/gpt-5-5` (capability_score 97, $5/$30 per MTok, 1M context, tags include `frontier` + `agent-loop`) and `codex/gpt-5-4-mini` (capability_score 72, $0.75/$4.50, 400K context, tags include `balanced` + `agent-loop`). Capability scores are set 1 point higher than the matching `openai/*` tiers to encode the agent-loop advantage on multi-file refactors and audits. For pure one-shot reasoning, the router still prefers `openai/*` (no agent-loop round-trip overhead).
- **New `puppetmaster codex` CLI subcommand**, parallel to `puppetmaster cursor`, `puppetmaster claude`, and `puppetmaster openai`. Supports `--model`, `--sandbox`, `--approval-policy`, `--dangerously-bypass-approvals-and-sandbox`, `--executable`, `--timeout-seconds`, `--worker-mode`, `--allow-dirty`, `--disable-codegraph`.
- **End-to-end validated.** Live smoke test through `CodexAdapter` against the real `codex` CLI returned a structured `verification` artifact (result=passed, confidence 0.9, returncode 0) plus a parsed `finding` artifact from the `{"artifacts": [...]}` envelope. Token counts captured from the event stream: 9757 in / 106 out / 8064 cached / 64 reasoning. Thread id captured. No artifact loss.
- **Failure surface mapped.** New `classify_codex_failure()` helper maps Codex error strings into 9 known buckets: `not_authenticated`, `rate_limit`, `billing_or_quota`, `model_unavailable`, `missing_cli`, `approval_denied`, `sandbox_denied`, `timeout`, `network_error`. Failure classification is set on the verification artifact payload so dashboards and rerun heuristics can react.
- **Tests**: +8 new unit tests covering `CodexAdapter` missing-CLI path, command-builder shape (default + danger-bypass), event-stream parser (banner / invalid-line robustness), `last_codex_agent_message` for both multi-message and turn-failed cases, and every failure-classifier bucket. The pre-existing `test_provider_stub_returns_blocked_verification_artifact` was rewritten to exercise `UnconfiguredProviderAdapter` directly (since the codex slot is no longer a stub). **Suite: 189 passing (was 182).**
- **Docs updated**: `docs/ADAPTERS.md` gains a `codex` section and an `openai` section that had been missing; `docs/ROADMAP.md` strikes out the Codex adapter entry as shipped; README "What Works Today" table promotes Codex to a first-class row; README "Live Adapters" section gains a Codex subsection.

## v0.6.3

- **Claude-Code-only users now get tiered routing too.** v0.6.2 shipped routing parity for the Cursor + OpenAI stack but the Claude Code tier only had Opus 4.6 and Opus 4.7 — so a Claude-Code-only user routing a trivial "format these files" task would land on a $5/$25 Opus call. The starter registry now ships `claude-code/haiku-4-5` (capability_score 55, $1/$5 per MTok, 200K context) as the Anthropic-side counterpart to Cursor's `composer-2.5` and OpenAI's `gpt-5-4-nano`. The router still prefers `cursor/composer-2-5` ($0) or `openai/gpt-5-4-nano` ($0.15/$0.90) when those are present in the registry; Haiku is for the case where they aren't.
- **Anthropic Opus 4.6 / 4.7 pricing corrected to current rates.** The starter registry was shipping these at $15/$75 per MTok — the older Opus 4.1 rate schedule. Anthropic's current 4.x rate is $5/$25 (verified against Anthropic's official pricing page and multiple secondary sources, May 2026). `puppetmaster/model_registry.py:starter_registry()` and `docs/sample-models.json` updated to match. **This is a 3x correction**, not a small one — anyone running router cost estimates against the old Opus pricing was overestimating Anthropic spend by 3x.
- **Benchmark receipts re-run against the corrected pricing.** Headline numbers updated where the frontier baseline figure was derived from Opus pricing: `bench/router_savings.py` total savings is still **35.1% cheaper** because both the picked Opus tasks and the frontier baseline scaled together; `bench/followup_cost.py` "always-frontier replay" reference dropped from $4.92 to **$1.64** at the 40-query mark. The README receipts paragraph and the v0.6.0+ routing example are both updated to cite the new $0.127550 figure for "security audit every endpoint" (was $0.382650 under the old Opus rate). The OpenAI A/B in `bench/router_live_ab.py` is unaffected — its $0.019530 → $0.000141 receipt used real OpenAI tokens and OpenAI pricing.
- **Demonstrable end-to-end**: a synthetic Claude-Code-only registry (3 tiers: haiku-4-5, opus-4-6, opus-4-7) routes `format these files` → `claude-code/haiku-4-5` ($0.002), `implement password reset endpoint` → `claude-code/opus-4-6` ($0.078), `security audit every endpoint` → `claude-code/opus-4-7` ($0.128). Same routing logic, same audit trail, same `--model` arg piped to the `claude` CLI as for the Cursor adapter.
- Tests: 182 still passing — the test suite uses synthetic registries with arbitrary pricing, so Haiku addition + Opus repricing has no test impact.

## v0.6.2

- **Receipts (not vibes).** Adds three reproducible benchmark harnesses that put real numbers behind the project's marketing claims, plus a top-level `TALKING_POINTS.md` truth table that pairs every defensible phrasing with the receipt that justifies it. Existing benches (`codegraph_ab.py`, `three_way.py`, `mcp_stress.py`) explicitly disclaimed "we don't capture live token billing" — these new ones close that gap.
- **`bench/router_savings.py`**: dry-run benchmark over a 6-task fixture (2 easy / 2 medium / 2 hard) that runs the production router for each task and compares `Puppetmaster pick` cost vs an "always frontier" baseline using the registry's pricing. On the starter registry under `balanced` policy, the router stayed on frontier for the 2 audit/architect tasks (honest) and picked cheaper-than-frontier for the other 4 — net **35.1% cheaper** at the model layer for the workload, **100% cheaper** on the four routable tasks (Cursor-tier models are $0 because billing rolls into the Cursor plan). Writes markdown + JSON receipt under `bench/results/router_savings_<timestamp>.{md,json}`. No API key required.
- **`bench/router_live_ab.py`**: live OpenAI A/B with **real** `tokens_in` / `tokens_out` pulled from `usage.prompt_tokens` / `usage.completion_tokens` in the API response — not estimates. Arm A pins the strongest OpenAI model (`gpt-5.5`); Arm B lets the router pick from the OpenAI-adapter pool. First production run on a simple definitional task: arm A spent **$0.019530 in 14.957 s**, arm B (router picked `gpt-5.4-nano`) spent **$0.000141 in 2.491 s** — **99.3% cheaper, 83.3% faster**, equivalent finding artifacts. Costs real money (~$0.02 of spend at the default prompt); supports `--dry-run` for routing-only inspection.
- **`bench/followup_cost.py`**: verifies the "0-token follow-ups" claim on a real completed job. Runs K rounds of 4 different reads (`get_job`, `list_artifacts`, filter findings, filter verifications) against the live SQLite store, measures wall time per query, asserts adapter calls and token usage are both 0. First run: 40 queries answered in **20 ms total** (avg **0.5 ms / query**), **0 adapter calls, 0 tokens, $0.00**. The receipt's "what re-running would cost" column references the same router math the other receipts use, so all three benchmarks cite consistent numbers. **Critical bug fix during build**: the harness initially used `SwarmStore(state_dir)` directly (file backend), which silently returns 0 jobs because production writes to the sqlite backend. Switched to `create_store("sqlite", state_dir)`.
- **`README.md` Receipts section.** A new "Receipts (not vibes)" section between the opening pitch and "The Problem" surfaces the headline numbers and links each LinkedIn-style claim to the script that produced it. Includes an explicit row for what is **not** currently defensible ("more accurate" needs a graded eval set we don't have yet).
- **`TALKING_POINTS.md`**: a truth table pairing "use this" phrasings with "avoid" overclaims for anyone posting about Puppetmaster. Includes a rewritten LinkedIn-ready version of the pitch where every quantitative claim has a script behind it.
- **`bench/README.md`**: expanded to register all six harnesses (router_savings, router_live_ab, followup_cost, three_way, codegraph_ab, mcp_stress) with "what it proves" / "what it does NOT prove" sections.

## v0.6.1-beta.2

- **Adapter stdout no longer silently loses the middle bytes.** All three production adapters (`cursor`, `claude-code`, `openai`) shipped a head-or-tail-only capture pattern: the verification artifact kept the last 8KB (12KB for Claude), the degraded risk artifact kept the first 1KB, and anything in between was dropped without trace. A worker that emitted ~50KB of markdown without a `{"artifacts":[...]}` envelope would lose ~41KB of evidence before a human ever saw it. Every adapter now spools the **full** subprocess stdout (and stderr where relevant) to a sidecar log at `<state_dir>/jobs/<job_id>/tasks/<task_id>/<adapter>_stdout.log`, and the verification + degraded risk artifacts both carry a new `stdout_capture` payload with explicit truncation markers: `stdout_total_chars`, `stdout_truncated`, `stdout_head_excerpt`, `stdout_tail_excerpt`, `stdout_sidecar_path`. Spooling is only attempted when content exceeds head+tail, so short outputs stay fully inline (no extra file churn). On a 67KB synthetic Cursor response with no artifacts envelope, the full text — including a middle-marker that the old code dropped — is now recoverable from the sidecar referenced by both artifacts.
- **`worker_runtime` exports `PUPPETMASTER_STATE_DIR`.** Adapter subprocesses inherited the worker's env but never knew which state directory owned the in-flight job. `worker_runtime.main` now resolves `--state-dir` once and stamps the resolved absolute path into `os.environ["PUPPETMASTER_STATE_DIR"]` before constructing the store. This lets `capture_subprocess_stdout` land sidecar logs in the same `jobs/<job_id>/tasks/<task_id>/` tree the store already owns — `puppetmaster show <job_id>` / `artifacts` show them next to everything else for the task without a separate cleanup path.
- **Graceful fallback when the env isn't set** (direct `CursorAdapter().run()` in unit tests, `bench/codegraph_ab.py` invocations, ad-hoc CLI explorations). The capture helper returns `stdout_sidecar_path: null` instead of trying to fall back to the workspace-hashed default state dir (which could land logs in a project that doesn't own the job). The new head/tail/total markers are still emitted so callers can detect — and surface — that bytes were dropped, even without a sidecar file.
- **Starter registry no longer ships placeholder model names.** The first live cursor swarm under v0.6.1-beta.1 routed to `cursor/gpt-5-5` which the registry mapped to `adapter_model_name: "gpt-5"` — Cursor's SDK rejected that with `Cannot use this model: gpt-5. Available models: default, composer-2.5, composer-2, gpt-5.5, ...`. The placeholder strings from v0.6.0 (`composer-1`, `gpt-5`, `claude-opus-4-5` for both Opus 4.6 and 4.7) are replaced with the real IDs the Cursor SDK and Claude Code CLI accept today: `composer-2.5`, `gpt-5.5`, `claude-opus-4-6`, `claude-opus-4-7`. `puppetmaster/model_registry.py:starter_registry()` and `docs/sample-models.json` are aligned, and `notes` fields drop the "PLACEHOLDER:" prefix because the values are now live-callable end-to-end. Live verification: a balanced-policy `puppetmaster_start_cursor_swarm` job picked `cursor/gpt-5-5 → gpt-5.5`, returned `cursor_status: finished`, `result: passed`, and a 0.95-confidence FINDING artifact with concrete code-line evidence — vs the v0.6.1-beta.1 path which failed in ~1 second with `failure: unknown`.
- Tests: 182 passing (added 6 covering the helper directly with short / long / no-state-dir inputs, plus end-to-end CursorAdapter regression tests proving the verification *and* degraded-risk artifacts both carry a sidecar pointer and that the middle bytes survive the round trip).

## v0.6.1-beta.1

- **Native OpenAI adapter.** Puppetmaster ships a third built-in adapter alongside `cursor` and `claude-code`: `openai` calls OpenAI's Chat Completions API directly via `OPENAI_API_KEY`, bypassing the Cursor SDK entirely. No new pip dependencies — pure `urllib` so the install footprint stays the same. Same structured artifact contract as the Cursor SDK adapter (JSON `{"artifacts":[...]}` reply parsed into `FINDING` / `RISK` / `DECISION` artifacts), same CodeGraph context injection, same promoted-memory enrichment. Token usage (`prompt_tokens` / `completion_tokens` / `total_tokens`) is captured on every `VERIFICATION` artifact for real cost accounting, not just estimates.
- **GPT-5 family in the starter registry.** `puppetmaster models init` now writes four new tiers alongside the Cursor/Claude entries — all with the **real** model ids and the public catalog pricing: `openai/gpt-5-5` ($5 / $30 per MTok, cap 96, 1M context, frontier + `detailed-vision`), `openai/gpt-5-4` ($2.50 / $15, cap 86, 1M context, quality workhorse), `openai/gpt-5-4-mini` ($0.75 / $4.50, cap 70, 400K context, balanced subagent), `openai/gpt-5-4-nano` ($0.15 / $0.90, cap 52, 400K context, cheap reader). These replace the v0.6.0 placeholder ids (`composer-1`, `gpt-5`) on the OpenAI tier — for the first time the routing decision picks a model that actually exists end-to-end. `docs/sample-models.json` synced to match.
- **Router pickups change for vision tasks.** Under `balanced`, a "OCR every detail of the diagram" prompt previously routed to `claude-code/opus-4-7` (cap 98, $0.12 estimated). It now routes to `openai/gpt-5-5` (cap 96, $0.048) because both carry the `detailed-vision` tag and the OpenAI tier undercuts Opus 4.7 on price. Audit tasks (need=98) still land on Opus 4.7 because gpt-5-5 caps at 96. Under `quality` policy Opus 4.7 still wins. The detailed-vision routing test was rewritten to assert the *tag* invariant rather than pin a specific model id.
- **New CLI subcommand.** `python -m puppetmaster openai "<prompt>" --model gpt-5.5 --cwd <repo> --max-output-tokens N --reasoning-effort high|medium|low|none|xhigh --temperature 0.4 --base-url <openai-compat-url> --organization <org-id> --legacy-max-tokens --disable-codegraph --timeout-seconds 300`. Daily-driver runs inline by default like the `cursor` / `claude` subcommands. The `--legacy-max-tokens` opt-in is for OpenAI-compatible providers (Together AI, etc.) that still want the deprecated parameter name.
- **Two new MCP tools.** `puppetmaster_openai` (sync) and `puppetmaster_start_openai` (async returning `job_id` immediately) raise the MCP tool surface from 30 to 32. Env passthrough: `openai_api_key`, `openai_base_url`, `openai_organization` MCP args map to `OPENAI_API_KEY` / `OPENAI_BASE_URL` / `OPENAI_ORG_ID` for the spawned worker.
- **`doctor` reports `OPENAI_API_KEY`.** Added alongside the existing `CURSOR_API_KEY` check so the first-time setup story is one `python -m puppetmaster doctor` invocation.
- **GPT-5 reality fixes.** The first live call surfaced two API breaks in the GPT-5 family: (a) `max_tokens` is rejected with `unsupported_parameter` — the adapter now sends `max_completion_tokens` by default and exposes `legacy_max_tokens` for old providers; (b) reasoning models accept a `reasoning_effort` parameter that the adapter now forwards from `payload.reasoning_effort`.
- **Cleanup: project-level `.cursor/mcp.json` removed.** It duplicated entries that the user-level `~/.cursor/mcp.json` already provides and showed up as "Disabled" duplicates in `Cursor Settings → Tools & MCPs`. Single source of truth is now the user-level config.
- Tests: 177 passing (added 11 covering happy-path artifact parsing, missing API key fail-fast, payload key override, HTTP 401/429 classification, timeout handling, base-url/organization/temperature passthrough, `max_completion_tokens` default + `legacy_max_tokens` opt-in, `reasoning_effort` forwarding, failure classifier buckets, and starter-registry tier structure). Live end-to-end smoke test against the real OpenAI API returned 3 artifacts (`VERIFICATION` + 1 `FINDING` + 1 `RISK`) in 4 seconds, `result=passed`, `finish_reason=stop`, tokens captured.

## v0.6.0-beta.3

- **Opus 4.7 starter score 96 → 98.** A real-world audit task scores 98 on the classifier (audit base 85 + `security` +15 + `every endpoint` +10, capped at 98). With the previous 96, every audit dry-run printed a noisy *"NO model meets capability need (98); falling back to highest-capability available"* — cosmetic, not actually broken, but it looked like a misconfiguration. Bumping Opus 4.7 to 98 makes it an exact-clear of the audit bar so the warning only fires when a user explicitly sets `payload.min_capability = 100`. `docs/sample-models.json` synced to the same value.
- **Test isolation: `tests/conftest.py` forces an empty registry during `pytest`.** After v0.6.0-beta.2's `auto_route: True` default on `DEFAULT_WORKERS`, any developer who had run `puppetmaster models init` on their machine would see Orchestrator tests start invoking the real `claude-code` / `cursor` adapters during the suite — tests are now hermetic regardless of `~/.puppetmaster/models.json` state. The fixture sets `PUPPETMASTER_MODELS_PATH` to a nonexistent path, which makes `_apply_auto_routing` a clean no-op. Tests that exercise routing directly still write their own registry into a `TemporaryDirectory` and pass `payload.registry_path` explicitly, so this fixture does not interfere with them.
- 162/162 tests passing on a machine with a populated user registry (previously: 8 regressions).

## v0.6.0-beta.2

- **Auto-routing is now the default on built-in swarm workers.** `DEFAULT_WORKERS` (used by `puppetmaster_start_cursor_swarm`, `puppetmaster_start_swarm`, and `python -m puppetmaster run`) now ship with `payload.auto_route = True`. Any swarm started through Puppetmaster's MCP surface now consults the router automatically — no per-spec opt-in required. If `~/.puppetmaster/models.json` doesn't exist yet, the orchestrator emits one `router.registry_empty` event and falls back to the spec's declared adapter, so this change is safe even before running `models init`.
- **Vision-aware routing.** Classifier now scans the instruction for vision signals (image, screenshot, diagram, chart, OCR, visual, photo) and auto-adds `vision` to the required tags. "Detailed" vision tasks (OCR / "every detail of the diagram" / explicit `detailed` qualifiers) also add `detailed-vision`. Models in the registry that don't carry those tags get filtered out with a clear rejection reason, so a vision request never lands on a text-only model.
- **Starter registry rebuilt to match the user mental model.** `puppetmaster models init` now writes four tiers: `cursor/composer-2-5` (fast / cheap / reading), `cursor/gpt-5-5` (balanced), `claude-code/opus-4-6` (high-quality, vision), `claude-code/opus-4-7` (frontier, detailed-vision). `adapter_model_name` defaults to model ids that already work today (`composer-1`, `gpt-5`, `claude-opus-4-5`); each entry's `notes` field flags what to edit when newer versions land. Tier ids stay stable.
- **Balanced-policy tie-break flipped.** When two sufficient models cost the same (common with `$0` Cursor-plan models), the router now picks the **lower** capability_score instead of the higher one — right-sizing the model to the task instead of wasting capability. Matches user intent ("easy tasks get the cheap tier even when expensive tiers are technically 'free' under my Cursor plan").
- **New `puppetmaster cost <job_id>` CLI + `puppetmaster_job_cost` MCP tool.** Sums `estimated_cost_usd` across all `ROUTING` artifacts for a job and breaks it down per-model. Answers "how much did this swarm cost?" with the same data the router already wrote.
- Tests: 162 passing (added 9 covering vision detection, detailed-vision detection, auto-required vision tag, starter-registry tier structure + monotone capability, balanced tie-break, default-workers opt-in, empty-registry pass-through, and the cost command).

## v0.6.0-beta.1

- **Intelligent model orchestration.** Puppetmaster now ships a task-aware **model router** that picks the right LLM for each task instead of pinning one model per adapter. Every swarm role can opt in by setting `payload.auto_route = true`; the orchestrator then:
  1. Runs a transparent capability classifier over the task's role + instruction + payload to assign a 0..100 score (e.g. `verify-runtime` ≈ 25, `explore` ≈ 50, `implement` ≈ 75, `audit/security-review` ≈ 90+).
  2. Picks a `ModelSpec` from the user-owned registry using one of four policies — `balanced` (default, cheapest sufficient), `cheap` (lowest cost regardless of fit), `quality` (highest capability), `escalating` (ordered chain for retries).
  3. Stamps the chosen `adapter` + model name into the task so the existing adapter pipeline runs the routed model with zero plumbing changes.
  4. Persists an `ArtifactType.ROUTING` artifact recording the chosen model, the classifier output, the estimated USD cost, and the full list of rejected alternatives with the reason each was rejected — so every routing decision is fully auditable in `puppetmaster show` / `puppetmaster artifacts`.
- **User-owned model registry.** Lives at `~/.puppetmaster/models.json` (override with `$PUPPETMASTER_MODELS_PATH`). Users describe their own models, prices, and asserted capability scores; Puppetmaster never hardcodes model names or fetches live prices. `puppetmaster models init` writes a starter registry; `puppetmaster models list` prints it as a table.
- **`puppetmaster route "<instruction>"` CLI** and `puppetmaster_route_task` / `puppetmaster_list_models` MCP tools. Dry-run a routing decision before kicking off a swarm — see which model would run, the estimated cost, and why the cheaper alternatives were rejected.
- **Per-task overrides:** `payload.min_capability` (force classifier output), `payload.max_cost_usd` (hard budget cap), `payload.required_tags` (e.g. `["long-context"]`), and `payload.routing_policy` (one of `balanced`/`cheap`/`quality`/`escalating`).
- **Honest scope:** v1 routes to adapters we already have (`claude-code`, `cursor`) — those alone cover Sonnet/Haiku/Opus and any Cursor SDK model variant. Raw HTTP adapters for additional providers (Gemini, DeepSeek, Kimi, OpenAI direct) slot in as new `adapter` values; the registry + router/classifier framework doesn't need to change. Pricing and capability scores stay user-asserted — Puppetmaster makes the **decision** transparent, not the **value judgments**.
- Tests: 153 passing (added 18 covering classifier behavior across roles, all four policies, tag filters, cost budget caps, registry round-trip, env override, CLI `route --json`, and end-to-end orchestrator integration that emits a `ROUTING` artifact tied to the real task id).

## v0.5.6-beta.1

- **Root-cause fix for "MCP drops on robust questions".** Found via a new live-fire stress harness (`bench/mcp_stress.py`) that drives the real MCP server over stdio with six load patterns. The failing pattern: `puppetmaster_doctor` (which fans out to multiple `subprocess.run` calls for `git`/`node`/`npm`/`codegraph status`) called in parallel from many requests would silently kill the server with `exit_code=0` and `stdin EOF`. Every `subprocess.run`/`subprocess.Popen` in the server-touched code path was inheriting the parent's fd 0 by default. Under concurrent spawn pressure, that inheritance somehow caused the parent's `for line in sys.stdin` loop to receive a phantom EOF and exit cleanly — looking from Cursor's side exactly like "Tool execution error. Not connected". The fix: every subprocess call in `codegraph.py`, `codegraph_repair.py`, `codegraph_index_runner.py`, `state.py`, `diagnostics.py`, and `mcp_server.py` now passes `stdin=subprocess.DEVNULL`, so children never touch the parent's stdin.
- **New stress harness for regression detection.** `python -m bench.mcp_stress` runs six scenarios against a real-spawned MCP server: 20 parallel doctor calls, 64 simultaneous senders, large payload, doctor + codegraph mixed, sustained 5-rps traffic, and idle->busy->idle transitions for the idle keepalive. All six pass on v0.5.6. Run anytime to verify the production server behavior in <90s.
- **New `PUPPETMASTER_MCP_DIAG_EXIT=1` env flag** that prints the main-loop exit reason on stderr (`stdin_eof`, `sigint_clean_shutdown`, `main_loop_exception: ...`). Off by default; turn on temporarily when an orphan server exits unexpectedly to learn why.
- New regression test (`test_parallel_doctor_calls_do_not_kill_mcp_server`) spawns the real server over stdio, sends 30 parallel doctor calls from 30 sender threads, and asserts every response returns and the server stays alive. This is the unit-level guard for the v0.5.6 fix.
- Tests: 135 passing (added 1 regression). The stress harness was the diagnostic that surfaced the bug; the unit test is the day-to-day guard.

## v0.5.5-beta.1

- **CodeGraph indexes are now per-repo, not machine-wide.** The previous lock was over-eager: each repo has its own SQLite DB at `<repo>/.codegraph/codegraph.db`, so two indexers on two different repos can't trash each other's data. The lock now keys on the resolved repo root path hash (`codegraph-indexer-<repo>-<digest>.lock`), so `ff-data-engineering`, `ff-ios`, and `ios-qa-agent-1` can index in parallel. The lock only blocks legitimate overlap on the same SQLite DB. Legacy callers passing no `repo_root` still get the global lock file for backwards compatibility.
- **Stale-PID auto-clear on lock acquire.** If the lock file records a PID that is no longer alive (`os.kill(pid, 0)` raises `ProcessLookupError`), the new claimant truncates the file and retries the `flock` once before reporting busy. This fixes the post-`kill -9` failure mode where a runaway `codegraph init --index` leaves a lock file pointing at a dead PID and every subsequent indexer attempt sees `Another CodeGraph indexer is already running (pid 80417)` indefinitely. Lock files owned by a *different* user are never blown away (treated as alive by design).
- **Idle MCP-pipe keepalive.** Complements v0.5.3's per-tool-call keepalive: a new `_IdleKeepalive` thread emits a small `notifications/message` every 25s when no tool call is in flight and the server is otherwise idle. Some Cursor builds close the stdio MCP transport for a chat that has been quiet for a while, producing `Tool execution error. Not connected` on the agent's very next call — even though the daemon is healthy. Bytes flowing on the pipe defeat that heuristic. Frames suppress automatically while any tool call is running (the per-call keepalive already handles that window) and stop on `BrokenPipeError`. Cost: ~150 bytes per frame, ~22 KB/hour. Tunable via `PUPPETMASTER_MCP_IDLE_KEEPALIVE_INTERVAL_SECONDS` (default 25, minimum 5) and `PUPPETMASTER_MCP_IDLE_KEEPALIVE_DISABLED=1`.
- **Agents now know how to fall back to the CLI when MCP drops.** The Cursor rule (`.cursor/rules/puppetmaster-workflow.mdc`) and `AGENTS.md` both gained a "When MCP fails" section with a complete mapping from every `puppetmaster_*` MCP tool to its `python -m puppetmaster ...` CLI equivalent (plus the bundled `codegraph` commands). The rule explicitly tells agents to **not** stop work on `Tool execution error. Not connected`, and to only ask the user to restart MCP in Cursor Settings if `python -m puppetmaster mcp list` shows zero alive servers. Read-only CLI commands auto-pivot to the project state dir that owns the job (v0.5.4-beta.2), so the fallback works from any cwd with zero manual state-dir wrangling.
- Tests: 134 passing (added 9 covering per-repo lock paths, parallel-different-repo claims, same-repo busy, stale-PID auto-clear, the `_pid_is_alive` helper, idle keepalive emission, suppression-during-tool-call, broken-pipe stop, env disable). End-to-end smoke-tested: `notifications/message` `idle_keepalive` frames arrive on the wire at the configured interval while the server is idle.

## v0.5.4-beta.2

- **Adapter detection now looks where the SDK actually lives.** `puppetmaster adapters` and `puppetmaster doctor` previously reported `cursor: configured=false` from any workspace other than the Puppetmaster install dir, because `_cursor_sdk_installed` only checked `<cwd>/node_modules/@cursor/sdk` — but `cursor_sdk_runner.mjs` resolves the SDK from the Puppetmaster *package* dir at runtime. The check now searches both the workspace and the package install (and an explicit `PUPPETMASTER_HOME` escape hatch), so diagnostics agree with what the swarm actually does.
- **Cross-workspace job lookup for read-only CLI commands.** `puppetmaster show`, `artifacts`, `diff`, `feed`, `logs`, `events`, `status`, `memory`, and `open` now auto-pivot to the project state dir that owns a given `job_id` if it isn't present in the current workspace's state dir. No more `PUPPETMASTER_STATE_DIR=~/Library/.../ff-data-engineering-cfbfad67d9fc python -m puppetmaster show ...` — just paste the job id from any cwd. Explicit `--state-dir` or `$PUPPETMASTER_STATE_DIR` overrides still take precedence (write-side commands always use the caller's workspace, never pivot).
- **New `puppetmaster projects` command + `puppetmaster jobs --all-projects` flag.** `projects` lists every Puppetmaster project state dir on this machine with job counts and last activity. `jobs --all-projects` flattens jobs from every project into one stream with a project column. Together they make "which workspace owns this job?" a one-liner.
- **Orphan self-termination via stdin staleness watcher.** Cursor's MCP "lease" lifecycle periodically re-creates the logical client without killing the previous Python server, so before this release Puppetmaster could accumulate one MCP server per lease cycle — each still holding SQLite handles and competing for the CodeGraph indexer lock. The new `_InputStalenessWatcher` measures inbound JSON-RPC traffic directly (not just process liveness like the heartbeat thread): if no stdin message arrives for 10 minutes **and** there are zero in-flight tool calls, the watcher closes stdin so the main loop sees EOF and exits through the normal `finally` block (deregister, stop heartbeat, shut down executor). No `os._exit`, no leaked tracking files. Tunable via `PUPPETMASTER_MCP_INPUT_STALE_SECONDS`, `PUPPETMASTER_MCP_INPUT_STALE_CHECK_SECONDS`, and `PUPPETMASTER_MCP_INPUT_STALE_DISABLED`.
- **Explicit Cursor-Node invocation for CodeGraph.** New `resolve_codegraph_invocation()` in `puppetmaster.codegraph` returns `[cursor_node, codegraph.js]` whenever Cursor's bundled Node and the global `@colbymchenry/codegraph` install are both discoverable. Every subprocess that previously invoked the bare `codegraph` shim — `codegraph_context`, `codegraph_status_line`, `run_codegraph_cli`, and the background `codegraph_index_runner` — now invokes CodeGraph under Cursor's Node first, with a transparent fallback to the PATH shim when Cursor isn't installed (Linux, headless CI). This closes the 18-hour-runaway-indexer class of bug, where a stray shell invocation under Homebrew Node would fall back to WASM SQLite, lock the DB, and burn 99% CPU for hours. `PUPPETMASTER_CODEGRAPH_NODE` / `PUPPETMASTER_CODEGRAPH_JS` env vars are an escape hatch for non-standard installs.
- **Doctor distinguishes shell-Node WASM from Cursor-Node WASM.** `puppetmaster doctor`'s `codegraph` check used to warn whenever the shim on PATH reported WASM, even after `repair-codegraph` had successfully rebuilt better-sqlite3 for Cursor's Node — leading to misleading "broken" diagnoses when MCP was actually healthy. The check now runs against the same invocation Puppetmaster uses at runtime (`resolve_codegraph_invocation()`) and reports `ok (verified under Cursor's bundled Node)` when the runtime that matters is healthy.
- New tests covering invocation-resolution under Cursor Node, environment overrides, the fallback path, staleness-watcher trigger/hold-off/reset, env-disable, dispatcher counter, doctor's Cursor-Node disambiguation, SDK detection from the package install dir, and cross-workspace job lookup. Total suite at 125 tests, all passing.

## v0.5.3-beta.1

- **Tool-call keepalive notifications.** Every long-running MCP `tools/call` now spawns a per-call `_ToolCallKeepalive` daemon thread that emits MCP-spec `notifications/message` frames every 10 seconds (after a 5-second grace period) for as long as the handler runs. Short calls pay zero protocol cost; the goal is to keep bytes flowing on the stdio pipe so Cursor's MCP client stops treating slow tool calls as a dead transport. This is the prevention complement to v0.5.2's cleanup — together they cover both "stop the drop from happening" and "recover cleanly when it does."
- All keepalive frames are unidentified JSON-RPC notifications (no `id`) and carry a structured `data.kind = tool_call_progress` payload with the tool name, request id, and elapsed time so logs and progress UIs can render them.
- Tunable via `PUPPETMASTER_MCP_KEEPALIVE_AFTER_SECONDS`, `PUPPETMASTER_MCP_KEEPALIVE_INTERVAL_SECONDS`, and `PUPPETMASTER_MCP_KEEPALIVE_DISABLED`. Default behaviour is on and conservative.
- Broken-pipe detection: if the emitter sees a dead stdio (`BrokenPipeError`/`OSError`), the keepalive thread shuts down cleanly instead of spinning. Post-response races are also closed via a stop-check inside the `_STDOUT_LOCK`.
- README troubleshooting section now distinguishes the prevention layer (this release) from the recovery layer (v0.5.2 mcp registry + cleanup tools).

## v0.5.2-beta.1

- **MCP server process registry + orphan cleanup.** Every Puppetmaster MCP server now writes a tracking file (PID, workspace, started_at, last_heartbeat) at startup, bumps its heartbeat from a daemon thread, and removes its file on a clean exit. New servers prune dead tracking files on startup so orphans from prior `Tool execution error. Not connected` cycles get reaped automatically — no more `pkill -f puppetmaster.mcp_server` from a shell.
- New `puppetmaster/mcp_registry.py` module with `register()`, `heartbeat()`, `deregister()`, `list_entries()`, `prune_dead()`, `kill_stale()`, and a `HeartbeatThread`. Per-user storage lives under `~/Library/Caches/puppetmaster/mcp-servers/` on macOS and `$XDG_CACHE_HOME/puppetmaster/mcp-servers/` on Linux; overridable via `PUPPETMASTER_MCP_REGISTRY_DIR`.
- New CLI surface: `python -m puppetmaster mcp list` (with `--json`) and `python -m puppetmaster mcp cleanup` (`--kill-stale`, `--stale-after-seconds`, `--json`). Lists every tracked server with alive/stale/dead flags and reclaims dead entries; `--kill-stale` SIGTERMs (and SIGKILLs after a grace period) servers whose heartbeat is older than the threshold. The current PID is never signalled.
- New MCP tools `puppetmaster_mcp_status` and `puppetmaster_mcp_cleanup` so an agent can self-diagnose and self-heal right after a transport reconnect without you running anything in a shell.
- `puppetmaster doctor` now includes an `mcp-servers` check that warns when dead tracking files or stale-but-alive servers are present, and points at the new cleanup command. This is the smoking gun behind a stale `Tool execution error. Not connected` symptom.
- README: new "`Tool execution error. Not connected` from Cursor" Troubleshooting section explaining the failure mode (Cursor closes the stdio transport while the swarm keeps running), the diagnostic command, the cleanup command, and the durable-state implication that orphan reconnects are recoverable.

## v0.5.1-beta.1

- **Self-healing CodeGraph SQLite repair.** New `python -m puppetmaster repair-codegraph` CLI command and matching `puppetmaster_repair_codegraph` MCP tool that auto-detect Cursor's bundled Node, locate the global `@colbymchenry/codegraph` install, and `npm rebuild better-sqlite3` against the right runtime — then verify the backend reports as native. This closes the most common Cursor footgun: a shell-Node `npm rebuild` builds for the wrong ABI (Homebrew Node 23 vs. Cursor's bundled Node 22), so the Puppetmaster MCP keeps falling back to slow WASM SQLite and emits spurious `database is locked` errors.
- Rewrite `CODEGRAPH_NATIVE_SQLITE_HINT` and `puppetmaster doctor`'s warn message to explain the dual-Node ABI trap and point users at `puppetmaster repair-codegraph` instead of the generic `npm rebuild` command that lands on the wrong runtime.
- New `puppetmaster/codegraph_repair.py` module with `find_cursor_node()`, `find_codegraph_install()`, and `repair_codegraph_sqlite()` — covered by tests for macOS/Linux/Windows candidate paths, explicit overrides, rebuild failure surfacing, and verification parsing.
- README: new Troubleshooting section documenting the dual-Node-ABI gotcha, the ABI tradeoff (rebuilding for Cursor breaks the terminal until you rebuild back), and the one-command fix.

## v0.5.0-beta.1

- **Multi-threaded MCP request dispatch.** Replace the single-threaded `for line in sys.stdin` loop with a `ThreadPoolExecutor` so long-running tool calls never block the stdio transport. This fixes the "Tool execution error. Not connected" failure mode where a heavy CodeGraph call would silently freeze every other agent tool call. Worker count is configurable via `PUPPETMASTER_MCP_WORKERS` (default 8).
- **Background CodeGraph indexer.** `puppetmaster_codegraph_init(index=true)` now runs the fast init synchronously and dispatches the slow `codegraph index` to a detached subprocess via the new `puppetmaster/codegraph_index_runner.py` launcher, returning immediately with a `run_id`, `pid`, and stdout/stderr log paths. Added a dedicated `puppetmaster_codegraph_index` MCP tool for the standalone background-index case.
- **Global indexer lock.** A user-scoped lock file (`~/Library/Caches/puppetmaster/codegraph-indexer.lock` on macOS, `$XDG_CACHE_HOME/puppetmaster/...` on Linux, overridable with `PUPPETMASTER_CODEGRAPH_LOCK_DIR`) serializes CodeGraph indexers so two parallel runs can never trash a SQLite database. Lock-busy returns a clear `CodegraphLockBusy` payload with the holder PID and a `pkill` hint.
- **Broken native-SQLite detection.** `puppetmaster doctor` and `puppetmaster_codegraph_status` now flag the `better-sqlite3` WASM fallback (Node ABI mismatch) and surface the `npm rebuild better-sqlite3` fix-it hint. `codegraph_native_sqlite_broken(status_output)` is the helper.
- Shorten `DEFAULT_INIT_TIMEOUT_SECONDS` from 600 → 60 since synchronous init is now bounded to the fast scaffold-creation step; indexing is always asynchronous.

## v0.4.0-beta.1

- Bundle six CodeGraph CLI commands into the Puppetmaster MCP (`puppetmaster_codegraph_search`, `_context`, `_affected`, `_files`, `_status`, `_init`) so Cursor Agent only needs one MCP server for both orchestration and repo intelligence.
- Auto-inject CodeGraph context into Cursor and Claude Code worker prompts when the target repo is initialized; tag verification artifacts with `context:codegraph` so the operator can confirm shared intelligence was used.
- Make MCP swarm starts asynchronous: `puppetmaster_start_swarm`, `_start_cursor_swarm`, and friends return a `job_id` immediately instead of holding the MCP call open.
- Add push-style live artifact streaming: `puppetmaster_live_artifacts_follow` (MCP) and `python -m puppetmaster feed <job> --follow` (CLI) long-poll the durable SQLite event cursor and return as soon as a new artifact lands.
- Add warm worker daemon mode (`python -m puppetmaster daemon --roles ...`) for local swarms that want to avoid repeated worker process startup.
- Make Puppetmaster the default subagent runtime: `.cursor/rules/puppetmaster-workflow.mdc` (`alwaysApply: true`) plus a top-level `AGENTS.md` direct Cursor Agent and Claude Code to route non-trivial work through Puppetmaster by default.
- Move runtime state out of the repository by default (macOS: `~/Library/Application Support/puppetmaster/projects/...`, Linux: `~/.local/state/puppetmaster/projects/...`). Override with `--state-dir` or `PUPPETMASTER_STATE_DIR`.
- Parse Cursor SDK swarm output into typed artifacts instead of leaving raw stdout blobs.
- Add cost-structure benchmark `bench/three_way.py` (Agent only vs CodeGraph alone vs Puppetmaster + CodeGraph) and prompt-enrichment harness `bench/codegraph_ab.py`. Numbers in the README and `bench/README.md` are reproducible from these scripts.
- Reposition the README so Puppetmaster's standalone identity (durable state for parallel coding-agent swarms) is the headline, with CodeGraph as an explicit optional integration.

## v0.3.0-beta.1

- Ship the Cursor / VS Code extension with a Puppetmaster activity-bar control panel.
- Add the Cursor Agent MCP server so Agent chat can call Puppetmaster tools directly.
- Add MCP tools for `doctor`, Cursor review/plan, Claude Code implementation, logs, artifacts, and partial summaries.
- Add the Claude Code full-edit adapter with dirty-worktree protection and patch artifact capture.
- Fix editable Python install for local source-checkout usage.
- Expand Cursor, MCP, Claude Code, and daily-driver docs.

## v0.2.0-beta.1

- Position Puppetmaster as a supervised daily-driver beta for Cursor swarm workflows.
- Add run-management commands for latest job lookup, logs, reruns, cleanup, patch artifact review, and approval events.
- Add dry-run Cursor workflow support.
- Add full-edit Claude Code CLI adapter and `puppetmaster claude` command.
- Harden job and worker failure handling.
- Add SQLite schema version metadata and doctor validation.
- Retrieve promoted memory into new task payloads.
- Expand production, security, contribution, and release documentation.

## v0.1.0

- Establish the core local swarm runtime.
- Add subprocess workers, structured artifacts, stitching, and memory promotion.
- Add file and SQLite coordination backends.
- Add shell and optional Cursor adapters.
- Add crash recovery demo, CI, and starter docs.

## Planned v0.2.0

- Broaden provider adapters beyond Cursor.
- Improve patch artifact generation and isolated apply flows.
- Add richer watch output and scripting-friendly JSON modes.
