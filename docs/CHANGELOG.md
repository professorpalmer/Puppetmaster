## v1.19.8

**Hashline content-hash edits + mtime read cache for agentic workers (OMP-inspired).**

- New `apply_hashline` tool: surgical line edits anchored to a 4-hex file tag; stale tags reject before write.
- Tagged `read_file` output (`[path#TAG]` + `N:line`) feeds anchors; `edit_file` / `write_file` remain as fallback.
- Per-run mtime/size `fs_cache` for repeated reads; invalidated on mutators.
- Kill switches: `PUPPETMASTER_HASHLINE=0`, `PUPPETMASTER_FS_CACHE=0`. Block ops (`*.BLK`) deferred.

## v1.19.7

**Hermes-style segmented tool-batch concurrency for agentic workers.**

- Parallel-safe reads (`read_file` / `search_code` / graph / `web_fetch` / `list_dir`) in a mixed batch run concurrently; barrier tools stay sequential.
- Preserves model emission order for tool results (submit/plan remain barriers).
- Kill switch: `PUPPETMASTER_TOOL_BATCH_PARALLEL=0`.
# Changelog

## Unreleased

## v1.19.6

**Same-adapter model fallback (OMP fallbackChains analogue).**

- On `model_unavailable` / `no_model` / `unknown_provider` / `rate_limit`, auto-fallback can re-route to another registry model on the **same** adapter when no other platform is funded.
- Cross-adapter billing/auth recovery still preferred when a healthy alternate platform exists.
- Tracks `tried_models` / `fallback_from_model` on the ROUTING artifact. Coverage in `AutoFallbackTests`.

## v1.19.5

**Per-role swarm routing_policy defaults.**

- Built-in analysis roles stamp routing_policy under auto_route: explore/test cheap; architect/plan balanced; redteam/review quality.
- No model pins; ANALYSIS_NO_EDIT_PAYLOAD preserved.
- Coverage in tests/test_role_routing_defaults.py.

## v1.19.4

**Plan-then-cheap prewalk (OMP-style).**

- `python -m puppetmaster prewalk "<goal>"` / `puppetmaster_start_prewalk`: quality-routed read-only plan worker then cheap edit-capable implement (`depends_on_roles`).
- Runtime injection of plan/decision artifacts into implement prompts via `with_prewalk_plan`.
- Unit coverage in `tests/test_prewalk.py`.
## v1.19.3

**Bedrock ConverseStream for live agentic chat deltas.**

- Real `ConverseStream` eventstream parsing (text / reasoning / toolUse deltas) via stdlib  no boto3.
- `bedrock_chat_stream` preferred over fake chunked non-SSE; `provider_chat` streaming routes Bedrock through it.
- Hermetic tests cover event framing, delta fold, and usage normalization on the stream path.

## v1.19.2

**Keep default swarms read-only through adapter routing.**

- `DEFAULT_WORKERS` now carry the same explicit no-edit payload fields as generated MCP swarms (`read_only`, `sandbox=read-only`, no sandbox bypass), so a bare `puppetmaster_start_swarm` / `puppetmaster run` stays analysis-mode when auto-route lands on Claude Code or Codex.
- Shared `ANALYSIS_NO_EDIT_PAYLOAD` constant so `DEFAULT_WORKERS` and `write_generated_swarm_config` cannot drift.
- Regression: default `implement` planning role routed to Claude Code uses `permission_mode=plan` and skips the write-capable worktree guard.

## v1.19.1

**Bedrock Converse multi-model daily driver: account discovery, prompt cache, cost/token parity.**

- Chat path uses Bedrock **Converse** (not Anthropic-only InvokeModel) so Claude, Nova, Llama, Mistral, DeepSeek, Z.AI GLM, Moonshot, Qwen, MiniMax, etc. share one native schema under `provider=bedrock`.
- `list_chat_model_ids()` discovers the caller's allow-listed catalog via `ListFoundationModels` + `ListInferenceProfiles` (IAM auth  account-specific, not a hardcoded Claude list).
- Agentic registry seeding / `models discover --source agentic` merges live Bedrock ids (diversified across providers) when AWS credentials are present.
- Converse **prompt cache**: stamps `cachePoint` on system, `toolConfig`, and the last two messages (same kill switch as other providers: `PUPPETMASTER_PROMPT_CACHE=0`). Live-verified cache write/read on Claude Haiku + Nova.
- Usage normalization folds Bedrock's uncached-only `inputTokens` + cache read/write into `prompt_tokens` so `tokens_cached` stays a subset of `tokens_in`  swarm cost, `price_job` cache-read discount (0.1x), and Marionette `cache_savings_usd` meter Bedrock like Anthropic/OpenRouter.
- `provider_key_pool("bedrock")` only returns bearer tokens  access-key / `~/.aws` IAM never enters the rotation pool (would be misread as Bearer).
- Model-id gate accepts any Bedrock provider-shaped id (`deepseek.v3.2`, `zai.glm-5`, `amazon.nova-`), not only `anthropic.*`.
- SigV4 canonical URI double-encodes path segments so inference-profile ids with `:` sign correctly (`%3A` -> `%253A`).
- Content-Type for Bedrock Runtime JSON is `application/json`.

## v1.19.0

**AWS Bedrock as a first-class agentic provider (BYOK).**

- New `provider=bedrock` in `PROVIDER_REGISTRY` (label AWS Bedrock, Anthropic wire shapes) with region-derived `https://bedrock-runtime.{region}.amazonaws.com`.
- Stdlib-only `puppetmaster/bedrock.py`: bearer (`AWS_BEARER_TOKEN_BEDROCK`) or SigV4 `InvokeModel`  no boto3. Short model ids rejected with an actionable inference-profile message; missing AWS creds fail cleanly (`aws configure` / bearer / access keys).
- `provider_chat` / streaming path routes `provider=bedrock` through the new client (not Anthropic.com x-api-key).
- Hermes effort-home: if `os.symlink` fails (common on Windows without Developer Mode), copy `auth.json` / `.env` instead of dropping credentials.

## v1.18.1

**Windows CodeGraph WinError 2: spawn `.cmd` shims and new npm-shim layout.**

- Route `codegraph.CMD` / `npx.CMD` through `COMSPEC /c` so `CreateProcess` no longer fails with WinError 2 on Windows (Marionette indexer + MCP).
- Recognize current `@colbymchenry/codegraph` entrypoints (`npm-shim.js` and platform optionalDependency CLIs), not only legacy `dist/bin/codegraph.js`.
- Prefer `node` + JS entry over the bare PATH shim when the install is discoverable.

## v1.18.0

**AGNT-style all-1h Anthropic prompt cache (agentic BYOK).**

- Claude breakpoints (system, last tool, and moving history) all default to `ttl:1h`  hybrid 1h-stable/5m-history double-wrote history each turn.
- Qwen stays ephemeral-only (no ttl). Automatic providers (OpenAI/Gemini/DeepSeek/Grok/Moonshot) remain unmarked.
- Override with `PUPPETMASTER_ANTHROPIC_CACHE_TTL=5m|off`. Paired with Marionette v0.9.29.

## v1.17.0

**OpenAI-wire explicit prompt cache for Claude + Qwen (OpenRouter).**

- Agentic `_openai_chat` / `_openai_chat_stream` stamp `cache_control` for Claude (1h stable / 5m history) and Qwen (ephemeral) when the model requires explicit breakpoints.
- OpenRouter sticky `session_id` on those paths. Automatic-cache providers (OpenAI/Gemini/DeepSeek/Grok/Moonshot) left unmarked.
- Kill switch `PUPPETMASTER_PROMPT_CACHE=0`; Claude TTL override `PUPPETMASTER_ANTHROPIC_CACHE_TTL=5m|off`.


## v1.16.0

**Anthropic prompt-cache: 1h stable breakpoints, 5m moving history.**

- Stable markers (system + last tool) default to `{"type":"ephemeral","ttl":"1h"}` so long agent loops keep cheap cache reads.
- Moving history markers omit `ttl` (Anthropic's implicit 5m write) so each turn does not pay 2x on content that shifts immediately.
- Override dense loops with `PUPPETMASTER_ANTHROPIC_CACHE_TTL=5m|off`.

## v1.15.0

**Objective job receipts: was this run useful, or mostly transport tax?**

- **`puppetmaster receipt <job_id>`:** compact run-efficiency telemetry â degraded tasks, typed artifacts (finding/risk/decision/patch), empty/unstructured + stdout-salvage signals, token totals, tokens per typed artifact. Complements `cost` (dollars) without prose synthesis. `--json` for machines.
- **`puppetmaster_job_receipt` MCP tool:** same payload as CLI `--json`, next to `puppetmaster_job_cost`.
- **PATCH counts as typed:** implement-only jobs are not scored as zero-yield when they shipped a reviewable diff.

## v1.14.2

**GPT-5.6 Sol/Terra/Luna registry + safer nested artifact unwrap.**

- **GPT-5.6 family:** starter registry and curated codex/agentic/hermes catalogs gain `gpt-5.6-sol` (alias `gpt-5.6`), `gpt-5.6-terra`, and `gpt-5.6-luna` with public pricing ($5/$30, $2.50/$15, $1/$6). OpenAI CLI/MCP `--reasoning-effort` accepts `max`.
- **Nested wrapper unwrap guard:** `cursor_artifact_from_item` only unwraps `{"finding":{...}}`-style wrappers when top-level `type` is absent, so a typed artifact that also carries a nested finding/risk/decision dict is not clobbered (follow-up to #16).

## v1.14.1

**Price the run that actually worked after router fallback.**

- **Final routing wins:** `price_job` prefers `router-escalation` / `router-fallback` over the initial `router` pick so a failed plan-billed Cursor attempt (`sdk_not_installed`) does not zero a successful agentic fallback.
- **Best usage record:** token aggregation and pricing pick the successful / measured / higher-volume usage stamp per task instead of first-wins, so failed first attempts stop poisoning job totals.

## v1.14.0

**Worker thrift: savings-gated tool offload, history compaction, shared job brief.**

- **Tool-output offload:** large agentic tool results spill to durable blobs with head/tail previews and measured ledger credit (`puppetmaster/tool_offload.py`); kill switch `PUPPETMASTER_TOOL_OFFLOAD=0`. Surfaces in `puppetmaster savings` as `tool_offload`.
- **History compaction:** older tool results stub after N turns or over context budget while the static system prefix stays byte-stable for prompt cache (`adapters/_context_budget.py`); kill switch `PUPPETMASTER_HISTORY_COMPACT=0`.
- **Per-job CodeGraph/repo brief:** one census + goal CodeGraph block computed at job start, persisted under the job dir, injected identically into sibling workers via `insert_before_task`; kill switch `PUPPETMASTER_JOB_BRIEF=0`.

## v1.13.0

**Shared platform.json with Marionette + swarm prompt-cache lifts.**

- **Platform lock read-modify-write:** `_write_disabled` preserves foreign keys in `~/.puppetmaster/platform.json` (notably Marionette's `harness_initialized` marker) instead of replacing the whole document. Previously `platform enable` stripped unknown keys and the next Marionette boot re-applied agentic-only defaults, silently undoing the operator's enable. Defense-in-depth pair with Marionette v0.9.17's gentler `_init_platform_lock`.
- **Anthropic prompt-cache breakpoints:** `provider_chat` marks system + tools + two rolling history blocks with `cache_control` (kill switch `PUPPETMASTER_PROMPT_CACHE=0`); usage parses `cache_write_tokens`.
- **Static-first worker prompt assembly:** implement/structured prompts put invariant boilerplate first and the task instruction last so parallel swarm workers share a cacheable prefix; CodeGraph/memory/skills inject before the task section.

## v1.12.1

**Grok 4.5 Cursor workhorse + truthful `sdk_not_installed` failures.**

- **`cursor/grok-4-5` in the starter registry:** SpaceXAI Grok 4.5 (Cursor catalog id `grok-4.5`, released 2026-07-08) lands as the plan-billed Cursor workhorse at `capability_score=97` â above Opus 4.6 / GPT-5.5, under Opus 4.7/4.8 and Fable 5. CursorBench 3.2 High is 66.7% (above Opus 4.8 Max 62.3%, below Fable 5 Max 70.5%) at ~1/10th Fable Max cost per task; balanced routing prefers Grok for most hard Cursor work and reserves Opus/Fable for tip-of-stack. Existing registries: `puppetmaster models discover --source cursor --write` (or re-init) plus set `capability_score=97` / tags if discover seeded the default 60.
- **`sdk_not_installed` is recoverable:** a Cursor worker that fast-fails because `@cursor/sdk` is missing now records a FAILED task (same class as `no_model` / `unknown_provider`) instead of a green COMPLETE dead run.

## v1.12.0

**Platform lock enforced everywhere + truthful dead-swarm status.**

- **Platform lock kernel gate:** `Orchestrator._create_tasks` now refuses to create tasks on a lock-disabled adapter â it emits a `platform.locked` event and raises `PlatformLockedError` naming the blocked adapters, the enabled set, and the fix. Previously only routing consulted the lock, so a verb with a hardcoded adapter (e.g. `start_cursor_swarm`) ran its workers on a disabled platform whenever the router fell through with no eligible model.
- **MCP verb preflight:** the platform-specific verbs (cursor/claude/codex/agentic/openai run + start variants, `start_cursor_swarm`, `start_swarm` with an explicit adapter) fail fast with remediation before spawning a launcher when their platform is lock-disabled. Internal adapters (`local`, `shell`) remain exempt.
- **Dead swarms read FAILED, not green:** `no_model` and `unknown_provider` fast-fails are now recoverable failure classes, so a worker dispatched without a routed model records a truthful FAILED task (and auto-fallback can re-route it). A job whose every task failed finishes FAILED with a `job.all_tasks_failed` event instead of stitching into a healthy-looking COMPLETE at $0.

## v1.11.1

**Cancelled job status + poison-proof store reads.**

- **`JobStatus.CANCELLED`:** deliberate operator stops (cooperative cancellation, host UI cancel buttons) are now a first-class terminal status instead of an unknown string. Terminal-status sets (`await`, gc/lifecycle, `completed_at` stamping) include it.
- **Tolerant deserialization:** `job_from_dict` coerces an unknown persisted status to `stalled` instead of raising. Previously one row written by a newer or foreign host poisoned `list_jobs()` for the whole store with `ValueError` â every reader (including Marionette's swarm tracker) saw an empty feed until the row was hand-repaired.

## v1.11.0

**Claude Code effort levels (issue #15).**

- **`claude-code` effort support:** Claude Code >= 2.1.204 exposes `--effort low|medium|high|xhigh|max`; the registry now maps `effort=<level>` on claude-code entries to `payload_defaults.extra_args = ["--effort", <level>]` (wizard effort variants included), and `puppetmaster claude` gains a manual `--effort` flag. Verified end-to-end against the live CLI + API. `max` gets a conservative 4x output-token cost multiplier.
- **Escalating effort = variants + existing policies:** documented in MODEL_ROUTING.md. Register effort variants of the same model and the `escalating` policy / confidence escalation / review-gate escalation walk up the ladder â no separate blind low-to-max retry policy (no reliable "failure was effort-related" signal to key on; a five-rung retry ladder multiplies worst-case cost).

## v1.10.3

**Instant cooperative job cancellation.**

- **`puppetmaster.cancellation`:** in-process kill switch keyed by job id. A host embedding the orchestrator (e.g. Marionette's backend running inline agentic workers) calls `request_cancel(job_id)`; every agentic worker on that job stops mid-stream (the delta sink aborts the in-flight provider stream within one chunk) and per-turn (checked before each provider call). Cancelled runs finish with `stop_reason: "cancelled"` in their verification artifact instead of burning turns to `max_turns`.

## v1.10.2

**Honest cost accounting: provider-reported spend, cache-aware pricing.**

- **Ground-truth cost capture:** OpenAI-compatible calls to OpenRouter now request usage accounting (`usage.include`), and the provider layer records the provider-reported `cost_usd` plus `cached_tokens` on every turn (OpenAI-compatible and Anthropic, streaming included). The agentic loop accumulates both and stamps `tokens_cached` / `real_cost_usd` onto artifact payloads.
- **`price_job` prefers reality over reconstruction:** a task carrying `real_cost_usd` is priced at exactly what the provider charged (cache discounts included) â even when no registry spec matches. Without a reported cost, cached prompt tokens are billed at a 0.1 cache-read multiplier instead of the full input rate. Previously every prompt token was priced as fresh input, overcounting input-heavy agentic work ~3-4x at typical cache hit rates.

**Routing capability ceiling + Windows test portability.**

- **`max_capability` routing ceiling:** new `payload.max_capability` (and `TaskSignals.explicit_max_capability`) clips the TOP of the classifier output without touching the bottom. Callers that used `min_capability` as a cost guardrail were forcing every task's capability need to one exact value, so the balanced policy pinned every worker to the single cheapest model that cleared it; with the ceiling, cheap roles still classify low and route to cheap models while hard roles stop at the cap. Exposed on the `puppetmaster_route_task` MCP tool as `max_capability`. When both are set, `min_capability` is clipped by the ceiling.
- **Windows-clean test suite:** the suite now strips a developer's `PUPPETMASTER_CODEGRAPH_NODE`/`_JS` repair pins (they short-circuited invocation resolution and disabled auto-heal in a dozen tests), uses portable Python one-liners instead of `cat`/`true`/`false`, and skips the shim-symlink test when the host cannot create symlinks.

## v1.10.0

**RQGM Wave 11: retrieval diversity, injection cost accounting, degraded-run honesty.**

- **MMR diversity rerank:** after Wave 10 scoring, `retrieve_memory` reranks the top `3 * limit` candidates with maximal marginal relevance (Jaccard word-set similarity) so injected memory stays relevant without near-duplicates. Toggle with `PUPPETMASTER_MEMORY_MMR`; tune with `PUPPETMASTER_MEMORY_MMR_LAMBDA`.
- **Memory injection cost log:** every dispatch that injects promoted memory writes one numbers-only ledger entry (job id, record count, estimated tokens, estimated USD). Totals surface in `python -m puppetmaster savings` as labeled spend overhead. Disable with `PUPPETMASTER_MEMORY_COST_LOG=0`.
- **Degraded agentic honesty:** `assess_run_quality` recognizes `empty_or_unstructured_agentic_result` and max-turns-with-no-findings patterns so empty agentic swarms classify as degraded instead of looking successful. Agentic degraded artifacts advise rerun with a higher-capability model or higher `max_turns`.
- **No more console flashes on Windows:** every Puppetmaster entry point (CLI, MCP server, worker runtime, codegraph index runner) installs a process-wide `CREATE_NO_WINDOW` default for child processes, so background work (workers, git, pytest, codegraph) never pops visible console windows under console-less hosts like Cursor. Escape hatch: `PUPPETMASTER_SHOW_CONSOLES=1`.

## v1.9.0

**RQGM Wave 10: memory retrieval ranking.**

- **Weighted retrieval scoring:** `retrieve_memory` ranks by query-term overlap fraction (long statements gain nothing mechanically) times a scope weight (findings/decisions 1.0, general 0.7, verification 0.4) times a recency factor (full weight within 7 days, decaying to a 0.5 floor at 56 days). Ties break by confidence then newest first, in both the file and SQLite stores.
- **Relevance floor on injection:** the orchestrator passes `min_overlap=0.2` so weak matches are not injected just because the limit allows five (`PUPPETMASTER_MEMORY_MIN_OVERLAP` override; negative disables the floor). Empty-query retrieval is exempt.

## v1.8.0

**RQGM Wave 9: promoted-memory hygiene.**

- **Promotion quality gate:** the stitcher skips VERIFICATION artifacts that echo worker prompts (`Role:`, boilerplate markers, or >600 chars) and skips failed/blocked/degraded verification results.
- **Store dedupe, cap, and expiry:** `promote_memory` dedupes by scope+statement, caps at 200 records (evict oldest), `retrieve_memory` accepts `max_age_days`, and `prune_memory` deletes by scope and/or age. Orchestrator retrieval defaults to 14-day freshness (`PUPPETMASTER_MEMORY_MAX_AGE_DAYS` override).
- **Memory CLI:** `python -m puppetmaster memory` lists records grouped by scope; `--json` preserves the full dump; `--prune [--scope] [--older-than-days]` cleans promoted memory (unfiltered prune requires `--yes`).

## v1.7.0

**RQGM Wave 8: evaluator feedback loop from failed review verdicts.**

- **Draft criteria store** (`evaluators/drafts.json`): append-only journal with dedupe, 50-draft cap per slot, and best-effort recording helpers.
- **Review gate capture:** epoch-rubric rejections with parseable judge reasons write drafts for the slot that judged them; GATE artifacts record `draft_recorded`.
- **CLI:** `evaluators drafts [slot_id] [--json] [--clear SLOT_ID]` to inspect or clear accumulated drafts.
- **Promote handoff:** `evaluators promote --from-drafts` folds up to 10 verbatim judge reasons into `draft_note_<n>` criteria keys (explicit `--criteria-json` wins on collision), still gated by the anchor battery; clears drafts on success.

## v1.6.0

**RQGM Wave 7: evaluator-aware review gate and epoch lineage surfacing.**

- **Full-fidelity epoch snapshot.** Job-start epoch artifacts now freeze each evaluator's `instruction` and `criteria` (not just slot/version/role), so mid-job registry edits cannot change what a running job's gates read.
- **Review gate consumes frozen criteria.** `_gate_review` resolves rubric text as explicit gate-spec rubric, then frozen epoch criteria (review slot, then task role), then the default rubric. GATE artifacts record `rubric_source` plus evaluator slot/version when the epoch supplied the rubric.
- **Lineage surfacing.** Dashboard job snapshots expose `evaluator_epoch` (slot/version/role only); job view renders compact `slot@vN (role)` chips. New read-only CLI: `python -m puppetmaster evaluators epoch <job_id>`.

## v1.5.1

**Windows hardening + CodeGraph CLI compatibility.**

- **Windows self-interrupt fix.** `liveness._pid_alive` and `mcp_registry._pid_alive` used `os.kill(pid, 0)` as a liveness probe. On Windows, signal value 0 is `CTRL_C_EVENT`, which delivers a real Ctrl+C to the target's console group â a probe against a pid sharing the console interrupted the caller itself (test runs and long CLI sessions died with `KeyboardInterrupt` mid-flight). Both now use a non-destructive `OpenProcess`/`GetExitCodeProcess` probe on Windows, matching the existing probes in `codegraph.py` and `dashboard.py`.
- **CodeGraph subcommand rename compatibility.** Newer CodeGraph releases renamed `search` to `query` and replaced `context` with `explore`. `run_codegraph_cli` now translates legacy spellings (including `--max-nodes`/`--format` flag mapping) and falls back to the original arguments on older CLIs, so `python -m puppetmaster codegraph search|context ...`, worker context injection, and the MCP context tool all work against any installed CodeGraph version.
- **utf-8 subprocess decoding.** CodeGraph subprocess output is now decoded as utf-8 with replacement instead of the Windows cp1252 default, which crashed reader threads on emoji-bearing CLI output.

## v1.5.0

**RQGM v1 evaluator lifts: durable evaluator slots, epoch freezing at job start, deterministic anchor-set promotion, and evaluator metadata stamped on VERIFICATION artifacts.**

- **Evaluator slot registry** (`puppetmaster/evaluators.py`): versioned specs under `{state_dir}/evaluators/registry.json`; `active_evaluators`, `register_evaluator`, sample registry in `docs/sample-evaluator-registry.json`.
- **Epoch snapshot**: orchestrator persists a frozen evaluator set as a job-scoped `DECISION` artifact at creation; mid-job registry edits do not affect running jobs.
- **Anchor promotion**: `docs/sample-anchor-set.json` battery; `python -m puppetmaster evaluators list|promote` CLI; promotion requires passing the anchor battery (local adapter, no LLM judge).
- **Verification stamping**: `evaluator_slot` / `evaluator_version` on `VERIFICATION` payloads when task role matches the job epoch.

## v1.3.0

**A dead, revoked, or wrong provider key no longer hides behind a generic degrade â the `agentic` adapter now fails loud on auth rejection, naming the provider and the exact env var to fix.** Before this, a 401/403 (or a pre-flight `not_authenticated`) got laundered into a vague "failed verification" / "completed without structured findings" result, sending everyone hunting for a weak-model or bad-prompt cause instead of the real one: the key.

- **Loud auth-failure RISK.** When a `ProviderError` is an auth rejection â HTTP 401/403, or a pre-flight `not_authenticated` (key missing/blank, status `None`) â *after* key-pool rotation is exhausted, the adapter emits a dedicated high-confidence (`0.95`) `RISK` artifact stamped `failure="auth_failed:<status>"`. It names the provider and the precise env var to fix (`_PROVIDER_ENV_HINTS`), so the diagnosis is immediate instead of a scavenger hunt. Fires in both analyze and implement modes. Evidence tags: `adapter:agentic`, `provider:<name>`, `auth_failed:<status>`, `keys:exhausted`. Covered by two new tests; agentic suite green (58 passed).
- **CI hygiene.** Hardened the flaky `test_reaper_thread_reaps_on_interval_and_final_sweep` timing test: its reaper-sweep deadline was 1.0s, which a starved background thread on a contended macOS runner could miss (it flaked red on the v1.2.0 *tag* run while the identical commit passed on `main` and every other OS/Python leg). Widened to 5.0s â the poll loop still exits the instant the reaper sweeps, so the slack is free and only guards against runner contention.
- Full suites green under CI's isolated environment: **`unittest discover` 1101 (OK)**, dedicated **`test_agentic_standalone.py` 58 passed** and **`test_eval_harness.py` 6 passed**.

## v1.2.0

**The `agentic` adapter closes the last gaps between "reliable" and "daily-driver": it now self-verifies before it dares call a run done, streams tokens durably to disk for subprocess/CLI swarms (not just in-process hosts), records a visible plan, and ships with an objective seeded-bug eval harness so quality is measured, not asserted.** v1.1.0 fixed the "empty findings" failure mode and reached reliability parity; this release pushes toward *feature* parity with Codex/Claude Code toolchains so Marionette â and anyone running the bare adapter â gets a harness worth reaching for every day.

- **Verify-before-submit loop.** An implement worker no longer accepts its own `submit_report` on faith. When a verification command is available (explicit `payload.verify_command`, or auto-detected â `pytest`/`npm test`/etc.), the adapter runs it *before* accepting submission; on failure it feeds the output back and bounces the model to fix it, bounded by `DEFAULT_VERIFY_MAX_RETRIES` (2) with a 300s per-run cap and the destructive-command denylist. A run that submits over a still-failing verify is reported `degraded` (`verification_failed`), not `passed`; advisory mode (`verify` without a hard gate) tags `verify:advisory-failed` without degrading a real diff. Evidence tags: `verify:passed` / `verify:advisory-failed` / `verify:failed`.
- **Durable subprocess streaming.** Token deltas now persist to a per-task `agentic_deltas.ndjson` in the state dir via a new `DurableDeltaWriter`, composed alongside the existing in-process `_delta_bus`. Subprocess and CLI/MCP swarm workers finally get a live token feed â closing the "inline-only streaming" limit called out in v1.1.0's known-limits. New `puppetmaster deltas <job_id> [--task <id>] [--follow]` tails the stream (JSON or text); redaction is applied on write.
- **Seeded-bug eval harness.** New `puppetmaster/eval_harness.py` + `puppetmaster eval` CLI materializes curated seeded-bug git repos (off-by-one, wrong parity, missing return), runs any implement adapter against them through a real `Orchestrator`, and scores each case by verification pass/fail **and** diff quality (`git diff --numstat`, with build-noise like `.pyc`/`__pycache__` filtered out). Objective pass-rate and diff-scope numbers instead of vibes.
- **Plan/TODO tool + higher turn budget.** A non-terminal `update_plan` tool lets the model record and update a step-by-step checklist mid-run; the final plan is persisted as a `DECISION` artifact (`plan:<done>/<total>`), so the host can audit the intended approach. Turn budgets raised (analyze 16, implement 64) to give the plan-then-execute pattern headroom. Gated by `payload.plan_tool` (default on). `spawn_worker` sub-agent deferred â recursion-safety warrants its own design.
- **Bugfix (found by the plan-tool live smoke).** The plan `DECISION` artifact omitted the required `payload.why`, so the worker failed on save (`decision artifacts require payload.why`) and the task tripped the `require_diff` gate despite real edits. Now supplies `why`; a `decision.validate()` assertion was added to the plan test to lock the artifact contract the runtime enforces on save.
- Full suites green: **`test_puppetmaster.py` 1037 passed**, dedicated **`test_agentic_standalone.py` + `test_eval_harness.py` 63 passed**, and **unittest discovery 1100 (OK)** under CI's isolated environment. Live implement+plan smoke on `gemini-2.5-flash` returns `result=passed` with `stop:submitted`, `plan:2`, `verify:passed`, a persisted plan checklist, and a real diff.

## v1.1.0

**The `agentic` adapter graduates to Hermes-grade reliability: structured output moves off fragile final-message JSON onto the provider-native tool-calling channel, wrapped in a robustness envelope â fixing the intermittent "empty findings" / `empty_or_unstructured_agentic_result` degrade at its root.** Landed in canonical source, so every agentic-adapter consumer benefits (CLI, MCP, inline hosts like Marionette), not just one host.

- **Native structured output.** New terminal tools `submit_findings` (analyze) / `submit_report` (implement) whose JSON-schema parameters the provider constrains; `_agent_loop` intercepts the call, converts args to artifacts, and treats submission as the terminal signal. Free-text JSON is kept as a backward-compatible fallback â a run degrades only when **both** the tool call and the fallback yield nothing.
- **Robustness envelope.** Tool-argument repair, `finish_reason=length` continuation, empty-response nudge, jittered exponential backoff on transient `ProviderError`, and `tool_choice` plumbing so the final nudge turn can force `submit_findings` (OpenAI/Anthropic/Gemini wires).
- **Provider failover.** Credential rotation across numbered env keys on auth/429; guarded model/provider failover mid-run restarts the loop cleanly rather than degrading the worker.
- **Live context compression.** `_context_budget` elides old large tool results before long runs hit context 413s.
- **Token streaming (inline).** `provider_chat_streaming` (SSE) + `on_delta` threaded through `_agent_loop`; a process-local `_delta_bus` streams tokens live to in-process hosts (inline-only by design in this release â subprocess workers stayed blocking; durability landed in v1.2.0).
- **Routing.** Documented the agentic-analyze routing floor with `min_capability` / `routing_policy=quality` as the escape hatch; regression-tested.
- Validated: full suite green (**1082**) under pytest and isolated unittest; live analyze and implement smokes on `gemini-2.5-flash` both return `result=passed` with `submit:tool` evidence and a real PATCH (no degrade).

## v1.0.0

**The `agentic` adapter graduates from a keys-only floor into a legitimate provider-native worker harness â the engine Marionette rides on.** The standalone in-process tool loop (its own `provider_chat` loop against any provider key, no external CLI) was deliberately conservative: 12 turns, `os.walk` grep, opt-in terminal, and â critically â an implement run that changed nothing still reported `passed`, so a no-op could masquerade as success. This release lifts the worker-grade patterns from the Hermes agent core (as patterns, not vendored code) and closes that gap, so the loop is strong enough to power a standalone harness ("providers, not platforms") instead of only serving as a fallback.

This is also the semver **1.0.0** cutover: the `0.9.x` line (through `0.9.102`) used the patch field as a running build counter and predates the policy in [`docs/RELEASE_CHECKLIST.md`](RELEASE_CHECKLIST.md); every bump from here carries semver meaning.

- **No-op is now `degraded`, never `passed`.** An implement run that produces no attributable git diff is reported `degraded` with a `RISK` artifact (`failure=no_diff_produced`) â the exact "returned in ~1s with no diff" failure can no longer read as a successful implementation.
- **Single no-op nudge.** When the model stops having changed nothing, it is re-prompted once to actually implement (or to explicitly cite the code that already satisfies the task) before the run is closed out (`nudge:applied`). Gated by `payload.noop_nudge` (default on).
- **JSON-only retry on analyze** (lifted from the Hermes adapter): a clean run that returned prose the parser couldn't structure gets one stricter reprompt before being accepted as degraded (`retry:recovered` / `retry:exhausted`). Gated by `payload.analyze_retry` (default on).
- **Mode-aware budget governor.** Analyze stays tight (14 turns / 300s); implement gets a real envelope (50 turns / 900s) so non-trivial edits aren't forced into a no-op. New optional cumulative `payload.token_budget`. Every run stamps a `stop_reason` (`model_stopped` / `max_turns` / `token_budget`).
- **CodeGraph in the loop.** New `graph_search` (symbol index) and `graph_context` (task-scoped subgraph) tools, registered only when the repo is graphed â so the worker explores the symbol graph, not just a substring scan. `search_code` stays for plain-text matches.
- **Richer, safer edit vocabulary.** New `delete_file`; `edit_file` gains `replace_all` and near-miss diagnostics so a failed exact match self-corrects instead of dead-ending.
- **Headless guardrails lifted from Hermes.** `run_terminal` is on by default in implement mode for self-verification (opt out with `payload.allow_terminal=false`) but refuses an unambiguously destructive command via a conservative denylist (`rm -rf /`, fork bombs, `mkfs`, `dd of=/dev/â¦`, `git push --force`, `sudo`, pipe-to-shell). Filesystem tools gain binary-read/write protection alongside the existing symlink-escape confinement, and a latent `relative_to` bug on symlinked temp roots is fixed.
- **Skills + report parity.** The adapter now injects orchestrator-selected skills (`prompt_with_skills`) and parses implement-mode final reports via `implement_report_artifacts`, matching the CLI adapters.
- **Docs.** `docs/ADAPTERS.md` agentic section rewritten to reflect the modes, tools, guardrails, and per-task knobs (`max_turns`, `timeout_seconds`, `token_budget`, `allow_terminal`, `allow_web`, `analyze_retry`, `noop_nudge`).
- Full suites green: **`test_puppetmaster.py` 1037 passed**, **`test_agentic_standalone.py` 27 passed** (10 new: JSON-retry recovery, no-op degrade + nudge, writeâpass+PATCH, `delete_file` (+analyze gating), `edit_file` `replace_all`, destructive-command refusal, benign-command allow, binary-write refusal).

## v0.9.102

**Watch your swarms from your phone with near-zero setup â the dashboard now runs as a detached background service and hands you a scannable QR, and the mobile UI is a clean, phone-shaped board.** Getting the board onto a phone previously meant holding a foreground terminal open and eyeballing a URL; and the Marionette-style "Swarm Tracker" hero (v0.9.100) turned out to be redundant with the jobs index + job detail. This release makes the phone path the easy path and simplifies the UI to match.

- **Detached background service.** `puppetmaster dashboard --background` (`-b`) runs the server in its own session and returns you to the prompt â no terminal to babysit. `--status` reports whether it's up (URL + pid), `--stop` tears it down. A small `dashboard.run.json` runfile in the state dir tracks the process so status/stop don't have to hunt for the port owner.
- **Phone handoff from the agent.** The MCP `puppetmaster_dashboard` verb gains `mobile: true` (detect Tailscale/LAN, bind a phone-reachable address, return the URL **and a QR image path** to embed inline), `qr: true`, and `stop: true`. It already spawned detached; now it can serve the phone case end to end. Idempotent â an already-listening server on the host:port is reused, never double-spawned.
- **QR that just works.** New `[mobile]` extra (`pip install 'puppetmaster-ai[mobile]'`) pulls in `qrcode` + Pillow so the pilot can render a scannable PNG. Without it the board still works â you get an ASCII QR, then the bare URL.
- **Swarm Tracker removed.** The v0.9.100 hero card, its `/api/tracker` endpoint, server-side builders, client JS/CSS, and tests are gone; the **jobs index is the default landing on every viewport**, and the job view keeps the "Swarm Overview" hero. Less surface, one source of truth.
- **Mobile-clean layout.** A `max-width: 640px` pass stops horizontal overflow (grid `minmax(0, 1fr)`, `min-width: 0`, `overflow-wrap`) and stacks each job row into a grid-areas card so a long job hash can't stretch the row.
- **Docs.** New [`docs/MOBILE.md`](MOBILE.md) â a full walkthrough of the Tailscale setup (Mac + phone), the agent and CLI start paths, the `[mobile]` install, and a troubleshooting table (including why an old QR URL can return `ERR_CONNECTION_REFUSED` after the server is stopped). Linked from the README, the docs index, and `DASHBOARD.md`.
- New focused tests: background runfile roundtrip + idempotent stop, ASCII QR, and MCP mobile mode (external bind + QR, no-address error, stop delegation, schema surface).

## v0.9.101

**New `puppetmaster keys` command â a guided setup for agentic provider API keys, so the keys-only path is genuinely keys-*easy*.** The `agentic` adapter calls provider HTTP APIs directly with the user's own key, resolving credentials purely from the process environment (`puppetmaster/providers.py`). When Puppetmaster is driven through Cursor's MCP client, that "process" is the MCP server Cursor launches â so a key only reaches the worker if it lives in the `env` block of the `puppetmaster` entry in a Cursor `mcp.json`. Until now that meant hand-editing JSON; there was no key-setup wizard (the existing `models` wizard only edits the model registry, never credentials).

- **Interactive wizard.** `puppetmaster keys` walks every keyed provider (OpenAI, Anthropic, Gemini, OpenRouter, xAI, DeepSeek, Groq, Mistral, Together, Nous), reads each value with a hidden prompt, and marks which providers are already visible to the shell.
- **`keys status`.** A per-provider table of PROCESS (visible to this shell now) vs STORED (saved in the target MCP config). Values are never printed.
- **`keys set <provider>`.** Non-interactive single write, reading the value from a hidden prompt or `--stdin` (handy for `printenv KEY | puppetmaster keys set openai --stdin`).
- **Writes where the worker actually reads.** Keys land in the `puppetmaster` MCP entry's `env` block in `~/.cursor/mcp.json` by default (`--workspace` â `<cwd>/.cursor/mcp.json`, `--target PATH` overrides). A missing config or entry is created (matching what `install-cursor-mcp` writes); existing env keys and neighbouring MCP servers are preserved verbatim.
- **Secret hygiene by construction.** Each value is registered with `register_secret_value` the moment it's handled (so any later log/artifact output is scrubbed), is never echoed back, and the config file is tightened to `0600` because it now holds a credential. Provider keys are never written into Puppetmaster-owned state (`~/.puppetmaster/`), only into the host MCP config the user already owns.
- **Docs.** `docs/ADAPTERS.md` gains a "Setting keys" section under the agentic adapter.
- 7 new focused tests (`KeysWizardTests`): create/write, entry+env preservation, empty-value rejection, wizard selection, out-of-range rejection, closed-stdin exit, and "status never prints values."

## v0.9.100

**The dashboard gains a Marionette-style "Swarm Tracker" hero â every job now gets the same at-a-glance tracking, routing transparency, and progress the Marionette app shows, on top of the existing detail.** The local web board (`python -m puppetmaster dashboard` / `puppetmaster_dashboard`) already rendered per-task cards, cost, and typed artifacts; this adds a summary hero at the top of every job view and surfaces the router's audit trail.

- **Swarm Tracker hero card** (rendered first, nothing existing removed): job title + status, the primary routed model, worker count, and adapter; the job's total **cost and total tokens**; the completion progress bar; and an aggregate **verification score**.
- **Routing rollup.** One card per routed worker showing the model, its estimated cost, a plain-language "why this model won" line derived from the routing policy (`balanced` â "Right-sized: cheapest model that clears the task's need", etc., with a plan-billed note when applicable), and a collapsible **`N alternatives considered`** control that expands to the rejected models (rejection reason on hover) â straight from each `ROUTING` artifact's audited `rejected` list.
- **Sticky job-total footer** â cost, tokens, and primary model, always in view; hidden on the jobs index.
- **Data layer.** `build_job_snapshot` gained `tokens_total` (via `aggregate_token_usage`), `primary_model`, `worker_count`, `adapters` + `primary_adapter` (deterministic tie-break by adapter name, independent of task-store ordering), `verification_score`, and `routing_rollup` â all additive, all from existing helpers; existing keys unchanged so the detailed cards below are untouched.
- **Docs.** New [`docs/DASHBOARD.md`](DASHBOARD.md), linked from the README and the docs index.
- Zero new dependencies; the page stays offline and loopback-only, all artifact-derived text is HTML-escaped, and the no-op DOM-diff guard is preserved. Full suite green: **1009 passed**.

## v0.9.99

Docs-only release so the PyPI project page reflects the keys-only story shipped in v0.9.98.

- **README** now leads with the no-external-CLI path: the built-in `agentic` adapter runs the whole tool-use loop against any provider API key (OpenAI/Anthropic/Gemini/OpenRouter) â ideal for CI, containers, and headless servers â instead of framing Puppetmaster solely as a wrapper around agent CLIs.
- **"Why it's credible"** adds an accurately-caveated external-validation paragraph: on the independent third-party NL2Repo-Bench, durable-state orchestration reaches a 91.1% mean test-pass rate (~2.28Ã the ~40% published SOTA), with the honest framing carried over (field/single-vs-swarm comparison, agent-only swap, packaging-bound tasks kept in the denominator).
- **docs index** gains a "Research & external results" section linking the paper site, the Zenodo concept DOI (latest version), and the SWE-bench Lite cost/quality study.

No code, API, or behavior changes.

## v0.9.98

**The `agentic` adapter gets the full first-class-platform treatment.** v0.9.96 shipped the standalone provider-agnostic `agentic` worker (its own tool-use loop against a provider HTTP API on the user's own key, no external cursor/claude/codex/hermes CLI), but it was only wired into the runtime core (adapter registry, `KNOWN_ADAPTERS`, static catalog, key-aware router). This release brings it to parity with codex/openai/hermes across every user-facing and safety surface, so it is a real keys-only platform you can drive from the CLI/MCP â not just an internal building block Marionette consumes.

- **MCP verbs.** New `puppetmaster_agentic` (sync) and `puppetmaster_start_agentic` (async) tools with `agentic_schema()` / `agentic_command()` / `run_agentic()` / `start_agentic()`, mirroring the codex/openai verbs (analyze + implement modes, provider/model/max_turns/timeout/effort/routing knobs).
- **CLI subcommand.** `puppetmaster agentic "<task>"` with `--mode analyze|implement`, `--provider`, `--model`, `--max-turns`, `--timeout-seconds`, `--temperature`, `--reasoning-effort`, worktree/guard flags, and the standard routing flags.
- **Generic implement/edit + swarms.** `agentic` is now in `IMPLEMENT_ADAPTER_PRIORITY`, `_EDIT_CAPABLE_ADAPTERS`, `_implement_command`, the MCP `implement_schema`/`edit_schema` enums + `puppetmaster_start_implement` description, and `SWARM_ANALYSIS_ADAPTERS` â so `start_implement`/`edit` and analysis swarms work under an agentic-only platform lock. `adapter_is_available()` / `pick_implement_adapter()` are provider-key-aware.
- **Doctor / billing / preflight are honest about credentials.** `detect_agentic_billing` reports the API-billed/out-of-pocket posture of whichever provider key is actually visible; doctor's credential-env and billing checks include agentic provider keys; `adapter_status()` reports `configured=False` when no provider key is present (instead of defaulting to configured); preflight blocks dispatch with an actionable message when no key is visible.
- **Setup wizard.** Detects agentic when any provider key is visible, explains the keys-only / no-external-CLI path (naming `OPENAI_API_KEY`/`ANTHROPIC_API_KEY`/`GEMINI_API_KEY`/`GOOGLE_API_KEY`/`OPENROUTER_API_KEY`), and can seed curated agentic models filtered by `providers.available_providers()` â never seeding uncallable keyless entries, never writing keys into PM-owned files.
- **Models discovery.** `models discover --source agentic` (and `--source all`) merges the curated agentic catalog filtered by available provider keys; agentic-aware `reasoning_effort` defaults.
- **Docs + rules + examples.** New `docs/ADAPTERS.md` agentic section; README/FEATURES/MODEL_ROUTING/CLI_REFERENCE/DAILY_DRIVER/docs index updated; `.env.example` documents the provider keys; new `examples/agentic-analyze.json` + `examples/agentic-implement.json`; generated rules mention the keys-only option and the two new verbs; platform/preflight help now lists agentic (and hermes).
- **Deliberately NOT done (honest scoping):** no `install-agentic-mcp`/uninstall/hook installers, no OAuth-login probing, no agentic global rule-file target (agentic has no external host to register into), and browser QA stays Hermes-only. Provider keys are never copied into Puppetmaster-owned config.
- Full suites green: **`test_puppetmaster.py` 1008 passed**, **`test_agentic_standalone.py` 18 passed** (19 new first-class-surface tests: MCP tools/schema/commands, CLI parser+dispatch, implement auto-pick/availability, doctor billing/env, preflight gate, models discovery filtering, effort defaults, rules text, swarm adapter acceptance).

## v0.9.97

**Inline worker mode now runs its per-role workers concurrently â `worker_mode="inline"` is a first-class parallel, in-process execution mode instead of a serial fallback.** Previously `Orchestrator._run_inline_workers` dispatched roles strictly one-at-a-time inside each round (`for role in sorted(...): runtime.run_until_idle()`), so an inline swarm of N roles took N Ã per-role wall time â even though the subprocess path already spawned all roles at once. Inline was the *only* serial mode, and it's the mode in-process consumers (pm-harness/Marionette analysis swarms, sandboxes, embedded/test use) opt into for clean key/env inheritance without the subprocess spawn tax.

- **Bounded parallel dispatch.** A new `Orchestrator._dispatch_inline_role_workers` submits one `WorkerRuntime.run_until_idle()` per ready role to a `concurrent.futures.ThreadPoolExecutor` capped by `_INLINE_ROLE_WORKER_POOL_CAP = 8` (`min(len(roles), cap)`), mirroring the subprocess path's "all roles at once" shape while bounding thread count on wide swarms. Completions are summed and still gate the round's no-progress exit.
- **Safe by construction, no new locking.** This only parallelizes the caller â the store's concurrency substrate was already built for N concurrent workers: `SQLiteSwarmStore` opens a fresh WAL connection per operation (`busy_timeout=5000`), and `claim_task` is an atomic compare-and-swap (`UPDATE â¦ WHERE status IN (queued, blocked, stale-running)` guarded by `rowcount != 1`), so two racing role workers can never double-claim. No global locks were added and the connection model is unchanged.
- **Semantics preserved.** The round-based `while` loop, `PUPPETMASTER_STATE_DIR` scoping, heartbeat/`recover_stale_tasks`/`refresh_blocked_tasks`, and both fail-closed exits are unchanged. Role dispatch order no longer affects correctness (claims are atomic) but `sorted(...)` is kept for stable worker ids. Exceptions from any role propagate via `future.result()` (raising `RuntimeError` like the subprocess path) with remaining threads joined on executor shutdown â no orphaned threads, no deadlock.
- **Scope:** subprocess mode (the CLI/MCP default) and daemon mode are byte-identical; single-role inline jobs are unaffected. Benchmark (0.5s GIL-releasing per-role work, median of 3): 2 roles 2.00Ã, 4 roles 3.98Ã, 8 roles 7.97Ã, 16 roles 7.98Ã (two waves at the cap) â inline wall time drops from `N Ã work` to `ceil(N / 8) Ã work`.
- Full suite: **992 passed** (pytest, `test_puppetmaster.py`), with a `threading.Barrier(2)` test that times out if dispatch serializes (proving overlap) and a failure-propagation test asserting a raising role surfaces `RuntimeError` rather than hanging.

## v0.9.96

**A standalone, provider-agnostic `agentic` worker adapter: run PM workers directly against a provider HTTP API on your own key, with no external agent CLI (hermes/cursor/claude/codex) required.** The `agentic` adapter runs its own tool-use loop against the provider wire, so a fresh install can do real agentic work with nothing but a provider API key.

- **`providers.py` â stdlib-only descriptor registry** (methodology lifted from Hermes) for openai/anthropic/gemini/openrouter/xai/deepseek/groq/mistral/together/nous plus local ollama/lmstudio. Two wires (OpenAI-compatible + native Anthropic) are normalized behind `provider_chat â AssistantTurn`; `available_providers()` reports which have a usable credential.
- **`adapters/agentic.py` â `AgenticAdapter`** (subclasses `FullEditWorkerAdapter`) with analyze (read-only) and implement (full-edit, git-attributed diff) modes. Confined filesystem tools; terminal/web are opt-in; all tool output, diffs, and provider error bodies are scrubbed via `redact_secrets`.
- **Key-aware routing.** `router.py` drops agentic models whose provider key is absent, so a fresh install offers exactly what the user's keys unlock; `static_catalog.py` carries a curated provider-stamped agentic catalog; `platform_lock.py` governs `agentic` as a first-class adapter.
- Full suite green with 18 new hermetic tests (`tests/test_agentic_standalone.py`) covering the registry, both provider wires, key-aware routing, confined tools, and loop mechanics.

## v0.9.95

**The claude-code adapter now honors read-only intent, so analysis-swarm workers backed by Claude Code no longer trip the clean-tree guard.** Previously `ClaudeCodeAdapter` silently ignored the `read_only` / `sandbox=read-only` flags that analysis swarms stamp on every worker: it always ran with an edit-capable permission mode (`acceptEdits`) and always enforced the full-edit dirty-worktree guard. The result was an asymmetry â cursor analysis roles (which run `--dry-run`) reviewed a dirty tree fine, but a claude-code-backed analysis worker was blocked with `dirty_worktree` on the same tree, even though it was supposed to be read-only. This mirrors the fix codex already had.

- **Read-only intent maps to a real non-editing mode.** In `puppetmaster/adapters/claude_code.py`, `_prepare_cli_invocation` now derives the effective permission mode from the task payload: an explicit `permission_mode` still wins; otherwise a read-only signal (`read_only` truthy or `sandbox == "read-only"`) selects Claude Code's read-only `plan` mode, and everything else keeps defaulting to `acceptEdits`. `write_capable = effective_mode != "plan"` is threaded through `CliInvocation.extras`.
- **The dirty guard is skipped only when the worker genuinely cannot write.** `ClaudeCodeAdapter._apply_pre_run_guards` now mirrors codex: a non-write-capable (plan-mode) run takes a git snapshot but skips the worktree/dirty guards, so an analysis swarm can review the caller's existing diff. Write-capable implement runs are unchanged â they still default to `acceptEdits`, stay write-capable, and still enforce the clean-tree guard exactly as before.
- **Verification artifacts report the effective mode.** The `permission_mode:` evidence tag and payload now reflect the mode actually used (e.g. `plan`), instead of the raw payload default.
- **Deliberately not done:** no `allow_dirty` flag was added to `start_swarm`. That would have masked the real bug (read-only intent being ignored) and let a write-capable claude-code worker loose on a dirty tree â reintroducing the diff-attribution corruption the guard exists to prevent. `allow_dirty` remains where it belongs: the write-capable implement/edit verbs.
- Full suite: **990 passed** (pytest), with a new test asserting a read-only claude-code run on a dirty tree skips the guard, runs `--permission-mode plan`, and reports `write_capable=False`, plus a strengthened test asserting a normal implement run still blocks on a dirty tree with `acceptEdits`.

## v0.9.94

**First-class browser swarms: live-site QA as a real PM verb, not a hand-rolled `hermes chat -t browser` loop.** A browser swarm is N independent Hermes workers, each driving a real browser against a live site to capture real network payloads â the QA that mock-backend tests and read-only repo analysis can't reach. This promotes the proven manual workflow into a supported verb with the hard-won guardrails baked into code, plus a safety posture for acting agents.

- **New `puppetmaster/browser.py` (single source of truth).** `build_browser_spec` / `browser_swarm_specs` produce Hermes analyze workers carrying the `browser` toolset (which the MCP swarm specs strip and the cursor adapter lacks), with three guardrails encoded: (1) React-controlled-input native-setter + dispatched `input`/`change` events, (2) network-truth (an HTTP 200 can carry an error body â judge by the body), and (3) a strong-model capability floor (`min_capability=80`, pinned to `allowed_adapters=["hermes"]`) because cheap models fail browser grounding and report false failures. Private/VPN hosts use Hermes' local-engine fallback.
- **Hermes-only by design.** Hermes is the only adapter exposing a `browser` toolset, so the verb routes there and fails fast (with exact remediation) if Hermes is disabled by the platform lock. The Hermes adapter already honored `payload.toolsets`; this threads `browser` through the verb/spec layer that previously hardcoded the toolset list.
- **Acting-agent safety posture.** A browser worker edits no repo files (`swarm_mode` stays `analysis`, no clean-tree guard) but acts on the world (logins, form fills). New `workers.spec_has_side_effects` / `swarm_is_acting` + `payload.side_effecting` drive an `ACTING AGENT` banner through `RunResult.acting` â `print_mode_banner`, so the read-only framing is never mistaken for harmless.
- **Surfaces:** `puppetmaster browser "<task>" ["<task2>" â¦]` (CLI, parallel `subprocess` workers by default) and `puppetmaster_start_browser_swarm` (async MCP, returns `job_id`).
- **Verified live end-to-end** against a local harness reproducing all three gotchas (HTTP-200-with-error-body + a React-style controlled input on a private `localhost` URL): the routed worker picked `hermes/gemini-3-5-flash` @cap 88 (rejecting the cap-58/50 cheap models and every non-Hermes adapter), read the 200-error body as a failure, and confirmed native input events flipped failureâsuccess. Two workers ran concurrently (~74s vs ~58s for one) with isolated browsers and no cross-talk. Full suite: **989 passed**.
- **Known limits:** browser QA is Hermes-only â making it cross-adapter would require wiring a per-adapter browser capability (e.g. a Playwright MCP for Cursor/Claude), a separate effort. Per-worker isolation was validated on the **local** engine (each worker is its own subprocess with its own browser); the cloud `browser-use` engine may share a surface and was not exercised. Auto-routing requires a registry with a Hermes model at/above the capability floor.

## v0.9.93

**Cost reporting is decoupled from the router: a job's actual spend is now a pure function of (tokens actually consumed) Ã (registry price of the model actually used), independent of whether routing ran.** Previously `puppetmaster cost` / `puppetmaster_job_cost` read only `ROUTING` artifacts, so a *pinned* run â which never invokes the router and emits no `ROUTING` artifact â dead-ended at "$0, didn't auto-route". That conflated *how a model was chosen* with *what the work cost*. This release inverts that: cost is computed downstream from the token usage every adapter already records.

- **New `puppetmaster/cost.py`.** `price_job(artifacts, registry)` resolves which registry model each task actually ran on â the router's recorded `model_id` first, then the model the adapter stamped on its verification artifact (matched by `id` or `adapter_model_name`) â and prices its measured/estimated tokens via `ModelSpec.marginal_cost_usd`. Measured vs char/4-estimated spend is kept separate; plan-billed models contribute $0 marginal cost but still report token volume; a model absent from the registry is counted as *unpriced* (tokens still attributed, never a fabricated price). `job_counterfactual` prices the same volume against the flagship reference model so "what would this have cost at metered rates?" is answerable for any run, pinned or not.
- **`cost` command + `puppetmaster_job_cost` rewired.** Both now always report **actual measured spend** plus a counterfactual, and show the **pre-flight routing estimate** only as a secondary section when the job auto-routed. The "$0, didn't auto-route" dead end is gone. The JSON shape is backward-compatible (`total_estimated_cost_usd`, `by_model`, `tasks` preserved) with an additive `actual_cost` block and `counterfactual`.
- **Hermes analyze mode now stamps token usage.** It was the one adapter path that emitted no usage on its verdict, making analysis-mode Hermes runs unmeasurable; it now records the honest char/4 estimate (flagged `tokens_estimated`), matching its implement sibling and the other adapters. (The cursor/claude-code/codex/hermes *implement* paths already recorded usage.)
- **Known limits:** actual cost prices against the *current* registry, not price-at-time-of-run (Puppetmaster keeps no historical price store and calls no billing API) â it answers "what would this cost at today's asserted prices". A model the user hasn't registered can't be priced (its tokens are still counted and surfaced as unpriced). Hermes usage remains a char/4 estimate, since it is shelled out as a CLI with no SDK usage object.
- Full suite: **979 passed** (pytest), with new unit + CLI + adapter tests covering pinned pricing, router-id precedence, estimated bucketing, the unpriced fallback, the plan-billed counterfactual, and hermes analyze usage.

## v0.9.92

**The deferred god-module split, done as its own dedicated change: `adapters.py` and `cli.py` are now packages â behavior-, surface-, and CLI-preserving, test-gated, and live-validated across every enabled adapter.** v0.9.91 intentionally left this structural churn (the highest import-regression surface in the repo) to a separate, separately-reviewed release; this is that release. Both splits move code only â no public name, function signature, argparse wiring, command behavior, event, artifact shape, or DB schema changed. Each split was guarded by a golden contract captured from the pre-split module and verified after.

- **`puppetmaster/adapters.py` (4163 lines) â `puppetmaster/adapters/` package.** Split into shared seams (`_streaming`, `_git`, `_prompts`, `_facade`, `_base` for the common run lifecycle) and one module per provider (`cursor`, `claude_code`, `codex`, `hermes`, `openai`, `local`) plus `registry`. The `__init__` facade re-exports the full former surface: **all 113 public names remain importable** from `puppetmaster.adapters`, and pyflakes reports zero undefined names.
- **`puppetmaster/cli.py` (6289 lines) â `puppetmaster/cli/` package.** The fragile coupling is kept intact and together â `_parser` (the whole `build_parser`) and `_dispatch` (`main`/`_main` command routing) â with handlers grouped into cohesive command modules (`commands_install`, `_mcp`, `_models`, `_platform`, `_codegraph`, `_jobs`, `_gate`), plus `helpers` and `guidance`. **All 162 public names remain importable, all 65 subcommands parse, and every per-subcommand `--help` is byte-identical** to the pre-split contract. No file now exceeds ~1700 lines.
- **Verified end-to-end on the refactored code.** Full suite **972 passed** (pytest + CI `unittest discover`); entrypoint smoke (`--help`, `doctor`, `jobs`, `adapters`) routes correctly; and every enabled adapter drove its real CLI through both new packages with zero import/crash errors â cursor via the full daemonâCLIâworker pipeline (real Cursor SDK), claude-code emitting a real report+patch, hermes running in analyze mode, and codex correctly classifying a provider-auth failure.
- **Known limits:** the package `__init__` facades import their submodules eagerly (a broken submodule fails the whole import) â identical in blast radius to the former single-file modules, and guarded by the import-surface + full-suite gates. The split is pure structure: runtime behavior, performance, and outputs are unchanged, so the payoff is maintainability, not new capability.

## v0.9.91

**Staff-level code-quality audit and overhaul â surfaced by a read-only Puppetmaster self-audit swarm, then fixed and verified end-to-end.** A read-only multi-role swarm audited the codebase for duplication, drifted contracts, error-handling gaps, and weak types; the highest-value findings were overhauled behavior-preserving, each phase test-gated, with two live end-to-end swarm runs proving the orchestrator/store/adapter/stitcher stack still runs on the new code. No public name, signature, event, artifact shape, or DB schema changed.

- **One canonical failure taxonomy.** Per-adapter failure classifiers had drifted into divergent vocabularies (e.g. Cursor/OpenAI emitted `missing_api_key` where Claude/Codex emitted `not_authenticated`). New `puppetmaster/failure.py` defines the single source of truth â canonical category constants plus a table-driven `classify_adapter_failure` with per-adapter overrides â and every `classify_*_failure` is now a thin wrapper. A back-compat alias is kept so already-persisted artifacts and the `RECOVERABLE_FAILURES` re-route set keep resolving; consumers were traced and confirmed unchanged.
- **Store coordination decided once, written atomically per backend.** `SwarmStore` and `SQLiteSwarmStore` had each fully reimplemented `claim_task` / `update_task_status` / `renew_task_lease` / `recover_stale_tasks`, and the decision logic (max-attemptsâFAILED, BLOCKED/dependency handling, terminal lease clearing, `completed_at` rules, emitted events) had drifted between them. The decision logic now lives once in the base; each backend overrides only a small atomic primitive â the file backend a snapshot-compare under its per-task lock, SQLite a single CAS `UPDATE` (no read-modify-write). **A file-backend double-claim race was fixed** by internalizing the lock into `claim_task`. Event names/payloads are byte-identical.
- **Adapter run() lifecycle written once.** The four full-edit adapters (Cursor, Claude Code, Codex, Hermes) each hand-orchestrated the same snapshot â dirty-guard â CLI â snapshot â patch-artifact dance. A `CliWorkerAdapter` template method now owns that lifecycle; adapters supply only their CLI invocation. Cross-class private-method borrowing was removed.
- **Subprocess + error-handling hardening.** Git subprocess calls take explicit timeouts and return a typed `GitSnapshot`; the Cursor analyze path joins the streamed runner (live logs/heartbeat/process-group kill) for parity. Router override loading narrowed its blanket `except` to `(OSError, json.JSONDecodeError)`, warns once on a parse error, and is mtime-memoized so the hot path stops re-reading the file.
- **Test gaps closed.** Added focused concurrency tests (two concurrent `claim_task` calls yield exactly one winner on both backends; max-attemptsâFAILED; stale-lease recovery) and full-branch coverage for the previously-untested `validate_openai_base_url` security guard.
- **Deferred by design:** splitting the `adapters.py` / `cli.py` god-modules into packages is pure structural churn with the highest import-regression surface, so it is intentionally left to its own dedicated, separately-reviewed change rather than folded into a release.
- Full suite: **972 passed** â verified under both `pytest` and the CI `unittest discover` command, plus two live end-to-end swarm runs on the refactored code.

## v0.9.90

**Opt-in PyPI update awareness plus an explicit `puppetmaster self-update` command â Puppetmaster can tell you when it's behind and upgrade on request, but it never auto-installs and deliberately never upgrades on error.** The long-lived stdio MCP server cannot hot-reload its own code (the client owns the pipe), so "self-update" means *detect + one-command upgrade + restart nudge*, not an in-process swap. Auto-upgrading mid-run â especially keyed on errors â would change code out from under a running swarm and turn one bad release into an outage, so it is intentionally not built.

- **Opt-in PyPI latest-version check.** New `puppetmaster/update_check.py` queries the PyPI JSON API for the latest `puppetmaster-ai` release and, when newer than what's installed, surfaces a one-line nudge. Off by default; gated behind `PUPPETMASTER_PYPI_UPDATE_CHECK=1`. The network call uses the stdlib `urllib` (no new dependency) with a 3s timeout, fails completely silently (returns `None`) on any network/parse error, and is cached with a 6h TTL so the MCP hot path never makes a per-call network request.
- **`puppetmaster self-update` CLI command.** Runs `pip install -U puppetmaster-ai` with the same interpreter, then prints the restart instruction (toggle the MCP server, or `puppetmaster mcp cleanup --kill-stale`) because the running stdio server can't reload in place. `--dry-run` prints the exact pip command and executes nothing. No code path ever invokes it automatically â it only runs when a user types it.
- **Update nudge on errors, informational only.** When the opt-in check knows a newer release exists, the one-liner is appended to MCP error responses â purely as text, with zero install side effects and no retry-with-upgrade loop. Disabled or up-to-date behavior is unchanged.
- **No duplication.** The version-parsing helpers (`_version_tuple` / `_on_disk_is_newer`) moved into `update_check.py`; the existing disk-vs-running staleness nudge now shares them.
- Full suite: **962 passed** â verified under both `pytest` and the CI `unittest discover` command.

## v0.9.89

**The optional Signal-maximizer output style now takes a custom directive â bring your own rules instead of the two built-in tiers.** The presets (`terse`, `lithic`) are unchanged; this adds a verbatim path for teams that want their own house style without forking the rule text.

- **Per task:** `payload.output_style_text` â the directive string itself, used exactly as written.
- **Globally:** `PUPPETMASTER_OUTPUT_STYLE_TEXT` (inline) or `PUPPETMASTER_OUTPUT_STYLE_FILE` (path to a directive file). A missing or empty file resolves to "off" rather than injecting nothing or failing dispatch.
- **Precedence (highest first):** payload custom text â payload tier â env custom text â env file â env tier. A disabled value (`off`/`none`/`""`) at the payload layer still opts a single spec out even when a global default is on. A spec running a custom directive is stamped `output_style: "custom"` for telemetry.
- Custom directives are used verbatim â you own the content and its failure modes. Like the tiers, the directive is applied equally to every candidate model, so it does not bias routing.
- Full suite: **955 passed** (pytest + CI `unittest discover`). Docs: `docs/OUTPUT_STYLE.md`, bundled skill updated.

## v0.9.88

**Killed the hot-path N+1 query patterns in the data layer, added an optional output-style directive, and rewrote the README â found and verified with Puppetmaster auditing itself.** A read-only Puppetmaster swarm audited the SQLite persistence layer for per-row database access; it confirmed the cross-job/feed/stitcher paths are already correctly batched and surfaced three genuinely unbounded hot-path patterns, all fixed here with regression tests.

- **Task claim no longer re-fetches each dependency by id.** `claim_next_task` already builds a `task_map` for its queue scan, but `claim_task` re-ran `dependencies_complete` without it â one `get_task_by_id()` SELECT per dependency edge on every worker claim. The map is now threaded through `claim_task` (both the file base and the SQLite override), so a dependency-heavy DAG checks dependencies from memory. Dependencies are monotonic toward `COMPLETE`, so the reused map is exactly as fresh as the scan that selected the task.
- **`recover_stale_tasks` batches a crashed-worker wave into one transaction.** The SQLite override opened a fresh session (commit + fsync) per stale task; it now recovers every stale lease inside a single transaction, preserving the per-row CAS guard and `rowcount` check verbatim, and releases task locks only after the commit. N expired leases now cost one commit, not N.
- **Worker-wait progress polling is O(1) again.** `_wait_for_worker` compared `len(read_events(job_id))` before and after every timeout slice â re-reading and deserializing the entire event stream each tick (O(polls Ã events)). It now uses `event_cursor()` (SQLite `MAX(id)`, file backend's size-keyed cache), honoring the SwarmStore cursor-read contract in AGENTS.md.
- **Deferred (bounded N):** the liveness reaper and the worker daemon's per-running-job sweeps loop over *concurrent active jobs* (â1 in the common single-orchestrator case), and their inner per-job calls are exactly what the three fixes above made cheaper. Batching their outer loop needs new cross-job bulk store APIs and is left as a follow-up rather than risking concurrency-sensitive recovery code in a release.
- **Optional Signal-maximizer output style.** A new opt-in directive (two tiers: `terse`, `lithic`) that the orchestrator prepends to worker prompts through a single adapter-agnostic seam, mirroring memory/skill injection. Off by default; gated by env or per-spec `output_style`. It shapes only output form, never reasoning, and is applied equally to every candidate model so it does not bias routing. See `docs/OUTPUT_STYLE.md`.
- **Bring-your-own-compression docs.** `docs/COMPRESSION.md` documents why Puppetmaster does not bundle input-side compression tools (RTK/Headroom/Caveman) â empirically marginal savings plus the re-read tax â and gives a clean recipe for users who still want to wire their own.
- **README rewrite.** The root README is now a tight, index-led page (Contents tree, trimmed sections, no emoji/marketing filler) with in-page anchors converted to absolute GitHub URLs so it also renders correctly on PyPI.
- Full suite: **951 passed** â verified under both `pytest` and the CI `unittest discover` command, plus the SQLite/memory/crash demos that exercise the claim and recovery paths.

## v0.9.87

**Removed the last leftover Cursor coupling so people who never intend to use Cursor aren't blocked or nudged toward it.** Puppetmaster was originally built on Cursor; a platform-agnostic audit swarm found two real blockers and a cluster of cursor-first defaults still hiding in shared/core paths. All legitimately Cursor-specific code (the cursor adapter, `install-cursor-mcp`, `models discover --source cursor`) is unchanged and still works exactly as before when Cursor is chosen.

- **Implement/edit no longer silently defaults to Cursor when no platform lock is set.** With no persisted lock, `enabled_adapters()` returns *every* known adapter, and `pick_implement_adapter` is cursor-first â so a user with only Claude Code / Codex / Hermes configured could be routed to Cursor and then fail on a missing `CURSOR_API_KEY` / Node / `@cursor/sdk`. The auto-pick now selects the first priority adapter that is **actually runnable** on the machine (CLI/credentials present, via a new `adapter_is_available()` that reuses `doctor`'s detection), and raises an actionable "install/configure one of â¦" error if none are â instead of landing on Cursor. An **explicit** choice is never second-guessed: a set `requested` adapter, or a lock pinned to exactly one implement-capable adapter, is honored verbatim.
- **Generic MCP swarms work for every analysis-capable adapter.** `write_generated_swarm_config` previously allowed only `{cursor, local}` and raised a raw `ValueError` for claude-code / codex / hermes; the custom-role error even told users to "use `puppetmaster_start_cursor_swarm`". It now supports all analysis adapters (`SWARM_ANALYSIS_ADAPTERS`), pins `allowed_adapters=[adapter]` so the swarm actually dispatches to the chosen platform (not just Cursor), forwards an explicit model pin to non-cursor adapters, and returns a **structured** tool error (listing supported adapters) rather than a raw exception. Guidance text is now platform-neutral and lists the user's enabled adapters.
- **`doctor` stops making non-Node / non-Cursor installs look broken.** `node` and `npm` are now reported as `optional` (not `missing`) with wording that names the only flows that need them (the Cursor adapter's SDK runner and CodeGraph); the local / OpenAI / Hermes / Claude Code core flows don't. CodeGraph doctor/repair guidance says "the runtime Node" instead of "Cursor's bundled Node". Exit semantics unchanged.
- **`install-rules` / `install-hooks` stop writing `.cursor/` artifacts into non-Cursor repos.** With no explicit `--target`, both now filter to the persisted platform lock, and the Cursor rule/hook is written only when Cursor is actually *indicated* â an existing `.cursor/` directory or a lock that includes cursor. An explicit `--target` still wins, and `setup`'s behavior is unchanged.
- **`models discover` is no longer cursor-first.** `--source` now defaults to a lock-derived set (a single locked platform is discovered directly; unrestricted enumerates everything reachable) instead of hard-defaulting to `cursor` and failing on a missing `CURSOR_API_KEY`. `--source cursor` remains the explicit Cursor path, and a cursor-only-but-unavailable run prints an actionable hint.
- **`setup`'s next steps are rendered from the platform(s) you enabled** â Cursor / Codex / Claude Code / Hermes each get their correct "pick up the MCP server" instruction, and a local/file-only install is told it needs no host, instead of a blanket "restart Cursor".
- Confirmed already-clean (no change needed): no import-time `@cursor/sdk` coupling in core Python, and `doctor`/`setup` already treat Cursor as optional and gate `install-cursor-mcp` on the lock.
- +16 regression tests pin every fix. Full suite: **941 passed** â verified under both `pytest` and the CI `unittest discover` command in a clean environment.

## v0.9.86

**Correctness hardening across the router, the SQLite/file store seam, and subprocess teardown â found by a read-only Puppetmaster audit swarm, fixed in one in-place pass, all backed by new regression tests.** A multi-role swarm (explore + review + redteam) converged on a cluster of sharp edges in the load-bearing seams; this release closes them without changing any happy-path behavior.

- **Router config overrides are now validated and pure.** The new `~/.pmharness/routing.json` overrides feature is hardened: a per-role override is honored only when it's a real number, clamped to `0..100`, and otherwise ignored â a malformed value like `{"overrides": {"audit": "high"}}` can no longer leave the base score a string and blow up later arithmetic. Reading the file is factored into one `_load_routing_overrides()` helper so the classifier and `route_task` share a single source.
- **An explicit `policy=` is never silently overridden.** `route_task`'s default is now an `Optional[str] = None` sentinel; the saved `routing.json` policy is consulted only when the caller leaves policy unspecified, falling back to `balanced`. Passing `policy="balanced"` (or any explicit policy) is now respected verbatim.
- **`escalating` truly returns the cheapest *sufficient* model.** The previous sort ordered by capability first, so a barely-sufficient but expensive model could beat a cheaper higher-capability one. It now filters to sufficient models first, then orders by `(plan_rank, marginal_cost, capability)`; the `rejected[]` list remains the ordered escalation chain (sufficient first).
- **Strict payload coercion.** `bool("false")` no longer evaluates truthy for `prefer_plan_billed` / `allow_api_billing`, and a bare string like `allowed_adapters: "openai"` / `required_tags: "vision"` is treated as a single element instead of exploding into characters. New `_coerce_bool` / `_coerce_str_list` readers reject ambiguous values and fall back to the safe default.
- **SQLite can claim a previously-BLOCKED task once its deps complete.** The claim CAS now includes `status = BLOCKED` (alongside `QUEUED` and stale `RUNNING`), matching the file-backed `SwarmStore` contract â a task blocked on dependencies that later finished is no longer stranded on the SQLite backend.
- **Per-claim lease tokens fence terminal writes and renewals.** Tasks now carry a monotonic `lease_id` stamped fresh on every claim. Terminal status updates and lease renewals are fenced on `(lease_owner AND lease_id)` â re-checked in SQL, not just in Python â so a worker that reuses the same `worker_id` after a stale-lease reclaim can't fence a *newer* claim. Fully backward-compatible: the token is only required when both sides have one, so older persisted tasks keep working. SQLite gets an atomic `renew_task_lease` override (CAS) instead of the racy read-then-`save_task`.
- **Subprocess teardown no longer orphans grandchildren.** On timeout, adapters launched with `start_new_session=True` (e.g. Hermes) now kill the whole process group via `os.killpg` on POSIX (with a direct-child fallback on non-POSIX/Windows), so an agent CLI's own spawned tree can't survive a timeout and keep editing.
- **Spawn failures and missing executables fail structured, not loud.** `run_streamed_subprocess` wraps `Popen` so a missing executable / bad cwd closes the live-log handle and returns a `spawn_error` instead of leaking the handle and raising; `ShellAdapter` catches `OSError` and emits a `failed` verification artifact; the Cursor analyze path now normalizes + redacts its legacy stdout/stderr tails like the other adapters.
- **Regression tests for every fix** (+301 lines): escalating cost ordering, malformed/ignored overrides, explicit-policy precedence, `route_task` purity with `HOME` patched, claiming an unblocked SQLite task, lease-token fencing against a stale same-`worker_id` completion, and process-group kill of a grandchild on timeout. The existing SQLite claim-race test now captures thread exceptions and asserts both threads finished. Full suite: 925 passed (verified under both `pytest` and the CI `unittest discover` command in a clean environment).

## v0.9.77

**Hermes worker sessions self-clean â no more pile-up of throwaway `source=tool` sessions under random worktree dirs.** Every Puppetmaster `hermes` worker shells out to `hermes chat`, and Hermes persists each run as a session. The adapter already tagged them `--source tool` (the documented "don't show in user lists" tag), but Hermes still writes them to its state DB and the desktop sessions panel lists them regardless â so a busy swarm left a trail of disposable worker sessions.

- **Auto-prune after every worker run.** `HermesAdapter.run()` now prunes ended `source=tool` sessions in a `finally` block (so it runs whether the worker passed, failed, or raised). It shells out to Hermes' **own** `hermes sessions prune --source tool --older-than 0 --yes` â never touching Hermes' SQLite directly â so deletion is cascade-correct (messages + compression_locks + on-disk transcripts) and version-safe.
- **Race-safe by construction.** Hermes' prune only deletes *ended* sessions, so a sibling worker still running in the same swarm is never disturbed. The helper also hard-guards on `source == "tool"` â it can never delete a real user source (`cli`/`tui`/`telegram`) even if a payload sets a weird source.
- **Best-effort, never fatal.** A missing CLI, timeout, or any error during cleanup is swallowed â session hygiene can't fail a worker's result.
- **Opt-out:** set `PUPPETMASTER_HERMES_PRUNE_SESSIONS=0` to keep worker sessions (e.g. to resume one for debugging). Manual cleanup anytime: `hermes sessions prune --source tool --older-than 0 --yes`.
- Verified live: a real `puppetmaster edit` ran a Hermes worker, landed the edit, and left **0** `source=tool` sessions behind. Bundled skill updated. Full suite: 865 passed.

## v0.9.76

**The invocation gate now hands ALL structural code lookups to CodeGraph, regardless of score â closing the last "Hermes still greps it itself" gap.** Single edits and implements already auto-delegated (v0.9.73); broad searches ("find all", "trace") already routed to CodeGraph. But *point lookups* â "where is X", "who calls Y", "what implements Z" â scored below the delegate threshold and stayed inline, so the host fell back to grepping the tree.

- **CodeGraph lookups delegate unconditionally.** When the gate infers a CodeGraph lookup intent (`where is / who calls / what implements / find all / trace / call graph`), it now delegates regardless of capability score. Rationale: unlike an edit â where a tiny change *might* be faster inline, so the score gate guards the round-trip â a structural lookup is cheap, fast, and strictly beats an inline grep. There's no penalty to protect against, so score must not hold it back.
- **Conceptual questions still stay inline.** "what is a SwarmStore" / "what does the --cwd flag do" are NOT structural lookups and remain inline; only the where/who/what-implements/find-all/trace family delegates. The explicit-inline opt-out ("just answer me" / "no puppetmaster") and the trivial carve-out still win over the new rule.
- **Net effect:** Hermes now pilots edits, implements, AND searches to Puppetmaster automatically, keeping only trivial edits and conceptual Q&A inline. Verified live through the real Hermes hook.
- Bundled skill updated to state the lookups-always-delegate rule. Hermes-only behavior surface; no change to how Cursor/Claude/Codex hosts are treated beyond the same (correct) routing. Full suite: 858 passed.

## v0.9.75

**Puppetmaster now ships a bundled Hermes skill â a fresh `pip install` makes Hermes a fully native host (MCP + hooks + skill) in one command.** Closes the last "native package" gap: previously the rich `puppetmaster` skill existed only on the maintainer's machine (hand-built across sessions), so a new user got the MCP server and auto-invoke hooks but **no durable procedural knowledge** â Hermes only learned Puppetmaster through the per-turn hook nudge.

- **Bundled skill shipped in the wheel.** `puppetmaster/skills/puppetmaster/SKILL.md` is now package-data (verified present in the built wheel). It teaches the verb decision tree (`edit` for one focused change, `start_implement` for coupled multi-file work, swarm for read-only analysis), the CodeGraph-first exploration flow, routing, the async monitoring pattern, and the trust gate.
- **`install-hermes-mcp` / `setup` install it.** A new `install_hermes_skill` copies the bundled skill into `~/.hermes/skills/autonomous-ai-agents/puppetmaster`. Idempotent and **non-destructive**: a fresh install writes it, an unchanged install is a no-op, and an existing *customized* skill is left untouched unless `--force` (so the maintainer's richer local skill is never clobbered). Verified end-to-end from a clean venv + built wheel into a temp `HERMES_HOME`.
- **One command = full native host.** `install-hermes-mcp` now wires all three: MCP server registration, the `pre_llm_call` / `pre_tool_call` hooks, and the skill. Docs (ADAPTERS.md, Hermes next-steps guidance) updated to match.
- **No regressions on other platforms** â the skill is Hermes-only (its install path is the Hermes skills dir); Cursor/Claude/Codex are untouched. Full suite: 854 passed.

## v0.9.74

**`puppetmaster setup` now makes Hermes a full auto-invocation host in one shot â no regressions on the other harnesses.** Follow-up hardening for the v0.9.73 `edit` verb.

- **Setup-wizard parity for Hermes hooks.** The `setup` wizard's Hermes step previously installed only the MCP server and silently skipped Hermes' native `pre_llm_call` / `pre_tool_call` hooks â so `puppetmaster setup` left Hermes a non-delegating host while Cursor/Claude got their hooks. The wizard now installs the Hermes hooks too (respecting `--skip-hooks`), matching the standalone `install-hermes-mcp` command. Regression-tested both ways.
- **`edit` verb advertised everywhere.** The installed agent rules (`rules.py`, written into every harness) and the Hermes next-steps guidance now document `puppetmaster_edit`, so all hosts discover the lightweight single-edit path.
- **Cross-harness regression audit (no behavior change, verified).** The v0.9.73 gate change (focused single-edit intent â `puppetmaster_edit`) is host-agnostic; confirmed it routes correctly and injects the right host-shaped directive on Cursor, Claude Code, Codex, and Hermes, with no cursor-verb leakage into non-cursor hosts, `verb_for_host` passing the new verb through unchanged, and the `puppetmaster_edit` MCP tool visible to every client.

## v0.9.73

**New `edit` verb â the lightweight, in-place single-edit path between an inline edit and a full implement job.** Focused single-file changes ("fix this function", "add a flag", "wire up retries") no longer have to choose between editing inline yourself (no CodeGraph, no cheap-model routing, no receipt) or spinning up a heavyweight `start_implement` worktree job. The new `edit` verb is the snappy middle: cheapest sufficient model, CodeGraph to locate the site, edits the working tree directly, returns the diff synchronously, and still captures a reviewable PATCH artifact.

- **`puppetmaster edit "<instruction>"` (CLI) + `puppetmaster_edit` (MCP).** Both run a single full-edit worker through the existing orchestrator â no new orchestration. Defaults are the verb's policy: `auto_route` with `routing_policy=cheap` (stop burning a frontier model on mechanical edits), CodeGraph on, `allow_dirty`/`allow_non_worktree` so the edit lands in place, and synchronous delivery so you see the diff immediately. A pinned `--model` overrides routing; `--no-auto-route` opts out. Picks the highest-priority enabled implement adapter (cursor â claude-code â codex â hermes) unless `--adapter` forces one.
- **The require_diff safety gate applies.** An `edit` run that produces no diff fails closed â a "done" edit that changed nothing can't masquerade as success.
- **The invocation gate now routes focused single-edit intent to `puppetmaster_edit`.** A focused implementation prompt with *no* broad-scope signal (no audit/refactor/migrate/"across the repo"/"every caller") steers to the in-place `edit` verb; broad-scope coupled work keeps `start_implement`, where an isolated worktree + one coherent PATCH is the right shape. The Hermes/Cursor/Claude hooks all inject the refined directive.
- **Single source of truth for implement-adapter selection.** Extracted `pick_implement_adapter` / `IMPLEMENT_ADAPTER_PRIORITY` into `puppetmaster.workers`; `start_implement` and `edit` both use it, so the priority order and the disabled/invalid-adapter errors can never drift.
- **Fix:** the `puppetmaster_start_implement` MCP schema's `adapter` enum was missing `hermes` (it's implement-capable). Added.

## v0.9.72

**Hermes is now a full auto-invocation host â single edits get steered to Puppetmaster automatically.** Puppetmaster's deterministic invocation-gate previously only spoke Cursor and Claude Code; it now speaks Hermes' native shell-hook protocol, so the same delegate-vs-inline brain runs on every supported host. Validated end-to-end inside a live Hermes turn.

- **`hermes` host in the hook runner.** `pre_llm_call` (per-turn) maps to the user-prompt gate event; the directive is returned as `{"context": "..."}` and injected into the user message (cache-safe â never the system prompt). `pre_tool_call` maps to the tool-gate event and returns Hermes' canonical `{"action": "block", "message": ...}`. The user message is read from Hermes' `extra.user_message` nesting. A trivial edit returns `{}` (silent no-op); an absent live MCP server makes the whole hook a no-op.
- **`install-hermes-mcp` now wires hooks too.** One command makes Hermes a full host: MCP server registration **plus** `pre_llm_call` + `pre_tool_call` hooks merged idempotently into `~/.hermes/config.yaml` (user-authored hooks and every other section preserved). `uninstall` removes them cleanly.
- **Cross-host shell-tool fix.** The broad-search deny-redirect now recognizes Hermes' `terminal` tool (and extracts the command from `{"command": ...}` payloads) so the read-only carve-out (`git log`, `ls`, `cat`) still works and only genuinely recursive searches are redirected.

## v0.9.70

**Hotfix: a non-path `npm root -g` result no longer crashes CodeGraph install resolution.** The v0.9.68 shim-fallback work made `find_codegraph_install` reachable on hosts where it previously short-circuited (any host with `node` on PATH but no Cursor). On a misconfigured npm â or, as caught by CI, a test that globally stubs `subprocess.run` to return a large non-path blob â the raw stdout was fed into `Path(root).is_dir()`, raising `OSError: File name too long` (ENAMETOOLONG). Two CI legs went red on v0.9.68/v0.9.69 because of this.

- **Sanity-gate `npm root -g` output before touching the filesystem.** A new `_looks_like_single_path` check rejects empty, multi-line, NUL-bearing, or oversized (>4096 char) results, and the `is_dir()` probe is wrapped in `try/except (OSError, ValueError)`. A non-path result now falls through to the shim resolver instead of crashing.
- **Harden the shim resolver too.** `_codegraph_install_from_shim` wraps its path walk in the same guard so a malformed shim path can never raise.
- Regression tests added for the garbage/giant `npm root -g` case and the path sanity-gate.

## v0.9.69

**Hermes is now documented and marketed as a first-class worker adapter â validated end-to-end through the orchestrator.** The NousResearch [Hermes](https://hermes-agent.nousresearch.com) CLI joins Cursor, Claude Code, Codex, and OpenAI as a supported analyze + full-edit adapter. (The adapter code shipped earlier; this release validates it on a real run and surfaces it across the README, FEATURES, ADAPTERS, and COMPARISON docs.)

- **`hermes` adapter (analyze + full-edit).** Shells out to `hermes chat` headlessly, mirroring the Claude Code / Codex subprocess + git-snapshot + sidecar-spool + PATCH-attribution semantics. Handles Hermes' two quirks â process-group isolation (`start_new_session=True`) and unreliable exit codes (success is read from the git diff in implement mode, from stdout in analyze mode). CodeGraph context is auto-injected when `.codegraph/` exists. Wire the MCP into Hermes with `puppetmaster install-hermes-mcp`. Adapter docs in [docs/ADAPTERS.md](ADAPTERS.md#hermes).
- **Validated end-to-end.** A real analyze run through the orchestrator returned `result: passed`, read the repo (cited `router.py`, `models.py`, `sqlite_store.py`), confirmed `context:codegraph` injection, and parsed the worker output into 3 typed `finding` artifacts + 1 `verification` that stitched and promoted to memory cleanly. 29 Hermes adapter unit tests pass.

## v0.9.68

**The CodeGraph better-sqlite3 repair/resolve path works on every harness, not just Cursor.** The repair that was hardwired to Cursor's bundled Node now resolves a runtime Node on any harness.

- **CodeGraph repair works on every harness.** `find_cursor_node` â `find_runtime_node` (env override â Cursor's Node â `node` on PATH), and `find_codegraph_install` now follows the `codegraph` shim back to its package root when `npm root -g` points at the wrong prefix. Harness-neutral user-facing messages throughout.

## v0.9.67

**Findings dedup that actually collapses paraphrases: evidence-locus-anchored clustering.** The 0.9.66 dedup was wired correctly (the stitch path renders Findings through `_bullet_payloads(dedupe=True)`, and `show`/`await` read that stitched output) â but its similarity test required exact/substring match or â¥0.8 token overlap, which paraphrase-level duplicates from different workers ("multiply uses an O(n) loop" vs "multiplication is implemented inefficiently") fall well under. On a live run, one bug still surfaced 3â5Ã because the workers cited the same files but worded it differently. (A stress run that showed *zero* collapse, including exact dupes, was a stale-server artifact â the daemon was still on pre-0.9.66 code; the 0.9.65 staleness nudge now surfaces exactly that.) Full suite **812** green.

- **Cluster on the shared code locus, not just wording.** Two claims now merge when they cite a shared file (parsed from the structured `evidence` list, e.g. `arithmetic.py:5-8`) **and** share at least two *content* words (stopwords excluded) â paraphrases of one bug clear this easily, while two genuinely distinct bugs at the same file (a `KeyError` vs a missing-division bug both in `cli.py`, zero shared content) stay apart. Claims with no shared locus still require strong wording overlap (â¥0.6) to merge, so an un-anchored merge stays conservative.
- **Match against any cluster member, not just the representative.** Workers paraphrase in chains (AâB, BâC, AâC); anchoring only on the representative stranded the far end. A claim now joins a cluster if it's similar to any member, so all three phrasings of one finding collapse to a single *"(reported by N workers)"* bullet keeping the highest-confidence representative. On the live fixture this took 9 findings â 5 distinct, with both `cli.py` bugs correctly kept separate.
- Per-worker Verification lines remain un-collapsed (each is meaningful run status).

## v0.9.66

**The last two Hermes stress-test edges: the `empty_or_unstructured` flicker and duplicated findings in the stitch.** Both observed across two concurrent swarms on 0.9.65 â mechanically green, but a minimal-effort worker occasionally degraded on prose, and the same finding from three workers read as three near-identical bullets. Full suite **809** green (+7 tests).

- **One stricter JSON-only retry before accepting a degrade (Hermes analyze).** A clean run (no timeout, no process failure) that returns prose the parser can't structure now gets a single reprompt â *"respond with ONLY a JSON object `{\"artifacts\":[...]}`â¦ if you genuinely found nothing, return `{\"artifacts\":[]}`"* â before being marked degraded. This is exactly the `gemini-2.5-flash-minimal` flicker class: cheapest model, minimal reasoning, occasionally chatty. The verification artifact records `retry:recovered` or `retry:exhausted` so the behavior is auditable; gated by `analyze_retry` (default on), and a process failure or timeout never triggers a retry. Scoped to the Hermes adapter because the minimal-effort tier that causes this is Hermes-only.
- **Near-identical findings collapse in the stitched summary.** The stitcher now runs an order-preserving, greedy dedup over Findings/Decisions/Risks: claims that are exact, substring-contained, or â¥0.8 token-overlap join one cluster rendered as a single bullet with *"(reported by N workers)"*, keeping the highest-confidence/most-detailed claim as the representative. Per-worker Verification lines are never collapsed (each is meaningful on its own). Tightens the summary without dropping signal.

## v0.9.65

**The MCP reconnect dance is now a visible nudge, not a silent guess.** A long-lived stdio MCP server keeps whatever code it loaded at startup; an in-place `pip install -U puppetmaster-ai` changes what's on disk but can't hot-reload the running server (the client owns the pipe). v0.9.62 made this *detectable* on pull (`mcp status`/`mcp list`); now it's *pushed* â every tool response self-reports staleness so a daily-driver user discovers it without going looking. Full suite **802** green (+7 tests).

- **Per-call `server_update_available` warning.** `call_tool` â the single dispatch chokepoint every `tools/call` flows through â compares the version this server loaded at startup (`_SERVER_RUNNING_VERSION`, snapshotted at import) against `installed_puppetmaster_version()` (read fresh from disk). When a newer version is installed, the tool response carries a one-line nudge: *"running 0.9.64 but 0.9.65 is installed â restart the MCP server (toggle it in your client, or `puppetmaster mcp cleanup --kill-stale`) to load the new code."*
- **Cheap and quiet by default.** The check is TTL-cached (30s) so the hot path doesn't re-scan package metadata per call, only nudges on a genuine *upgrade* (a downgrade or unknown disk version stays silent), and `setdefault` never clobbers a handler's own payload. No warning is emitted when the server is current â the common case adds nothing.

## v0.9.64

**Fix: the first/cheapest analyze worker can no longer hallucinate an empty repository.** On a 6-file fixture the `explore` worker â first in the DAG (no deps), routed to the cheapest model at `reasoning_effort: minimal` â reported *"The repository is empty."* at confidence **1.00** while every downstream worker analyzed the same files in detail. Root cause was a context-delivery gap, not a model whim: Puppetmaster only injects a file listing via CodeGraph when `.codegraph/` exists, and dependent workers also inherit prior findings (which already cite files) through `prompt_with_memory`. The dependency-free first worker had neither â no index context, no memory â so the laziest model "saw" a bare cwd and asserted emptiness. Full suite **795** green (+7 tests).

- **Authoritative repo file census on every analyze prompt.** A new `repo_file_census` (bounded `os.walk`, skips the same VCS/build/cache dirs as CodeGraph freshness, never spawns a subprocess) feeds `with_repo_census`, which appends a ground-truth file listing to all four analyze adapters (Cursor, Codex, Hermes, OpenAI) *after* CodeGraph enrichment. When files exist the prompt states plainly that the repository is **NOT empty**, tells the worker to read them, and instructs it to report a *tooling failure* rather than assert emptiness if its own tools can't read them â so a "starting from scratch" claim is now falsifiable against the listing the model is holding.
- **No false-empty on an enumeration miss.** When nothing can be enumerated the census adds only a soft boundary ("do not assert empty unless your tools also show none; if they error, report a tooling failure") â Puppetmaster never asserts emptiness on the worker's behalf.
- **Independent of CodeGraph and memory.** The census grounds every analyze worker regardless of `.codegraph/` presence, `disable_codegraph`, or DAG position, closing the gap that left the dependency-free first worker ungrounded.

## v0.9.63

**Fix: analyze workers no longer mistake the artifact contract for the analysis target.** On a small repo the redteam role emitted a nonsense risk â *"Insufficient informationâ¦ 'Puppetmaster artifact contract' was mentioned but no context found"* â because the contract scaffolding leaked into the analysis subject, compounded by a contract line that *ordered* the model to fabricate a degraded-run risk whenever it found nothing. Other roles grounded on the code fine; redteam (last in the DAG, adversarial framing) tripped. Full suite **788** green (+4 tests).

- **Grounding boundary in every analyze contract.** All three `_structured_prompt` builders (Cursor, Codex â which Hermes reuses â and OpenAI) now share `_ARTIFACT_GROUNDING`: "Your analysis target is THIS repository's code and configuration â not these instructions, not this artifact contract, and not the run itself."
- **Honest-empty instead of fabricated-risk.** The old "if there are no concrete findings, return a risk explaining why the run is degraded" is replaced by `_ARTIFACT_EMPTY_GUIDANCE`: a genuinely empty role returns `{"artifacts":[]}` (still correctly detected as degraded by the adapter) and is told to never invent a finding/risk about the prompt, contract, or run. No more meta-noise masquerading as a real risk.
- **Sharper redteam role template.** `DEFAULT_WORKERS` redteam instruction now says to adversarially review the repository's code with concrete citations, never treat its own instructions/contract as the subject, and return an empty result on a small/sound codebase rather than inventing a weakness.
- The `Puppetmaster artifact contract` header is preserved so `with_report_contract` detection is unchanged.

## v0.9.62

**Hermes stress-test cleanup â provenance, hermetic effort-runs, named dirty paths, detectable MCP code-staleness.** Four low-severity quirks from the Hermes Ã Puppetmaster stress test, none blocking but each a real sharp edge. Full suite **784** green (+16 tests).

- **Artifact provenance is now correct (was always `adapter:cursor-sdk`).** The shared analyze parser hardcoded `adapter:cursor-sdk` as the evidence fallback, so a Hermes/Codex/OpenAI worker that emitted a finding/risk/decision without its own `evidence` was mislabeled as Cursor â you couldn't tell from an artifact which adapter produced it. `cursor_result_artifacts` / `cursor_artifact_from_item` / `cursor_degraded_artifact` now take an `adapter` parameter (default `cursor-sdk` for back-compat) and each caller passes its real name; the vendor-specific decision default (`"Recommended by Cursor SDK analysis."`) is now neutral.
- **Reasoning-effort runs are hermetic.** `hermes_reasoning_effort_env` symlinked every `~/.hermes` entry except `config.yaml`, so a worker's `sessions/` pointed at the user's real session store and any session it wrote landed in `~/.hermes/sessions/`. `sessions/` is no longer symlinked â the temp home gets its own throwaway dir, so effort-runs never touch personal state.
- **The clean-tree guard names the offending paths.** A blocked implement run reported `dirty_worktree` without listing what was dirty, so a stray `__pycache__/` read like a real failure. All four adapter guards (Cursor/Claude/Codex/Hermes) now append a named, truncated sample of the modified/untracked paths to the message via the shared `dirty_worktree_paths_note`.
- **A long-lived MCP server on pre-upgrade code is now detectable.** An in-place `pip install -U` bumps the installed version but can't reload the running stdio server, so it keeps serving old code until the user happens to restart it. The registry now compares each alive server's registered version against the installed package; `mcp status` / `mcp list` flag mismatches as `code_stale` / `old` with a restart hint. (Detection only â auto-killing a heartbeating server mid-call is left to the explicit `mcp cleanup --kill-stale`.)

## v0.9.61

**Fix: trailing-brace trailers no longer falsely degrade a good analyze run.** The shared analyze-result parser (`parse_cursor_artifact_payload`, used by the Hermes *and* Cursor analyze paths) sliced from the first `{`/`[` to the **last** `}`/`]`. When a worker emitted valid JSON followed by any brace-bearing trailer â a Hermes `-Q` `[session abc | 1,240 tokens | $0.003]` cost footer, a courtesy `Done {ok}`, or a second JSON blob â the slice over-reached into the trailer, `json.loads` raised, and the run was falsely marked `degraded` â `empty_or_unstructured_*_result`. This was the deterministic root cause of the intermittent "good work silently downgraded" nit (it only fired when the trailer happened to carry a brace). Full suite **768** green (+13 tests).

- **`_json_prefix_decode` (new candidate, pure addition).** A final fallback in the candidate loop: scans each `{`/`[` opener and lets `json.JSONDecoder().raw_decode` consume exactly one complete value, ignoring trailing junk. Tries every opener so a false opener in prose (`Use {x} then: {â¦realâ¦}`) still resolves to the real payload. Added *after* the existing `text` / `_strip_json_fence` / `_json_object_slice` candidates, so happy paths are untouched â no regression risk.
- **Guarded against over-correction.** Only structured containers (`dict`/`list`) count; a bare scalar after a brace or genuine unstructured prose (no parseable opener) still returns `None`, so real degrades aren't masked. A true-prose test row pins this.
- **Test matrix (13).** Clean JSON, session/cost footer, courtesy brace line, second JSON object, no-brace trailer, fenced JSON + footer, leading prose, CRLF, false-opener-then-JSON, genuine prose â `None`, prose with unparseable braces â `None`, plus end-to-end `cursor_result_artifacts` recovery through a trailer and zero-artifacts for prose.

## v0.9.60

**CodeGraph index-freshness detection + background self-heal.** A stale index was CodeGraph's worst failure mode: nothing re-read the repo after `codegraph index`, so once the working tree moved on (an agent edits files, a `git pull` lands commits) queries silently returned a view that no longer matched the code â the agent "couldn't find" code that plainly existed. PM now *knows* when its own index is behind and says so, instead of relying on the user knowing to run `repair-codegraph` / `codegraph sync`. Full suite **755** green (+12 tests).

- **`codegraph_freshness(cwd)` (new).** Cheap, never-raises drift probe. Git fast-path (`git status` enumerates only changed files; one `git log` settles the commit side) with a bounded filesystem fallback for non-git trees (file-count + wall-clock budget; runs out â `unknown`, never a false `fresh`). Returns a structured verdict: `fresh` / `stale` / `unknown` / `no_index` / `uninitialized`, with a changed-file sample and commits-since-index count. A 2s mtime tolerance keeps a freshly-built index from being flagged against its own inputs.
- **Loud surfacing where agents actually look.** `puppetmaster_codegraph_status` / `_search` / `_context` now attach an `index_freshness` block and fold a `STALE` warning into the result `hint`; injected worker prompts get the same warning appended; `doctor` reports the codegraph leg as `warn` when the index has drifted. The silent-wrong failure becomes visible at every entry point.
- **Background auto-sync (self-heal).** When a query/worker path sees a stale index, PM kicks a deduped, detached `codegraph index` re-sync (reusing the per-repo indexer lock, so it cleanly no-ops when an indexer is already running) â the *next* call gets fresh data without blocking this one. Per-repo cooldown prevents a herd of indexers; opt out with `PUPPETMASTER_CODEGRAPH_AUTOSYNC=0`.
- **`python -m puppetmaster codegraph freshness` (new CLI).** Scriptable freshness check â exit 1 when stale â so a pre-commit hook or CI step can gate on a drifted index.

## v0.9.59

**Fix: Hermes workers no longer inherit Puppetmaster's `PYTHONPATH` (cross-interpreter poisoning).** Hermes is the first Python-CLI adapter; the others spawn Node CLIs that ignore `PYTHONPATH`. The Hermes worker env was built with `inject_worker_cli_env`, which prepends this install's source root (the pyenv-3.9 site-packages) to `PYTHONPATH` â correct for `python -m puppetmaster codegraph` children, but poison for the foreign Hermes interpreter, which then imports the parent's stale `python-dotenv` and crashes with `load_dotenv() got an unexpected keyword argument 'override'`. Full suite **743** green (+2 tests).

- **`scrub_foreign_interpreter_env` (new, in `codegraph.py`).** Strips `PYTHONPATH`/`PYTHONHOME` from an env before it is handed to a non-Puppetmaster Python. Fixes the whole bug class: any future foreign-Python adapter gets the same treatment instead of re-discovering the clash.
- **`HermesAdapter._run_implement` / `_run_analyze` use the scrub** instead of `inject_worker_cli_env` â Hermes workers don't self-serve `python -m puppetmaster codegraph` from the parent's tree, so they never needed the injected source root. Node-based adapters (Cursor/Claude/Codex) are unchanged: they ignore `PYTHONPATH` and still get the CLI-resolving injection.
- Regression tests pin the helper's behavior and that both Hermes paths scrub (and never re-introduce `inject_worker_cli_env`).

## v0.9.58

**Coupling-aware delegation â stop fanning out a single feature into a swarm.** The invocation gate was steering *every* delegated task toward a "fan it out to a swarm" framing, even single implementations. Fanning out one tightly-coupled change makes parallel workers re-ingest the same context and stack commits that are unaware of each other â conflicts, rework, broken delivery, and zero net token savings. The gate now matches the verb to the task shape. Full suite **741** green (+4 tests).

- **Implementation intent routes to ONE implement worker, not the swarm default.** `infer_role_and_verb` previously matched only a narrow verb list (`refactor|implement|build|â¦`), so plain feature work ("add a CSV export endpoint", "create the webhook handler", "wire up retries") fell through to the read-only `*_swarm` default â the exact task-shape mismatch behind the collision reports. The implement pattern is broadened to catch ordinary feature/fix verbs and maps to `puppetmaster_start_implement` (a single worker in a clean worktree that yields one reviewable `PATCH`).
- **Verb-aware injected directive.** `DelegationDecision.directive()` is no longer one-size-fits-all: implement verbs are described as a single clean-worktree worker ("not a fan-out swarm; parallel editors stack commits that are unaware of each other"), CodeGraph verbs as a structural lookup, and only genuinely read-only analysis keeps the swarm framing. The implement directive explicitly reserves swarms for the explore/review/audit passes *around* the feature.
- **Trivial carve-out hardened against the broadening.** An explicit trivial signal ("add a comment", "fix a typo", rename, one-line/single-file) now stays inline regardless of the role-inflated capability score, so widening implement detection never starts delegating routine one-liners (the fastest way to train users to disable the gate).
- **Guidance follows the code.** `AGENTS.md` gains a "match the verb to the task shape" section: single feature â single implement worker; independent slices â fan out split by non-overlapping files; broad investigation â read-only swarm. It also states the honest positioning â Puppetmaster wins on mixed workloads, trivial-heavy batches, long-context horizons, and zero-token artifact recall, and output-compression tools (e.g. RTK) are **additive**, not competitors.

## v0.9.57

**Reasoning-effort routing for Hermes + two frontier models on your own keys.** Hermes lets you dial each model's reasoning depth (`minimal`â`xhigh`); the router now treats `(model Ã effort)` as a first-class cost/capability dimension, so a hard task can land on a high-effort mid-tier model instead of paying frontier prices. Full suite **737** green (+6 tests).

- **`HermesAdapter` honors `payload.reasoning_effort`.** Hermes has no `hermes chat` effort flag â the headless source of truth is `agent.reasoning_effort` in the loaded `config.yaml` (read at every CLI startup). The adapter applies per-task effort via an **ephemeral `HERMES_HOME`** that symlinks the real home (preserving `auth.json`, `.env`, sessions, and MCP servers verbatim) and overrides only `config.yaml` with the chosen level. No mutation of the user's `~/.hermes`, so parallel swarm workers can run at different efforts safely; the temp home is torn down after the run. Degrades to the default effort when the level is empty/invalid, PyYAML is absent, or the home can't be read â a routing knob never fails a worker.
- **`models set <id> effort=<level>` and the setup wizard now support `hermes`** (`minimal`/`low`/`medium`/`high`/`xhigh`), alongside the existing `openai`/`codex` support. Effort is stamped as `payload_defaults.reasoning_effort` with an `effort:<level>` tag and an output-token-cost multiplier so estimates stay honest (high effort â 2Ã output burn). The wizard's effort-variant builder now **merges** onto the base entry's defaults instead of replacing them, so Hermes' adapter-critical `provider` survives a variant (also benefits any future provider-bearing model).
- **Frontier models added to the curated Hermes catalog:** `gpt-5.5` (cap 96, OpenAI) and `claude-opus-4-8` (cap 99, Anthropic) â so `models discover --source hermes` seeds a full frontier suite on your own API keys, and they survive a registry reset.

## v0.9.56

**Hermes joins the model router â credential-aware, so `auto_route` can pick Hermes the moment setup finishes without ever landing on a provider you can't call.** v0.9.53 made Hermes a routable worker and shipped a curated catalog; v0.9.55 wired the host MCP. This closes the last gap: the curated Hermes models are now *seeded into the router registry* during `setup`, filtered to the providers you actually have credentials for. Full suite **731** green (+9 tests).

- **Per-provider credential detection.** New `available_hermes_providers()` resolves which Hermes providers (`gemini`/`anthropic`/`openai-api`) are callable â reading process env, `~/.hermes/.env`, and OAuth state in `~/.hermes/auth.json` (so a `hermes login` provider counts too). `GOOGLE_API_KEY` or `GEMINI_API_KEY` satisfies `gemini`. The existing `hermes_credentials_available()` is refactored onto the same shared key-detection helper (behavior unchanged).
- **Credential-filtered seeding (no dead routes).** `curated_to_specs` / `merge_curated_into_registry` take an optional `allowed_providers` filter; a curated entry whose `payload_defaults.provider` lacks a credential is skipped and reported under `skipped` (with its provider), so a missing key surfaces as an actionable line instead of a model the router would route to and then fail on. `allowed_providers=None` is the default and preserves every existing caller byte-for-byte.
- **Wizard auto-seeds on enable.** `setup` step 7 (now "install-hermes-mcp + router registry") seeds the credential-backed Hermes models into `models.json` right after the MCP install, prints `added`/`refreshed`/`skipped`, and â when no provider credential is found â tells you exactly how to fix it (`add a key, re-run models discover --source hermes --write`). Best-effort: registry seeding never aborts the wizard.
- **`models discover` is credential-aware for Hermes.** `--source hermes` now seeds only callable models (filtered + `skipped` reporting), and `--source all` folds Hermes in **only when the `hermes` CLI is actually present** â so users who don't run Hermes don't get `hermes/*` entries injected just because they happen to have an `OPENAI_API_KEY` set.

## v0.9.55

**`puppetmaster install-hermes-mcp` â turnkey host wiring so Hermes can orchestrate Puppetmaster.** v0.9.53 made Hermes a routable *worker*; this closes the other direction (Hermes-on-top), registering Puppetmaster's MCP server into Hermes for parity with `install-cursor-mcp` / `install-codex-mcp` / `install-claude-mcp`. Verified end-to-end: Hermes lists `puppetmaster â enabled` from the config we write. Full suite **722** green (+10 tests).

- **`install-hermes-mcp` (idempotent, schema-correct).** Unlike Codex/Claude, `hermes mcp add` is discovery-first and drops into an interactive tool-selection checklist that can't run headlessly â so the installer edits the `mcp_servers.puppetmaster` block in the `config.yaml` Hermes owns directly (the same direct-write approach `install-cursor-mcp` uses for `mcp.json`), honoring `$HERMES_HOME`. It preserves every other server/section and any user-set `env` / `tools` / `timeout` keys on the entry, runs the same MCP `tools/list` handshake smoke test before writing (refuses to register a broken launch command), and reports `unchanged` on a match. `--force`, `--dry-run`, `--skip-handshake`, `--path` mirror the sibling installers; a matching `uninstall_hermes_mcp` is wired into `puppetmaster uninstall`, and `setup` gains a gated Hermes step.
- **Zero-dependency contract intact.** Editing Hermes' YAML needs PyYAML, so the installer imports it lazily and prints an actionable hint (`pip install puppetmaster-ai[hermes]`) when it's absent â the core package still declares no runtime dependencies. PyYAML is declared only in the new optional `[hermes]` extra.
- **Rules already flow for free; hooks deliberately deferred.** Hermes auto-injects `AGENTS.md` on every session, so the Puppetmaster directive `install-rules` already writes reaches Hermes today â no Hermes-specific rules target needed. A Hermes hooks target is intentionally *not* shipped: Hermes hooks are shell-script hooks gated by a first-use consent allowlist (`shell-hooks-allowlist.json`), and auto-installing one would subvert that security boundary by design. MCP + AGENTS.md is the established parity bar for a non-Cursor/Claude platform (Codex has no hooks target either).

## v0.9.54

**Docs: README polish + a visual setup guide.** No code changes â refreshes the project page on GitHub and PyPI.

- Adds a one-glance setup graphic (install once, wire your platform, verify) to the README, hosted via an absolute raw-GitHub URL so it renders identically on GitHub and PyPI.
- Tightens the Install section: condensed the dense auto-invocation paragraph to its essentials and pointed the detail at the existing [Auto-invocation](https://github.com/professorpalmer/Puppetmaster#auto-invocation) section.

## v0.9.53

**NousResearch Hermes is now a first-class Puppetmaster platform â analyze + full-edit implement across Gemini, Anthropic, and OpenAI â with hermetic worker isolation.** The Hermes CLI (`hermes chat`) joins Cursor / Claude Code / Codex as a routable adapter, verified end-to-end across all three providers for both modes. Full suite **712** green (+10 tests).

- **`HermesAdapter` (analyze + implement).** Shells out to `hermes chat` headlessly. Two Hermes quirks are handled at the root: it kills its own process group on exit (every run uses `start_new_session=True` so teardown can't reach the orchestrator), and it returns unreliable exit codes (implement success is read from the git diff and emitted as a `PATCH` artifact; analyze success is parsed from the structured stdout). Failure classification maps provider errors â including `HTTP 404: model` â `model_unavailable` â to actionable reasons.
- **Hermetic worker isolation (the correctness fix).** Hermes leaks state across sessions two ways: it injects stored **memory** into the system prompt, and the **`session_search`** tool lets a worker read prior sessions (observed: a worker recalled an unrelated earlier session's secret). Workers now run with `--ignore-rules` (suppresses memory injection + the memory tool) **and** default toolsets that exclude `memory`/`session_search` (`file,terminal,code_execution,web,vision` for implement; `file,web,vision` for analyze). A forced cross-session recall now returns `UNKNOWN`. Power users can opt back in with `--use-hermes-rules` / a custom `--toolsets`.
- **First-class platform wiring.** Added to the platform lock's known adapters, the MCP implement priority + command builder (`puppetmaster_start_implement` can target `adapter="hermes"`), a `puppetmaster hermes` CLI subcommand (`--mode implement|analyze`, `--provider`, `--model`, `--max-turns`, `--toolsets`, â¦), and `doctor`/`adapter_status` configured-detection (CLI on PATH + a reachable provider credential).
- **Auto-routing across providers.** A curated, API-billed Hermes catalog (gemini-2.5-flash/pro, claude-sonnet-4-5, gpt-5) stamps the required `provider` via `payload_defaults`, so the router picks the cheapest sufficient model *and* the worker reaches the right credential/wire protocol (a bare model name would otherwise route to unconfigured OpenRouter). Seed it with `python -m puppetmaster models discover --source hermes --write`.
- **Upstream fix in flight.** The OpenAI Responses-vs-Chat-Completions mismatch (GPT-4.1/4o failing on the direct `api.openai.com` Responses path) is fixed in [NousResearch/hermes-agent#47820](https://github.com/NousResearch/hermes-agent/pull/47820) rather than worked around with a non-portable local config.

## v0.9.52

**Community PR batch: Codex MCP credential inheritance, read-only generated swarms, throttled worker heartbeats, compact `status`, and a cross-project dashboard â each validated end-to-end before merge.** Five external contributions (four from @kbentonferguson, one from @jwellman80) reviewed under hard scrutiny, integrated with authorship preserved, and hardened with follow-up fixes/tests where they had gaps. Full suite **702** green (+19 tests).

- **Codex MCP credential inheritance is explicit and secret-safe (#10).** `install-codex-mcp` gains `--env`, `--inherit-env`, `--env-file`, `--map-env`, and `--force-env`: users opt specific provider variables into the MCP server environment instead of depending on whatever the host launched with. Secret-like values and env-file contents go through a managed Python wrapper backed by a private `0600` JSON env file, so raw credentials never land in Codex TOML, installer logs, or output; existing env keys are preserved unless `--force-env`. Codex billing detection now reads `$CODEX_HOME/auth.json` before `~/.codex/auth.json`, and `doctor --json` surfaces credential-env visibility and billing-source evidence without printing values. **Follow-up fix:** the PR also tightened `adapter_status` to gate Codex availability on billing health, which would have demoted an `OPENAI_API_KEY`-only setup to "unconfigured." Availability and billing are now decoupled â the CLI plus *any* credential signal marks Codex usable, while the `billing:codex` check reports the account context independently (non-regressive superset of prior behavior, pinned by a regression test). The whole env-inheritance path is opt-in: with no env flags the install is byte-for-byte the previous behavior, no wrapper file created.
- **Generated MCP swarms stay read-only when routed to an edit-capable adapter (#11/#12).** Generated analysis swarms now carry `read_only`/`sandbox: "read-only"`/`dangerously_bypass_approvals_and_sandbox: false` through their task payload, so when auto-routing lands on Codex the worker reviews the caller's dirty diff on the adapter's read-only path instead of tripping the full-edit clean-tree guard. Implement/edit paths (`mode=implement`) still require a clean tree. Defense in depth: classification skips the guard *and* the adapter physically can't write.
- **Worker heartbeat noise throttled without weakening lease safety (#11).** Lease renewal / heartbeat moved off the hot `poll_seconds=0.1` loop to a dedicated cadence (default 2s, capped at `lease/3` so short leases still get â¥3 renewals before expiry); `--heartbeat-seconds` is explicit, with non-default `--poll-seconds` honored as the compatibility fallback. Inline worker mode now scopes `PUPPETMASTER_STATE_DIR` to the owning store root so adapter sidecar logs land under the correct project state dir.
- **Compact `status` for prompt-heavy jobs (#13).** `status --compact` (and the MCP `puppetmaster_status` tool's `compact` arg) omits high-churn prompt/goal/instruction bodies and replaces them with deterministic `chars` + `sha256` refs, so identity is still comparable without paying the context cost. Default output is unchanged. Measured **97.4%** reduction on a prompt-heavy job (149,644 â 3,886 bytes) in end-to-end validation.
- **Dashboard `--all-projects` for a single cross-project view (#14).** A new `--all-projects` switch aggregates jobs from every Puppetmaster project state dir on the machine, labeling each row with the digest-stripped project slug; cross-project deep-links resolve via `find_state_dir_for_job`. The existing job-id traversal guard still rejects bad ids with `400` before any path join, and the server stays loopback-only unless `--allow-external`. **Follow-ups:** added the cross-project aggregation test the PR was missing, and wired `--all-projects` through the MCP `puppetmaster_dashboard` tool for CLI parity.

## v0.9.51

**Routing and fallback now gate on whether an adapter can *actually run*, not just whether it's enabled â plus Bedrock-aware Claude Code model IDs.** Field report (Rishi): a worker tried Cursor-style Claude IDs (`claude-opus-4-8`) that Bedrock rejected as invalid, then fell back to a Codex that wasn't installed and died. Two independent root causes, both fixed at the root with defense in depth. Full suite **683** green (+11 tests).

- **Fallback no longer cascades into an uninstalled CLI.** All three reroute paths already gated on the platform lock *and* billing health â but a stale `~/.codex/auth.json` / `~/.claude.json` keeps an adapter reading billing-*healthy* long after its CLI binary is gone, so billing-healthy â  runnable. A new single-source `preflight.adapter_cli_present` resolves the adapter's real binary on PATH (honoring `CLAUDE_CODE_COMMAND` / `CODEX_COMMAND`); CLI-less adapters (Cursor's bundled SDK runner, OpenAI's HTTP path) stay "present" since their readiness is purely a credentials question. Reroute now skips an adapter whose binary isn't there and leaves the task cleanly FAILED instead of dispatching a doomed worker.
- **The router won't first-pick a model it can't launch either.** After registry reconciliation, `_route_specs` drops CLI-missing adapters â with a strict **never-fail-closed** guard: if that would empty the registry (e.g. CI with no CLIs at all), it keeps the registry intact and lets dispatch/fallback surface the precise error. Emits `router.adapter_cli_missing` for the audit trail.
- **Claude Code on Bedrock gets a real model id or a precise error â never an invalid one.** The `claude` CLI on Bedrock accepts only inference-profile IDs / ARNs (`us.anthropic.claude-opus-4-1-20250805-v1:0`), never the registry's short `claude-opus-4-8`. `resolve_claude_code_model` is now Bedrock-aware: off Bedrock, unchanged; on Bedrock, precedence is an explicit override (`ANTHROPIC_MODEL` / `payload.bedrock_model`) â an already-Bedrock-shaped request â **omit `--model`** so the CLI uses its configured default, with a clear, actionable note (rather than forwarding a short name Bedrock will reject). No region-specific ARNs are guessed.
- **Setup detection unchanged â already correct.** `setup` already auto-detects and pre-selects only genuinely usable platforms (CLI resolvable, `CURSOR_API_KEY` + SDK, `OPENAI_API_KEY`); the new runtime gating is the defense-in-depth that also protects zero-config users who never run `setup`.

## v0.9.50

**Setup honors the platform lock, real Cursor-SDK token metering, a fixed Windows `doctor` probe, and CodeGraph that reaches every agentic harness.** A batch of field-reported rough edges (Pawel's setup/`doctor` notes plus a large parallel-swarm test) resolved end-to-end. Full suite **672** green; the token fix verified against a live Cursor composer run.

- **Setup no longer installs Cursor when Cursor isn't selected.** The step-2 platform lock is now the single source of truth across the whole wizard, not just the SDK bootstrap. `install_rules` and `install_hooks` gained an optional `enabled_adapters` filter (default `None` = unfiltered, so standalone `install-rules`/`install-hooks` are unchanged), and steps 4/7/8 thread the locked set through. A Claude-only machine now gets Claude hooks/rules + the cross-tool `AGENTS.md`, with Cursor cleanly skipped and a "skipped â disabled by the platform lock" line instead of silent always-on installs.
- **`doctor` no longer surfaces a misleading `npm â WinError 2` on Windows.** `subprocess(shell=False)` can't launch the `npm.cmd` shim that `shutil.which` finds via PATHEXT (CreateProcess only runs real binaries), so probing bare `npm` raised `FileNotFoundError`. A new `_resolve_probe_command` routes `.cmd`/`.bat` shims through `%COMSPEC% /c`. Actually fixed, not a "harmless quirk."
- **CodeGraph is impossible to miss (and to misinstall) in `doctor`.** Both messages now name it as the shared repo-intelligence layer, give the exact Node/npm command (`npm install -g @colbymchenry/codegraph`), explicitly warn against the unrelated `pip install codegraph`, and point init at `python -m puppetmaster codegraph init --index`.
- **Cursor-SDK token accounting is real now, not a ~500Ã undercount.** `Agent.prompt()`'s `RunResult` carries no usage, so the runner always read `null` and the Python side fell back to a `char/4` estimate that made tokens useless as a cost axis. The runner now drives `Agent.create` + `agent.send({ onDelta })` and accumulates measured input/output/cache tokens off the streaming `turn-ended` delta (verified live: a trivial prompt reports ~10.9k input tokens, not ~20). Cache read/write tokens are preserved for the cost axis; a runtime that reports no usage still falls back to a clearly-labeled estimate (`tokens_estimated: true`) rather than a fake-measured zero.
- **CodeGraph CLI resolves to the current tree in every agentic harness.** Only the Cursor adapter wrapped its worker env with `inject_worker_cli_env` (source root â `PYTHONPATH`); Codex and Claude omitted it, so on a host with a stale pip `puppetmaster` their workers hit `unknown command` on `python -m puppetmaster codegraph` and fell back to grep â defeating the CodeGraph-first directive in their own prompts. Both now match Cursor, with a regression test pinning the invariant across all three harnesses.

## v0.9.49

**Fix: v0.9.48's CodeGraph auto-provisioning could hang the MCP server on any plain-Node host â `doctor` (and every per-worker adapter) synchronously triggered a global `npm install -g` and blocked.** v0.9.48 added the npx leg to `codegraph_available()`, so on a host with Node but no `codegraph` shim and no Cursor (i.e. most non-Cursor users â and the CI runners), the cheap "is CodeGraph ready?" probe started returning **true**. That probe gates hot paths: `doctor` then ran `codegraph status` â `run_codegraph_cli` â `ensure_codegraph_provisioned()` â a blocking `npm install -g @colbymchenry/codegraph` (up to 300s) on the request thread. Caught by red CI on the v0.9.48 `main` lane â `test_parallel_doctor_calls_do_not_kill_mcp_server` failed `windows-latest` with `handshake never returned` (the doctor reply never came back because it was stuck installing), reproducing twice. Full suite **659** green (+1 regression test).

- **`codegraph_available()` is a cheap, no-network probe again.** It counts only what resolves instantly â the `codegraph` shim on PATH or Cursor's bundled Node + installed package â and never the npx leg. npx "availability" means *Node exists*, not that CodeGraph is warm; treating it as ready is what dragged health checks and adapters into a cold install. Restores v0.9.47's fast-skip on non-Cursor hosts.
- **The npx fallback + provisioning now live behind the explicit-command gate only.** `run_codegraph_cli()` gates on `ensure_codegraph_provisioned()`'s return (False only when there's literally no Node), so an *explicit* `puppetmaster codegraph status/search/context` still bootstraps via npx on any Node host â Victor's v0.9.48 win is preserved â while `doctor`, prompt-injection, and per-worker context never pay a provisioning cost.
- **Regression test.** `test_codegraph_available_is_cheap_and_never_provisions` asserts the probe reports not-ready on an npx-only host **and** shells out to nothing (`subprocess.run` asserted uncalled), pinning the invariant that broke.

## v0.9.48

**Fix: CodeGraph is now auto-provisioned on any host with Node â it stops giving up with "codegraph CLI not on PATH" the moment a global shim and Cursor's bundled Node are both absent (field report: Victor on Codex, `IdeaProjects/ff-data-engineering`, no Cursor + no global install â CodeGraph unreachable).** Previously the resolver had exactly two legs (global `codegraph` shim, or Cursor's bundled Node), so a non-Cursor host without the manual `npm install -g` got a hard `ok: false`. Now there's a universal third leg and a one-time provisioner, so the only remaining floor is "no Node at all" â which a Node CLI genuinely cannot cross. Full suite **658** green (+4 tests); verified end-to-end by masking the global shim off PATH and running `python -m puppetmaster codegraph status` through the npx fallback.

- **Universal `npx` fallback.** When neither the shim nor Cursor's Node resolves, CodeGraph runs via `npx -y @colbymchenry/codegraph` â zero manual setup on any machine with Node. Because the package's native `better-sqlite3` is built and run under the *same* (system) Node, the ABI matches, so this path never falls back to slow WASM SQLite (that only happens when crossing Node runtimes). Opt out with `PUPPETMASTER_CODEGRAPH_NO_NPX=1`.
- **One-time provisioning on first use.** `ensure_codegraph_provisioned()` runs once before the first timed CLI call so it doesn't race a cold npx download/native build against a tight timeout. It prefers a persistent global install (`npm install -g @colbymchenry/codegraph` â a real shim on PATH, no per-call npx overhead, this session and every session after); if that can't run or doesn't land on PATH, it warms the npx cache instead. Best-effort and fail-open at every step. Opt out of the global install with `PUPPETMASTER_CODEGRAPH_NO_GLOBAL_INSTALL=1`; tune the bootstrap ceiling with `PUPPETMASTER_CODEGRAPH_PROVISION_TIMEOUT_SECONDS` (default 300).
- **Honest floor + clearer hint.** The passive prompt-injection path stays non-blocking (no provisioning stall on worker dispatch â it still degrades to "no context" gracefully). The only `ok: false` left is a host with no Node, and the hint now says exactly that: install Node 18+, and that Puppetmaster auto-provisions the CLI once Node is present.

## v0.9.47

**Fix: the MCP server no longer self-reaps on a false orphan signal under Codex â the root cause of Victor's recurring "Transport closed."** The input-staleness watcher treated `os.getppid() == 1` as "my host died, reap me" **unconditionally**, even on Codex where idle-reaping is deliberately off. But `ppid == 1` only means the *immediate* parent exited: when a host spawns the server through an intermediate that exits, the server reparents to `launchd` **while the real owner (Codex) is alive and still holding the stdio pipe** â and then kills itself. Codex sees stdin EOF and reports "Transport closed"; Cursor masks the same reap by transparently respawning, which is exactly why Codex was "the only outlier." Reproduced deterministically (grandchild reparented to init + Codex handshake + owner alive holding the pipe â server self-reaps: `reason=sigint_clean_shutdown`), then fixed and re-verified to stay alive. Validated by driving real `codex exec` (codex-cli 0.134.0) against the MCP â doctor, a `start_codex` worker (completed, 2 artifacts), and a 5-call sustained run all clean, no transport drop. Full suite **654** green (+1 regression, 1 stale test flipped).

- **Parent-death reaping is host-gated, like idle-staleness.** The authoritative owner-death signal for a stdio server is **stdin EOF** â the host holds the write end, so when it dies the pipe closes and the main loop exits cleanly on every host, no watcher needed. The `ppid == 1` heuristic now only fires on hosts that transparently respawn a reaped server (Cursor, or an explicit `PUPPETMASTER_MCP_INPUT_STALE_FORCED`), where a wrong reap costs nothing. On Codex/Claude Code a leaked-but-alive server is far cheaper than a false "Transport closed."
- **Live handshake override closes the env-leak hole.** The reap gate freezes at startup from env, but Codex scrubs `CODEX_*` from the server env, so a Codex session launched from a Cursor terminal leaks `CURSOR_*` and is misread as a respawn host. Once the `initialize` handshake reveals a Codex client (`_client_is_codex()`), the server never self-reaps regardless of the startup env guess.
- **Known limits.** This fixes the reproducible transport drop; it does not address the separate "review worker stalled with no output" report (could not reproduce â a Codex worker completes normally and a keyless Cursor worker fast-fails with `preflight_blocked`, not a stall). If that recurs it needs the exact verb and the job's `stderr_path` to trace.

## v0.9.46

**Fix: the auto-invocation hook no longer wedges native tooling â it stops hard-denying the built-in `Task`/`Agent` tool and goes fully silent when no Puppetmaster MCP server is reachable (field reports: Victor on Codex, and a Cursor session, both blocked from delegating to native subagents while the swarm verbs weren't connected).** The pre-tool hook denied every `Task`/`Agent` call and redirected to `puppetmaster_start_*swarm` â but when those MCP tools aren't connected the turn is wedged (native tool blocked, suggested verb uninvokable), and the blanket deny also broke the native subagents Puppetmaster explicitly does **not** replace (`browser-use`, `ci-investigator`, `cursor-guide`, `best-of-n-runner`). Verified end-to-end across cursor/claude/codex hosts; full suite **653** green.

- **Native `Task`/`Agent` is never hard-denied.** Steering toward a swarm stays at the non-blocking prompt-submit layer (inject a directive, never block the tool). The hook payload can't tell a swarm-worthy fan-out from a browser-use run, so blocking the agent tool was both a wedge and an over-reach.
- **No live MCP server â the hook is a complete no-op.** New availability probe reads the per-user MCP server registry; with nothing alive there's nothing to redirect to, so the hook allows every native tool and injects no directive (it never nudges toward a verb the agent can't call). Fails closed to "allow" on any error. `PUPPETMASTER_HOOK_ASSUME_TOOLS=1/0` overrides the probe.
- **The lone remaining redirect is recoverable.** A genuinely recursive/repo-wide *shell* search (`rg -r`, `grep -R`, `find -name`) is still steered to CodeGraph (only when a server is alive), and the deny now names the CLI fallback (`python -m puppetmaster codegraph search '<query>'`) so it's never a dead-end even if the MCP transport is down. Read-only shell (`git log`, `ls`, `cat`) and narrow `grep pattern file` stay allowed as before.
- **Known limits.** The registry probe can't detect a "server process alive but the host's MCP transport silently dropped" state; since the agent-tool deny is gone, the only thing that can fire there is the recursive-shell redirect, which now points at the CLI. The kill switch (`PUPPETMASTER_AUTO_INVOKE_DISABLED=1`) still disables the whole gate.

## v0.9.44

**CI/test reliability: green the macOS release lane â no product behavior change from v0.9.43.** The v0.9.43 *tag* CI went red on `macos-latest` only (the same commit was green on `main`): `test_input_staleness_watcher_resets_when_message_arrives` failed on `self.assertFalse(triggered.wait(timeout=0.1))`, and a rerun reproduced it â so it's environmental, not a rare flake. By construction that test uses `stale_after=0.2s` with a fresh inbound mark, so the idle-staleness path *mathematically cannot* fire inside the 0.1s window; the only other trigger is the watcher's parent-death reap (`os.getppid() == 1`), and loaded macOS GitHub runners transiently reparent a test process to `launchd`. That orthogonal orphan signal was leaking into three idle-focused watcher tests. Full suite **647** green.

- **Idle-staleness tests pin off the parent-death reap.** All three `_InputStalenessWatcher` idle tests now patch `_parent_is_dead -> False`, so each exercises only the idle-staleness guarantee it claims. Parent-death keeps its own dedicated test (which patches it `True`), so coverage is unchanged.
- **Product code untouched.** `mcp_server.py` and every shipping module are byte-identical to v0.9.43; the Codex auto-detach fix from v0.9.43 stands. This release exists solely to carry the test de-flake on a green release tag.

## v0.9.43

**Fix: synchronous worker verbs are now Codex-safe â they auto-detach to a `job_id` instead of blocking past Codex's hard tool timeout (investigation: Victor's recurring "Transport closed" on Codex).** Driving real Codex (codex-cli 0.134.0) against the MCP with full `rmcp=trace` exonerated the prime suspects with receipts: the idle keepalive's `notifications/message` frames are parsed and accepted by rmcp (7 frames over 45s, zero errors), the `2024-11-05` handshake is accepted, and a per-tool timeout firing mid-call does **not** drop the transport (the next call succeeds). The one real Codex gap: Codex enforces a hard `tool_timeout_sec` and sends **no `progressToken`**, so our progress keepalive can never extend it â any synchronous worker verb (cursor/claude/codex/openai reviewÂ·planÂ·implement) that runs past the budget gets cancelled and a healthy job reads as failed. Full suite **647** green (+8 focused tests); fix live-verified end-to-end against real Codex.

- **`run_worker_cli` auto-detaches on hard-timeout hosts.** The four blocking worker verbs (`puppetmaster_cursor_review`/`_cursor_plan`/`_cursor_implement`, `_claude_implement`, `_codex`, `_openai`) now route through a wrapper that, under a Codex host, starts the same worker asynchronously and returns its `job_id` immediately with poll guidance (`puppetmaster_await_job`/`status`/`show`) â the only delivery Codex can actually complete. Work and artifacts are identical; only inline-blocking becomes poll-for-result. Cursor and Claude Code (no hard cap) keep the inline behavior unchanged.
- **Host detected from the `initialize` handshake, not env.** Codex spawns the MCP server with a scrubbed environment (no `CODEX_*` vars), so env sniffing misses it â verified empirically. We now capture `clientInfo` at `initialize` (Codex sends `name: "codex-mcp-client"`, `title: "Codex"`) and key off that, falling back to `CODEX_*` env markers only when the handshake identity is unknown. A known non-Codex client is never misread from stray env vars.
- **Escape hatches.** `PUPPETMASTER_MCP_SYNC_AUTODETACH=0` keeps verbs blocking inline (for users who raised Codex's `tool_timeout_sec` high enough to wait out a run); a per-call `autodetach` boolean overrides both directions.
- **Known limits.** This does not change Victor's root cause if his drop is environmental (still-alpha codex-cli, or a server that can't import puppetmaster from his cwd) â those need his `codex --version` / `python -m puppetmaster mcp list` output. It hardens the one reproducible Codex-specific failure mode found. Claude Code is treated as non-hard-timeout (no field-proven hard cap); revisit if that changes.

## v0.9.42

**Fix: "Cursor SDK not found" on machines where it was installed and working (field report: Zane, with a valid `@cursor/sdk` at `site-packages/puppetmaster/node_modules`).** The diagnostics probe checked exactly one directory level (`parent.parent` of the package â `site-packages/node_modules`), but `cursor_sdk_runner.mjs` resolves the SDK with Node's upward `node_modules` walk, whose *first* hop is the package dir itself. Result: runtime worked, diagnostics reported the SDK missing, detection/setup disagreed with reality. Our dev tree masked the bug because the repo root happens to have `node_modules` at the one level the old probe checked. Full suite **640** green (+1 regression test on the exact reported layout); `ruff` clean; probe live-verified.

- **Probe mirrors Node's resolution.** `_find_cursor_sdk_install` now walks `node_modules/@cursor/sdk` from the package directory up through every ancestor â same algorithm the runner uses â plus the workspace root and `PUPPETMASTER_HOME` as before. Diagnostics and runtime can no longer disagree about where the SDK lives.
- **Doctor advice un-staled.** The cursor-sdk warning said "run `npm install` in the Puppetmaster package dir" â pre-v0.9.39 advice that points pipx users into a venv they shouldn't touch. It now says what works: "run `puppetmaster install-cursor-mcp` to bootstrap @cursor/sdk (needs Node/npm)".
- **Known limits.** The probe checks directory existence, not SDK integrity â a corrupt or half-installed package still reads as present; the preflight generation check remains the runtime truth.

## v0.9.41

**Fix: implement workers no longer look "degraded" after doing the job â the final report now becomes durable artifacts (field report: the v0.9.40 CI fix itself, where a cursor implement worker shipped the right patch and correct diagnosis but the job read "Findings: None").** The cursor implement path never asked for a report and never parsed the agent's final message; Claude's path had the same gap; Codex labeled every prose-reporting write run a degraded RISK. Full suite **639** green (+9 focused tests); `ruff check` clean on touched files.

- **Reporting contract in implement prompts.** Cursor's implement wrapper and Claude worker prompts now end with a contract: close your final message with what changed, files touched, and what you ran to verify. Prompts that already carry the structured JSON artifact contract (swarm review/plan roles) are left alone â no conflicting instructions.
- **Final message parsed into artifacts, with a prose fallback.** `implement_report_artifacts` first tries the structured JSON contract (typed findings/decisions/risks, same parser as analyze mode); anything else is preserved verbatim as a FINDING (`claim` = report headline, `payload.report` = full text, evidence `report:final-message`). Cursor implement, Claude runs, and write-capable Codex runs all route through it on success. Quality verdicts flip from "degraded" to "ok" organically because a substantive artifact now exists.
- **Codex prose semantics split by capability.** A workspace-write/bypass Codex run that reports in prose passes with a report FINDING; read-only (review-style) runs keep the strict degraded-RISK semantics â their whole contract is structured findings.
- **CLI surfaces the report.** `puppetmaster cursor --implement` (and every edit-mode run) prints a `report:` headline after the outcome line, plus the `puppetmaster artifacts <job_id>` pointer to the full report.
- **Known limits.** Reports are capped at 20k chars (tail-biased) inside the FINDING; the full transcript stays in the stdout sidecar. Failed runs (non-zero exit) intentionally emit no report finding â output from a crashed agent isn't a report. The OpenAI adapter's analyze-only degraded semantics are unchanged.

## v0.9.40

**Fix: green CI on Windows runners (the v0.9.39 npm bootstrap built a backslash path the test rightly rejected) plus a timing flake on macOS runners.** Two distinct failures were red on `main`: `test (windows-latest, 3.12)` since v0.9.39, and an intermittent keepalive flake that even hit a README-only commit. Diagnosed from the actual Actions logs, not local guessing â the local macOS suite was green the whole time.

- **Windows: `ensure_cursor_sdk` now passes npm a forward-slash prefix.** `str(Path)` stringifies to backslashes on Windows (`\fake\site-packages`), which both failed the exact-command test and risks Git-Bash-style mangling in the field â the same bug class as the v0.9.34 hook-path fix. `as_posix()` is correct on every platform; npm accepts forward slashes everywhere.
- **Flake: `test_keepalive_emits_progress_only_with_token` polls instead of sleeping.** A fixed 0.12s window assumed the keepalive thread got scheduled promptly; loaded CI runners starved it past the window (observed: `'notifications/message' not found in set()` on a docs-only commit). The test now polls up to 5s for the frames it asserts on â deterministic on slow runners, still fast (~0.05s) locally.
- **Known limits.** The Windows fix is verified by the platform-exact unit test on CI, not by a live `npm install` on a Windows machine. The flake fix removes the observed starvation mode; other timing-sensitive tests in the suite were not audited in this pass.

## v0.9.39

**Fix: PyPI installs never had the Cursor SDK â setup now bootstraps `@cursor/sdk` via npm (field report: our own demo recording showed "cursor (not detected on this machine)" on a Cursor machine).** Wheels can't ship `node_modules`, so every pip/pipx install landed without the SDK that `cursor_sdk_runner.mjs` resolves at runtime: platform detection read cursor as absent and the cursor adapter itself couldn't run. Dev-tree installs masked this for months because the repo has `node_modules` sitting next to the package. Full suite **630** green (+7 focused tests); `ruff check puppetmaster/` clean; verified end to end on a fresh wheel venv â detection True pre-bootstrap, `install-cursor-mcp` npm-fetches the SDK into site-packages, Node resolves it from the runner's directory, MCP handshake 42 tools, `setup` shows `[on ] cursor` unannotated.

- **`ensure_cursor_sdk` bootstrap.** `install-cursor-mcp` (and `setup` step 4, when the platform lock enables cursor) now runs `npm install @cursor/sdk --prefix <package dir>` when the SDK is missing â the same directory Node's resolution walks up to from the runner script, so diagnostics and runtime agree afterward. Idempotent (`unchanged` when resolvable), honest when blocked (`skipped` with remediation when npm is absent, `error` with npm's last line on failure).
- **Detection counts a bootstrappable SDK.** `_detected_platforms` no longer demands the SDK be pre-installed: cursor is detected when `CURSOR_API_KEY` is set and either the SDK resolves *or* npm is available to fetch it â so first-run setup doesn't lock cursor off one step before the bootstrap that would fix it.
- **Step 1 stops overclaiming.** The billing probe printed "Cursor SDK authenticated" while only checking an env var â directly contradicting step 2's "not detected" in the same run. It now says what it verified: "CURSOR_API_KEY is set; work bills against the Cursor plan."
- **Known limits.** The bootstrap needs Node/npm on PATH â without it, cursor is honestly not detected and the skip message says what to install. npm fetches over the network at setup time (one-time, ~5s); air-gapped machines must vendor `node_modules` manually next to the installed package. Detection still does not validate the API key itself â doctor's billing checks remain the auth truth.

## v0.9.38

**Docs: the PyPI/README tagline now credits AWS Bedrock as a Claude Code billing path.** Bedrock support itself shipped in v0.9.27 (posture detection, credential health, live-verified completion through `CLAUDE_CODE_USE_BEDROCK`) but the project description never mentioned it. Docs-only release so the frozen PyPI project page picks it up; no code changes.

- **Known limits.** Bedrock remains a Claude Code billing path, not a standalone adapter â there is no native Bedrock API worker, and Vertex AI (`CLAUDE_CODE_USE_VERTEX`) is still undetected.

## v0.9.37

**Managed rules now mandate CodeGraph-first exploration, not just suggest it.** The installed rules (Cursor `.mdc` + the `AGENTS.md` managed block read by Claude Code and Codex) forced Puppetmaster delegation hard â trigger convention, delegate-first gate, hooks â but CodeGraph got one "prefer" sentence buried in the gate. Now it has its own must-obey section. Full suite **623** green (+1 focused test); `ruff check puppetmaster/` clean; repo `AGENTS.md` block refreshed via `install-rules` live.

- **"CodeGraph-first exploration (must obey)" section in the managed rule body.** Graph every directory you interact with: check `puppetmaster_codegraph_status` before exploring any directory (root or subtree), `puppetmaster_codegraph_init` (`index: true`) if no `.codegraph/`, then answer where/what-calls/what-implements questions from `_search`/`_context`/`_affected`/`_files` instead of crawling the tree.
- **Unsupported-language policy: partial coverage is still coverage.** CodeGraph indexes the languages it supports; ungraphable files simply don't enter the graph. The rule now tells agents to keep answering from the graph for everything it covers, scope native search narrowly to the ungraphed paths only, and reuse that shared context â never let multiple workers/agents each re-crawl directories the graph already covers.
- **Honest carve-outs and fallback baked in.** Native search stays legitimate for plain-text matches, single known file paths, and explicit "just grep"; transport failures route to the ABI-safe `python -m puppetmaster codegraph â¦` passthrough, never a bare `codegraph` shell call.
- **Known limits.** This is rule pressure, not deterministic enforcement â a host agent can still ignore prose; the `install-hooks` deny-redirect gate remains the hard backstop for broad native exploration. Existing installs pick up the new body on the next `puppetmaster install-rules` (or `setup`) run; rules already on disk don't self-update.

## v0.9.36

**Fix: `setup` locks the platform set to what's actually on the machine (field report: "it still says I have cursorâ¦ I assumed it detects them, it simply supports them").** The platform-lock step defaulted to all-on, so a Claude-Code-only Windows user ran for a day with cursor enabled â cursor verbs recommended, cursor preferred by implement routing, "[on ] cursor" in every status display. Full suite **622** green (+4 focused tests); `ruff check puppetmaster/` clean; detection verified live on this machine (cursor+codex+openai found, npx-only claude correctly not; existing lock respected, not stomped).

- **Detection-based first-run default.** On a first run (no `platform.json` yet), the non-interactive path now locks to the platforms detected on the machine; the interactive prompt offers the detected set as the Enter default. Probes are presence checks, not auth audits: cursor = bundled SDK + `CURSOR_API_KEY`; claude-code/codex = CLI resolvable; openai = `OPENAI_API_KEY`.
- **Existing locks are never stomped.** A re-run of `setup` respects a prior platform choice (printed as `unchanged`); explicit `--platforms` still always wins, now with an honest note when it enables something undetected.
- **Status display says why.** Every platform listing in the setup step annotates undetected entries â `[on ] cursor   (not detected on this machine)` â so "why does it say I have cursor" answers itself.
- **Known limits.** Detection is resolvability-based: an npx-only Claude Code (no `claude` on PATH, no `CLAUDE_CODE_COMMAND`) is not detected â set `CLAUDE_CODE_COMMAND` or use `--platforms`. Codex login state is not probed (subscription auth is opaque); a logged-out codex CLI still counts as detected, and doctor's billing checks remain the auth truth. Existing all-on locks created by older setups are left as-is â re-run `puppetmaster setup` after `rm ~/.puppetmaster/platform.json`, or use `puppetmaster platform only ...`, to adopt detection.

## v0.9.35

**Fix: idle self-termination is now Cursor-only (field report: Claude Code "Connection status failed" after an idle gap), plus non-git directories fail fast with a real remediation (field report: `not_a_worktree` in experiment dirs).** Full suite **618** green (+10 focused tests); `ruff check puppetmaster/` clean; live smoke â server handshake under a simulated Claude Code env (42 tools), preflight exercised against real `git rev-parse` calls.

- **Host-gated idle reaping.** The staleness watcher (reap after 10 idle minutes, zero in-flight calls) exists to clean up Cursor's lease-cycle orphans â and only Cursor transparently respawns a reaped server. Claude Code and Codex mark the connection failed and stay dead until the user restarts their session: that was Claude Code's "Connection status failed" this morning and part of Codex's "Transport closed" pattern. Idle reaping now requires Cursor host markers (`CURSOR_*` env) and is vetoed by Claude/Codex markers or any unknown host â a leaked-but-alive server is far cheaper than a dead transport. `PUPPETMASTER_MCP_INPUT_STALE_FORCED=1` re-enables it anywhere; `..._DISABLED=1` still kills it everywhere.
- **Parent-death reaping (every host).** True orphans are still caught, host-agnostically: when the spawning host process dies and the server is reparented to init, it reaps immediately â no 10-minute wait, no host guessing.
- **Worktree preflight on full-edit verbs.** `start_implement` and the cursor/claude/codex implement verbs now check cwd is inside a git work tree *before* spawning a worker, returning a structured error in the same turn instead of a failed job to excavate. Remediation now leads with `git init` (restores diff capture completely); read-only Codex sandboxes are exempt, matching the worker-level guard.
- **`allow_non_worktree` is finally reachable.** The worker-level guard has always documented a `payload.allow_non_worktree` escape hatch that no CLI flag or MCP arg actually exposed â an agent following our own remediation hit a dead end. Now plumbed end-to-end: `--allow-non-worktree` on the cursor/claude/codex CLI commands and an `allow_non_worktree` arg on the implement-capable MCP verbs (schema-visible, so agents discover it).
- **Known limits.** Host detection is env-marker-based and biased toward not reaping: a Cursor host that strips its own `CURSOR_*` vars idle-leaks servers instead of reaping them (the parent-death check still bounds the leak's lifetime). Parent-death detection is POSIX-only â Windows has no reparenting signal, so Windows falls back to idle-only behavior, gated the same way. The Claude Code reap was reproduced by code-path analysis matching the field report's shape, not by a live multi-hour Claude session on this machine.

## v0.9.34

**Fix: hook commands survive Windows â forward-slash python paths instead of backslash escape-layer roulette (field report: Claude Code on Windows, "when claude tried to execute the hook there were no \ at all").** `install-hooks` embedded `sys.executable` verbatim in the hook command string; on Windows that path has to survive two unescape layers (JSON decode, then the POSIX-style shell â Git Bash â that hosts run hook commands through). It survived only the first, so Git Bash ate every backslash and the invocation gate never executed. Full suite **608** green (+3 focused tests); `ruff check puppetmaster/` clean.

- **`_shell_safe_executable`.** The shared gate-command builder (Claude + Cursor hooks) now writes the python path with forward slashes â valid Windows separators at every layer, nothing to escape â and double-quotes it when it contains spaces (`Program Files`-style paths), which is safe in both POSIX shells and cmd.
- **Affected users:** anyone on Windows who ran `puppetmaster setup` or `install-hooks`. Re-run `python -m puppetmaster install-hooks --force` after updating to rewrite the broken command; the merge logic identifies our entries by marker, so user-authored hooks are untouched.
- **Known limits.** macOS/Linux installs are byte-identical unless the python path contains a space (then it gains quotes â harmless, but the entry rewrites once). Verified by unit tests asserting the rendered command shape, not by a live Windows host; Pawel's machine is the live confirmation loop.

## v0.9.33

**Feature: `install-claude-mcp` â `puppetmaster setup` now registers the MCP server with Claude Code (field report: "ran setup, Claude enabled, but no MCP in Claude's list").** Setup wired Cursor and Codex hosts but never Claude Code â the "Claude enabled" line users saw was the platform lock (Claude as a *worker*), not host registration, so driving Puppetmaster *from* Claude Code silently got nothing. Full suite **605** green (+6 focused tests); `ruff check puppetmaster/` clean; verified live end to end â real `claude mcp add` (user scope), `claude mcp get` shows `â Connected`, re-run reports `unchanged`.

- **`puppetmaster install-claude-mcp`.** Mirrors the Codex installer: shells out to `claude mcp add --scope user` (Claude Code owns `~/.claude.json`'s schema â it also stores OAuth state there, so we never hand-edit it), runs the same pre-registration handshake (spawn server, `tools/list`, refuse to register a broken entry), and is idempotent via `claude mcp get`. `--force`, `--dry-run`, `--skip-handshake`, and `--claude <cmd>` (multi-word OK, e.g. `npx -y @anthropic-ai/claude-code`) match the sibling installers; the CLI resolves from `claude` on PATH, then `CLAUDE_CODE_COMMAND`.
- **Wired into `setup` (now 8 steps) and `uninstall`.** Setup step 6 registers with Claude Code when the `claude-code` platform is enabled and the CLI is resolvable â same gating shape as the Codex step. `puppetmaster uninstall` removes the user-scope entry alongside the Cursor and Codex ones.
- **User scope on purpose.** Cursor's installer defaults to workspace-local because `.cursor/mcp.json` travels with the repo; Claude Code registration goes to user scope so the server is available in every project, matching how people drive Puppetmaster from Claude.
- **Known limits.** Only the user scope is managed: an existing local/project-scope `puppetmaster` entry shadows ours in Claude's precedence order and is left untouched. The installer needs the Claude CLI resolvable at install time (`npm install -g @anthropic-ai/claude-code` or `CLAUDE_CODE_COMMAND`); there is no config-file fallback, by design. No timeout tuning is written â Claude Code's MCP timeouts are env-var driven (`MCP_TIMEOUT`/`MCP_TOOL_TIMEOUT`), and the server's own block cap keeps calls short.

## v0.9.32

**Fix: deny-redirect hooks no longer recommend Cursor-only verbs on non-Cursor hosts (field report: Claude Code on Windows, no Cursor installed).** The pre-tool hook denied the native Agent/Task tool and pointed at `puppetmaster_start_cursor_swarm` â a verb that host cannot invoke â so the turn was blocked with an unusable recommendation. Full suite **599** green (+3 focused tests); verified live through the real CLI hook path (`invocation-gate --host claude --event PreToolUse`).

- **Host-portable verb translation.** Hooks now map cursor-specific verbs to the platform-routing equivalents for any non-Cursor host: `start_cursor_swarm`/`_review`/`_plan` â `puppetmaster_start_swarm`, `start_cursor_implement` â `puppetmaster_start_implement`. Applied to both the pre-tool deny reason and the prompt-submit directive; Cursor hosts keep the cursor daily-drivers.
- **Known limits.** Mapping keys off the hook's `--host` flag, not off which platforms are actually installed â a Cursor host with a missing Cursor SDK still gets cursor verbs (the platform lock catches that at execution time, not at recommendation time). The generic verbs assume the MCP server is registered with the host; if only hooks are installed without the MCP, any recommendation remains uninvokable â `PUPPETMASTER_AUTO_INVOKE_DISABLED=1` stays the escape hatch.

## v0.9.31

**Feature: per-model effort levels in the registry, plus an interactive registry wizard (field request: "the same model on different effort can have a huge impact on cost and capability").** Effort variants are now plain registry rows â `codex/gpt-5-5-high` and `codex/gpt-5-5-medium` compete in routing as distinct models with their own capability scores and honest token-burn cost estimates. No source edits required: everything is data in `models.json`, managed via `puppetmaster models setup` (interactive wizard) or `puppetmaster models set` (one-shot). Full suite **596** green (+13 focused tests); `ruff check puppetmaster/` clean; verified live end to end â routed effort variants executed against the real OpenAI API (`reasoning_effort=high` in the request) and the real Codex CLI (`-c model_reasoning_effort=high` on the invocation).

- **`payload_defaults` on registry entries.** A free-form dict merged under the task payload whenever the router picks that entry (explicit task-payload keys always win â single `merge_routing_payload` seam used by all four routing stamp sites). Effort rides the existing adapter knobs: `{"reasoning_effort": "high"}` for openai, `{"extra_args": ["-c", "model_reasoning_effort=high"]}` for codex. ROUTING artifacts record what the entry injected, keeping the audit trail complete. Non-dict values are rejected at registry load, not at routing time.
- **`output_token_multiplier` for honest cost estimates.** Effort changes token *volume*, not per-token price (a high-effort run can burn 3â9x the output tokens of a medium run on the same task â see Artificial Analysis' index measurements). The multiplier scales estimated output tokens in `estimate_cost_usd`, so a high-effort variant can't masquerade as the same price as its medium sibling in routing decisions or budget caps.
- **`puppetmaster models setup` â the interactive wizard.** Numbered table of your registry (with an effort summary column), guided add-effort-variant flow (suggested derived id, prefilled capability score, suggested multiplier per level), field editing, add/remove entries, y/n previews before every mutation, atomic private-perms save. EOF/closed stdin exits cleanly instead of looping; duplicate ids are refused.
- **`puppetmaster models set <id> key=value ...` â the scriptable one-shot.** `effort=high` applies the same per-adapter translation as the wizard, *merging* into existing `payload_defaults` (unrelated keys survive) and swapping the `effort:*` tag. Also accepts `capability_score`, `enabled`, `notes`, `billing`, `output_token_multiplier`, and raw `payload_defaults.<key>=<json>`.
- **Honest platform gaps, enforced in tooling.** Only openai (`none|low|medium|high|xhigh`) and codex (`low|medium|high`) expose an effort knob today. The wizard and `models set` refuse to fabricate effort variants for cursor and claude-code entries with a clear message instead of writing flags those platforms would ignore.
- **Rode along: MCP protocol-stream isolation.** The MCP server now reserves the real stdout fd for JSON-RPC frames and repoints fd 1 at stderr, so a stray `print()` (ours, a library's, or an fd-inheriting child's) can't corrupt the protocol stream â Codex's rmcp client treats the first non-frame byte as a fatal, unrecoverable transport error. Best-effort with a stderr warning when fd isolation is unavailable.
- **Known limits.** Effort for claude-code (thinking budgets) and cursor is not expressible until those CLIs/SDKs grow a knob â entries can still carry arbitrary `payload_defaults`, but nothing downstream consumes an effort field for them. The payload merge is shallow by design: a task that explicitly sets `extra_args` fully replaces the entry's `extra_args` (documented precedence â explicit task values win). Bedrock-billed Claude Code inherits the same no-effort-knob limit; Bedrock posture detection itself is unchanged from v0.9.27 and was not re-verified live this release (no AWS credentials on the release machine). `output_token_multiplier` is a user-asserted estimate, not a measured value.

## v0.9.30

**Community fix wave: stalled jobs can't masquerade as success, diff outcomes are attributable, and MCP diagnostics stop assuming Cursor.** Three contributor PRs (#7, #8, #9 â a community contributor), merged with one post-merge tidy. Full suite **584** green (+4 focused tests); `ruff check puppetmaster/` clean.

- **Stalled `await` is an error (#8).** `stalled` was already a terminal state, but CLI `await` exited 0 and MCP `puppetmaster_await_job` returned `isError: false` for it â automation gating on await would sail past a degraded run. Both entry points now match `wait`'s existing behavior: non-zero exit / `isError: true`, with regression coverage for each.
- **Diff-source outcome reporting (#7).** The ambiguous `diff_present=True` is unpacked into three honest signals on full-edit verification and PATCH payloads, edit-run CLI outcome lines, and status outcome snapshots: `baseline_diff_present` (tree was dirty before the worker), `worker_diff_present` (the worker made an attributable change), `patch_artifact_emitted` (a PATCH artifact actually exists). The patch-emission gate and the new labels share one `snapshot_has_diff` predicate, so they can't drift. `diff_present` is kept as a legacy alias of `patch_artifact_emitted` for existing consumers.
- **Client-neutral MCP diagnostics (#9).** `mcp doctor` / `mcp cleanup` no longer tell every user to "restart MCP in Cursor Settings" â Puppetmaster runs from Codex and Claude Code hosts too. Same failure-mode explanations, host-neutral wording; the no-server remedy now names both installers.
- **Known limits.** `worker_diff_present` is `False` for unattributable changes in a pre-dirty tree without a worker-scoped diff â by design, matching the patch-emission gate. Outcome lines on stderr changed format (`diff_present=` â the three new keys); anything scraping that human-facing line needs updating, while the structured `outcome` snapshot keeps `diff_present`.

## v0.9.29

**Feature: `puppetmaster_dashboard` MCP verb â "show me the job dashboard" is now a first-class ask in any IDE with the MCP registered.** Until now the dashboard only launched via the CLI, and nothing told agents that; an agent fielding "open the dashboard" had to already know the fallback. Full suite **580** green (+4 focused tests); verified live â real server spawn, HTTP probe, idempotent reuse, deep link.

- **The verb.** `puppetmaster_dashboard [job_id] [port]` checks whether a dashboard already answers on the port (reused if so â no duplicate servers), otherwise spawns the CLI server detached (`start_new_session`, loopback-only as always) and waits for it to come up before returning. The response carries the URL (deep-linked via `?job=` when `job_id` is given), pid, state dir, and an explicit `started` / `already_running` so the agent can tell the user what happened. A server that dies on startup returns a structured error with the CLI command to reproduce.
- **Agents are taught the trigger.** The managed rules block (`install-rules`) now includes: when the user asks to see/open the job dashboard, call `puppetmaster_dashboard` and open the returned URL in a browser tab â CLI fallback `python -m puppetmaster dashboard [job_id]`. Docs updated (`CURSOR_AGENT_MCP.md`, `AGENTS.md`).
- **Rode along:** the two pre-existing ruff errors (F541 f-string, F401 unused import) are cleared; `ruff check puppetmaster/` is clean again.
- **Known limits.** Port reuse is honest but blind: an already-listening dashboard may be serving a *different* project's state dir â the response says so rather than pretending. `--allow-external` is deliberately not exposed over MCP; exposing the unauthenticated board beyond loopback stays a manual CLI decision.

## v0.9.28

**Hardening + dashboard release: concurrency/security audit fixes, privacy-by-default state, and a real ops-console dashboard.** Rolls up two audit-driven fix waves (jobs `job_50c71c87c5ff` â `job_21250fad1985` and `job_20c605263497` â `job_2caeeb332399`), three community PRs (#4 parent-process visibility, #5 no-edit classification, #6 hermetic test env), and the dashboard overhaul. Full suite **576** green on all platforms (Windows CI fixed in-release); dedicated end-to-end stress harness ALL GREEN (33 checks: subprocess swarms on both backends, 12-thread claim races, lease fencing, lock TTL, redaction, permissions, live dashboard HTTP + security probes).

- **Concurrency correctness (audit fixes).** SQLite task claims are now a single conditional `UPDATE ... WHERE` (two racing workers can't both win â verified under a 12-thread race); terminal status writes are lease-fenced by `worker_id` on both backends; the worker heartbeat detects a lost lease and aborts instead of publishing artifacts it no longer owns; gated tasks fail **closed** when the gate engine errors (ungated tasks still pass); orchestrator worker subprocesses are always reaped via `try/finally`; SQLite events are written in the same transaction as the rows they describe; file-store locks carry owner+timestamp JSON with TTL-based stale-lock breaking.
- **Privacy/security defaults (hardening pass).** All sensitive state is owner-only on POSIX (dirs `0700`, files `0600` â stores, sidecars, registries, locks; CodeGraph lock was world-readable `0644`); secret-bearing payload keys (`*_api_key`, `*_token`, `*_secret`) are scrubbed at the store serialization seam so credentials never persist while live in-memory payloads keep working; live-log sidecars and shell/OpenAI stdout/stderr tails pass through `redact_secrets` before disk, which now also catches GitHub/AWS/Slack/JWT token shapes and explicitly registered MCP argument keys; the `OPENAI_BASE_URL` host allowlist moved to shared `openai_security.py` and is enforced in `api_discovery` and `preflight` too (no path can send `OPENAI_API_KEY` to an unvalidated endpoint); the CodeGraph usage ledger stores a `sha256[:12]` hash of cwd instead of the raw path; gate commands never touch a shell (`shlex` argv on POSIX, raw CreateProcess string on Windows).
- **Dashboard ops console.** Per-task cards with role/status/model/attempts, readable markdown output (hand-rolled, escape-first renderer with XSS tests executed in Node), color-coded unified-diff viewer from PATCH artifacts, plain-English routing/verification "why" lines, summary bar with per-status chips + total cost, task filters, and flicker-free 1.5s live refresh. Jobs index redesigned as aligned rows with status filter chips and relative timestamps. Token chips (`â in / â out`) now appear for **every** adapter â estimated counts are marked `~` so they're never mistaken for measured; cost/turns/duration chips appear when the adapter records them (Claude Code envelopes).
- **Community PRs.** #4: `mcp list` shows parent PID + process name per server. #5: payloads explicitly marked `read_only`/`no_edit`/`dry_run` classify as analysis (and Codex `read-only` sandboxes keep the worktree guard even with the bypass flag). #6: pytest isolates `PUPPETMASTER_MODELS_PATH` and the platform lock, so personal config can't flip suite results.
- **Known limits.** POSIX file modes are best-effort no-ops on Windows. Payload scrubbing means a worker restarted from *persisted* state must re-read credentials from env (by design). Per-task cost/duration chips require adapter-recorded data â cursor NDJSON runs show token estimates only. Routing receipts predating this release are not rewritten.

## v0.9.27

**Feature: AWS Bedrock account support for Claude Code (field report: a Bedrock-authenticated VSCode-extension user read as "not authenticated" and lost every claude-code model).** `detect_claude_billing` only knew OAuth and `ANTHROPIC_API_KEY`, so a `CLAUDE_CODE_USE_BEDROCK` setup had no recognizable credentials: the probe returned unhealthy, `reconcile_registry` dropped all claude-code models, and `doctor` told a working Bedrock user to sign in. Full suite **537** green (+6 focused tests); verified live end to end â aws-vault STS credentials â probe reports `api`/healthy with region â `doctor` flips to ok â real Claude Code completion through Bedrock (`us.anthropic.claude-3-5-haiku`).

- **Bedrock posture detection.** Bedrock mode is recognized from a truthy `CLAUDE_CODE_USE_BEDROCK` in the process env *or* the `env` block of `~/.claude/settings.json` (how the CLI and VSCode extension persist it), parsed defensively like the OAuth probe. The branch is evaluated before `ANTHROPIC_API_KEY`, matching Claude Code's own routing precedence when both are set.
- **Honest billing classification.** Bedrock bills per-token to the AWS account, so the posture is `api` (out-of-pocket) â never misreported as subscription-covered. Detail includes the AWS region when `AWS_REGION`/`AWS_DEFAULT_REGION` is set.
- **Presence-based credential health.** `healthy=True` when AWS credentials look usable, checked in order with distinct evidence: `AWS_BEARER_TOKEN_BEDROCK` â `AWS_ACCESS_KEY_ID`+`AWS_SECRET_ACCESS_KEY` â `AWS_PROFILE` â non-empty `~/.aws/credentials`/`config`. Bedrock on with no credentials is honestly unhealthy, with an actionable detail (`aws configure` / `AWS_PROFILE` / `AWS_ACCESS_KEY_ID`).
- **Known limits.** Credential checks are presence-only by design (no boto3, no network, matching the pure-probe philosophy) â expired STS/SSO sessions still read healthy and fail at dispatch time instead of preflight. Bedrock enabled solely in the VSCode extension's internal settings (not env or `settings.json`) is invisible to subprocess probes. Vertex AI (`CLAUDE_CODE_USE_VERTEX`) is not yet detected.

## v0.9.26

**Feature: `puppetmaster uninstall` â the exact inverse of `setup` (field report: "How do I uninstall?" had no good answer).** Until now `pip uninstall puppetmaster-ai` removed the code but stranded every host-side integration: dead MCP entries in Cursor/Codex config, `beforeSubmitPrompt` hooks firing a missing module on every prompt submit, and orphaned rules instructing agents to use tools that no longer exist. The new command removes all of it, then tells you to run `pip uninstall puppetmaster-ai` as the final step. Full suite **531** green (+6 focused tests); dry-run verified live against a real install.

- **MCP registrations.** Removes only the `puppetmaster` entry from workspace and global `.cursor/mcp.json` (other servers untouched) and the `[mcp_servers.puppetmaster]` table from `~/.codex/config.toml` â prefers `codex mcp remove`, with a TOML-strip fallback so a missing CLI can't strand the table.
- **Rules.** Deletes `.cursor/rules/puppetmaster.mdc` (owned wholesale) and strips the managed marker block from `AGENTS.md`, `CLAUDE.md`, `~/.claude/CLAUDE.md`, and `~/.codex/instructions.md`, leaving surrounding content byte-identical; files left whitespace-only are deleted.
- **Hooks.** Removes Puppetmaster hooks from `.cursor/hooks.json` / `.claude/settings.json` at both project and global scopes; foreign hooks are preserved.
- **Processes & state.** Kills stale MCP daemons (same logic as `mcp cleanup --kill-stale`). State dirs (`~/.puppetmaster/`, `<cwd>/.puppetmaster/`, `.codegraph/`) are kept unless `--purge-state`.
- **UX.** `--dry-run` previews every action without writing; `--yes` skips confirmation; per-target status lines (`removed` / `unchanged` / `would_remove` / `error`); idempotent â a second run reports `unchanged`; exit 1 if any target errored.
- **Known limits.** Project-scoped artifacts are removed only for the `--cwd` workspace â other repos where `install-rules` / `install-hooks` ran at project scope must run `uninstall` from each of those directories (global-scope artifacts are removed once, for all). The pip package itself is step 2: `pip uninstall puppetmaster-ai`.

## v0.9.25

**Fix: every relative link in the README 404'd on PyPI (field report from Slack â `pypi.org/project/puppetmaster-ai/docs/WHY.md`).** GitHub rewrites relative markdown links into the repo; PyPI's renderer does no such rewriting, so all `docs/*.md` links, directory links, and the three `<img src="docs/...">` tags were broken on the project page. All 27 relative refs are now absolute (`github.com/...` for links, `raw.githubusercontent.com/...` for images). Docs-only; no code changes. (v0.9.24 shipped the absolute-link rewrite; v0.9.25 follows up because PyPI *also* strips heading `id`s â verified against `readme_renderer`, PyPI's own pipeline â so the in-page anchor links `#what-it-does` / `#quickstart` / `#auto-invocation` pointed at nothing and now target the GitHub README's anchors.)

- **Known limits.** Anchor links on the PyPI page now bounce readers to GitHub instead of jumping in-page â the only behavior PyPI supports. PyPI never re-renders a published version's description, hence the two version bumps for a README fix.

## v0.9.23

**Fix: a Claude-Code-plan user's swarms never routed to Claude Code (field report, reproduced with diagnostics).** Three coordinated defects, all fixed: the router costed every model at its *nominal* registry price (so a plan-billed Claude model with real $5/$25 prices lost every cost comparison to a $0 plan peer and got costed out of budgets on dollars the user would never spend); the main routing path (`_apply_auto_routing`) never reconciled detected billing/health onto the registry (OAuth-plan Claude stayed `unknown`-billed, credential-less adapters stayed candidates and could only fail preflight); and even when a plan model was picked, the ROUTING artifact reported the fictional nominal cost, poisoning the savings ledger. Full suite **525** green; cross-platform stress harness ALL GREEN.

- **Marginal-cost routing, honest end to end.** New `ModelSpec.marginal_cost_usd()` â $0 for in-subscription (plan-billed) models, the registry rate otherwise. `route_task` optimizes on marginal cost in the budget gate, every policy's selection keys, and every "pricier than" rejection. `RoutingDecision.estimated_cost_usd` is now the marginal truth, with new `nominal_cost_usd` / `baseline_nominal_cost_usd` fields preserving registry-rate transparency on the artifact; the savings baseline compares marginal-to-marginal.
- **Audited registry reconciliation before routing.** `reconcile_registry()` (platform_billing) upgrades `unknown` billing to each adapter's *detected* posture and drops adapters with no usable credentials â fail-open on detection errors, never returns empty. The orchestrator now runs it on the main routing path, emits a `router.registry_reconciled` event, and appends dropped candidates to each ROUTING artifact's `rejected` list, so "why not X" stays answerable. No silently vanished candidates.
- **TTL-cached billing detection.** `detect_adapter_billing_cached()` (default 300s, `PUPPETMASTER_BILLING_TTL_SECONDS`, `clear_billing_cache()` for tests) makes per-job reconciliation affordable â detection can shell out with a 15s subprocess timeout. The auto-fallback path uses the same cache.
- **Known limits.** Billing posture is cached for up to 5 minutes, so signing into an adapter mid-session may take one TTL window (or a new process) to be noticed. Reconciliation drops adapters only on a *definitive* unhealthy probe; an erroring probe keeps the adapter as a candidate (fail-open by design). Routing receipts created before this release recorded nominal costs for plan-billed picks and are not rewritten.

## v0.9.22

**Feature: Claude Fable 5 is the new frontier tier, with graceful degradation when an account doesn't have it.** Anthropic shipped Claude Fable 5 (API id `claude-fable-5`, 2026-06-09) above Opus 4.8 on effectively every published benchmark â but access is genuinely uneven: fully available on the Claude API and consumption-based Enterprise, included in subscriptions only through 2026-06-22, and absent from many seat-based enterprise plans. Puppetmaster now routes the hardest tasks to Fable 5 wherever it exists and degrades loudly-but-automatically where it doesn't. Full suite **514** green; new cross-platform mock stress harness (`bench/stress_fable5_rollout.py`) ALL GREEN (39 checks, zero spend).

- **Registry seeds.** `cursor/fable-5` (capability 100, plan-billed $0 â SOTA on CursorBench) and `claude-code/fable-5` (`claude-fable-5`, $10/$50 per MTok, 1M context, `billing="unknown"` with honest staged-access notes). Opus 4.8 stays at 99 as the declared fallback tier. Cursor catalog discovery learned a frontier-kin alias (`fable-5` â `claude-fable-5`) so a plan-discovered Fable ranks at its true capability instead of the conservative seed.
- **Classifier ceiling 99 â 100.** `classify_capability_needed` now demands the new flagship for the absolute-hardest tasks instead of saturating one notch below it. Trivial and mid-tier routing is unchanged â the 2Ã price tier is structurally reserved for work that needs it.
- **`model_unavailable` is now a recoverable failure.** Adapters classify provider-shaped "model not on this account" responses (Anthropic `not_found_error` / `permission_error`, Cursor forbidden/unknown-model rejections, OpenAI/Codex `model_not_found`) into `model_unavailable`, and the existing auto-fallback loop re-routes the task to a funded platform with `fallback_reason` recorded on the ROUTING artifact. An enterprise account without Fable degrades to Opus 4.8 instead of failing the task.
- **Known limits.** No pre-dispatch availability probe yet â the first dispatch to a Fable-less account spends one failed worker run before fallback re-routes (deliberate: build the probe when fallback artifacts show real churn, likely after the 2026-06-22 subscription cliff). Fable 5's own server-side safety classifiers route some cyber/bio-flavored prompts to Opus 4.8 (<5% of sessions, per Anthropic) â precisely the security-review/audit roles most likely to score capability 100 â and that handoff is invisible to Puppetmaster's audit trail. Savings receipts now baseline against the pricier flagship, so routing "savings" rise mechanically; read them accordingly.

## v0.9.21

**Fix: Codex couldn't keep the Puppetmaster MCP server alive (field report).** Codex's MCP client enforces a hard per-tool `tool_timeout_sec` â default **60 seconds** â that cancels a call no matter what, while Cursor tolerates arbitrarily long calls as long as bytes flow. Puppetmaster's long-poll tools (`live_artifacts_follow`, `await_job`) passed the caller's `timeout_seconds` straight into a blocking wait, so a Codex agent that asked for a multi-minute block got guillotined mid-wait, saw a dead tool, and concluded Puppetmaster was broken. Three compounding gaps, three layers of fix â verified live against the real Codex client (`codex exec`, gpt-5.5): a 120s requested block now returns capped in 45s with a clean completion. No breaking API changes; full suite **510** green (+8 focused tests).

- **Server-side block cap (the keystone).** `live_artifacts_follow` and `await_job` now clamp their effective block to `PUPPETMASTER_MCP_MAX_BLOCK_SECONDS` (default **45s**, comfortably under Codex's 60s ceiling; `0` disables). Both tools are designed to be re-called (`next_cursor` / `timed_out`), so the cap turns one long block into a sequence of short polls â the push-feeling stream still works, on every client. When the cap shortens a request the response is stamped `capped: true` with `requested_timeout_seconds` / `effective_timeout_seconds`, so the agent reads "nothing yet, call me again" instead of inferring an empty job.
- **Spec-compliant progress notifications.** The tool-call keepalive now reads `params._meta.progressToken` from each `tools/call` and emits strictly-monotonic `notifications/progress` frames alongside the existing `notifications/message` liveness logs â the actual MCP mechanism compliant clients use to keep a long call alive. Only clients that send a token receive progress; Cursor's behavior is unchanged. `initialize` now also advertises the `logging` capability, making the keepalive frames spec-legal for strict clients.
- **Install-time Codex timeouts.** `install-codex-mcp` now writes `startup_timeout_sec = 30` / `tool_timeout_sec = 300` into the `[mcp_servers.puppetmaster]` table (`codex mcp add` exposes no flag for them). Absence-only: an existing user value is never clobbered, and a missing table is a warning, not an error. Cold Python imports no longer race the 10s startup default, and a genuinely-long single call survives up to 300s.
- **Known limits.** The synchronous verbs (`puppetmaster_swarm` et al.) still block until the job finishes by design â on Codex they survive only up to the configured 300s ceiling. For long jobs on Codex, use the async pattern: a `start_*` verb, then bounded `await_job` / `live_artifacts_follow` re-polls. The post-install guidance now says exactly that.

## v0.9.20

**Fix: Codex (and Claude Code) adapter runs looked stalled because they ran behind a flat, silent blocking wait (field report).** A long `codex exec` produced **zero visible output and no liveness signal** until it either finished or hit the 600s timeout â so an actively-working run was indistinguishable from a hung one, and at least one user stopped relying on the Codex adapter for exactly that reason. The Cursor implement adapter already solved this in v0.9.12 (stream output to a live sidecar log + a `still working` heartbeat); Codex and Claude Code never got that treatment and kept using `subprocess.run(..., capture_output=True)`. This routes both through the same proven `run_streamed_subprocess` path. No breaking API changes; full suite **502** green + the E2E stress harness (`bench/stress_invocation.py`) still ALL GREEN.

- **Codex and Claude Code now stream.** Both adapters run via `run_streamed_subprocess`, teeing stdout/stderr line-by-line to `<state>/jobs/<job>/tasks/<task>/{codex_exec,claude_implement}_live.log` and writing a `[puppetmaster] still working: Ns elapsed â¦` heartbeat every 30s, so a multi-minute run is visibly alive. The verification (and timeout) artifacts now carry a `live_log` path for parity with Cursor, so `puppetmaster show <job>` points the operator straight at the streaming log.
- **stdin is closed for every streamed run (the actual stall fix).** The shared runner now launches the child with `stdin=subprocess.DEVNULL`. A CLI that reads stdin on a non-interactive worker would otherwise block forever waiting on terminal input â a silent, indefinite stall that no timeout shorter than the wall-clock cap would surface cleanly. The legacy blocking path passed `input=""` to get this EOF behavior; the streamed path now guarantees it for Codex, Claude Code, and Cursor alike. (Safe no-op for the Cursor node runner, which takes its input via env, not stdin.)
- **Timeout semantics unchanged.** A timed-out run still reports `failed` + `timeout`, preserves any partial diff as a `PATCH`, and now also surfaces the `live_log` so you can see exactly how far it got. The default 600s ceiling is intentionally untouched â the fix is visibility + a closed stdin, not a shorter leash that would fail legitimately-long runs.
- Added 4 focused tests: Codex/Claude route through the streamed runner and never touch the blocking `subprocess.run`, both surface `live_log` (incl. on timeout), and a behavioral test proving a stdin-reading child sees EOF immediately instead of hanging.

## v0.9.19

**Fix: auto-invocation hooks were over-aggressive on read-only inspection (field report).** v0.9.16/0.9.17's pre-tool deny-redirect hard-denied *every* native `Grep`/`Glob`, and any shell command where a search tool was followed by a `/` anywhere â so benign read-only work (a plain `git log`, listing a config dir, grepping a single file, even reading this changelog) got blocked, contradicting the "never wedge the session" contract the hooks promise. The classifier is now scoped to redirect only genuinely broad operations, with an explicit read-only carve-out. No breaking API changes; full suite **498** green + the E2E stress harness (`bench/stress_invocation.py`) extended with carve-out checks.

- **Native search tools (`Grep` / `Glob` / `codebase_search`) are no longer hard-denied.** Their per-call scope isn't visible in the host hook payload, so there's no reliable way to tell "grep one file" / "list one config dir" from a repo-wide sweep â and blocking them all obstructs legitimate inspection. Steering toward Puppetmaster for broad search now rides entirely on the **non-blocking** prompt-submit directive. The built-in `Task` fan-out (unambiguously "should be a swarm") is still redirected.
- **Read-only shell inspection is explicitly carved out** â `git log` / `show` / `diff` / `status` / `blame`, `ls`, `cat`, `head` / `tail`, `tree`, etc. always pass through.
- **Broad-shell detection tightened.** Only a search tool with an explicit recursive signal (`-r` / `-R` / `--recursive`, `**`, or `find -name/-path`) is redirected; `rg pattern src/app.ts` and `grep TODO file.py` are not. The old rule matched any `/` in the command, so a single-file path tripped it.
- Also made a non-hermetic doctor test hermetic â it read the real `~/.codex` / `~/.claude`, which started failing once v0.9.17's `--global-rules` made global rule installs a normal thing. The check itself was correct; the test now pins a clean home.

## v0.9.18

**Semantic review gate â a strictly-stronger model must approve an implement diff before it can COMPLETE.** The hole this closes: when Puppetmaster fans out implement work across many auto-routed (often cheap) workers, the only objective acceptance check was `require_diff` â "files changed", not "the code is good". A worker that exited 0 got a hardcoded `confidence=0.9` regardless of quality (`adapters.py`: `confidence=0.9 if returncode == 0`), and the confidence-based auto-escalation keyed off exactly that self-report, so a confidently-mediocre cheap model sailed through. This adds the missing semantic acceptance control as a non-bypassable, fail-closed completion gate, riding the existing gate chokepoint with no new lifecycle machinery. Off by default (one extra model call is a deliberate, flagged spend); flip `PUPPETMASTER_REVIEW_GATE=1` to enforce. No breaking API changes; full suite **495** green (+23 focused tests).

- **New `review` gate kind (`puppetmaster/gates.py`).** After the cheap gates pass, a judge model reads the actual diff and must approve it before the task reaches COMPLETE. `resolve_judge_model` picks the cheapest model **strictly stronger** than the implementer (the router-stamped `router_model_id`), falling back to the top-tier peer when the implementer is already at the ceiling â so the writer is never its own judge. `build_review_prompt` / `parse_review_verdict` are pure and unit-tested; the live call is isolated behind an injectable `_REVIEW_JUDGE` seam so the suite never hits a model. **Fail-closed:** a judge that runs but returns no parseable verdict, times out, or crashes rejects the diff. The one deliberate fail-*open* is "no judge configured / feature off" â no-op, so an unconfigured environment is never bricked.
- **Auto-attached for implement behind one flag.** `task_gate_specs` adds the review gate to `mode=implement` tasks when `review=true` (per task) or `PUPPETMASTER_REVIEW_GATE` (globally), ordered **last** so a no-op or test-failing task never pays for a judge call. Opt a single task out with `review=false` / `allow_unreviewed`. Cost controls: bounded diff (`max_diff_chars`), deterministic per-task `sample` fraction for spot-check mode on large swarms.
- **Objective escalation (`puppetmaster/orchestrator.py`).** A review rejection now re-routes the task one capability tier up (`_auto_review_escalate` / `_reroute_failed_review`), keyed off the **review GATE result** rather than self-reported confidence â and therefore **on by default**, unlike the confidence path. Bounded by `_MAX_ESCALATION_ATTEMPTS`; a routing artifact records each upgrade.
- **Fail-closed reachability fix.** A review-rejected task is FAILED, which would have made `_run_workers` raise (fail-closed) *before* the escalation sweep ran â leaving the better model no turn. `_review_pending_reroute_ids` treats a rejected task with remaining budget as "pending re-route" (like a recoverable failure) so escalation gets its turn; once the budget is exhausted the job still fails loudly. (Caught by the design swarm's redteam pass and covered by a regression test.)
- **Known limits.** The guarantee is only as strong as the judge you point at it â it's a quality bar, not a proof of correctness; pair it with a `command`/test gate for objective checks. The judge reads a bounded slice of large diffs. Capability scores remain hand-authored (no feedback loop into routing â deliberately not built). With the cursor platform lock + plan-billed models, judge calls bill against the plan ($0 marginal); on api-billed models a rejection's re-implementation is real out-of-pocket spend, capped at the escalation bound.

## v0.9.17

**Global (user-level) auto-invocation hooks â cover every repo without re-running setup per project.** v0.9.16 installed hooks per-workspace (`.cursor/hooks.json` / `.claude/settings.json`), which only protects the repo you ran `setup` in. This adds a `global` scope that writes user-level hooks to `~/.cursor/hooks.json` and `~/.claude/settings.json`, so a single install makes the host agent delegate-aware in *every* repo the user opens. No breaking API changes; full suite **472** green + the E2E stress harness (`bench/stress_invocation.py`) extended with global-scope checks.

- **`puppetmaster install-hooks --global`** and **`puppetmaster setup --global-hooks`** install at user scope instead of the workspace. Only the base directory differs between scopes â the `.cursor` / `.claude` subpaths are identical â and the hook command is an absolute `python -m puppetmaster invocation-gate` (no relative script path), so a user-level Cursor hook (which Cursor runs from `~/.cursor/`) resolves the same as a project one. Idempotent and non-destructive at both scopes; an unknown scope is a loud error rather than a silent no-op.
- **`setup` reports the scope it used** and nudges toward `--global-hooks` when it installed per-repo, so the broader option is discoverable.
- Added 5 focused tests (global writes under `~` not cwd, idempotent + labeled, unknown-scope error, `install-hooks --global`, `setup --global-hooks`) plus a global round-trip in the stress harness.

## v0.9.16

**Auto-invocation: make host agents reach for Puppetmaster on their own, with as little user prompting as possible.** The #1 field complaint was that Cursor / Claude Code / Codex grind multi-file work inline unless the user manually reminds them to delegate â the soft rule text Puppetmaster writes decays with context distance and task momentum. This release adds a layered, classifier-gated enforcement system on top of the rules: a pure delegation gate, deterministic host hooks, and an optional provider proxy â wired into `setup` so a fresh install gets it all. Honest about the ceiling: universal *deterministic* invocation is impossible on closed harnesses (you can't sit on Cursor's provider wire or force an MCP call), so the design is explicitly tiered â soft everywhere, hard where hooks/proxy exist â and fully kill-switchable. No breaking API changes; full suite **467** green + a 6-stage end-to-end stress harness (`bench/stress_invocation.py`).

- **The delegation gate (new `puppetmaster/invocation_gate.py`).** `should_delegate(prompt) -> DelegationDecision` reuses the router's existing pure-function classifier (`classify_capability_needed`) â one capability heuristic in the codebase, no second model, no LLM call, microsecond-scale. It layers the delegate-vs-inline *policy* on top: a trivial-task carve-out first (typos / renames / format / one-liners / quick factual questions stay inline), a conservative default threshold (60), explicit `use puppetmaster` / `do it inline` overrides, a broad-scope override that catches *implied* multi-file work (audit / refactor / migrate / trace / data-flow), and `infer_role_and_verb` mapping each prompt to the right Puppetmaster verb. Dry-run it with `puppetmaster should-delegate "<prompt>"`.
- **Deterministic host hooks (new `puppetmaster/hook_runner.py` + `puppetmaster/hook_installers.py`).** `puppetmaster install-hooks` writes idempotent, non-destructive entries into Cursor's `.cursor/hooks.json` and Claude Code's `.claude/settings.json`. On prompt submit they **inject** a "delegate now with `<verb>`" directive; on a broad native tool call (Grep / Glob / built-in Task / repo-wide shell search) they **deny-redirect** to the Puppetmaster / CodeGraph equivalent â always exempting `puppetmaster_*` and focused reads. Each hook shells into `python -m puppetmaster invocation-gate`, stays import-light, and **fails open** so a hook can never wedge the host session.
- **Optional provider proxy (new `puppetmaster/provider_proxy.py`).** A zero-dependency OpenAI-compatible local proxy (`puppetmaster proxy`) for the API-key / SDK clients that closed harnesses can't hook â runs the gate on inbound prompts and either *advises* (synthetic local delegate reply, no upstream) or *injects* a system directive and forwards to a vetted upstream. An HTTPS + loopback allowlist refuses to forward keys to an arbitrary host. Headroom's `wrap` / proxy referenced as transport prior art only â not adopted (its compression / memory / code-graph layers overlap what Puppetmaster already owns).
- **Sharper soft layer + one-shot setup.** `install-rules` now tells the agent to prefer Puppetmaster *before* native Grep/Glob/Task and to run the gate when unsure; the `puppetmaster_*` MCP tool descriptions self-advertise against the built-in Task tool. `puppetmaster setup` gains a 7th step that installs the hooks, so `pipx install -U puppetmaster-ai && puppetmaster setup` provisions the whole stack. Global kill switch: `PUPPETMASTER_AUTO_INVOKE_DISABLED=1` (alias `PUPPETMASTER_INVOCATION_GATE_DISABLED=1`); tune the bar with `PUPPETMASTER_AUTO_INVOKE_THRESHOLD`.
- Added 28 focused tests (gate policy, hook payload translation, proxy logic + upstream allowlist, idempotent + non-destructive installer, `setup` step gating); full suite **467** green.

## v0.9.15

**Windows hotfix for v0.9.14's per-worktree port isolation.** The B1 `reserve_port` probe set `SO_REUSEADDR`, which has inverted semantics on Windows â it lets `bind()` succeed *over* an actively-listening socket, so the liveness check reported a busy port as free and `reserve_port` handed back occupied ports. On Windows, collision avoidance was effectively a no-op. The probe now only sets `SO_REUSEADDR` on POSIX (where it correctly distinguishes a live listener from a reclaimable `TIME_WAIT` socket); Windows uses the default exclusive bind, which already fails on an occupied port. Verified green on the `windows-latest` CI matrix. POSIX behavior is unchanged. No API changes.

## v0.9.14

**Data-loss guards, parallel-worktree isolation, and progress-aware lifecycle â the safety pass on top of v0.9.13's loud-failure work.** A real multi-worktree migration session surfaced the next tier of sharp edges: state GC that could in principle reach the active worktree, fixed ports that collided when many worktrees ran at once, "implement" tasks that could complete without a diff, generated artifacts polluting worker commits, and long-but-healthy workers killed by a flat timeout. This release hardens the destructive paths, isolates per-worktree runtime, makes completion honest about whether work actually landed, and adds a generic seam for "validate only affected specs." No breaking API changes; full suite **439** green + a 10-scenario end-to-end stress harness (`bench/stress_new_features.py`).

- **State deletion can't escape its job tree (D1) â `puppetmaster/store.py` + `sqlite_store.py`.** `delete_job` now routes through a `_assert_safe_job_dir` containment guard that refuses any path resolving outside the store's jobs root (traversal/symlink/`..`), so a malformed job id can never delete unrelated state. The CLI `gc --force --all-projects` sweep additionally **never hard-deletes the active worktree's own state** â the current project is reported dry-run-only even under a machine-wide force sweep, while other projects reap normally.
- **Per-worktree port isolation (B1) â new `puppetmaster/ports.py`.** Workers no longer share a fixed port. A deterministic hash maps each worktree to a port block (range capped below 32768 to stay clear of the Linux ephemeral range), and a `reserve_port` bind-test-and-increment finds an *actually free* port from that hint â collapsing observed collisions at 16 parallel worktrees from a measured rate to zero by falling back to an OS-assigned port when a block is busy. Injected into the Cursor and Claude adapter subprocess env via `apply_worktree_ports`.
- **Implement tasks must show a diff (A3) â `puppetmaster/gates.py`.** `mode=implement` tasks now default to `require_diff`, so a worker can't report `COMPLETE` after a silent no-op; opt out explicitly only when a no-diff outcome is legitimate.
- **Declarative write-scope + cross-worker conflict prediction (B3/C1) â new `puppetmaster/conflicts.py` + a `write_scope` gate.** A task can declare the path globs it's allowed to touch; the gate fails a worker that writes outside its declared scope. Before launch, the orchestrator predicts overlapping write scopes across parallel tasks and emits a conflict-warning artifact so colliding edits surface up front instead of as a merge surprise.
- **Generated artifacts stay out of commits (C2).** The `committed` gate's auto-commit accepts an `exclude` list: matching paths are unstaged and appended to `.gitignore` instead of being committed, so build output / caches / logs don't pollute a worker's diff.
- **Progress-aware worker timeouts (A4) â `puppetmaster/orchestrator.py`.** The flat `process.wait` is replaced by an adaptive wait that extends a worker's deadline while it's demonstrably progressing (rising event count) and still hard-kills a genuinely wedged worker at a cap â long, healthy runs no longer die on the clock, and stuck ones still get reaped with a clear timeout artifact.
- **Honest terminal-quality signal (A2/F2).** `status` and run completion now surface an `outcome` block â run quality plus whether a diff and a commit are actually present â so a "completed" job that produced nothing is visible at a glance instead of masquerading as success.
- **"Validate only affected specs" seam (B2) â new `puppetmaster/affected.py` + `puppetmaster affected` CLI.** Puppetmaster supplies the changed set (explicit, stdin, or `git diff` range); you supply the changed-fileâspec mapping via declarative rules (with `{stem}`/`{dir}` templating and glob resolution) or a command. Resolves the affected spec/test paths generically without Puppetmaster guessing anyone's test layout.
- **Carries the prior in-flight batch:** `partial_summary` phrasing, collapsed/quieter `logs` output, honest MCP `launcher_pid` reporting, worker memory-distillation in the adapters, and a `--no-edit` run flag â all previously uncommitted, now shipped together.

## v0.9.13

**Loud failures, non-bypassable completion gates, and honest token metering â driven by a long real migration session (35 jobs / 37 workers / 18 worktrees).** The throughline of the field report: Puppetmaster's orchestration was solid but its failure modes were too quiet â silent no-ops on a dirty tree, workers that "succeeded" without committing, degraded SDK results indistinguishable from empty ones, and no token truth for the dominant plan-billed runtime. This release makes failures loud, bakes in an anti-drift gate, meters consumption across every adapter, and adds lifecycle tooling for efforts that sprawl across many worktrees. No breaking API changes.

- **Zero-work runs fail loudly (#1, #10).** A worker that refused to run (`result="blocked"`, e.g. a dirty worktree) or produced only verification/degraded artifacts no longer reports `COMPLETE`. A new `_blocked_verdict` veto forces such tasks to `FAILED`, a built-in `assess_run_quality` classifies every run `ok`/`degraded`/`empty`/`blocked`, and the CLI prints the verdict and exits non-zero so a "completed" job that did nothing can't masquerade as success.
- **Non-bypassable completion gates (#2, #11) â new `puppetmaster/gates.py` + `ArtifactType.GATE`.** An agent can't mark a task `COMPLETE` unless its post-conditions hold: `require_diff` (an edit task must produce a diff), `command` (a semantic oracle must exit 0), `ratchet` (a monotonic metric the runtime owns and only ever tightens â gaming the scalar gets caught by the oracle), and `committed` (clean tree, with optional runtime-performed commit + author). Each gate emits a `GATE` artifact so audits explain exactly why a task passed or failed. **New `puppetmaster gate` CLI + `puppetmaster_gate` MCP verb** replay the same gates against any working tree on demand, for a parent agent or CI.
- **Honest token metering, now universal (#5, #6).** Per-run `tokens_in`/`tokens_out` are captured even when marginal cost is `$0` (plan-billed): measured from the SDK usage object where available, else a clearly-flagged `tokens_estimated` char/4 approximation. Wired through **every adapter** â Cursor and Claude Code via the runner's JSON `usage`, Codex via `turn.completed.usage`, OpenAI via the API `usage` block â so the dominant runtime is no longer invisible. Pre-flight router numbers are now explicitly labeled routing-only estimates, never presented as consumption.
- **Salvage degraded stdout + loud liveness (#3, #9).** When SDK capture comes back empty but the structured result is sitting in raw stdout, the analyze path re-parses stdout before declaring a degraded run. `status` now prints a glanceable liveness verdict (pid alive? heartbeat age?) so a wedged job screams instead of quietly showing `running`.
- **Workers can self-serve the CodeGraph CLI (#4).** Every worker subprocess is handed a `PYTHONPATH` that puts this install first (`inject_worker_cli_env`), so a worker's `python -m puppetmaster codegraph â¦` resolves the same subcommands the parent has instead of a stale pip build.
- **Effort rollup + state GC (#7, #8) â new `puppetmaster/lifecycle.py`.** Jobs spread across many worktrees can be tagged with one `effort_id` (`PUPPETMASTER_EFFORT_ID` / `run --effort`) and aggregated with **`puppetmaster rollup`** (jobs/artifacts/cost/tokens across every project state dir; **`puppetmaster_rollup`** MCP verb). **`puppetmaster gc`** reaps durable state for old terminal jobs so per-worktree state dirs stop piling up â dry-run by default, `--force` to delete, `--all-projects` to sweep the machine (**`puppetmaster_gc`** MCP verb).
- **Windows store hardening.** `read_json`/`write_json` retry briefly on a Windows sharing-violation (`PermissionError`) to fix a flaky CI file-lock race under concurrent worker I/O.
- Added ~30 focused tests (gates, quality verdicts, token aggregation, salvage/liveness, lifecycle GC/rollup, universal Claude metering, gate-replay CLI, gc/rollup/gate MCP verbs); full suite **416** green.

## v0.9.12

**Field-report fix pass: a dead orchestrator can no longer masquerade as a running job, and the run/CLI surface stops hiding read-only swarms and silent worker output.** Driven by a real JAD UI migration session where a backgrounded orchestrator got reaped and left its job wedged in `running` forever, `show` crashed with a raw `Errno 2`, and an analysis swarm's `implement` role wrote nothing while looking like it should. Eleven grounded fixes; no breaking API changes.

- **Stalled-job liveness + reaper (new `puppetmaster/liveness.py`).** The orchestrator now stamps a heartbeat sidecar (`orchestrator.json`: pid/host/time) and refreshes it across worker batches. A new reaper transitions a dead-orchestrator job `running -> stalled` (new `JobStatus.STALLED`) when its pid is provably gone â or when no task holds a live lease and nothing has progressed past `PUPPETMASTER_STALL_AFTER_SECONDS` (default 180) â and **auto-requeues lease-expired tasks** so no manual `recover` is needed. Wired into `status`/`jobs`, plus a new `reap` command. A dead job is never reported as live again.
- **`finalize` / `wait` / graceful `show`.** `show` now degrades to a live preview with a friendly "job not finalized â run `finalize`" note instead of a raw `[Errno 2] â¦ stitched.md`. New `puppetmaster finalize <job>` force-stitches and completes a job whose orchestrator died before stitching. New `puppetmaster wait <job> --timeout` blocks until `complete`/`failed`/`stalled` and exits non-zero on the bad states; `await` now treats `stalled` as terminal.
- **Honest swarm mode banner.** A read-only analysis run no longer looks like it edits code: a mode classifier + one-line banner (`mode=analysis (read-only)` vs `mode=edit`) prints on `run`/`cursor`/`claude`/`codex`/`openai` and emits a `job.mode` event; the analysis-swarm `implement` role is documented as plan-only.
- **`cursor --implement` streams progress.** Implement runs tee stdout/stderr to a live sidecar log line-by-line with a `still working` heartbeat every 30s â no more 0-byte-log-looks-hung. Worker startup failures now leave a trace (`startup_error-*.log` + `worker.startup_failed` event) instead of vanishing.
- **Router on direct adapters.** `cursor`/`claude`/`codex`/`openai` gain `--auto-route` (+ `--routing-policy` / `--max-cost-usd` / `--min-capability`), pinned to the invoked adapter, so a direct run emits a ROUTING artifact + cost stamp like a swarm does.
- **Quality-of-life.** `codegraph` passthrough accepts `--cwd`/`--timeout` after the subcommand (e.g. `codegraph init --cwd â¦`) and disables child pagers when stdout isn't a TTY. New `mcp doctor` distinguishes a dropped stdio pipe (daemon healthy â restart MCP in Cursor) from a genuinely dead server.
- Added 11 focused friction-fix tests; full suite **388** green.

## v0.9.11

**User-feedback pass: fresh-perspective audits, delegate-first agent rules, and first-class Codex verbs.** Driven by field reports that PM audits were inheriting their own prior conclusions (circular self-evaluation) and that IDE chat agents (e.g. JetBrains AI Chat + Codex) kept solving multi-step tasks inline instead of delegating. This release makes evaluative and swarm runs judge fresh by default, hardens the rules `install-rules` writes from a soft nudge into a delegate-first directive, and promotes the existing Codex CLI adapter to first-class MCP verbs. No breaking API changes.

- **Audits/reviews now run with a fresh perspective by default** â evaluative roles (`audit`, `redteam`, `review`, `conflict-auditor`, `test-coverage-reviewer`, â¦) no longer inherit prior promoted conclusions, which previously let an audit anchor on the very claims it was meant to test. Added a `disable_memory` payload override (`True` â always fresh, `False` â always inject, absent â role-based). Implement runs still receive memory for continuity.
- **Swarm runs default to a fresh perspective** â `puppetmaster_start_swarm` / `puppetmaster_start_cursor_swarm` (the read-only analysis entry points) stamp `disable_memory: true` on every worker unless the caller passes `disable_memory: false`; the CLI `run` verb gains `--disable-memory` / `--enable-memory`.
- **Hardened delegate-first agent rules** â `puppetmaster install-rules` now writes a trigger convention (`Use Puppetmaster to â¦` / `PM this â¦`) and a delegate-first gate for multi-file/audit/refactor work, with a tight inline carve-out for trivial edits and a native-tooling fallback. One change updates the Cursor `.mdc`, `AGENTS.md`, Codex instructions, and `CLAUDE.md` alike.
- **First-class Codex MCP verbs** â `puppetmaster_codex` and `puppetmaster_start_codex` mirror the Claude/OpenAI worker verbs, driving the existing `codex exec --json` CLI adapter directly instead of only via `start_implement --adapter codex`.
- Full suite **377** green; `ruff check puppetmaster/` clean.

## v0.9.10

**Workers can now self-serve CodeGraph mid-task; plus the deferred lint cleanup.** Puppetmaster runs each worker in a hermetic SDK sandbox, so CodeGraph reaches workers as an injected one-time context snapshot rather than as live tools â which meant reverse-dependency / blast-radius checks fell back to ripgrep/git once the snapshot went stale. This release tells workers they can refresh that view themselves. No sandbox or behavior change to the orchestrator.

- **Workers told they can refresh their own CodeGraph view.** Every adapter (cursor/claude/codex) injects CodeGraph through one helper, `codegraph_prompt_section`. It now instructs workers that the snapshot is a one-time view and that they can refresh or expand it on demand â to trace callers, reverse dependencies, or a change's blast radius â via the ABI-safe `python -m puppetmaster codegraph search/context/affected` CLI they can already shell to, with a graceful ripgrep/git fallback when the CLI isn't importable in the worker's environment. This is prompt-level guidance only: no change to the `settingSources: []` sandbox and no per-worker MCP boot.
- **Lint cleanup (carried over from the v0.9.9 cycle).** Cleared the pre-existing ruff errors that predated the audit work: hoisted `dashboard.py`'s models/store/stitcher imports to the top of the module (E402) and moved `worker_runtime.py`'s `SwarmStore` import under `TYPE_CHECKING` (F821, zero runtime cost with `from __future__ import annotations`). `ruff check puppetmaster/` is now clean.
- Added `test_codegraph_prompt_section_advertises_self_serve_cli`; full suite **368** green.

## v0.9.9

**Fifth-pass audit: fixes two store regressions the v0.9.8 optimization pass introduced, plus parity hardening.** A follow-up CodeGraph + review swarm against this repo caught that two of the v0.9.8 store changes diverged the SQLite backend from the file backend. This patch restores exact cross-backend parity (with one verified repro fixed) and tightens the new batch APIs. No public API changes.

- **REGRESSION fix â SQLite `retrieve_memory` multi-term queries silently dropped results.** v0.9.8 pushed each query term into SQL as a separate **ANDed** `instr(lower(data), ?)` clause, so a record had to contain *every* term to be fetched â while the file backend keeps any record matching *any* term (score > 0). A two-term query like `"workers nonexistent"` returned matches on the file store but **0 rows** on SQLite. The term clauses are now combined into a single **OR** group, AND'd with the exact-match filters, so SQL is a pure superset prefilter and the existing Python scoring stays authoritative â results now match the file backend exactly.
- **REGRESSION fix â `save_artifacts` event atomicity.** v0.9.8's batch `save_artifacts` wrote all artifact rows in one transaction, then emitted `artifact.saved` events afterward in separate transactions; a crash in between could leave artifacts durable with missing events (unlike `save_tasks`, which writes rows + events atomically). `save_artifacts` now inserts the event rows via `executemany` in the **same** transaction as the artifact upserts.
- **Parity â batch list methods.** The `SwarmStore` base defaults for `list_tasks_for_jobs` / `list_artifacts_for_jobs` now de-duplicate `job_ids` (first-seen order) to match the SQLite overrides; the contract (dedup, no per-job ordering guarantee) is documented on the base methods.
- **Scale â chunked `IN` lists.** `get_artifacts_by_ids` and `list_artifacts_by_type(job_ids=...)` now use the `_chunked` (900) helper so very large id/job windows can't exceed SQLite's bound-variable limit.
- Added four focused parity/edge tests (multi-term memory parity across backends, batch `artifact.saved` event emission, duplicate-`job_id` dedup, chunked `IN` queries); full suite **367** green.

## v0.9.8

**CodeGraph-driven performance audit pass: N+1 query elimination, hot-path optimization, and dead-code cleanup.** A fourth-pass audit (run through Puppetmaster's own CodeGraph + review swarm against this repo) surfaced a batch of read-amplification and recompute issues; this release fixes the high-confidence ones with batched store APIs and per-tick snapshots, plus the usual lint/dead-code sweep. No behavior changes â store contract preserved across both backends.

- **N+1 query elimination across the orchestrator, CLI, audit, and stitcher.** `_reroute_low_confidence` was calling `_latest_verification_confidence` once per completed task, and that helper re-ran `list_artifacts(job.id)` on *every* call â O(tasks Ã all-artifacts) per reroute pass. It now loads artifacts once and builds a `task_id â latest-verification-confidence` map. `cli.artifact_job_id` no longer scans every job calling `list_artifacts` per job; a new `get_artifact_job_id()` store method does a single indexed `SELECT job_id FROM artifacts WHERE id = ?` under SQLite (file backend keeps a direct existence check). `audit.collect_records` now batches via new `list_tasks_for_jobs()` / `list_artifacts_for_jobs()` (SQLite uses chunked `WHERE job_id IN (...)`) instead of two reads per job. Task creation and routing-artifact emission use new batch `save_tasks()` / `save_artifacts()` (SQLite `executemany` + matching events in one transaction) rather than a transaction per row; stitching uses a new batch `promote_memories()`. Every new method is on the `SwarmStore` base with a default loop implementation so both backends satisfy the contract.
- **Hot-path / resource optimization.** The daemon wait loop (a 0.2s poll) now takes one tasks snapshot and one artifacts snapshot per tick and passes them into `_has_hard_failure`, `_daemon_settled`, and `_recoverable_failure_by_task` instead of each re-reading and re-deserializing full state. Router signal patterns (`_HARD/_EASY/_VISION/_DETAILED_VISION_SIGNAL_PATTERNS`) are precompiled with `re.compile()` at module load instead of recompiling on every routing call. The reads/usage ledger loaders (`reads_log.py`, `codegraph_usage.py`) stream the JSONL line-by-line instead of `read_text().splitlines()` so a long-lived log doesn't allocate the whole file on every savings report. `retrieve_memory` pushes term filters into SQL (`instr(lower(data), ?)`) so filtered-out rows aren't JSON-decoded.
- **Dead code & lint.** Removed the unreferenced `codegraph_status_line()` and the legacy `_CODEGRAPH_AUTOHEAL_STATE` alias; dropped unused imports (`CODEGRAPH_COMMAND` in `mcp_server`/`codegraph_index_runner`, `AdapterInfo` in `diagnostics`, `Any` in `installers`, `field` in `savings`); `redact_secrets` returns its final substitution directly (RET504).
- Added `test_batch_store_methods_across_backends` covering the new batch/lookup APIs on **both** the file and SQLite backends; full suite **363** green.
- *Known limits:* the memory-search optimization is the conservative version only â no FTS5 index was added this pass (deferred as too invasive), so very large memory tables still do a full candidate scan after the cheap SQL prefilter.

## v0.9.7

**CodeGraph Node-ABI self-heal + ABI-safe CLI passthrough; removed the stale Cursor extension.**

- **CodeGraph now self-heals the better-sqlite3 Node-ABI break instead of just hinting about it.** The recurring Cursor footgun: a bare `codegraph` call from your shell runs under your shell's Node, whose ABI differs from Cursor's bundled Node that compiled `better-sqlite3`, so it dies with `NODE_MODULE_VERSION` / "compiled against a different Node.js version" (or silently drops to slow WASM). Every CodeGraph call routed through `run_codegraph_cli` (the `puppetmaster_codegraph_*` MCP tools and the new CLI passthrough) now detects that failure, rebuilds the native binding against Cursor's Node, and retries â no manual `repair-codegraph` needed. Opt out with `PUPPETMASTER_CODEGRAPH_AUTOHEAL=0`. Hardening: the trigger uses a **strict** ABI-load signature (`NODE_MODULE_VERSION` / "compiled against a different Node") so an unrelated non-zero exit can't kick off an expensive `npm rebuild`; the rebuild is **claimed under a lock** so concurrent MCP worker threads run at most one at a time; the command is **only retried when the rebuild succeeds**; a *failed* rebuild is gated by a 5-minute cooldown instead of permanently wedging the process, while a *successful* one latches for the process lifetime; and auto-heal is skipped when a custom install is pinned via `PUPPETMASTER_CODEGRAPH_NODE`/`_JS`. The native-broken backend detector still recognizes the hard module-load error surface, not just the WASM-fallback status line.
- **New `python -m puppetmaster codegraph <args>` passthrough.** Runs the CodeGraph CLI under Cursor's bundled Node (via `resolve_codegraph_invocation()`) with the same auto-heal, so `status` / `search` / `context` / `init --index` / etc. work regardless of your shell's Node. By default the passthrough imposes **no timeout** (so long ops like `index` / `affected` aren't cut off at the 30s context default); cap it with `--timeout SECONDS`, and pass CodeGraph's own flags after a literal `--` (e.g. `codegraph -- --version`). This is now the documented MCP-down fallback (AGENTS.md + rules updated); the old guidance to run a bare `codegraph â¦` was the source of the ABI error. `codegraph_available()` also returns true when the Cursor-Node invocation resolves even if the global shim isn't on PATH, and that lookup (which shells out to `npm root -g`) is now memoized so repeated availability checks don't re-probe the filesystem.
- **Removed the Cursor/VS Code extension (`cursor-extension/`).** It was stuck at `0.2.0-beta.1` with a command set predating implement/routing/savings/platform-lock, and added a Node CI leg plus eight doc references for no current value. The MCP tool surface is the supported Cursor integration. (Note: this removes the source only; any published marketplace listing must be unpublished manually.)
- Tests added for auto-heal trigger/skip/disable, strict-predicate gating, env-pinned skip, one-at-a-time + cooldown claim, retry-only-on-success, passthrough routing/no-timeout, and shim-less availability; full suite green.

## v0.9.6

**Cross-platform implement + an audit pass of hardening, correctness, and store performance.**

- **Implement is no longer Claude-Code-only â Cursor can ship code, and a generic `implement` verb routes to whatever platform you're locked to.** Previously a cursor-only platform lock could plan/review/analyze but not implement: the Cursor adapter only ran read-only analysis and the MCP path hard-coded `--dry-run`. Now the Cursor adapter has a full-edit **implement mode** (`payload.mode="implement"`) that mirrors the Claude/Codex contract â clean-worktree guard (override `payload.allow_dirty`), the agent edits files, and the resulting `git diff` is captured as a `PATCH` artifact (partial diffs are still captured on timeout/failure so work isn't lost). New surfaces: CLI `puppetmaster cursor --implement [--allow-dirty]`; MCP `puppetmaster_cursor_implement` / `puppetmaster_start_cursor_implement`; and a platform-agnostic `puppetmaster_start_implement` that picks the enabled full-edit adapter (cursor preferred, then claude-code), rejecting an explicitly-requested adapter that the lock disables.
- **Dashboard binds loopback-only by default.** The unauthenticated dashboard now refuses to bind to a non-loopback host (which would expose durable job state to the local network) unless `allow_external=True` / `--allow-external` / `PUPPETMASTER_DASHBOARD_ALLOW_EXTERNAL=1` makes the exposure a deliberate choice.
- **Orchestrator: no orphaned prerequisite workers.** When a prerequisite worker batch times out or one fails mid-batch, the remaining workers are now terminated (then killed) in a `finally` instead of being left to outlive the job.
- **Self-heal picks the *latest* recoverable failure per task.** `_recoverable_failure_by_task` now compares `created_at` so a stale failure reason can't win over a newer one produced by a retry.
- **Store correctness: torn-write and double-claim fixes.** SQLite `save_task` now writes the task row and its `task.saved` event in a single transaction (no crash window that leaves state with no event). `release_lock(owner=â¦)` only unlinks a lock the caller actually holds, so a late/stale caller can't free another worker's lock and let a second worker double-claim a task. Malformed persisted `confidence` values are coerced to `0.0` instead of crashing memory retrieval.
- **Store performance.** Idempotent SQLite `init()` short-circuits after first run; added `idx_tasks_job_id`, `idx_artifacts_job(id)`, and `idx_artifacts_type`; `last job` is a single indexed query; new `count_artifacts` / `get_artifacts_by_ids` / `list_artifacts_by_type` let status snapshots, the artifact feed, and the savings ledger avoid deserializing every artifact of every job; the claim sweep resolves dependencies from one in-memory task map instead of re-fetching each dependency. `schema_version` is no longer blindly overwritten on init, so a DB written by a newer Puppetmaster is surfaced by `schema_status()` rather than masked.
- Tests added across the implement feature and the store/orchestrator hardening; full suite **345** green.

## v0.9.5

**Cumulative savings ledger + counterfactual dollar headline.** A read-only, local, numbers-only receipt for what Puppetmaster actually saved â measured routing dollars, a counterfactual "avoided spend vs metered APIs" figure, CodeGraph exploration estimates, reliability counts, and $0 follow-up reads. Core stays emission-free; the org-wide rollup lives in a separate private plugin. Also fixes a routing baseline honesty bug and a savings double-counting bug surfaced by live benchmarks.

- **Cumulative savings ledger (`puppetmaster savings`).** A read-only, local, numbers-only receipt for what Puppetmaster actually saved â built so the value is auditable, not asserted. Two measured pillars: (1) **routing dollars saved**, where every routing decision now snapshots a `baseline_cost_usd` (the strongest model you could've used, same tokens) *at decision time* so the ledger compares like-for-like with no recompute drift; only cost-optimizing policies (`balanced`/`cheap`) count, while `quality`/`escalating` show as deliberate spend, never as a loss. (2) **CodeGraph exploration**, via a new global usage log that captures every query through the `puppetmaster_codegraph_*` tools â from *any* platform's agent, not just swarm jobs â recording context tokens fed (measured) plus avoided directory-crawl tokens (estimate, stated baseline). Plus a **reliability** line (real counts, never dollarized): tasks auto-recovered off a dead/unfunded provider and tasks re-run for confidence, read straight from existing fallback/escalation artifacts. Plus **$0 follow-up reads** â a numbers-only count of user-facing result reads (`show`/`artifacts`/`partial_summary`/`feed`) served from durable state at zero model cost; instrumented at the command entry points only (never the store's internal reads) so it can't over-count, and the streaming `feed --follow` long-poll is excluded. Plus a derived **`metrics`** block (rates, no $) for org dashboards â `capability_match_rate`, `escalation_rate`, `fallback_rate`, `reuse_reads_per_job`, `context_tokens_per_job`, each over an explicit `sample` denominator and `null` (never a misleading `0`) when the denominator is empty. Emits nothing over the network. `--window DAYS`, `--all-projects`, `--json`. Tune the estimate with `PUPPETMASTER_EXPLORATION_BASELINE_TOKENS` / `_PRICE_PER_MTOK`; disable the usage log with `PUPPETMASTER_CODEGRAPH_USAGE=0`.
- **Counterfactual "avoided spend" dollar headline.** `savings` now reports a `counterfactual` block that prices the exact token volume you actually routed against a single reference model at metered API rates and subtracts what the work actually cost (`avoided_usd = naive_cost_usd - actual_cost_usd`). On a plan-billed setup actual â $0, so the avoided figure â the naive cost â the big, monotonically-growing number leadership asks for, while staying honest: it's labeled a counterfactual ("what this would have cost on `<reference>` at API rates"), never "cash off your bill". Reference defaults to the highest-capability *priced* model in the registry; override with `PUPPETMASTER_COUNTERFACTUAL_MODEL`. An unpriced reference yields $0 and says so. Computed from the `estimated_tokens_in/out` already stored on each routing artifact â no schema change, no network emission. The org-wide rollup of this number lives in a separate private plugin so the public package stays emission-free.
- **Routing double-counting fix.** `collect_routing_records` now dedups `created_by="router"` artifacts by `task_id`, so a re-dispatched task can't inflate savings dollars or counts â surfaced by a live cursor-only review swarm.
- **Routing baseline honesty fix.** The decision-time savings baseline is now snapshotted from the *same eligible candidate set the pick is drawn from* â after every hard constraint (platform lock, required tags, cost cap, billing gate), not just platform lock. Previously a constrained run (e.g. `required_tags`, or `allow_api_billing=False`) could be measured against a model it could never actually have run, which both under-reported savings on a tag-restricted run and risked *over*-reporting them when the strongest model was billing-gated out. Surfaced by a live routing benchmark. `capability_match_rate` is now judged by model **identity** (ran a model other than the strongest available) rather than by dollars, so it stays meaningful for plan-billed shops where every model is $0. +3 regression tests.

## v0.9.4

**Routing self-audit + docs polish.** Closes the routing loop: a read-only recommender that reads back the artifacts you already store and tells you whether your capability scores still match reality â proposing conservative `models.json` changes you apply by hand. Also adds the "a layer above your agents" diagram to the README.

- **Routing self-audit (`puppetmaster audit`).** A read-only recommender that aggregates the routing/escalation/verification artifacts you already store and reports how each model *actually* behaved (picks, mean confidence, escalation rate, spend). It proposes conservative `models.json` score changes only for the defensible case â an under-delivering model that keeps getting escalated away from or finishing with low confidence â and lowers its score so harder work routes elsewhere. "Over-used" (a strong model on trivial work) is flagged but never auto-adjusted, since proving a cheaper model would've sufficed needs a counterfactual this audit doesn't run. Dry-run by default; `--apply` writes the diff. `--window DAYS` scopes it to recent jobs. Human stays in the loop â the registry is your assertion, not the audit's.

## v0.9.3

**Confidence-based mid-run escalation.** The counterpart to auto-fallback: when a cheap model finishes a task but isn't sure of itself, re-run it one capability tier up before accepting the result â directly addressing "tasks that look simple turn out hard once a worker is in them." Off by default so the cost-saving default is untouched. Also drops the internal `TALKING_POINTS.md`.

- **Confidence-based mid-run escalation** (opt-in). Static upfront routing can under-provision â a task that *looks* simple can turn out hard once a worker is in it, and the cheap model it was routed to says so via a low-confidence `VERIFICATION` artifact. When a confidence floor is configured (`$PUPPETMASTER_ESCALATE_CONFIDENCE=0.7` globally, or per-task `payload.min_confidence`), the orchestrator now re-dispatches a COMPLETE-but-low-confidence task to the cheapest **strictly stronger** funded + platform-enabled model and re-runs it *before* the result is accepted. **Off by default** so the cost-saving promise holds unless you opt in; bounded by `_MAX_ESCALATION_ATTEMPTS` (a task already on the top tier is left as-is); only escalates router-placed tasks (never a hand-pinned model); respects the platform lock and billing posture exactly like auto-fallback. Each escalation writes a `router-escalation` ROUTING artifact (with `escalated_from_model`, `escalated_from_confidence`, `confidence_threshold`) and emits a `router.auto_escalate` event; the local dashboard's reroute panel now surfaces both fallback and escalation. +8 tests; full suite **311** green.
- **Removed `TALKING_POINTS.md`** â it was an internal positioning/messaging cheat-sheet not meant to ship publicly. Dropped the file and the references to it in `README.md`, `bench/README.md`, `docs/CLAIMS.md`, and `docs/CODEGRAPH.md`. The defensible-numbers source of truth remains [docs/CLAIMS.md](CLAIMS.md) + the harnesses in [`bench/`](../bench/).

## v0.9.2

**Platform lock + launch packaging.** Lock Puppetmaster to the platforms you actually pay for (so a stray Claude Code / Codex login can never be picked behind your back), plus the security doc, positioning comparison, and self-healing README hero that make it launch-ready.

- **Platform lock â restrict Puppetmaster to the platforms you actually pay for** (`puppetmaster/platform_lock.py`, `puppetmaster platform â¦`). Single-platform users (e.g. a Cursor plan and nothing else) can now hard-lock routing to one adapter so a stray Claude Code / Codex login can never be picked, auto-discovered, or used for fallback â even if its CLI is installed and funded. Free-tier bouncers can flip several on and rotate across them. New CLI: `platform status` (shows each platform on/off + source), `platform only cursor`, `platform enable claude-code codex`, `platform disable openai`, `platform reset`. State is a tiny denylist persisted next to the model registry (`~/.puppetmaster/platform.json`); default is everything-on (no file, zero behavior change for existing users). Env override `PUPPETMASTER_ONLY_ADAPTERS=cursor,openai` wins for CI/ephemeral use. The lock is enforced at **all three** decision points â routing (`TaskSignals.allowed_adapters` rejects disabled adapters with a clear reason), first-run plan-catalog auto-discovery (disabled platforms are never enumerated), and auto-fallback (a failed run never reroutes onto a disabled platform) â and surfaced in `doctor` (`platform-lock` check). Per-task escape hatch via `payload.allowed_adapters`. +11 tests; full suite **303** green.
- **Security & threat model doc** ([docs/SECURITY.md](SECURITY.md)). The "is it safe to hand it my repo and my plan?" answer a skeptical first user looks for before installing: what Puppetmaster can do (spawn agent subprocesses, edit files, run shell, read local auth), how credentials are handled (uses existing auth; keys never inlined/stored â evidence records presence like `cursor_api_key:set`, never values), the exact edit-permission/sandbox model per adapter with dirty-tree guards, the complete network-egress list (only the providers your workers use; telemetry off by default; no phone-home/analytics), data-at-rest (local SQLite only), hardening recommendations, and honest limitations (it orchestrates autonomous agents â auditable and reversible, not risk-free; no independent audit yet). Linked from both indexes.
- **Launch packaging: positioning + a self-healing hero.** Added [docs/COMPARISON.md](COMPARISON.md) â an honest, calibrated side-by-side answering "why this over LangGraph / CrewAI / Claude Agent SDK / native subagents," with an explicit "pick X instead ifâ¦" section (Puppetmaster orchestrates the agent CLIs you already pay for; it isn't a framework for authoring an agent from primitives). Rebuilt the README hero around **two receipts** â ð¸ cheaper (98.8% live OpenAI A/B) and ð self-healing (the live `job_d82715bebc5d` auto-fallback: a `$0` Claude balance â rerouted to `cursor/gpt-5.5` â completed) â so the differentiated, shareable moment is above the fold. Docs index updated; all internal links verified.

## v0.9.1

**Plan-first for everyone, and a probe that proves it.** Closes the gap where only Cursor users got subscription-contained routing and where the live probe only covered API keys â so the "frontier work always rides the subscription you already pay for, and we verify it can actually serve before spending" promise now holds for Claude Code and Codex users too.

- **Live 1-token probe now covers the plan/subscription path, not just API keys.** The probe's whole reason to exist is the funded-*looking* account that's actually exhausted â and for subscriptions that case is real (a Cursor/ChatGPT/Max plan can be authenticated, expose the model, and still be rate-limited or over its monthly allowance). Claude Code (OAuth) and Codex (ChatGPT) already did a real CLI round-trip; the gap was **Cursor**, whose live probe was only a catalog reachability ping. Added `preflight._probe_cursor` â a minimal real generation through the same `cursor_sdk_runner.mjs` the adapter uses â so a plan that's out of allowance surfaces at preflight instead of mid-job. The probe classifier gained plan-side exhaustion markers (rate limit / usage limit / monthly limit / 429 / too many requests), and the block-only-on-definitive-rejection semantics are now shared across all adapters (`_verdict_from_probe`): a probe that can't reach a verdict (node missing, timeout, transient) degrades to an unverified pass, never a false block. *Verified live: `preflight cursor --live` did a real ~7s generation on the developer's plan and returned `live_probe:ok` (a genuine served token, not a catalog ping).*
- **Clean subscription detection (most users are subscribed, so this is the common path).** Plan-first routing only fires if we can *reliably* tell a subscription is signed in. The old heuristics were fragile: Claude Code detection trusted "`~/.claude.json` exists" (but that file stores onboarding/config state and **survives a logout**, a false positive), and Codex detection shelled out to `codex login status` (slow, text-parsed). Now both read the real local auth files directly: Claude Code parses the `oauthAccount` block (requires an `accountUuid`/`emailAddress`, and surfaces `seatTier` + `organizationName` in the detail/evidence), and Codex reads `~/.codex/auth.json` (`auth_mode`/`tokens` â `apikey` is out-of-pocket, `chatgpt` is plan-covered), falling back to `codex login status` only when that file is absent. Deterministic, subprocess-free for the common case, and `doctor`'s `billing:<adapter>` lines now name the actual subscription (e.g. *"signed in via OAuth (max seat, org 'Acme')"*).
- **Plan-first auto-discovery for Claude Code & Codex (curated catalogs).** Cursor was the only adapter that got first-run plan-catalog auto-discovery, because it's the only platform that self-enumerates its models (`Cursor.models.list()`). The two CLI agent loops â Claude Code (OAuth) and Codex (ChatGPT) â expose *billing mode* but no queryable model list, so a Cursor-less user on a Claude Max / ChatGPT subscription still fell off to a per-token account on hard tasks. New `puppetmaster/static_catalog.py` ships a **hand-curated catalog** of what each of those platforms offers (the one place to update when they ship models) plus the same auto-merge Cursor gets: when an `auto_route` job starts, the registry has no plan-billed frontier, and **Cursor didn't supply one**, the orchestrator detects the first subscription-authenticated CLI loop and merges its curated catalog in as **plan-billed** (`$0` marginal, capability/id/tags preserved from any existing registry row, new models seeded), so frontier work routes through the subscription the user already pays for. Idempotent (per-source discovery marker), never blocks a run (degrades to a loud `router.plan_catalog_unavailable` event), opt-out via `PUPPETMASTER_AUTODISCOVER=0`. Also exposed manually: `python -m puppetmaster models discover --source claude|codex` (billed as the detected subscription/API posture, dry-run by default). +7 tests; full suite **288** green.

## v0.9.0

**Truthful failure status, auto-fallback, live preflight, full-platform discovery, and real observability.** This release closes the gap between "looks healthy" and "actually verified before spending your money," and makes a degraded run re-route itself instead of silently lying.

- **Plan-first by default: first-run catalog auto-discovery.** The promise is "frontier work always routes through the subscription you already pay for, never falls off to a per-token or depleted account." That only holds if the plan-billed frontier models are actually *in* the registry â but a fresh install ships a thin starter registry (`cursor/gpt-5-5` cap 78 as its ceiling) and discovery was historically manual, so any task needing capability â¥ 79 silently fell through to a standalone adapter. Now, when an `auto_route` job starts and the registry has no plan-billed frontier (`capability_score â¥ 85`), the orchestrator enumerates the authenticated Cursor plan's catalog **once** and merges it in (plan-billed, capability inherited by model-name), so a cap-90 audit/redteam/implement task routes to `cursor/claude-opus-4-7` (`$0`, in-subscription) instead of a depleted API account. Idempotent (a discovery-meta marker prevents re-enumeration; steady-state runs pay nothing), never blocks a run (a node/SDK failure degrades to a loud `router.plan_catalog_unavailable` event + the existing `doctor` nudge), and opt-out via `PUPPETMASTER_AUTODISCOVER=0`. *Verified live: a thin 12-model starter â first swarm auto-discovered 28 plan models â `cursor/claude-opus-4-8`/`4-7` became routable; second swarm skipped with no node call.*
- **Fixed a correctness defect: a preflight block / adapter billing rejection now FAILS the task.** `WorkerRuntime.run_once` previously discarded the `AgentRun` returned by `LocalWorker.run` and marked every dispatched task `COMPLETE` â so a preflight-blocked task (or an adapter that came back with a `billing_or_quota`/auth verification artifact) was recorded as a success. That made telemetry lie, made `await_job` report success over a doomed run, and made auto-fallback impossible. The runtime now honors the worker's `FAILED` verdict and converts any recoverable adapter failure (`RECOVERABLE_FAILURES`: billing/quota, preflight-blocked, missing key/CLI, auth) into a truthful `FAILED` status.
- **Auto-fallback on auth/billing failure.** After the main worker pass, the orchestrator re-routes any task that FAILED because *its adapter's account* was unfunded/unauthenticated to the cheapest sufficient model on a *different, verified-funded* adapter, then re-runs it (bounded: `_MAX_FALLBACK_ATTEMPTS=2`, `_MAX_FALLBACK_ROUNDS=3`). Each re-route writes a `router-fallback` ROUTING artifact and emits a `router.auto_fallback` event. Genuine errors (bad adapter, exceptions) still fail closed; a billing failure with no funded alternative surfaces loudly in the stitched **Alerts** section instead of nuking the whole job.
- **Live 1-token preflight probe** (`preflight.live_probe`, `preflight_check(..., live=True)`, `puppetmaster preflight --live`, per-task `payload.live_preflight`). Static detection can't see a funded-*looking* account whose balance is actually $0 â only a real call can. The probe does the smallest possible round-trip per adapter (Claude/Codex CLI, OpenAI `/v1/chat/completions`, Cursor = plan-catalog reachability) and classifies the result with the existing adapter classifiers. Off by default (adds a real call); fully dependency-injected for tests. **A probe only *blocks* on a definitive provider rejection (`billing_or_quota`/`auth`); a probe that can't reach a verdict (missing optional CLI, timeout, network blip) degrades to an "unverified" pass so `--live` never false-blocks a working account.** The OpenAI probe sends `max_completion_tokens` (GPT-5+ rejects the legacy `max_tokens`), with an automatic legacy fallback for older OpenAI-compatible endpoints.
- **Catalog discovery for OpenAI + Anthropic** (`puppetmaster/api_discovery.py`). Both expose `/v1/models`, so `python -m puppetmaster models discover --source openai|anthropic|all` now enumerates them too (additive merge â never drops hand-tuned entries; inherits capability/price from matching registry rows, seeds the rest). Cursor remains the default source.
- **Catalog staleness tracking.** `models discover --write` records per-source `{refreshed_at, count}` in a `models.discovery.json` sidecar; `doctor` gains a `catalog-freshness` check that nudges when a catalog is >30 days stale or never discovered.
- **Real observability** (`puppetmaster/telemetry.py`): OTel **metrics** (`puppetmaster.jobs` / `.tasks` counters + `cost_usd`), **live per-task spans** emitted by workers as they finish (`PUPPETMASTER_OTEL_LIVE`), and **cross-process trace-context propagation** â the orchestrator mints a W3C `traceparent`, exports it to worker subprocesses via `TRACEPARENT`, and anchors the assembled job span to it, so a swarm renders as one correlated trace in Jaeger/Datadog. All gated/zero-dep by default.
- **TypeScript blocking-await client** (`clients/typescript/`). `awaitJob(jobId, opts)` blocks until a job is terminal and returns its final state + stitched summary by driving the durable CLI (`python -m puppetmaster await --json`) â true blocking await for CI/scripts/backends, free of the MCP stdio constraint. Zero runtime deps; typechecks clean under `--strict`.
- **End-to-end validation (live, real adapters).** The marquee resilience flow was proven against real providers, not mocks: a `claude-code` worker hit a genuine `$0` Anthropic balance â classified `billing_or_quota` â the task was marked `FAILED` â `router-fallback` re-routed it to `cursor/gpt-5.5` (plan-billed, `$0`) under the balanced policy â the funded adapter **completed** the task (job `job_d82715bebc5d`; `created_by` audit trail: `worker-implement` â `router-fallback` â `worker-implement`). Concurrency was validated by `bench/mcp_stress.py` (**6/6** scenarios: 64 simultaneous senders, sustained traffic, large payloads, idleâbusyâidle keepalive â 0 missing responses, 0 frame errors, server stayed alive). Benchmark receipts regenerated against this build: `router_savings` **35.1% cheaper** on a mixed workload (100% on routable tasks), `router_live_ab` **98.8% cheaper / 81.1% faster** with real OpenAI token receipts, `followup_cost` **0 adapter calls / 0 tokens** for 32 follow-up reads.
- **Bug fix (live probe false-block).** `preflight openai --live` blocked a fully-funded account because the probe sent the deprecated `max_tokens`, which GPT-5+ rejects with a 400 `unsupported_parameter` â misclassified as `probe_failed`. Fixed to `max_completion_tokens` (+ legacy fallback) and the block-only-on-rejection semantics above. Regression-tested.
- **Local dashboard** (`puppetmaster/dashboard.py`, `python -m puppetmaster dashboard [<job_id>]`). A zero-dependency live web board served from `http.server` â task graph, typed-artifact rollup, estimated cost, **auto-fallback reroutes**, and the same Alerts the stitcher surfaces. The page is fully inlined (no Flask/React/CDN; works offline) and polls every ~1.5 s, so it's live while a swarm runs and instant for a finished one. OTel remains the export path for Jaeger/Datadog; this is the no-collector local view. Data layer (`build_job_snapshot`) is a pure function; an HTTP smoke test drives the real server. **Hardened via dogfooding:** a Cursor swarm reviewing this very module (which itself survived a live `claude-code` `$0` failure â `router-fallback` â `openai/gpt-5.5` completion) flagged three real issues, now fixed â `/api/job` rejects non-job-id requests before the store path join (no `..`/absolute traversal), the catch-all handler returns a generic 500 instead of leaking `str(exc)` (paths/backend details), and the cost rollup tolerates a non-numeric `estimated_cost_usd` instead of 500-ing.
- **Cross-platform CI.** The GitHub Actions matrix now runs the full suite + CLI/demo smokes on **Linux, macOS, and Windows** (Python 3.9 + 3.12) with `shell: bash` so the heredoc steps run identically everywhere. Fixed a Windows-only runtime breaker: `mcp_registry` hard-kill used `signal.SIGKILL` (absent on Windows â `AttributeError`); now falls back to `SIGTERM` (which `os.kill` maps to `TerminateProcess` on Windows). The first live matrix run did its job and surfaced more: a flaky macOS timing assertion in the input-staleness watcher test (the watcher's first check could race the inbound-message mark â fixed by marking before start), and a real cross-platform bug in `codegraph._pid_is_alive` â `os.kill(pid, 0)` doesn't report a missing pid as dead on Windows, so a stale codegraph lock held by a long-gone process was unbreakable; now uses a Win32 `OpenProcess`/`GetExitCodeProcess` probe that distinguishes "missing pid" (dead) from "access denied" (alive, mirrors POSIX EPERM). A follow-up Windows-hardening pass then fixed the real cross-platform defects the matrix exposed: (1) **SQLite connection leak** â every store op used `with self.connect()`, which only commits/rolls back and leaves the handle open; harmless on POSIX but Windows holds a mandatory file lock, so temp-dir cleanup failed with `WinError 32`. A `_session()` context manager now always closes the connection. (2) **`command_parts` mangled Windows paths** â `shlex.split` defaults to POSIX mode and ate backslashes in `C:\â¦\claude.exe`, so every adapter reported `missing_cli`; now splits in non-POSIX mode on Windows. (3) **CodeGraph lock was a no-op on Windows** â the advisory lock was `fcntl`-only, so without `fcntl` *no* mutual exclusion happened; added an `msvcrt` non-blocking byte-range lock so concurrent indexers actually serialize. (4) **`doctor` could crash on Windows** â a CLI probe that raised (e.g. a node-shim that isn't a valid Win32 exe) aborted the whole report; each check is now guarded into an `error` Check. (5) De-flaked the macOS input-staleness watcher test and made the codex/claude adapter test fakes run via `sys.executable` (cross-platform) instead of relying on shebang exec; the two `codex` install tests that stub a **bash** CLI are skipped on Windows (POSIX-only scaffolding, not a product gap). A second pass fixed the last three Windows failures â the `msvcrt` lock was locking the same byte the PID text occupies (mandatory locks block reads, so the busy-check couldn't read the holder PID), now fixed by locking a sentinel byte at a 1 GiB offset; and `diagnostics._sqlite_state_check` leaked its connection the same way the store did. **Windows is now green and `continue-on-error` has been removed â all three OSes are required blocking legs.**
- **Demo.** `scripts/demo.sh` â a 60-second, no-API-key tour (cost routing â 6-role swarm fan-out â stitched summary â `$0` follow-up reads â dashboard pointer) that runs on a clean machine; `scripts/demo.tape` regenerates `docs/demo.gif` via VHS (GIF-as-code), and the rendered `docs/demo.gif` (~1.1 MB, 24 fps) is committed so the README shows it without a local VHS install.

## v0.8.0

**Platform-contained billing + dynamic Cursor catalog discovery.** Puppetmaster now treats the subscription you already pay for (Cursor plan, Claude Max, ChatGPT/Codex) as the default place to spend, and can discover what your Cursor plan actually exposes instead of relying on a hand-maintained registry. Also fixes a **critical packaging bug** that broke the Cursor adapter for every `pip install` user.

> **Packaging fix (affects all prior PyPI releases):** `puppetmaster/cursor_sdk_runner.mjs` â the Node runner the Cursor SDK adapter resolves via `Path(__file__).with_name(...)` â was **never included in the wheel/sdist** (no `package-data` rule), so `pip install puppetmaster-ai` shipped a Cursor adapter that couldn't launch. Added `[tool.setuptools.package-data] puppetmaster = ["*.mjs"]`; verified the runner is now present in both wheel and sdist and resolvable after a clean-venv install. **Anyone using the Cursor adapter from a pip install must upgrade to 0.8.0.**

- **First-class `ModelSpec.billing`** (`plan` | `api` | `unknown`, default `unknown`). Plan = covered by a subscription (no marginal API spend); api = out-of-pocket per-token; unknown = resolve at runtime. Serializes only when non-default; round-trips through `load_registry`/`save_registry`. Starter registry tags Cursor entries `plan` and OpenAI entries `api`. New `is_plan_billed` property.
- **Billing-aware router.** `TaskSignals` gains `prefer_plan_billed` (default **True**) and `allow_api_billing` (default **True**). At sufficient capability the router prefers a plan-billed model over an out-of-pocket one (tie-break across `cheap`/`balanced`/`quality`/`escalating`); `allow_api_billing=False` is a hard plan-only gate that rejects api-billed models with an explicit reason and raises `NoEligibleModelError` if nothing plan-billed qualifies. The picked model's `billing` is now stamped into the `ROUTING` artifact for audit. Overridable per task via `payload.prefer_plan_billed` / `payload.allow_api_billing`.
- **Cursor catalog discovery** (`puppetmaster/cursor_discovery.py` + `cursor_sdk_runner.mjs` `mode: "list-models"`). `python -m puppetmaster models discover` enumerates the authenticated Cursor plan's models via `Cursor.models.list()` and reconciles them into the registry as **plan-billed** entries (`--write` to persist, dry-run by default, `--json` for machines). Cross-adapter **capability inheritance**: a model the plan exposes (e.g. `claude-opus-4-8`) inherits its native capability/context (cap 99) by model-name match, so a plan-billed frontier ranks like the frontier it is. Stale cursor entries no longer in the catalog are dropped; non-cursor entries pass through untouched. *Live result on the developer's plan: 28 plan-billed models discovered, and a hard audit re-routed from `claude-code/opus-4-8` (api, ~$0.13/call) to `cursor/claude-opus-4-8` (plan, $0).*
- **Platform billing/auth detection** (`puppetmaster/platform_billing.py`). Pure, dependency-injected probes for each adapter: Cursor (keyed â plan), Claude Code (`ANTHROPIC_API_KEY` â api, else `~/.claude` OAuth â plan), Codex (`codex login status`: API key â api, ChatGPT â plan), OpenAI (api). Returns `BillingStatus{billing, healthy, detail, evidence}`. `doctor` now prints a `billing:<adapter>` line per platform.
- **Universal preflight** (`puppetmaster/preflight.py` + `python -m puppetmaster preflight <adapter> [--model] [--no-api-billing]`). Verifies an adapter can actually run *before* dispatch â credentials present, plan-vs-api gate, and (Cursor) the routed model is in the live catalog. Catalog-fetch failures degrade to a pass with a note (never block a run because discovery was offline). This is the safety net for the "returned in 391ms with billing failure and zero edits" degraded-run class.
- **Loud failure surfacing.** The stitched summary now leads with an **`## Alerts (action required)`** section that hoists hard failures (`billing_or_quota`, `missing_api_key`, `missing_cli`, `model_unavailable`, `permission_denied`, â¦) to the top with remediation, instead of burying them in a low-confidence verification line.
- **`await_job`.** New `puppetmaster_await_job` MCP tool (bounded long-poll: blocks up to `timeout_seconds` for a terminal state, returns `timed_out` so you can re-call) and `python -m puppetmaster await <job_id>` (true blocking await for the CLI/SDK path, `--timeout-seconds 0` blocks until done).
- **Pre-dispatch preflight gate.** `LocalWorker` now runs the fast (no-network) auth/billing check before dispatching to a paid adapter (cursor/claude-code/codex/openai). An unauthenticated/missing adapter â or an api-billed one under a plan-only task â yields a `blocked` verification artifact (`failure=preflight_blocked`) and a FAILED run instead of a wasted dispatch; opt out with `payload.skip_preflight`. Pairs with billing-aware routing (auto_route jobs already steer hard work to the plan-billed Cursor frontier, so they avoid a depleted API account entirely). Surfaced in the stitcher Alerts section. *Live token probes for the funded-but-zero-balance case remain a follow-up.*
- **Optional OpenTelemetry tracing** (`puppetmaster/telemetry.py`, `pip install 'puppetmaster-ai[otel]'`). Zero-cost and zero-dependency unless enabled via the standard env vars (`OTEL_EXPORTER_OTLP_ENDPOINT`, or `OTEL_TRACES_EXPORTER=console`, with a `PUPPETMASTER_OTEL_ENABLED` override). On completion each job emits a trace â a `puppetmaster.job` session span with `puppetmaster.task` children carrying role, adapter, `gen_ai.request.model`, status, and `puppetmaster.cost_usd`/`gitagent.cost_usd` â assembled from durable records (faithful per-worker tree without threading span context across the subprocess boundary) and exported via OTLP or printed to the console. Trace assembly (`build_job_trace`) is a pure, SDK-free function; a telemetry failure never breaks a swarm. Verified end-to-end with the console exporter.
- **Bug fix:** `_server_version()` looked up the dist as `puppetmaster`, but it's published as `puppetmaster-ai` â pip users got `None` in MCP `serverInfo.version`. Now tries `puppetmaster-ai` first, falls back to the legacy name.
- **Tests:** +27 tests (`214 â 241`) across billing field round-trip, billing-aware routing (plan tie-break, api gate), platform detection (all auth branches), Cursor discovery (catalog fetch/failure modes, overlay + cross-adapter inheritance, stale-drop merge), preflight (ready/blocked/catalog-unavailable), stitcher alerts, and `await_job_state` (terminal + timeout). Full suite green; clean-venv install of the built wheel verified end-to-end.

## v0.7.3

Adds **Anthropic Claude Opus 4.8** (released 2026-05-28) as the router's new frontier flagship â the "big bad" the hardest tasks route to.

- **New starter-registry tier `claude-code/opus-4-8`** (`adapter_model_name: claude-opus-4-8`, `capability_score: 99`, `$5 / $25` per MTok, **1M context window**, tags `frontier, vision, detailed-vision, reasoning, code`). Opus 4.8 ships at the *same price* as 4.7 with across-the-board benchmark gains and a 4x-larger context, so it strictly dominates 4.7 for the hardest work (security audits, red-team, deep reasoning, detailed vision). `claude-code/opus-4-7` is retained (capability 98) so existing routing configs and cost history stay valid, with its `notes` updated to mark it as the previous flagship. Starter registry is now **12 tiers** (was 11). `docs/sample-models.json` aligned.
- **Classifier ceiling raised 98 â 99** in `puppetmaster/router.py:classify_capability_needed`. The max capability-needed score tracks the current flagship's `capability_score`; with 4.7 (98) as the previous top, the hardest tasks saturated at 98 and the balanced policy's equal-cost tie-break (prefer the right-sized, lower-capability model) kept routing them to 4.7. Bumping the ceiling to 99 means the absolute-hardest tasks now *demand* 99 and route to Opus 4.8 â while merely-hard tasks (need â¤ 98) still right-size to 4.7 or below. No change to the tie-break itself, so the `$0` Cursor tier and every other pairing is unaffected.
- **Routing impact**: `route "Security audit across every endpoint" --role audit` (and other top-tier tasks classified at 99) now pick `claude-code/opus-4-8` instead of `claude-code/opus-4-7`. Under `quality` policy the flagship is also 4.8. Detailed-vision tasks below the ceiling (e.g. `OCR every detail of the diagram`, need ~92) still right-size to `claude-code/opus-4-7` since both carry `detailed-vision` at equal cost. The new entry was appended to the developer's existing `~/.puppetmaster/models.json` (preserving all prior entries) and verified live.
- **Tests**: updated the detailed-vision quality-policy assertion to expect `claude-code/opus-4-8`; extended `test_starter_registry_encodes_four_tiers` to assert 4.8 > 4.7, that 4.8 is the single highest-capability model, same price as 4.7, and a larger context window; added `test_starter_registry_routes_hardest_task_to_opus_4_8` locking the flagship as the destination for the hardest tier. Full router/registry subset (33 tests) green.

Also in this release (the previously-deferred "last-mile" setup work):

- **`python -m puppetmaster install-rules`** (new). Writes agent rule files that nudge the host (Cursor, `AGENTS.md`, Codex, Claude Code) to reach for Puppetmaster's MCP tools on the right tasks â the MCP installers give a host the *capability* to call Puppetmaster, but without a rule the agent won't reflexively use it. Auto-detects targets from cwd + tools on PATH; `--target` to pin a subset, `--global` for user-level rule files, `--force` to rewrite, `--dry-run` to preview. Uses a merge-block protocol (`<!-- puppetmaster:rules:begin/end -->`) so it inserts/updates managed content in existing files (e.g. `AGENTS.md`) without clobbering user content. New module `puppetmaster/rules.py` with `RulesInstallResult` / `TargetOutcome`.
- **`python -m puppetmaster setup`** (new). One-shot first-run wizard: `doctor` + `models init` + `install-cursor-mcp` + `install-codex-mcp` + `install-rules`, idempotently, skipping any step whose tool isn't present. This is now the single command the README leads with after `pip install`.
- **`doctor` gains an `agent-rules` check** that flags the half-installed state where an MCP host is registered but no agent rule file exists (returns `optional` when no MCP host is present, `ok` once a rule is found).
- **README thinned from a wall to a scannable page** (~1100 lines removed). Deep content moved into dedicated docs: `docs/MODEL_ROUTING.md`, `docs/CODEGRAPH.md`, `docs/WHY.md`, `docs/TROUBLESHOOTING.md`, `docs/CLI_REFERENCE.md`.

## v0.7.2

One-line MCP installers for Cursor and Codex, plus the previously-deferred Codex MCP documentation that sat ahead of the v0.7.1 tag.

> **First PyPI release** (2026-05-28). Published as `puppetmaster-ai` (https://pypi.org/project/puppetmaster-ai/0.7.2/) because PyPI's PEP-503 name normalization treats `puppetmaster` and `puppet-master` as the same name, and the bare `puppet-master` is held by an [abandoned 2019 single-release project](https://pypi.org/project/puppet-master/) that hasn't had activity in 6+ years. Import name stays `puppetmaster`; CLI, GitHub repo, and README branding are unchanged â only `pip install <name>` differs. Name-reassignment request tracked separately. Install:
>
> ```bash
> pip install puppetmaster-ai
> python -m puppetmaster install-cursor-mcp   # or install-codex-mcp
> ```

- **`python -m puppetmaster install-cursor-mcp`** (new). Resolves the exact Python that has `puppetmaster` importable via `sys.executable` â the single biggest source of "I followed the README but the MCP server won't load" friction was `command: "python"` in `mcp.json` resolving to a different interpreter under Cursor's launch environment than the one the user installed Puppetmaster into. Defaults to `<cwd>/.cursor/mcp.json`; pass `--global` to write `~/.cursor/mcp.json` (visible in every workspace) or `--path PATH` for explicit control. Runs a `tools/list` handshake against the registered Python before writing anything, so a broken interpreter or missing package is caught with a clear error rather than producing a half-broken config. Fully idempotent (re-run reports `unchanged`); `--force` repoints at the current Python even when the existing entry matches. Critically: merges into the existing JSON instead of overwriting â unrelated MCP servers and any pre-existing `env` block on the puppetmaster entry (CURSOR_API_KEY, OPENAI_API_KEY, PYTHONPATH, etc.) are preserved verbatim.
- **`python -m puppetmaster install-codex-mcp`** (new). Same pattern, targets the Codex CLI. Shells out to `codex mcp add puppetmaster -- <sys.executable> -m puppetmaster.mcp_server` so the registration goes through Codex's own CLI (forward-compatible with future TOML schema changes). Runs the same `tools/list` handshake before registration. Idempotent: if `codex mcp get puppetmaster` already reports a matching command + args, returns `unchanged` without touching `~/.codex/config.toml`. Prints the Codex sandbox guidance (interactive TUI approval flow vs `--dangerously-bypass-approvals-and-sandbox` for `codex exec`) as part of "next steps" output.
- **`puppetmaster/installers.py`** (new module). Hosts `handshake_mcp_server()`, `install_cursor_mcp()`, `install_codex_mcp()`, plus the `InstallResult` and `HandshakeResult` dataclasses. Designed so unit tests can stub the `codex` CLI with a shell script and assert on exact argv shape without touching the user's real `~/.codex/config.toml`. Both installers support `--dry-run` (preview without writing) and `--skip-handshake` (rare escape hatch for environments where spawning a subprocess is undesirable, e.g. CI bootstrap).
- **README quickstart updated** to call `install-cursor-mcp` and `install-codex-mcp` as third-and-fourth steps after `doctor` and `models init`. The Cursor and Codex MCP setup sections both lead with the one-line installer and demote the manual `mcp.json` / `codex mcp add` recipes to "equivalent if you prefer". The Codex MCP setup section itself (the one that sat ahead of the v0.7.1 tag) is now packaged in this release.
- **Tests**: +10 new tests in `tests/test_puppetmaster.py::InstallerTests` covering: handshake against the real MCP server (asserts 10+ tools advertised), handshake-with-missing-python failure path, Cursor install into an empty file, Cursor install preserving unrelated servers + existing env keys, Cursor install idempotency (file mtime unchanged on rerun), Cursor `--force` rewrites even when matching, Cursor `--dry-run` does not write, Codex install with missing CLI returns `error`, Codex install argv shape verified via shell-script stub, Codex install idempotency when entry already matches. **All 10 pass in 0.5s isolated.**
- **Live smoke**: both installers exercised end-to-end on a developer machine against a real Cursor `~/.cursor/mcp.json` (3 pre-existing servers, 4-key env block on puppetmaster entry) and a real Codex CLI install (codex 0.134.0). Both installs upgraded the brittle `command: "python"` to `command: "/Users/cary/.pyenv/versions/3.9.7/bin/python"`, preserved all 4 env keys verbatim, and left navdata + portable-llm-wiki MCP server entries untouched. `codex mcp get puppetmaster` confirmed the registration.

## v0.7.1

Polish pass after v0.7.0 â diagnostics, doctor honesty, and README freshness for new readers.

- **`puppetmaster doctor` now actually reflects the v0.7.0 adapter surface.** Added a `codex` check that reports `ok <command>` when the `codex` CLI is on PATH (or `CODEX_COMMAND` resolves) and `optional` with install instructions otherwise. The pre-existing `claude-code` check downgraded its severity from `missing` (which read as an alarm) to `optional` with cleaner install instructions, matching `codex`. Both adapters are genuinely optional; `missing` was misleading when only an alarm-level severity should be used for things doctor needs to function.
- **`puppetmaster adapters` no longer false-reports `openai` and `codex` as `configured=False` despite being usable.** The `adapter_status()` diagnostics function gained explicit branches for both: `openai` is `configured` when `OPENAI_API_KEY` is set; `codex` is `configured` when the CLI is installed AND `OPENAI_API_KEY` is set (a decent proxy for "you have something usable" without introspecting the codex login file outside Puppetmaster's purview). Before this fix, four out of six adapters showed `configured=False` regardless of state â a Cursor engineer skim-reading the output would have concluded nothing worked.
- **README headline benchmark numbers refreshed.** The opening A/B headline previously cited a specific 2026-05-08 run ($0.019530 â $0.000141, 99.3% cheaper / 83.3% faster). Re-running the same harness 3 times on 2026-05-28 against the current registry produced 98.1â98.7% cheaper / 68â88% faster. Updated to today's representative numbers ($0.006900 â $0.000132, 98.1% cheaper / 72.4% faster) and explicitly framed the cost ratio as "98.1â98.7% across 3 consecutive runs" rather than a single-point claim. Honest about variance.
- **README copy synced to the v0.7.0 adapter surface.** Opening sentence now lists all four production adapters (`cursor`, `claude-code`, `openai`, `codex`). Architecture diagram updated to show `Codex CLI` in the inbound row. "11-tier starter registry" called out (was "8-model" / "nine"). Model registry table extended to include the four OpenAI tiers + two Codex tiers (previously only the Cursor + Claude tiers appeared). CodeGraph integration paragraph now lists Codex as a 4th adapter that auto-injects context.
- **TALKING_POINTS.md updated** with the fresh A/B numbers and v0.7.0's four-adapter framing.
- **bench/README.md** sample-receipt table updated to today's run.
- Quickstart `puppetmaster doctor` comment now says "14 health checks" (was "12") and enumerates them so a new reader knows what to expect.
- **Tests**: still 189 passing. No code-behavior changes outside diagnostics.

## v0.7.0

- **Codex is no longer a stub â full Codex CLI integration shipped.** The `codex` slot has been a placeholder `UnconfiguredProviderAdapter("codex", "Codex")` since `puppetmaster/adapters.py` was first written. v0.7.0 promotes it to a real adapter, sibling to `CursorAdapter`, `ClaudeCodeAdapter`, and `OpenAIAdapter`. Puppetmaster now ships **four production adapters** instead of three.
- **`CodexAdapter` shells out to the official OpenAI Codex CLI (`codex exec --json`).** Required: `npm install -g @openai/codex`, then `printenv OPENAI_API_KEY | codex login --with-api-key` (or `codex login` for ChatGPT-account auth). The adapter is modeled on `ClaudeCodeAdapter` for subprocess + git-snapshot + sidecar-spool semantics, with one major addition: it parses Codex's structured JSONL event stream and captures **real billing-grade token telemetry** into the verification artifact payload â `input_tokens`, `output_tokens`, `cached_input_tokens`, `reasoning_output_tokens`, and `thread_id` per turn. This makes `codex` strictly more telemetry-rich than any other adapter Puppetmaster ships; `claude-code` doesn't surface usage at all, and `openai` only surfaces the basic prompt/completion split.
- **Default execution mode is non-interactive and safe.** `approval_policy="never"` (no automation hangs on approval prompts), `--sandbox workspace-write` (can edit files in `cwd`, not outside), `--ephemeral` (no persistent Codex sessions), `--skip-git-repo-check` (works in non-git directories). Callers can downgrade to `payload.sandbox="read-only"` for review/plan roles, or opt in to `payload.dangerously_bypass_approvals_and_sandbox=true` when the surrounding environment is already externally sandboxed.
- **Two new starter registry tiers.** `codex/gpt-5-5` (capability_score 97, $5/$30 per MTok, 1M context, tags include `frontier` + `agent-loop`) and `codex/gpt-5-4-mini` (capability_score 72, $0.75/$4.50, 400K context, tags include `balanced` + `agent-loop`). Capability scores are set 1 point higher than the matching `openai/*` tiers to encode the agent-loop advantage on multi-file refactors and audits. For pure one-shot reasoning, the router still prefers `openai/*` (no agent-loop round-trip overhead).
- **New `puppetmaster codex` CLI subcommand**, parallel to `puppetmaster cursor`, `puppetmaster claude`, and `puppetmaster openai`. Supports `--model`, `--sandbox`, `--approval-policy`, `--dangerously-bypass-approvals-and-sandbox`, `--executable`, `--timeout-seconds`, `--worker-mode`, `--allow-dirty`, `--disable-codegraph`.
- **End-to-end validated.** Live smoke test through `CodexAdapter` against the real `codex` CLI returned a structured `verification` artifact (result=passed, confidence 0.9, returncode 0) plus a parsed `finding` artifact from the `{"artifacts": [...]}` envelope. Token counts captured from the event stream: 9757 in / 106 out / 8064 cached / 64 reasoning. Thread id captured. No artifact loss.
- **Failure surface mapped.** New `classify_codex_failure()` helper maps Codex error strings into 9 known buckets: `not_authenticated`, `rate_limit`, `billing_or_quota`, `model_unavailable`, `missing_cli`, `approval_denied`, `sandbox_denied`, `timeout`, `network_error`. Failure classification is set on the verification artifact payload so dashboards and rerun heuristics can react.
- **Tests**: +8 new unit tests covering `CodexAdapter` missing-CLI path, command-builder shape (default + danger-bypass), event-stream parser (banner / invalid-line robustness), `last_codex_agent_message` for both multi-message and turn-failed cases, and every failure-classifier bucket. The pre-existing `test_provider_stub_returns_blocked_verification_artifact` was rewritten to exercise `UnconfiguredProviderAdapter` directly (since the codex slot is no longer a stub). **Suite: 189 passing (was 182).**
- **Docs updated**: `docs/ADAPTERS.md` gains a `codex` section and an `openai` section that had been missing; `docs/ROADMAP.md` strikes out the Codex adapter entry as shipped; README "What Works Today" table promotes Codex to a first-class row; README "Live Adapters" section gains a Codex subsection.

## v0.6.3

- **Claude-Code-only users now get tiered routing too.** v0.6.2 shipped routing parity for the Cursor + OpenAI stack but the Claude Code tier only had Opus 4.6 and Opus 4.7 â so a Claude-Code-only user routing a trivial "format these files" task would land on a $5/$25 Opus call. The starter registry now ships `claude-code/haiku-4-5` (capability_score 55, $1/$5 per MTok, 200K context) as the Anthropic-side counterpart to Cursor's `composer-2.5` and OpenAI's `gpt-5-4-nano`. The router still prefers `cursor/composer-2-5` ($0) or `openai/gpt-5-4-nano` ($0.15/$0.90) when those are present in the registry; Haiku is for the case where they aren't.
- **Anthropic Opus 4.6 / 4.7 pricing corrected to current rates.** The starter registry was shipping these at $15/$75 per MTok â the older Opus 4.1 rate schedule. Anthropic's current 4.x rate is $5/$25 (verified against Anthropic's official pricing page and multiple secondary sources, May 2026). `puppetmaster/model_registry.py:starter_registry()` and `docs/sample-models.json` updated to match. **This is a 3x correction**, not a small one â anyone running router cost estimates against the old Opus pricing was overestimating Anthropic spend by 3x.
- **Benchmark receipts re-run against the corrected pricing.** Headline numbers updated where the frontier baseline figure was derived from Opus pricing: `bench/router_savings.py` total savings is still **35.1% cheaper** because both the picked Opus tasks and the frontier baseline scaled together; `bench/followup_cost.py` "always-frontier replay" reference dropped from $4.92 to **$1.64** at the 40-query mark. The README receipts paragraph and the v0.6.0+ routing example are both updated to cite the new $0.127550 figure for "security audit every endpoint" (was $0.382650 under the old Opus rate). The OpenAI A/B in `bench/router_live_ab.py` is unaffected â its $0.019530 â $0.000141 receipt used real OpenAI tokens and OpenAI pricing.
- **Demonstrable end-to-end**: a synthetic Claude-Code-only registry (3 tiers: haiku-4-5, opus-4-6, opus-4-7) routes `format these files` â `claude-code/haiku-4-5` ($0.002), `implement password reset endpoint` â `claude-code/opus-4-6` ($0.078), `security audit every endpoint` â `claude-code/opus-4-7` ($0.128). Same routing logic, same audit trail, same `--model` arg piped to the `claude` CLI as for the Cursor adapter.
- Tests: 182 still passing â the test suite uses synthetic registries with arbitrary pricing, so Haiku addition + Opus repricing has no test impact.

## v0.6.2

- **Receipts (not vibes).** Adds three reproducible benchmark harnesses that put real numbers behind the project's marketing claims, plus a top-level `TALKING_POINTS.md` truth table that pairs every defensible phrasing with the receipt that justifies it. Existing benches (`codegraph_ab.py`, `three_way.py`, `mcp_stress.py`) explicitly disclaimed "we don't capture live token billing" â these new ones close that gap.
- **`bench/router_savings.py`**: dry-run benchmark over a 6-task fixture (2 easy / 2 medium / 2 hard) that runs the production router for each task and compares `Puppetmaster pick` cost vs an "always frontier" baseline using the registry's pricing. On the starter registry under `balanced` policy, the router stayed on frontier for the 2 audit/architect tasks (honest) and picked cheaper-than-frontier for the other 4 â net **35.1% cheaper** at the model layer for the workload, **100% cheaper** on the four routable tasks (Cursor-tier models are $0 because billing rolls into the Cursor plan). Writes markdown + JSON receipt under `bench/results/router_savings_<timestamp>.{md,json}`. No API key required.
- **`bench/router_live_ab.py`**: live OpenAI A/B with **real** `tokens_in` / `tokens_out` pulled from `usage.prompt_tokens` / `usage.completion_tokens` in the API response â not estimates. Arm A pins the strongest OpenAI model (`gpt-5.5`); Arm B lets the router pick from the OpenAI-adapter pool. First production run on a simple definitional task: arm A spent **$0.019530 in 14.957 s**, arm B (router picked `gpt-5.4-nano`) spent **$0.000141 in 2.491 s** â **99.3% cheaper, 83.3% faster**, equivalent finding artifacts. Costs real money (~$0.02 of spend at the default prompt); supports `--dry-run` for routing-only inspection.
- **`bench/followup_cost.py`**: verifies the "0-token follow-ups" claim on a real completed job. Runs K rounds of 4 different reads (`get_job`, `list_artifacts`, filter findings, filter verifications) against the live SQLite store, measures wall time per query, asserts adapter calls and token usage are both 0. First run: 40 queries answered in **20 ms total** (avg **0.5 ms / query**), **0 adapter calls, 0 tokens, $0.00**. The receipt's "what re-running would cost" column references the same router math the other receipts use, so all three benchmarks cite consistent numbers. **Critical bug fix during build**: the harness initially used `SwarmStore(state_dir)` directly (file backend), which silently returns 0 jobs because production writes to the sqlite backend. Switched to `create_store("sqlite", state_dir)`.
- **`README.md` Receipts section.** A new "Receipts (not vibes)" section between the opening pitch and "The Problem" surfaces the headline numbers and links each LinkedIn-style claim to the script that produced it. Includes an explicit row for what is **not** currently defensible ("more accurate" needs a graded eval set we don't have yet).
- **`TALKING_POINTS.md`**: a truth table pairing "use this" phrasings with "avoid" overclaims for anyone posting about Puppetmaster. Includes a rewritten LinkedIn-ready version of the pitch where every quantitative claim has a script behind it.
- **`bench/README.md`**: expanded to register all six harnesses (router_savings, router_live_ab, followup_cost, three_way, codegraph_ab, mcp_stress) with "what it proves" / "what it does NOT prove" sections.

## v0.6.1-beta.2

- **Adapter stdout no longer silently loses the middle bytes.** All three production adapters (`cursor`, `claude-code`, `openai`) shipped a head-or-tail-only capture pattern: the verification artifact kept the last 8KB (12KB for Claude), the degraded risk artifact kept the first 1KB, and anything in between was dropped without trace. A worker that emitted ~50KB of markdown without a `{"artifacts":[...]}` envelope would lose ~41KB of evidence before a human ever saw it. Every adapter now spools the **full** subprocess stdout (and stderr where relevant) to a sidecar log at `<state_dir>/jobs/<job_id>/tasks/<task_id>/<adapter>_stdout.log`, and the verification + degraded risk artifacts both carry a new `stdout_capture` payload with explicit truncation markers: `stdout_total_chars`, `stdout_truncated`, `stdout_head_excerpt`, `stdout_tail_excerpt`, `stdout_sidecar_path`. Spooling is only attempted when content exceeds head+tail, so short outputs stay fully inline (no extra file churn). On a 67KB synthetic Cursor response with no artifacts envelope, the full text â including a middle-marker that the old code dropped â is now recoverable from the sidecar referenced by both artifacts.
- **`worker_runtime` exports `PUPPETMASTER_STATE_DIR`.** Adapter subprocesses inherited the worker's env but never knew which state directory owned the in-flight job. `worker_runtime.main` now resolves `--state-dir` once and stamps the resolved absolute path into `os.environ["PUPPETMASTER_STATE_DIR"]` before constructing the store. This lets `capture_subprocess_stdout` land sidecar logs in the same `jobs/<job_id>/tasks/<task_id>/` tree the store already owns â `puppetmaster show <job_id>` / `artifacts` show them next to everything else for the task without a separate cleanup path.
- **Graceful fallback when the env isn't set** (direct `CursorAdapter().run()` in unit tests, `bench/codegraph_ab.py` invocations, ad-hoc CLI explorations). The capture helper returns `stdout_sidecar_path: null` instead of trying to fall back to the workspace-hashed default state dir (which could land logs in a project that doesn't own the job). The new head/tail/total markers are still emitted so callers can detect â and surface â that bytes were dropped, even without a sidecar file.
- **Starter registry no longer ships placeholder model names.** The first live cursor swarm under v0.6.1-beta.1 routed to `cursor/gpt-5-5` which the registry mapped to `adapter_model_name: "gpt-5"` â Cursor's SDK rejected that with `Cannot use this model: gpt-5. Available models: default, composer-2.5, composer-2, gpt-5.5, ...`. The placeholder strings from v0.6.0 (`composer-1`, `gpt-5`, `claude-opus-4-5` for both Opus 4.6 and 4.7) are replaced with the real IDs the Cursor SDK and Claude Code CLI accept today: `composer-2.5`, `gpt-5.5`, `claude-opus-4-6`, `claude-opus-4-7`. `puppetmaster/model_registry.py:starter_registry()` and `docs/sample-models.json` are aligned, and `notes` fields drop the "PLACEHOLDER:" prefix because the values are now live-callable end-to-end. Live verification: a balanced-policy `puppetmaster_start_cursor_swarm` job picked `cursor/gpt-5-5 â gpt-5.5`, returned `cursor_status: finished`, `result: passed`, and a 0.95-confidence FINDING artifact with concrete code-line evidence â vs the v0.6.1-beta.1 path which failed in ~1 second with `failure: unknown`.
- Tests: 182 passing (added 6 covering the helper directly with short / long / no-state-dir inputs, plus end-to-end CursorAdapter regression tests proving the verification *and* degraded-risk artifacts both carry a sidecar pointer and that the middle bytes survive the round trip).

## v0.6.1-beta.1

- **Native OpenAI adapter.** Puppetmaster ships a third built-in adapter alongside `cursor` and `claude-code`: `openai` calls OpenAI's Chat Completions API directly via `OPENAI_API_KEY`, bypassing the Cursor SDK entirely. No new pip dependencies â pure `urllib` so the install footprint stays the same. Same structured artifact contract as the Cursor SDK adapter (JSON `{"artifacts":[...]}` reply parsed into `FINDING` / `RISK` / `DECISION` artifacts), same CodeGraph context injection, same promoted-memory enrichment. Token usage (`prompt_tokens` / `completion_tokens` / `total_tokens`) is captured on every `VERIFICATION` artifact for real cost accounting, not just estimates.
- **GPT-5 family in the starter registry.** `puppetmaster models init` now writes four new tiers alongside the Cursor/Claude entries â all with the **real** model ids and the public catalog pricing: `openai/gpt-5-5` ($5 / $30 per MTok, cap 96, 1M context, frontier + `detailed-vision`), `openai/gpt-5-4` ($2.50 / $15, cap 86, 1M context, quality workhorse), `openai/gpt-5-4-mini` ($0.75 / $4.50, cap 70, 400K context, balanced subagent), `openai/gpt-5-4-nano` ($0.15 / $0.90, cap 52, 400K context, cheap reader). These replace the v0.6.0 placeholder ids (`composer-1`, `gpt-5`) on the OpenAI tier â for the first time the routing decision picks a model that actually exists end-to-end. `docs/sample-models.json` synced to match.
- **Router pickups change for vision tasks.** Under `balanced`, a "OCR every detail of the diagram" prompt previously routed to `claude-code/opus-4-7` (cap 98, $0.12 estimated). It now routes to `openai/gpt-5-5` (cap 96, $0.048) because both carry the `detailed-vision` tag and the OpenAI tier undercuts Opus 4.7 on price. Audit tasks (need=98) still land on Opus 4.7 because gpt-5-5 caps at 96. Under `quality` policy Opus 4.7 still wins. The detailed-vision routing test was rewritten to assert the *tag* invariant rather than pin a specific model id.
- **New CLI subcommand.** `python -m puppetmaster openai "<prompt>" --model gpt-5.5 --cwd <repo> --max-output-tokens N --reasoning-effort high|medium|low|none|xhigh --temperature 0.4 --base-url <openai-compat-url> --organization <org-id> --legacy-max-tokens --disable-codegraph --timeout-seconds 300`. Daily-driver runs inline by default like the `cursor` / `claude` subcommands. The `--legacy-max-tokens` opt-in is for OpenAI-compatible providers (Together AI, etc.) that still want the deprecated parameter name.
- **Two new MCP tools.** `puppetmaster_openai` (sync) and `puppetmaster_start_openai` (async returning `job_id` immediately) raise the MCP tool surface from 30 to 32. Env passthrough: `openai_api_key`, `openai_base_url`, `openai_organization` MCP args map to `OPENAI_API_KEY` / `OPENAI_BASE_URL` / `OPENAI_ORG_ID` for the spawned worker.
- **`doctor` reports `OPENAI_API_KEY`.** Added alongside the existing `CURSOR_API_KEY` check so the first-time setup story is one `python -m puppetmaster doctor` invocation.
- **GPT-5 reality fixes.** The first live call surfaced two API breaks in the GPT-5 family: (a) `max_tokens` is rejected with `unsupported_parameter` â the adapter now sends `max_completion_tokens` by default and exposes `legacy_max_tokens` for old providers; (b) reasoning models accept a `reasoning_effort` parameter that the adapter now forwards from `payload.reasoning_effort`.
- **Cleanup: project-level `.cursor/mcp.json` removed.** It duplicated entries that the user-level `~/.cursor/mcp.json` already provides and showed up as "Disabled" duplicates in `Cursor Settings â Tools & MCPs`. Single source of truth is now the user-level config.
- Tests: 177 passing (added 11 covering happy-path artifact parsing, missing API key fail-fast, payload key override, HTTP 401/429 classification, timeout handling, base-url/organization/temperature passthrough, `max_completion_tokens` default + `legacy_max_tokens` opt-in, `reasoning_effort` forwarding, failure classifier buckets, and starter-registry tier structure). Live end-to-end smoke test against the real OpenAI API returned 3 artifacts (`VERIFICATION` + 1 `FINDING` + 1 `RISK`) in 4 seconds, `result=passed`, `finish_reason=stop`, tokens captured.

## v0.6.0-beta.3

- **Opus 4.7 starter score 96 â 98.** A real-world audit task scores 98 on the classifier (audit base 85 + `security` +15 + `every endpoint` +10, capped at 98). With the previous 96, every audit dry-run printed a noisy *"NO model meets capability need (98); falling back to highest-capability available"* â cosmetic, not actually broken, but it looked like a misconfiguration. Bumping Opus 4.7 to 98 makes it an exact-clear of the audit bar so the warning only fires when a user explicitly sets `payload.min_capability = 100`. `docs/sample-models.json` synced to the same value.
- **Test isolation: `tests/conftest.py` forces an empty registry during `pytest`.** After v0.6.0-beta.2's `auto_route: True` default on `DEFAULT_WORKERS`, any developer who had run `puppetmaster models init` on their machine would see Orchestrator tests start invoking the real `claude-code` / `cursor` adapters during the suite â tests are now hermetic regardless of `~/.puppetmaster/models.json` state. The fixture sets `PUPPETMASTER_MODELS_PATH` to a nonexistent path, which makes `_apply_auto_routing` a clean no-op. Tests that exercise routing directly still write their own registry into a `TemporaryDirectory` and pass `payload.registry_path` explicitly, so this fixture does not interfere with them.
- 162/162 tests passing on a machine with a populated user registry (previously: 8 regressions).

## v0.6.0-beta.2

- **Auto-routing is now the default on built-in swarm workers.** `DEFAULT_WORKERS` (used by `puppetmaster_start_cursor_swarm`, `puppetmaster_start_swarm`, and `python -m puppetmaster run`) now ship with `payload.auto_route = True`. Any swarm started through Puppetmaster's MCP surface now consults the router automatically â no per-spec opt-in required. If `~/.puppetmaster/models.json` doesn't exist yet, the orchestrator emits one `router.registry_empty` event and falls back to the spec's declared adapter, so this change is safe even before running `models init`.
- **Vision-aware routing.** Classifier now scans the instruction for vision signals (image, screenshot, diagram, chart, OCR, visual, photo) and auto-adds `vision` to the required tags. "Detailed" vision tasks (OCR / "every detail of the diagram" / explicit `detailed` qualifiers) also add `detailed-vision`. Models in the registry that don't carry those tags get filtered out with a clear rejection reason, so a vision request never lands on a text-only model.
- **Starter registry rebuilt to match the user mental model.** `puppetmaster models init` now writes four tiers: `cursor/composer-2-5` (fast / cheap / reading), `cursor/gpt-5-5` (balanced), `claude-code/opus-4-6` (high-quality, vision), `claude-code/opus-4-7` (frontier, detailed-vision). `adapter_model_name` defaults to model ids that already work today (`composer-1`, `gpt-5`, `claude-opus-4-5`); each entry's `notes` field flags what to edit when newer versions land. Tier ids stay stable.
- **Balanced-policy tie-break flipped.** When two sufficient models cost the same (common with `$0` Cursor-plan models), the router now picks the **lower** capability_score instead of the higher one â right-sizing the model to the task instead of wasting capability. Matches user intent ("easy tasks get the cheap tier even when expensive tiers are technically 'free' under my Cursor plan").
- **New `puppetmaster cost <job_id>` CLI + `puppetmaster_job_cost` MCP tool.** Sums `estimated_cost_usd` across all `ROUTING` artifacts for a job and breaks it down per-model. Answers "how much did this swarm cost?" with the same data the router already wrote.
- Tests: 162 passing (added 9 covering vision detection, detailed-vision detection, auto-required vision tag, starter-registry tier structure + monotone capability, balanced tie-break, default-workers opt-in, empty-registry pass-through, and the cost command).

## v0.6.0-beta.1

- **Intelligent model orchestration.** Puppetmaster now ships a task-aware **model router** that picks the right LLM for each task instead of pinning one model per adapter. Every swarm role can opt in by setting `payload.auto_route = true`; the orchestrator then:
  1. Runs a transparent capability classifier over the task's role + instruction + payload to assign a 0..100 score (e.g. `verify-runtime` â 25, `explore` â 50, `implement` â 75, `audit/security-review` â 90+).
  2. Picks a `ModelSpec` from the user-owned registry using one of four policies â `balanced` (default, cheapest sufficient), `cheap` (lowest cost regardless of fit), `quality` (highest capability), `escalating` (ordered chain for retries).
  3. Stamps the chosen `adapter` + model name into the task so the existing adapter pipeline runs the routed model with zero plumbing changes.
  4. Persists an `ArtifactType.ROUTING` artifact recording the chosen model, the classifier output, the estimated USD cost, and the full list of rejected alternatives with the reason each was rejected â so every routing decision is fully auditable in `puppetmaster show` / `puppetmaster artifacts`.
- **User-owned model registry.** Lives at `~/.puppetmaster/models.json` (override with `$PUPPETMASTER_MODELS_PATH`). Users describe their own models, prices, and asserted capability scores; Puppetmaster never hardcodes model names or fetches live prices. `puppetmaster models init` writes a starter registry; `puppetmaster models list` prints it as a table.
- **`puppetmaster route "<instruction>"` CLI** and `puppetmaster_route_task` / `puppetmaster_list_models` MCP tools. Dry-run a routing decision before kicking off a swarm â see which model would run, the estimated cost, and why the cheaper alternatives were rejected.
- **Per-task overrides:** `payload.min_capability` (force classifier output), `payload.max_cost_usd` (hard budget cap), `payload.required_tags` (e.g. `["long-context"]`), and `payload.routing_policy` (one of `balanced`/`cheap`/`quality`/`escalating`).
- **Honest scope:** v1 routes to adapters we already have (`claude-code`, `cursor`) â those alone cover Sonnet/Haiku/Opus and any Cursor SDK model variant. Raw HTTP adapters for additional providers (Gemini, DeepSeek, Kimi, OpenAI direct) slot in as new `adapter` values; the registry + router/classifier framework doesn't need to change. Pricing and capability scores stay user-asserted â Puppetmaster makes the **decision** transparent, not the **value judgments**.
- Tests: 153 passing (added 18 covering classifier behavior across roles, all four policies, tag filters, cost budget caps, registry round-trip, env override, CLI `route --json`, and end-to-end orchestrator integration that emits a `ROUTING` artifact tied to the real task id).

## v0.5.6-beta.1

- **Root-cause fix for "MCP drops on robust questions".** Found via a new live-fire stress harness (`bench/mcp_stress.py`) that drives the real MCP server over stdio with six load patterns. The failing pattern: `puppetmaster_doctor` (which fans out to multiple `subprocess.run` calls for `git`/`node`/`npm`/`codegraph status`) called in parallel from many requests would silently kill the server with `exit_code=0` and `stdin EOF`. Every `subprocess.run`/`subprocess.Popen` in the server-touched code path was inheriting the parent's fd 0 by default. Under concurrent spawn pressure, that inheritance somehow caused the parent's `for line in sys.stdin` loop to receive a phantom EOF and exit cleanly â looking from Cursor's side exactly like "Tool execution error. Not connected". The fix: every subprocess call in `codegraph.py`, `codegraph_repair.py`, `codegraph_index_runner.py`, `state.py`, `diagnostics.py`, and `mcp_server.py` now passes `stdin=subprocess.DEVNULL`, so children never touch the parent's stdin.
- **New stress harness for regression detection.** `python -m bench.mcp_stress` runs six scenarios against a real-spawned MCP server: 20 parallel doctor calls, 64 simultaneous senders, large payload, doctor + codegraph mixed, sustained 5-rps traffic, and idle->busy->idle transitions for the idle keepalive. All six pass on v0.5.6. Run anytime to verify the production server behavior in <90s.
- **New `PUPPETMASTER_MCP_DIAG_EXIT=1` env flag** that prints the main-loop exit reason on stderr (`stdin_eof`, `sigint_clean_shutdown`, `main_loop_exception: ...`). Off by default; turn on temporarily when an orphan server exits unexpectedly to learn why.
- New regression test (`test_parallel_doctor_calls_do_not_kill_mcp_server`) spawns the real server over stdio, sends 30 parallel doctor calls from 30 sender threads, and asserts every response returns and the server stays alive. This is the unit-level guard for the v0.5.6 fix.
- Tests: 135 passing (added 1 regression). The stress harness was the diagnostic that surfaced the bug; the unit test is the day-to-day guard.

## v0.5.5-beta.1

- **CodeGraph indexes are now per-repo, not machine-wide.** The previous lock was over-eager: each repo has its own SQLite DB at `<repo>/.codegraph/codegraph.db`, so two indexers on two different repos can't trash each other's data. The lock now keys on the resolved repo root path hash (`codegraph-indexer-<repo>-<digest>.lock`), so `ff-data-engineering`, `ff-ios`, and `ios-qa-agent-1` can index in parallel. The lock only blocks legitimate overlap on the same SQLite DB. Legacy callers passing no `repo_root` still get the global lock file for backwards compatibility.
- **Stale-PID auto-clear on lock acquire.** If the lock file records a PID that is no longer alive (`os.kill(pid, 0)` raises `ProcessLookupError`), the new claimant truncates the file and retries the `flock` once before reporting busy. This fixes the post-`kill -9` failure mode where a runaway `codegraph init --index` leaves a lock file pointing at a dead PID and every subsequent indexer attempt sees `Another CodeGraph indexer is already running (pid 80417)` indefinitely. Lock files owned by a *different* user are never blown away (treated as alive by design).
- **Idle MCP-pipe keepalive.** Complements v0.5.3's per-tool-call keepalive: a new `_IdleKeepalive` thread emits a small `notifications/message` every 25s when no tool call is in flight and the server is otherwise idle. Some Cursor builds close the stdio MCP transport for a chat that has been quiet for a while, producing `Tool execution error. Not connected` on the agent's very next call â even though the daemon is healthy. Bytes flowing on the pipe defeat that heuristic. Frames suppress automatically while any tool call is running (the per-call keepalive already handles that window) and stop on `BrokenPipeError`. Cost: ~150 bytes per frame, ~22 KB/hour. Tunable via `PUPPETMASTER_MCP_IDLE_KEEPALIVE_INTERVAL_SECONDS` (default 25, minimum 5) and `PUPPETMASTER_MCP_IDLE_KEEPALIVE_DISABLED=1`.
- **Agents now know how to fall back to the CLI when MCP drops.** The Cursor rule (`.cursor/rules/puppetmaster-workflow.mdc`) and `AGENTS.md` both gained a "When MCP fails" section with a complete mapping from every `puppetmaster_*` MCP tool to its `python -m puppetmaster ...` CLI equivalent (plus the bundled `codegraph` commands). The rule explicitly tells agents to **not** stop work on `Tool execution error. Not connected`, and to only ask the user to restart MCP in Cursor Settings if `python -m puppetmaster mcp list` shows zero alive servers. Read-only CLI commands auto-pivot to the project state dir that owns the job (v0.5.4-beta.2), so the fallback works from any cwd with zero manual state-dir wrangling.
- Tests: 134 passing (added 9 covering per-repo lock paths, parallel-different-repo claims, same-repo busy, stale-PID auto-clear, the `_pid_is_alive` helper, idle keepalive emission, suppression-during-tool-call, broken-pipe stop, env disable). End-to-end smoke-tested: `notifications/message` `idle_keepalive` frames arrive on the wire at the configured interval while the server is idle.

## v0.5.4-beta.2

- **Adapter detection now looks where the SDK actually lives.** `puppetmaster adapters` and `puppetmaster doctor` previously reported `cursor: configured=false` from any workspace other than the Puppetmaster install dir, because `_cursor_sdk_installed` only checked `<cwd>/node_modules/@cursor/sdk` â but `cursor_sdk_runner.mjs` resolves the SDK from the Puppetmaster *package* dir at runtime. The check now searches both the workspace and the package install (and an explicit `PUPPETMASTER_HOME` escape hatch), so diagnostics agree with what the swarm actually does.
- **Cross-workspace job lookup for read-only CLI commands.** `puppetmaster show`, `artifacts`, `diff`, `feed`, `logs`, `events`, `status`, `memory`, and `open` now auto-pivot to the project state dir that owns a given `job_id` if it isn't present in the current workspace's state dir. No more `PUPPETMASTER_STATE_DIR=~/Library/.../ff-data-engineering-cfbfad67d9fc python -m puppetmaster show ...` â just paste the job id from any cwd. Explicit `--state-dir` or `$PUPPETMASTER_STATE_DIR` overrides still take precedence (write-side commands always use the caller's workspace, never pivot).
- **New `puppetmaster projects` command + `puppetmaster jobs --all-projects` flag.** `projects` lists every Puppetmaster project state dir on this machine with job counts and last activity. `jobs --all-projects` flattens jobs from every project into one stream with a project column. Together they make "which workspace owns this job?" a one-liner.
- **Orphan self-termination via stdin staleness watcher.** Cursor's MCP "lease" lifecycle periodically re-creates the logical client without killing the previous Python server, so before this release Puppetmaster could accumulate one MCP server per lease cycle â each still holding SQLite handles and competing for the CodeGraph indexer lock. The new `_InputStalenessWatcher` measures inbound JSON-RPC traffic directly (not just process liveness like the heartbeat thread): if no stdin message arrives for 10 minutes **and** there are zero in-flight tool calls, the watcher closes stdin so the main loop sees EOF and exits through the normal `finally` block (deregister, stop heartbeat, shut down executor). No `os._exit`, no leaked tracking files. Tunable via `PUPPETMASTER_MCP_INPUT_STALE_SECONDS`, `PUPPETMASTER_MCP_INPUT_STALE_CHECK_SECONDS`, and `PUPPETMASTER_MCP_INPUT_STALE_DISABLED`.
- **Explicit Cursor-Node invocation for CodeGraph.** New `resolve_codegraph_invocation()` in `puppetmaster.codegraph` returns `[cursor_node, codegraph.js]` whenever Cursor's bundled Node and the global `@colbymchenry/codegraph` install are both discoverable. Every subprocess that previously invoked the bare `codegraph` shim â `codegraph_context`, `codegraph_status_line`, `run_codegraph_cli`, and the background `codegraph_index_runner` â now invokes CodeGraph under Cursor's Node first, with a transparent fallback to the PATH shim when Cursor isn't installed (Linux, headless CI). This closes the 18-hour-runaway-indexer class of bug, where a stray shell invocation under Homebrew Node would fall back to WASM SQLite, lock the DB, and burn 99% CPU for hours. `PUPPETMASTER_CODEGRAPH_NODE` / `PUPPETMASTER_CODEGRAPH_JS` env vars are an escape hatch for non-standard installs.
- **Doctor distinguishes shell-Node WASM from Cursor-Node WASM.** `puppetmaster doctor`'s `codegraph` check used to warn whenever the shim on PATH reported WASM, even after `repair-codegraph` had successfully rebuilt better-sqlite3 for Cursor's Node â leading to misleading "broken" diagnoses when MCP was actually healthy. The check now runs against the same invocation Puppetmaster uses at runtime (`resolve_codegraph_invocation()`) and reports `ok (verified under Cursor's bundled Node)` when the runtime that matters is healthy.
- New tests covering invocation-resolution under Cursor Node, environment overrides, the fallback path, staleness-watcher trigger/hold-off/reset, env-disable, dispatcher counter, doctor's Cursor-Node disambiguation, SDK detection from the package install dir, and cross-workspace job lookup. Total suite at 125 tests, all passing.

## v0.5.3-beta.1

- **Tool-call keepalive notifications.** Every long-running MCP `tools/call` now spawns a per-call `_ToolCallKeepalive` daemon thread that emits MCP-spec `notifications/message` frames every 10 seconds (after a 5-second grace period) for as long as the handler runs. Short calls pay zero protocol cost; the goal is to keep bytes flowing on the stdio pipe so Cursor's MCP client stops treating slow tool calls as a dead transport. This is the prevention complement to v0.5.2's cleanup â together they cover both "stop the drop from happening" and "recover cleanly when it does."
- All keepalive frames are unidentified JSON-RPC notifications (no `id`) and carry a structured `data.kind = tool_call_progress` payload with the tool name, request id, and elapsed time so logs and progress UIs can render them.
- Tunable via `PUPPETMASTER_MCP_KEEPALIVE_AFTER_SECONDS`, `PUPPETMASTER_MCP_KEEPALIVE_INTERVAL_SECONDS`, and `PUPPETMASTER_MCP_KEEPALIVE_DISABLED`. Default behaviour is on and conservative.
- Broken-pipe detection: if the emitter sees a dead stdio (`BrokenPipeError`/`OSError`), the keepalive thread shuts down cleanly instead of spinning. Post-response races are also closed via a stop-check inside the `_STDOUT_LOCK`.
- README troubleshooting section now distinguishes the prevention layer (this release) from the recovery layer (v0.5.2 mcp registry + cleanup tools).

## v0.5.2-beta.1

- **MCP server process registry + orphan cleanup.** Every Puppetmaster MCP server now writes a tracking file (PID, workspace, started_at, last_heartbeat) at startup, bumps its heartbeat from a daemon thread, and removes its file on a clean exit. New servers prune dead tracking files on startup so orphans from prior `Tool execution error. Not connected` cycles get reaped automatically â no more `pkill -f puppetmaster.mcp_server` from a shell.
- New `puppetmaster/mcp_registry.py` module with `register()`, `heartbeat()`, `deregister()`, `list_entries()`, `prune_dead()`, `kill_stale()`, and a `HeartbeatThread`. Per-user storage lives under `~/Library/Caches/puppetmaster/mcp-servers/` on macOS and `$XDG_CACHE_HOME/puppetmaster/mcp-servers/` on Linux; overridable via `PUPPETMASTER_MCP_REGISTRY_DIR`.
- New CLI surface: `python -m puppetmaster mcp list` (with `--json`) and `python -m puppetmaster mcp cleanup` (`--kill-stale`, `--stale-after-seconds`, `--json`). Lists every tracked server with alive/stale/dead flags and reclaims dead entries; `--kill-stale` SIGTERMs (and SIGKILLs after a grace period) servers whose heartbeat is older than the threshold. The current PID is never signalled.
- New MCP tools `puppetmaster_mcp_status` and `puppetmaster_mcp_cleanup` so an agent can self-diagnose and self-heal right after a transport reconnect without you running anything in a shell.
- `puppetmaster doctor` now includes an `mcp-servers` check that warns when dead tracking files or stale-but-alive servers are present, and points at the new cleanup command. This is the smoking gun behind a stale `Tool execution error. Not connected` symptom.
- README: new "`Tool execution error. Not connected` from Cursor" Troubleshooting section explaining the failure mode (Cursor closes the stdio transport while the swarm keeps running), the diagnostic command, the cleanup command, and the durable-state implication that orphan reconnects are recoverable.

## v0.5.1-beta.1

- **Self-healing CodeGraph SQLite repair.** New `python -m puppetmaster repair-codegraph` CLI command and matching `puppetmaster_repair_codegraph` MCP tool that auto-detect Cursor's bundled Node, locate the global `@colbymchenry/codegraph` install, and `npm rebuild better-sqlite3` against the right runtime â then verify the backend reports as native. This closes the most common Cursor footgun: a shell-Node `npm rebuild` builds for the wrong ABI (Homebrew Node 23 vs. Cursor's bundled Node 22), so the Puppetmaster MCP keeps falling back to slow WASM SQLite and emits spurious `database is locked` errors.
- Rewrite `CODEGRAPH_NATIVE_SQLITE_HINT` and `puppetmaster doctor`'s warn message to explain the dual-Node ABI trap and point users at `puppetmaster repair-codegraph` instead of the generic `npm rebuild` command that lands on the wrong runtime.
- New `puppetmaster/codegraph_repair.py` module with `find_cursor_node()`, `find_codegraph_install()`, and `repair_codegraph_sqlite()` â covered by tests for macOS/Linux/Windows candidate paths, explicit overrides, rebuild failure surfacing, and verification parsing.
- README: new Troubleshooting section documenting the dual-Node-ABI gotcha, the ABI tradeoff (rebuilding for Cursor breaks the terminal until you rebuild back), and the one-command fix.

## v0.5.0-beta.1

- **Multi-threaded MCP request dispatch.** Replace the single-threaded `for line in sys.stdin` loop with a `ThreadPoolExecutor` so long-running tool calls never block the stdio transport. This fixes the "Tool execution error. Not connected" failure mode where a heavy CodeGraph call would silently freeze every other agent tool call. Worker count is configurable via `PUPPETMASTER_MCP_WORKERS` (default 8).
- **Background CodeGraph indexer.** `puppetmaster_codegraph_init(index=true)` now runs the fast init synchronously and dispatches the slow `codegraph index` to a detached subprocess via the new `puppetmaster/codegraph_index_runner.py` launcher, returning immediately with a `run_id`, `pid`, and stdout/stderr log paths. Added a dedicated `puppetmaster_codegraph_index` MCP tool for the standalone background-index case.
- **Global indexer lock.** A user-scoped lock file (`~/Library/Caches/puppetmaster/codegraph-indexer.lock` on macOS, `$XDG_CACHE_HOME/puppetmaster/...` on Linux, overridable with `PUPPETMASTER_CODEGRAPH_LOCK_DIR`) serializes CodeGraph indexers so two parallel runs can never trash a SQLite database. Lock-busy returns a clear `CodegraphLockBusy` payload with the holder PID and a `pkill` hint.
- **Broken native-SQLite detection.** `puppetmaster doctor` and `puppetmaster_codegraph_status` now flag the `better-sqlite3` WASM fallback (Node ABI mismatch) and surface the `npm rebuild better-sqlite3` fix-it hint. `codegraph_native_sqlite_broken(status_output)` is the helper.
- Shorten `DEFAULT_INIT_TIMEOUT_SECONDS` from 600 â 60 since synchronous init is now bounded to the fast scaffold-creation step; indexing is always asynchronous.

## v0.4.0-beta.1

- Bundle six CodeGraph CLI commands into the Puppetmaster MCP (`puppetmaster_codegraph_search`, `_context`, `_affected`, `_files`, `_status`, `_init`) so Cursor Agent only needs one MCP server for both orchestration and repo intelligence.
- Auto-inject CodeGraph context into Cursor and Claude Code worker prompts when the target repo is initialized; tag verification artifacts with `context:codegraph` so the operator can confirm shared intelligence was used.
- Make MCP swarm starts asynchronous: `puppetmaster_start_swarm`, `_start_cursor_swarm`, and friends return a `job_id` immediately instead of holding the MCP call open.
- Add push-style live artifact streaming: `puppetmaster_live_artifacts_follow` (MCP) and `python -m puppetmaster feed <job> --follow` (CLI) long-poll the durable SQLite event cursor and return as soon as a new artifact lands.
- Add warm worker daemon mode (`python -m puppetmaster daemon --roles ...`) for local swarms that want to avoid repeated worker process startup.
- Make Puppetmaster the default subagent runtime: `.cursor/rules/puppetmaster-workflow.mdc` (`alwaysApply: true`) plus a top-level `AGENTS.md` direct Cursor Agent and Claude Code to route non-trivial work through Puppetmaster by default.
- Move runtime state out of the repository by default (macOS: `~/Library/Application Support/puppetmaster/projects/...`, Linux: `~/.local/state/puppetmaster/projects/...`). Override with `--state-dir` or `PUPPETMASTER_STATE_DIR`.
- Parse Cursor SDK swarm output into typed artifacts instead of leaving raw stdout blobs.
- Add cost-structure benchmark `bench/three_way.py` (Agent only vs CodeGraph alone vs Puppetmaster + CodeGraph) and prompt-enrichment harness `bench/codegraph_ab.py`. Numbers in the README and `bench/README.md` are reproducible from these scripts.
- Reposition the README so Puppetmaster's standalone identity (durable state for parallel coding-agent swarms) is the headline, with CodeGraph as an explicit optional integration.

## v0.3.0-beta.1

- Ship the Cursor / VS Code extension with a Puppetmaster activity-bar control panel.
- Add the Cursor Agent MCP server so Agent chat can call Puppetmaster tools directly.
- Add MCP tools for `doctor`, Cursor review/plan, Claude Code implementation, logs, artifacts, and partial summaries.
- Add the Claude Code full-edit adapter with dirty-worktree protection and patch artifact capture.
- Fix editable Python install for local source-checkout usage.
- Expand Cursor, MCP, Claude Code, and daily-driver docs.

## v0.2.0-beta.1

- Position Puppetmaster as a supervised daily-driver beta for Cursor swarm workflows.
- Add run-management commands for latest job lookup, logs, reruns, cleanup, patch artifact review, and approval events.
- Add dry-run Cursor workflow support.
- Add full-edit Claude Code CLI adapter and `puppetmaster claude` command.
- Harden job and worker failure handling.
- Add SQLite schema version metadata and doctor validation.
- Retrieve promoted memory into new task payloads.
- Expand production, security, contribution, and release documentation.

## v0.1.0

- Establish the core local swarm runtime.
- Add subprocess workers, structured artifacts, stitching, and memory promotion.
- Add file and SQLite coordination backends.
- Add shell and optional Cursor adapters.
- Add crash recovery demo, CI, and starter docs.

## Planned v0.2.0

- Broaden provider adapters beyond Cursor.
- Improve patch artifact generation and isolated apply flows.
- Add richer watch output and scripting-friendly JSON modes.


