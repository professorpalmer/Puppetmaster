{
  "name": "puppetmaster",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Optional Cursor SDK bridge for Puppetmaster worker adapters.",
  "scripts": {
    "smoke:cursor-runner": "node --check puppetmaster/cursor_sdk_runner.mjs"
  },
  "optionalDependencies": {
    "@cursor/sdk": "latest"
  }
}

